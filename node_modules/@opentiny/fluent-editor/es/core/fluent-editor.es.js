var __defProp = Object.defineProperty;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);
import Quill from "quill";
import "../config/index.es.js";
import { EN_US } from "../config/i18n/en-us.es.js";
import { ZH_CN } from "../config/i18n/zh-cn.es.js";
import { merge } from "../utils/merge.es.js";
import { defaultLanguage } from "../config/editor.config.es.js";
const LEGACY_I18N_KEY_REGEX = /^_i18n(?:"([^"]+)"|'([^']+)'|`([^`]+)`)$/;
function resolveLegacyI18nKey(name) {
  const key = name.trim();
  const matched = key.match(LEGACY_I18N_KEY_REGEX);
  if (!matched) return name;
  return matched[1] || matched[2] || matched[3] || name;
}
class FluentEditor extends Quill {
  constructor(container, options = {}) {
    if (!options.modules) options.modules = {};
    if (!options.modules.i18n) options.modules.i18n = {};
    const i18nOptions = options.modules.i18n;
    const defaultMessages = {
      "en-US": EN_US,
      "zh-CN": ZH_CN
    };
    const userMessages = i18nOptions.messages || {};
    i18nOptions.messages = merge({}, defaultMessages, userMessages);
    if (!i18nOptions.interpolate) {
      i18nOptions.interpolate = (template, params) => {
        return template.replaceAll(/\{\{([\w]+)\}\}/g, (match, key) => params[key] != null ? String(params[key]) : match);
      };
    }
    super(container, options);
    __publicField(this, "isFullscreen", false);
  }
  static register(...args) {
    super.register(...args);
  }
  get lang() {
    const i18nModule = this.getModule("i18n");
    return i18nModule ? i18nModule.getLocale() : defaultLanguage;
  }
  getLangText(name, params, defaultValue) {
    const i18nModule = this.getModule("i18n");
    if (!i18nModule) return name;
    return i18nModule.t(resolveLegacyI18nKey(name), params, defaultValue);
  }
}
export {
  FluentEditor as default
};
//# sourceMappingURL=fluent-editor.es.js.map
