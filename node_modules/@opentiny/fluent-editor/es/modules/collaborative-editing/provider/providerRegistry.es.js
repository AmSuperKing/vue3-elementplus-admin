import { WebRTCProviderWrapper } from "./webrtc.es.js";
import { WebsocketProviderWrapper } from "./websocket.es.js";
const providerRegistry = {
  websocket: WebsocketProviderWrapper,
  webrtc: WebRTCProviderWrapper
};
function registerProviderType(type, providerClass) {
  providerRegistry[type] = providerClass;
}
function getProviderClass(type) {
  return providerRegistry[type];
}
function createProvider({
  type,
  ...props
}) {
  const ProviderClass = getProviderClass(type);
  if (!ProviderClass) {
    throw new Error(`Provider type "${type}" not found in registry`);
  }
  return new ProviderClass(props);
}
export {
  createProvider,
  getProviderClass,
  registerProviderType
};
//# sourceMappingURL=providerRegistry.es.js.map
