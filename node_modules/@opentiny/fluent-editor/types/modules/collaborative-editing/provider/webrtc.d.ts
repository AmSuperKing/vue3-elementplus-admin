import { Awareness } from 'y-protocols/awareness';
import { WebrtcProvider } from 'y-webrtc';
import { CollaborativeEditingDeps, ProviderEventHandlers } from '../types';
import { UnifiedProvider } from './providerRegistry';
import type * as Y from 'yjs';
export interface WebRTCProviderOptions {
    roomName: string;
    filterBcConns?: boolean;
    maxConns?: number;
    password?: string;
    peerOpts?: Record<string, unknown>;
    signaling?: string[];
}
export declare class WebRTCProviderWrapper implements UnifiedProvider {
    private provider;
    private _isConnected;
    private _isSynced;
    onConnect?: () => void;
    onDisconnect?: () => void;
    onError?: (error: Error) => void;
    onSyncChange?: (isSynced: boolean) => void;
    document: Y.Doc;
    awareness: Awareness;
    type: 'webrtc';
    connect: () => void;
    destroy: () => void;
    disconnect: () => void;
    constructor({ awareness, doc, options, onConnect, onDisconnect, onError, onSyncChange, deps, }: {
        options: WebRTCProviderOptions;
        awareness?: Awareness;
        doc?: Y.Doc;
        deps?: CollaborativeEditingDeps;
    } & ProviderEventHandlers);
    get isConnected(): boolean;
    get isSynced(): boolean;
    getProvider(): WebrtcProvider;
}
