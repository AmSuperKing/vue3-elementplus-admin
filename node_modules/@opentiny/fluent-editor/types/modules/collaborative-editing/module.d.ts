import { default as FluentEditor } from '../../fluent-editor';
export declare class CollaborationModule {
    quill: FluentEditor;
    options: any;
    private collaborativeEditor;
    constructor(quill: FluentEditor, options: any);
    getCursors(): import('quill-cursors').default;
    getAwareness(): import('y-protocols/awareness').Awareness;
    getProvider(): import('./provider').UnifiedProvider;
    destroy(): Promise<void>;
}
