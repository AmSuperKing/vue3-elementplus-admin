import { default as FluentEditor } from '../../core/fluent-editor';
import { Action } from './actions';
import { BlotFormatterOptions } from './options';
import { BlotSpec } from './specs';
export declare class BlotFormatter {
    quill: FluentEditor;
    options: BlotFormatterOptions;
    currentSpec: BlotSpec;
    specs: BlotSpec[];
    overlay: HTMLElement;
    actions: Action[];
    observer: MutationObserver;
    static register(): void;
    constructor(quill: FluentEditor, options?: Partial<BlotFormatterOptions>);
    show(spec: BlotSpec): void;
    hide(): void;
    update(): void;
    createActions(spec: BlotSpec): void;
    destroyActions(): void;
    repositionOverlay(): void;
    setUserSelect(value: string): void;
    onClick: () => void;
}
