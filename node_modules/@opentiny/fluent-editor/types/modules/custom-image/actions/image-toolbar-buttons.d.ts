import { ToolbarButtonOptions, ToolButtonOption } from '../options';
export declare const ALIGN_ATTR = "data-align";
export declare function setAlignStyle(el: HTMLElement, display: string | null, float: string | null, margin: string | null): void;
export declare const alignmentHandler: {
    left: (el: HTMLElement, toolbarButtons: ImageToolbarButtons) => void;
    center: (el: HTMLElement, toolbarButtons: ImageToolbarButtons) => void;
    right: (el: HTMLElement, toolbarButtons: ImageToolbarButtons) => void;
    download: (el: HTMLImageElement, toolbarButtons: ImageToolbarButtons) => void;
    copy: (el: HTMLImageElement, toolbarButtons: ImageToolbarButtons) => Promise<void>;
};
export declare class ImageToolbarButtons {
    buttons: Record<string, ToolButtonOption>;
    constructor(options: ToolbarButtonOptions);
    getItems(): ToolButtonOption[];
    clear(el: HTMLElement): void;
}
