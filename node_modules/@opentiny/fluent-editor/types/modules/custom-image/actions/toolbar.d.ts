import { BlotFormatter } from '../blot-formatter';
import { ToolButtonOption } from '../options';
import { ImageToolbarButtons } from './image-toolbar-buttons';
export declare class ImageToolbar {
    toolbar?: HTMLElement;
    buttons: HTMLElement[];
    constructor();
    create(formatter: BlotFormatter, toolButton: ImageToolbarButtons): HTMLElement;
    destroy(): void;
    getElement(): HTMLElement;
    addToolbarStyle(formatter: BlotFormatter, toolbar: HTMLElement): void;
    addButtonStyle(button: HTMLElement, index: number, formatter: BlotFormatter): void;
    addButtons(formatter: BlotFormatter, toolbar: HTMLElement, toolButton: ImageToolbarButtons): void;
    preselectButton(button: HTMLElement, toolButtonOption: ToolButtonOption, formatter: BlotFormatter, toolButton: ImageToolbarButtons): void;
    onButtonClick(button: HTMLElement, formatter: BlotFormatter, toolButtonOption: ToolButtonOption, toolButton: ImageToolbarButtons): void;
    clickButton(button: HTMLElement, target: HTMLImageElement, formatter: BlotFormatter, toolButtonOption: ToolButtonOption, toolButton: ImageToolbarButtons): void;
    selectButton(formatter: BlotFormatter, button: HTMLElement): void;
    deselectButton(formatter: BlotFormatter, button: HTMLElement): void;
}
