import { i as __toESM, n as __exportAll, t as __commonJSMin } from "./chunk-B4lejLz5.js";
import { A as keys$1, B as createAssigner, F as isBuffer, H as isArrayLike, K as copyObject, L as isArguments, M as isTypedArray, Y as baseAssignValue, Z as arrayEach, ct as identity, dt as isObjectLike, ft as baseGetTag, j as baseKeys, lt as isObject$1, m as cloneDeep, n as isEqual, q as assignValue, st as isFunction, t as merge, u as mapToArray, ut as isArray, v as _getTag_default, z as isPrototype } from "./merge-Bt8kegQx.js";
import { F as baseIndexOf, G as arrayMap, L as baseFindIndex, O as get, R as noop$1, c as map, d as debounce, g as baseIteratee, h as baseForOwn, n as set$1, o as isUndefined, p as baseEach, r as pick, s as isNil, t as throttle, u as last, x as hasPath, z as toInteger } from "./throttle-CiR1nPqO.js";
import { t as v4 } from "./v4-CDWFGXW5.js";
//#region node_modules/preact/dist/preact.module.js
var n, l$1, u$2, i$2, r$1, o$2, e$1, f$2, c$1, a$1, s$1, h, p, v$1, d = {}, w$1 = [], _ = /acit|ex(?:s|g|n|p|$)|rph|grid|ows|mnc|ntw|ine[ch]|zoo|^ord|itera/i, g$1 = Array.isArray;
function m$1(n, l) {
	for (var u in l) n[u] = l[u];
	return n;
}
function b(n) {
	n && n.parentNode && n.parentNode.removeChild(n);
}
function k$1(l, u, t) {
	var i, r, o, e = {};
	for (o in u) "key" == o ? i = u[o] : "ref" == o ? r = u[o] : e[o] = u[o];
	if (arguments.length > 2 && (e.children = arguments.length > 3 ? n.call(arguments, 2) : t), "function" == typeof l && null != l.defaultProps) for (o in l.defaultProps) void 0 === e[o] && (e[o] = l.defaultProps[o]);
	return x(l, e, i, r, null);
}
function x(n, t, i, r, o) {
	var e = {
		type: n,
		props: t,
		key: i,
		ref: r,
		__k: null,
		__: null,
		__b: 0,
		__e: null,
		__c: null,
		constructor: void 0,
		__v: null == o ? ++u$2 : o,
		__i: -1,
		__u: 0
	};
	return null == o && null != l$1.vnode && l$1.vnode(e), e;
}
function M$1() {
	return { current: null };
}
function S(n) {
	return n.children;
}
function C(n, l) {
	this.props = n, this.context = l;
}
function $(n, l) {
	if (null == l) return n.__ ? $(n.__, n.__i + 1) : null;
	for (var u; l < n.__k.length; l++) if (null != (u = n.__k[l]) && null != u.__e) return u.__e;
	return "function" == typeof n.type ? $(n) : null;
}
function I(n) {
	if (n.__P && n.__d) {
		var u = n.__v, t = u.__e, i = [], r = [], o = m$1({}, u);
		o.__v = u.__v + 1, l$1.vnode && l$1.vnode(o), q$1(n.__P, o, u, n.__n, n.__P.namespaceURI, 32 & u.__u ? [t] : null, i, null == t ? $(u) : t, !!(32 & u.__u), r), o.__v = u.__v, o.__.__k[o.__i] = o, D(i, o, r), u.__e = u.__ = null, o.__e != t && P$1(o);
	}
}
function P$1(n) {
	if (null != (n = n.__) && null != n.__c) return n.__e = n.__c.base = null, n.__k.some(function(l) {
		if (null != l && null != l.__e) return n.__e = n.__c.base = l.__e;
	}), P$1(n);
}
function A$1(n) {
	(!n.__d && (n.__d = !0) && i$2.push(n) && !H$1.__r++ || r$1 != l$1.debounceRendering) && ((r$1 = l$1.debounceRendering) || o$2)(H$1);
}
function H$1() {
	try {
		for (var n, l = 1; i$2.length;) i$2.length > l && i$2.sort(e$1), n = i$2.shift(), l = i$2.length, I(n);
	} finally {
		i$2.length = H$1.__r = 0;
	}
}
function L$1(n, l, u, t, i, r, o, e, f, c, a) {
	var s, h, p, v, y, _, g, m = t && t.__k || w$1, b = l.length;
	for (f = T$1(u, l, m, f, b), s = 0; s < b; s++) null != (p = u.__k[s]) && (h = -1 != p.__i && m[p.__i] || d, p.__i = s, _ = q$1(n, p, h, i, r, o, e, f, c, a), v = p.__e, p.ref && h.ref != p.ref && (h.ref && J$1(h.ref, null, p), a.push(p.ref, p.__c || v, p)), null == y && null != v && (y = v), (g = !!(4 & p.__u)) || h.__k === p.__k ? (f = j$2(p, f, n, g), g && h.__e && (h.__e = null)) : "function" == typeof p.type && void 0 !== _ ? f = _ : v && (f = v.nextSibling), p.__u &= -7);
	return u.__e = y, f;
}
function T$1(n, l, u, t, i) {
	var r, o, e, f, c, a = u.length, s = a, h = 0;
	for (n.__k = new Array(i), r = 0; r < i; r++) null != (o = l[r]) && "boolean" != typeof o && "function" != typeof o ? ("string" == typeof o || "number" == typeof o || "bigint" == typeof o || o.constructor == String ? o = n.__k[r] = x(null, o, null, null, null) : g$1(o) ? o = n.__k[r] = x(S, { children: o }, null, null, null) : void 0 === o.constructor && o.__b > 0 ? o = n.__k[r] = x(o.type, o.props, o.key, o.ref ? o.ref : null, o.__v) : n.__k[r] = o, f = r + h, o.__ = n, o.__b = n.__b + 1, e = null, -1 != (c = o.__i = O$1(o, u, f, s)) && (s--, (e = u[c]) && (e.__u |= 2)), null == e || null == e.__v ? (-1 == c && (i > a ? h-- : i < a && h++), "function" != typeof o.type && (o.__u |= 4)) : c != f && (c == f - 1 ? h-- : c == f + 1 ? h++ : (c > f ? h-- : h++, o.__u |= 4))) : n.__k[r] = null;
	if (s) for (r = 0; r < a; r++) null != (e = u[r]) && 0 == (2 & e.__u) && (e.__e == t && (t = $(e)), K$1(e, e));
	return t;
}
function j$2(n, l, u, t) {
	var i, r;
	if ("function" == typeof n.type) {
		for (i = n.__k, r = 0; i && r < i.length; r++) i[r] && (i[r].__ = n, l = j$2(i[r], l, u, t));
		return l;
	}
	n.__e != l && (t && (l && n.type && !l.parentNode && (l = $(n)), u.insertBefore(n.__e, l || null)), l = n.__e);
	do
		l = l && l.nextSibling;
	while (null != l && 8 == l.nodeType);
	return l;
}
function F$1(n, l) {
	return l = l || [], null == n || "boolean" == typeof n || (g$1(n) ? n.some(function(n) {
		F$1(n, l);
	}) : l.push(n)), l;
}
function O$1(n, l, u, t) {
	var i, r, o, e = n.key, f = n.type, c = l[u], a = null != c && 0 == (2 & c.__u);
	if (null === c && null == e || a && e == c.key && f == c.type) return u;
	if (t > (a ? 1 : 0)) {
		for (i = u - 1, r = u + 1; i >= 0 || r < l.length;) if (null != (c = l[o = i >= 0 ? i-- : r++]) && 0 == (2 & c.__u) && e == c.key && f == c.type) return o;
	}
	return -1;
}
function z$1(n, l, u) {
	"-" == l[0] ? n.setProperty(l, null == u ? "" : u) : n[l] = null == u ? "" : "number" != typeof u || _.test(l) ? u : u + "px";
}
function N(n, l, u, t, i) {
	var r, o;
	n: if ("style" == l) if ("string" == typeof u) n.style.cssText = u;
	else {
		if ("string" == typeof t && (n.style.cssText = t = ""), t) for (l in t) u && l in u || z$1(n.style, l, "");
		if (u) for (l in u) t && u[l] == t[l] || z$1(n.style, l, u[l]);
	}
	else if ("o" == l[0] && "n" == l[1]) r = l != (l = l.replace(s$1, "$1")), o = l.toLowerCase(), l = o in n || "onFocusOut" == l || "onFocusIn" == l ? o.slice(2) : l.slice(2), n.l || (n.l = {}), n.l[l + r] = u, u ? t ? u[a$1] = t[a$1] : (u[a$1] = h, n.addEventListener(l, r ? v$1 : p, r)) : n.removeEventListener(l, r ? v$1 : p, r);
	else {
		if ("http://www.w3.org/2000/svg" == i) l = l.replace(/xlink(H|:h)/, "h").replace(/sName$/, "s");
		else if ("width" != l && "height" != l && "href" != l && "list" != l && "form" != l && "tabIndex" != l && "download" != l && "rowSpan" != l && "colSpan" != l && "role" != l && "popover" != l && l in n) try {
			n[l] = null == u ? "" : u;
			break n;
		} catch (n) {}
		"function" == typeof u || (null == u || !1 === u && "-" != l[4] ? n.removeAttribute(l) : n.setAttribute(l, "popover" == l && 1 == u ? "" : u));
	}
}
function V$1(n) {
	return function(u) {
		if (this.l) {
			var t = this.l[u.type + n];
			if (null == u[c$1]) u[c$1] = h++;
			else if (u[c$1] < t[a$1]) return;
			return t(l$1.event ? l$1.event(u) : u);
		}
	};
}
function q$1(n, u, t, i, r, o, e, f, c, a) {
	var s, h, p, v, y, d, _, k, x, M, $, I, P, A, H, T = u.type;
	if (void 0 !== u.constructor) return null;
	128 & t.__u && (c = !!(32 & t.__u), o = [f = u.__e = t.__e]), (s = l$1.__b) && s(u);
	n: if ("function" == typeof T) try {
		if (k = u.props, x = T.prototype && T.prototype.render, M = (s = T.contextType) && i[s.__c], $ = s ? M ? M.props.value : s.__ : i, t.__c ? _ = (h = u.__c = t.__c).__ = h.__E : (x ? u.__c = h = new T(k, $) : (u.__c = h = new C(k, $), h.constructor = T, h.render = Q$1), M && M.sub(h), h.state || (h.state = {}), h.__n = i, p = h.__d = !0, h.__h = [], h._sb = []), x && null == h.__s && (h.__s = h.state), x && null != T.getDerivedStateFromProps && (h.__s == h.state && (h.__s = m$1({}, h.__s)), m$1(h.__s, T.getDerivedStateFromProps(k, h.__s))), v = h.props, y = h.state, h.__v = u, p) x && null == T.getDerivedStateFromProps && null != h.componentWillMount && h.componentWillMount(), x && null != h.componentDidMount && h.__h.push(h.componentDidMount);
		else {
			if (x && null == T.getDerivedStateFromProps && k !== v && null != h.componentWillReceiveProps && h.componentWillReceiveProps(k, $), u.__v == t.__v || !h.__e && null != h.shouldComponentUpdate && !1 === h.shouldComponentUpdate(k, h.__s, $)) {
				u.__v != t.__v && (h.props = k, h.state = h.__s, h.__d = !1), u.__e = t.__e, u.__k = t.__k, u.__k.some(function(n) {
					n && (n.__ = u);
				}), w$1.push.apply(h.__h, h._sb), h._sb = [], h.__h.length && e.push(h);
				break n;
			}
			null != h.componentWillUpdate && h.componentWillUpdate(k, h.__s, $), x && null != h.componentDidUpdate && h.__h.push(function() {
				h.componentDidUpdate(v, y, d);
			});
		}
		if (h.context = $, h.props = k, h.__P = n, h.__e = !1, I = l$1.__r, P = 0, x) h.state = h.__s, h.__d = !1, I && I(u), s = h.render(h.props, h.state, h.context), w$1.push.apply(h.__h, h._sb), h._sb = [];
		else do
			h.__d = !1, I && I(u), s = h.render(h.props, h.state, h.context), h.state = h.__s;
		while (h.__d && ++P < 25);
		h.state = h.__s, null != h.getChildContext && (i = m$1(m$1({}, i), h.getChildContext())), x && !p && null != h.getSnapshotBeforeUpdate && (d = h.getSnapshotBeforeUpdate(v, y)), A = null != s && s.type === S && null == s.key ? E$1(s.props.children) : s, f = L$1(n, g$1(A) ? A : [A], u, t, i, r, o, e, f, c, a), h.base = u.__e, u.__u &= -161, h.__h.length && e.push(h), _ && (h.__E = h.__ = null);
	} catch (n) {
		if (u.__v = null, c || null != o) if (n.then) {
			for (u.__u |= c ? 160 : 128; f && 8 == f.nodeType && f.nextSibling;) f = f.nextSibling;
			o[o.indexOf(f)] = null, u.__e = f;
		} else {
			for (H = o.length; H--;) b(o[H]);
			B$2(u);
		}
		else u.__e = t.__e, u.__k = t.__k, n.then || B$2(u);
		l$1.__e(n, u, t);
	}
	else null == o && u.__v == t.__v ? (u.__k = t.__k, u.__e = t.__e) : f = u.__e = G$1(t.__e, u, t, i, r, o, e, c, a);
	return (s = l$1.diffed) && s(u), 128 & u.__u ? void 0 : f;
}
function B$2(n) {
	n && (n.__c && (n.__c.__e = !0), n.__k && n.__k.some(B$2));
}
function D(n, u, t) {
	for (var i = 0; i < t.length; i++) J$1(t[i], t[++i], t[++i]);
	l$1.__c && l$1.__c(u, n), n.some(function(u) {
		try {
			n = u.__h, u.__h = [], n.some(function(n) {
				n.call(u);
			});
		} catch (n) {
			l$1.__e(n, u.__v);
		}
	});
}
function E$1(n) {
	return "object" != typeof n || null == n || n.__b > 0 ? n : g$1(n) ? n.map(E$1) : void 0 !== n.constructor ? null : m$1({}, n);
}
function G$1(u, t, i, r, o, e, f, c, a) {
	var s, h, p, v, y, w, _, m = i.props || d, k = t.props, x = t.type;
	if ("svg" == x ? o = "http://www.w3.org/2000/svg" : "math" == x ? o = "http://www.w3.org/1998/Math/MathML" : o || (o = "http://www.w3.org/1999/xhtml"), null != e) {
		for (s = 0; s < e.length; s++) if ((y = e[s]) && "setAttribute" in y == !!x && (x ? y.localName == x : 3 == y.nodeType)) {
			u = y, e[s] = null;
			break;
		}
	}
	if (null == u) {
		if (null == x) return document.createTextNode(k);
		u = document.createElementNS(o, x, k.is && k), c && (l$1.__m && l$1.__m(t, e), c = !1), e = null;
	}
	if (null == x) m === k || c && u.data == k || (u.data = k);
	else {
		if (e = "textarea" == x && null != k.defaultValue ? null : e && n.call(u.childNodes), !c && null != e) for (m = {}, s = 0; s < u.attributes.length; s++) m[(y = u.attributes[s]).name] = y.value;
		for (s in m) y = m[s], "dangerouslySetInnerHTML" == s ? p = y : "children" == s || s in k || "value" == s && "defaultValue" in k || "checked" == s && "defaultChecked" in k || N(u, s, null, y, o);
		for (s in k) y = k[s], "children" == s ? v = y : "dangerouslySetInnerHTML" == s ? h = y : "value" == s ? w = y : "checked" == s ? _ = y : c && "function" != typeof y || m[s] === y || N(u, s, y, m[s], o);
		if (h) c || p && (h.__html == p.__html || h.__html == u.innerHTML) || (u.innerHTML = h.__html), t.__k = [];
		else if (p && (u.innerHTML = ""), L$1("template" == t.type ? u.content : u, g$1(v) ? v : [v], t, i, r, "foreignObject" == x ? "http://www.w3.org/1999/xhtml" : o, e, f, e ? e[0] : i.__k && $(i, 0), c, a), null != e) for (s = e.length; s--;) b(e[s]);
		c && "textarea" != x || (s = "value", "progress" == x && null == w ? u.removeAttribute("value") : null != w && (w !== u[s] || "progress" == x && !w || "option" == x && w != m[s]) && N(u, s, w, m[s], o), s = "checked", null != _ && _ != u[s] && N(u, s, _, m[s], o));
	}
	return u;
}
function J$1(n, u, t) {
	try {
		if ("function" == typeof n) {
			var i = "function" == typeof n.__u;
			i && n.__u(), i && null == u || (n.__u = n(u));
		} else n.current = u;
	} catch (n) {
		l$1.__e(n, t);
	}
}
function K$1(n, u, t) {
	var i, r;
	if (l$1.unmount && l$1.unmount(n), (i = n.ref) && (i.current && i.current != n.__e || J$1(i, null, u)), null != (i = n.__c)) {
		if (i.componentWillUnmount) try {
			i.componentWillUnmount();
		} catch (n) {
			l$1.__e(n, u);
		}
		i.base = i.__P = null;
	}
	if (i = n.__k) for (r = 0; r < i.length; r++) i[r] && K$1(i[r], u, t || "function" != typeof n.type);
	t || b(n.__e), n.__c = n.__ = n.__e = void 0;
}
function Q$1(n, l, u) {
	return this.constructor(n, u);
}
function R(u, t, i) {
	var r, o, e, f;
	t == document && (t = document.documentElement), l$1.__ && l$1.__(u, t), o = (r = "function" == typeof i) ? null : i && i.__k || t.__k, e = [], f = [], q$1(t, u = (!r && i || t).__k = k$1(S, null, [u]), o || d, d, t.namespaceURI, !r && i ? [i] : o ? null : t.firstChild ? n.call(t.childNodes) : null, e, !r && i ? i : o ? o.__e : t.firstChild, r, f), D(e, u, f);
}
n = w$1.slice, l$1 = { __e: function(n, l, u, t) {
	for (var i, r, o; l = l.__;) if ((i = l.__c) && !i.__) try {
		if ((r = i.constructor) && null != r.getDerivedStateFromError && (i.setState(r.getDerivedStateFromError(n)), o = i.__d), null != i.componentDidCatch && (i.componentDidCatch(n, t || {}), o = i.__d), o) return i.__E = i;
	} catch (l) {
		n = l;
	}
	throw n;
} }, u$2 = 0, C.prototype.setState = function(n, l) {
	var u = null != this.__s && this.__s != this.state ? this.__s : this.__s = m$1({}, this.state);
	"function" == typeof n && (n = n(m$1({}, u), this.props)), n && m$1(u, n), null != n && this.__v && (l && this._sb.push(l), A$1(this));
}, C.prototype.forceUpdate = function(n) {
	this.__v && (this.__e = !0, n && this.__h.push(n), A$1(this));
}, C.prototype.render = S, i$2 = [], o$2 = "function" == typeof Promise ? Promise.prototype.then.bind(Promise.resolve()) : setTimeout, e$1 = function(n, l) {
	return n.__v.__b - l.__v.__b;
}, H$1.__r = 0, f$2 = Math.random().toString(8), c$1 = "__d" + f$2, a$1 = "__a" + f$2, s$1 = /(PointerCapture)$|Capture$/i, h = 0, p = V$1(!1), v$1 = V$1(!0);
//#endregion
//#region node_modules/mobx/lib/mobx.module.js
/** MobX - (c) Michel Weststrate 2015 - 2020 - MIT Licensed */
var OBFUSCATED_ERROR = "An invariant failed, however the error is obfuscated because this is a production build.";
var EMPTY_ARRAY = [];
Object.freeze(EMPTY_ARRAY);
var EMPTY_OBJECT = {};
Object.freeze(EMPTY_OBJECT);
function getNextId() {
	return ++globalState.mobxGuid;
}
function fail$2(message) {
	invariant$1(false, message);
	throw "X";
}
function invariant$1(check, message) {
	if (!check) throw new Error("[mobx] " + (message || OBFUSCATED_ERROR));
}
/**
* Prints a deprecation message, but only one time.
* Returns false if the deprecated message was already printed before
*/
var deprecatedMessages = [];
function deprecated(msg, thing) {
	if (thing) return deprecated("'" + msg + "', use '" + thing + "' instead.");
	if (deprecatedMessages.indexOf(msg) !== -1) return false;
	deprecatedMessages.push(msg);
	console.error("[mobx] Deprecated: " + msg);
	return true;
}
/**
* Makes sure that the provided function is invoked at most once.
*/
function once(func) {
	var invoked = false;
	return function() {
		if (invoked) return;
		invoked = true;
		return func.apply(this, arguments);
	};
}
var noop = function() {};
function unique(list) {
	var res = [];
	list.forEach(function(item) {
		if (res.indexOf(item) === -1) res.push(item);
	});
	return res;
}
function isObject(value) {
	return value !== null && typeof value === "object";
}
function isPlainObject(value) {
	if (value === null || typeof value !== "object") return false;
	var proto = Object.getPrototypeOf(value);
	return proto === Object.prototype || proto === null;
}
function convertToMap(dataStructure) {
	if (isES6Map(dataStructure) || isObservableMap(dataStructure)) return dataStructure;
	else if (Array.isArray(dataStructure)) return new Map(dataStructure);
	else if (isPlainObject(dataStructure)) {
		var map = /* @__PURE__ */ new Map();
		for (var key in dataStructure) map.set(key, dataStructure[key]);
		return map;
	} else return fail$2("Cannot convert to map from '" + dataStructure + "'");
}
function addHiddenProp(object, propName, value) {
	Object.defineProperty(object, propName, {
		enumerable: false,
		writable: true,
		configurable: true,
		value
	});
}
function addHiddenFinalProp(object, propName, value) {
	Object.defineProperty(object, propName, {
		enumerable: false,
		writable: false,
		configurable: true,
		value
	});
}
function isPropertyConfigurable(object, prop) {
	var descriptor = Object.getOwnPropertyDescriptor(object, prop);
	return !descriptor || descriptor.configurable !== false && descriptor.writable !== false;
}
function assertPropertyConfigurable(object, prop) {
	if (!isPropertyConfigurable(object, prop)) fail$2("Cannot make property '" + prop.toString() + "' observable, it is not configurable and writable in the target object");
}
function createInstanceofPredicate(name, clazz) {
	var propName = "isMobX" + name;
	clazz.prototype[propName] = true;
	return function(x) {
		return isObject(x) && x[propName] === true;
	};
}
function isES6Map(thing) {
	return thing instanceof Map;
}
function isES6Set(thing) {
	return thing instanceof Set;
}
/**
* Returns the following: own keys, prototype keys & own symbol keys, if they are enumerable.
*/
function getPlainObjectKeys(object) {
	var enumerables = /* @__PURE__ */ new Set();
	for (var key in object) enumerables.add(key);
	Object.getOwnPropertySymbols(object).forEach(function(k) {
		if (Object.getOwnPropertyDescriptor(object, k).enumerable) enumerables.add(k);
	});
	return Array.from(enumerables);
}
function stringifyKey(key) {
	if (key && key.toString) return key.toString();
	else return new String(key).toString();
}
function toPrimitive(value) {
	return value === null ? null : typeof value === "object" ? "" + value : value;
}
var ownKeys = typeof Reflect !== "undefined" && Reflect.ownKeys ? Reflect.ownKeys : Object.getOwnPropertySymbols ? function(obj) {
	return Object.getOwnPropertyNames(obj).concat(Object.getOwnPropertySymbols(obj));
} : /* istanbul ignore next */ Object.getOwnPropertyNames;
var $mobx = Symbol("mobx administration");
var Atom = function() {
	/**
	* Create a new atom. For debugging purposes it is recommended to give it a name.
	* The onBecomeObserved and onBecomeUnobserved callbacks can be used for resource management.
	*/
	function Atom(name) {
		if (name === void 0) name = "Atom@" + getNextId();
		this.name = name;
		this.isPendingUnobservation = false;
		this.isBeingObserved = false;
		this.observers = /* @__PURE__ */ new Set();
		this.diffValue = 0;
		this.lastAccessedBy = 0;
		this.lowestObserverState = IDerivationState.NOT_TRACKING;
	}
	Atom.prototype.onBecomeObserved = function() {
		if (this.onBecomeObservedListeners) this.onBecomeObservedListeners.forEach(function(listener) {
			return listener();
		});
	};
	Atom.prototype.onBecomeUnobserved = function() {
		if (this.onBecomeUnobservedListeners) this.onBecomeUnobservedListeners.forEach(function(listener) {
			return listener();
		});
	};
	/**
	* Invoke this method to notify mobx that your atom has been used somehow.
	* Returns true if there is currently a reactive context.
	*/
	Atom.prototype.reportObserved = function() {
		return reportObserved(this);
	};
	/**
	* Invoke this method _after_ this method has changed to signal mobx that all its observers should invalidate.
	*/
	Atom.prototype.reportChanged = function() {
		startBatch();
		propagateChanged(this);
		endBatch();
	};
	Atom.prototype.toString = function() {
		return this.name;
	};
	return Atom;
}();
var isAtom = createInstanceofPredicate("Atom", Atom);
function createAtom(name, onBecomeObservedHandler, onBecomeUnobservedHandler) {
	if (onBecomeObservedHandler === void 0) onBecomeObservedHandler = noop;
	if (onBecomeUnobservedHandler === void 0) onBecomeUnobservedHandler = noop;
	var atom = new Atom(name);
	if (onBecomeObservedHandler !== noop) onBecomeObserved(atom, onBecomeObservedHandler);
	if (onBecomeUnobservedHandler !== noop) onBecomeUnobserved(atom, onBecomeUnobservedHandler);
	return atom;
}
function identityComparer(a, b) {
	return a === b;
}
function structuralComparer(a, b) {
	return deepEqual(a, b);
}
function shallowComparer(a, b) {
	return deepEqual(a, b, 1);
}
function defaultComparer(a, b) {
	return Object.is(a, b);
}
var comparer = {
	identity: identityComparer,
	structural: structuralComparer,
	default: defaultComparer,
	shallow: shallowComparer
};
/*! *****************************************************************************
Copyright (c) Microsoft Corporation. All rights reserved.
Licensed under the Apache License, Version 2.0 (the "License"); you may not use
this file except in compliance with the License. You may obtain a copy of the
License at http://www.apache.org/licenses/LICENSE-2.0

THIS CODE IS PROVIDED ON AN *AS IS* BASIS, WITHOUT WARRANTIES OR CONDITIONS OF ANY
KIND, EITHER EXPRESS OR IMPLIED, INCLUDING WITHOUT LIMITATION ANY IMPLIED
WARRANTIES OR CONDITIONS OF TITLE, FITNESS FOR A PARTICULAR PURPOSE,
MERCHANTABLITY OR NON-INFRINGEMENT.

See the Apache Version 2.0 License for specific language governing permissions
and limitations under the License.
***************************************************************************** */
var extendStatics = function(d, b) {
	extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
		d.__proto__ = b;
	} || function(d, b) {
		for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
	};
	return extendStatics(d, b);
};
function __extends$44(d, b) {
	extendStatics(d, b);
	function __() {
		this.constructor = d;
	}
	d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
}
var __assign$53 = function() {
	__assign$53 = Object.assign || function __assign(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$53.apply(this, arguments);
};
function __values$3(o) {
	var m = typeof Symbol === "function" && o[Symbol.iterator], i = 0;
	if (m) return m.call(o);
	return { next: function() {
		if (o && i >= o.length) o = void 0;
		return {
			value: o && o[i++],
			done: !o
		};
	} };
}
function __read$39(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
}
function __spread() {
	for (var ar = [], i = 0; i < arguments.length; i++) ar = ar.concat(__read$39(arguments[i]));
	return ar;
}
var mobxDidRunLazyInitializersSymbol = Symbol("mobx did run lazy initializers");
var mobxPendingDecorators = Symbol("mobx pending decorators");
var enumerableDescriptorCache = {};
var nonEnumerableDescriptorCache = {};
function createPropertyInitializerDescriptor(prop, enumerable) {
	var cache = enumerable ? enumerableDescriptorCache : nonEnumerableDescriptorCache;
	return cache[prop] || (cache[prop] = {
		configurable: true,
		enumerable,
		get: function() {
			initializeInstance(this);
			return this[prop];
		},
		set: function(value) {
			initializeInstance(this);
			this[prop] = value;
		}
	});
}
function initializeInstance(target) {
	var e_1, _a;
	if (target[mobxDidRunLazyInitializersSymbol] === true) return;
	var decorators = target[mobxPendingDecorators];
	if (decorators) {
		addHiddenProp(target, mobxDidRunLazyInitializersSymbol, true);
		var keys = __spread(Object.getOwnPropertySymbols(decorators), Object.keys(decorators));
		try {
			for (var keys_1 = __values$3(keys), keys_1_1 = keys_1.next(); !keys_1_1.done; keys_1_1 = keys_1.next()) {
				var d = decorators[keys_1_1.value];
				d.propertyCreator(target, d.prop, d.descriptor, d.decoratorTarget, d.decoratorArguments);
			}
		} catch (e_1_1) {
			e_1 = { error: e_1_1 };
		} finally {
			try {
				if (keys_1_1 && !keys_1_1.done && (_a = keys_1.return)) _a.call(keys_1);
			} finally {
				if (e_1) throw e_1.error;
			}
		}
	}
}
function createPropDecorator(propertyInitiallyEnumerable, propertyCreator) {
	return function decoratorFactory() {
		var decoratorArguments;
		var decorator = function decorate(target, prop, descriptor, applyImmediately) {
			if (applyImmediately === true) {
				propertyCreator(target, prop, descriptor, target, decoratorArguments);
				return null;
			}
			if (!quacksLikeADecorator(arguments)) fail$2("This function is a decorator, but it wasn't invoked like a decorator");
			if (!Object.prototype.hasOwnProperty.call(target, mobxPendingDecorators)) {
				var inheritedDecorators = target[mobxPendingDecorators];
				addHiddenProp(target, mobxPendingDecorators, __assign$53({}, inheritedDecorators));
			}
			target[mobxPendingDecorators][prop] = {
				prop,
				propertyCreator,
				descriptor,
				decoratorTarget: target,
				decoratorArguments
			};
			return createPropertyInitializerDescriptor(prop, propertyInitiallyEnumerable);
		};
		if (quacksLikeADecorator(arguments)) {
			decoratorArguments = EMPTY_ARRAY;
			return decorator.apply(null, arguments);
		} else {
			decoratorArguments = Array.prototype.slice.call(arguments);
			return decorator;
		}
	};
}
function quacksLikeADecorator(args) {
	return (args.length === 2 || args.length === 3) && (typeof args[1] === "string" || typeof args[1] === "symbol") || args.length === 4 && args[3] === true;
}
function deepEnhancer(v, _, name) {
	if (isObservable(v)) return v;
	if (Array.isArray(v)) return observable.array(v, { name });
	if (isPlainObject(v)) return observable.object(v, void 0, { name });
	if (isES6Map(v)) return observable.map(v, { name });
	if (isES6Set(v)) return observable.set(v, { name });
	return v;
}
function shallowEnhancer(v, _, name) {
	if (v === void 0 || v === null) return v;
	if (isObservableObject(v) || isObservableArray(v) || isObservableMap(v) || isObservableSet(v)) return v;
	if (Array.isArray(v)) return observable.array(v, {
		name,
		deep: false
	});
	if (isPlainObject(v)) return observable.object(v, void 0, {
		name,
		deep: false
	});
	if (isES6Map(v)) return observable.map(v, {
		name,
		deep: false
	});
	if (isES6Set(v)) return observable.set(v, {
		name,
		deep: false
	});
	return fail$2("The shallow modifier / decorator can only used in combination with arrays, objects, maps and sets");
}
function referenceEnhancer(newValue) {
	return newValue;
}
function refStructEnhancer(v, oldValue, name) {
	if (isObservable(v)) throw "observable.struct should not be used with observable values";
	if (deepEqual(v, oldValue)) return oldValue;
	return v;
}
function createDecoratorForEnhancer(enhancer) {
	invariant$1(enhancer);
	var decorator = createPropDecorator(true, function(target, propertyName, descriptor, _decoratorTarget, decoratorArgs) {
		invariant$1(!descriptor || !descriptor.get, "@observable cannot be used on getter (property \"" + stringifyKey(propertyName) + "\"), use @computed instead.");
		var initialValue = descriptor ? descriptor.initializer ? descriptor.initializer.call(target) : descriptor.value : void 0;
		asObservableObject(target).addObservableProp(propertyName, initialValue, enhancer);
	});
	var res = typeof process !== "undefined" && process.env && true ? function observableDecorator() {
		if (arguments.length < 2) return fail$2("Incorrect decorator invocation. @observable decorator doesn't expect any arguments");
		return decorator.apply(null, arguments);
	} : decorator;
	res.enhancer = enhancer;
	return res;
}
var defaultCreateObservableOptions = {
	deep: true,
	name: void 0,
	defaultDecorator: void 0,
	proxy: true
};
Object.freeze(defaultCreateObservableOptions);
function assertValidOption(key) {
	if (!/^(deep|name|equals|defaultDecorator|proxy)$/.test(key)) fail$2("invalid option for (extend)observable: " + key);
}
function asCreateObservableOptions(thing) {
	if (thing === null || thing === void 0) return defaultCreateObservableOptions;
	if (typeof thing === "string") return {
		name: thing,
		deep: true,
		proxy: true
	};
	if (typeof thing !== "object") return fail$2("expected options object");
	Object.keys(thing).forEach(assertValidOption);
	return thing;
}
var deepDecorator = createDecoratorForEnhancer(deepEnhancer);
var shallowDecorator = createDecoratorForEnhancer(shallowEnhancer);
var refDecorator = createDecoratorForEnhancer(referenceEnhancer);
var refStructDecorator = createDecoratorForEnhancer(refStructEnhancer);
function getEnhancerFromOptions(options) {
	return options.defaultDecorator ? options.defaultDecorator.enhancer : options.deep === false ? referenceEnhancer : deepEnhancer;
}
/**
* Turns an object, array or function into a reactive structure.
* @param v the value which should become observable.
*/
function createObservable(v, arg2, arg3) {
	if (typeof arguments[1] === "string" || typeof arguments[1] === "symbol") return deepDecorator.apply(null, arguments);
	if (isObservable(v)) return v;
	var res = isPlainObject(v) ? observable.object(v, arg2, arg3) : Array.isArray(v) ? observable.array(v, arg2) : isES6Map(v) ? observable.map(v, arg2) : isES6Set(v) ? observable.set(v, arg2) : v;
	if (res !== v) return res;
	fail$2("The provided value could not be converted into an observable. If you want just create an observable reference to the object use 'observable.box(value)'");
}
var observableFactories = {
	box: function(value, options) {
		if (arguments.length > 2) incorrectlyUsedAsDecorator("box");
		var o = asCreateObservableOptions(options);
		return new ObservableValue(value, getEnhancerFromOptions(o), o.name, true, o.equals);
	},
	array: function(initialValues, options) {
		if (arguments.length > 2) incorrectlyUsedAsDecorator("array");
		var o = asCreateObservableOptions(options);
		return createObservableArray(initialValues, getEnhancerFromOptions(o), o.name);
	},
	map: function(initialValues, options) {
		if (arguments.length > 2) incorrectlyUsedAsDecorator("map");
		var o = asCreateObservableOptions(options);
		return new ObservableMap(initialValues, getEnhancerFromOptions(o), o.name);
	},
	set: function(initialValues, options) {
		if (arguments.length > 2) incorrectlyUsedAsDecorator("set");
		var o = asCreateObservableOptions(options);
		return new ObservableSet(initialValues, getEnhancerFromOptions(o), o.name);
	},
	object: function(props, decorators, options) {
		if (typeof arguments[1] === "string") incorrectlyUsedAsDecorator("object");
		var o = asCreateObservableOptions(options);
		if (o.proxy === false) return extendObservable({}, props, decorators, o);
		else {
			var defaultDecorator = getDefaultDecoratorFromObjectOptions(o);
			var proxy = createDynamicObservableObject(extendObservable({}, void 0, void 0, o));
			extendObservableObjectWithProperties(proxy, props, decorators, defaultDecorator);
			return proxy;
		}
	},
	ref: refDecorator,
	shallow: shallowDecorator,
	deep: deepDecorator,
	struct: refStructDecorator
};
var observable = createObservable;
Object.keys(observableFactories).forEach(function(name) {
	return observable[name] = observableFactories[name];
});
function incorrectlyUsedAsDecorator(methodName) {
	fail$2("Expected one or two arguments to observable." + methodName + ". Did you accidentally try to use observable." + methodName + " as decorator?");
}
var computedDecorator = createPropDecorator(false, function(instance, propertyName, descriptor, decoratorTarget, decoratorArgs) {
	invariant$1(descriptor && descriptor.get, "Trying to declare a computed value for unspecified getter '" + stringifyKey(propertyName) + "'");
	var get = descriptor.get, set = descriptor.set;
	var options = decoratorArgs[0] || {};
	asObservableObject(instance).addComputedProp(instance, propertyName, __assign$53({
		get,
		set,
		context: instance
	}, options));
});
var computedStructDecorator = computedDecorator({ equals: comparer.structural });
/**
* Decorator for class properties: @computed get value() { return expr; }.
* For legacy purposes also invokable as ES5 observable created: `computed(() => expr)`;
*/
var computed = function computed(arg1, arg2, arg3) {
	if (typeof arg2 === "string") return computedDecorator.apply(null, arguments);
	if (arg1 !== null && typeof arg1 === "object" && arguments.length === 1) return computedDecorator.apply(null, arguments);
	invariant$1(typeof arg1 === "function", "First argument to `computed` should be an expression.");
	invariant$1(arguments.length < 3, "Computed takes one or two arguments if used as function");
	var opts = typeof arg2 === "object" ? arg2 : {};
	opts.get = arg1;
	opts.set = typeof arg2 === "function" ? arg2 : opts.set;
	opts.name = opts.name || arg1.name || "";
	return new ComputedValue(opts);
};
computed.struct = computedStructDecorator;
var IDerivationState;
(function(IDerivationState) {
	IDerivationState[IDerivationState["NOT_TRACKING"] = -1] = "NOT_TRACKING";
	IDerivationState[IDerivationState["UP_TO_DATE"] = 0] = "UP_TO_DATE";
	IDerivationState[IDerivationState["POSSIBLY_STALE"] = 1] = "POSSIBLY_STALE";
	IDerivationState[IDerivationState["STALE"] = 2] = "STALE";
})(IDerivationState || (IDerivationState = {}));
var TraceMode;
(function(TraceMode) {
	TraceMode[TraceMode["NONE"] = 0] = "NONE";
	TraceMode[TraceMode["LOG"] = 1] = "LOG";
	TraceMode[TraceMode["BREAK"] = 2] = "BREAK";
})(TraceMode || (TraceMode = {}));
var CaughtException = function() {
	function CaughtException(cause) {
		this.cause = cause;
	}
	return CaughtException;
}();
function isCaughtException(e) {
	return e instanceof CaughtException;
}
/**
* Finds out whether any dependency of the derivation has actually changed.
* If dependenciesState is 1 then it will recalculate dependencies,
* if any dependency changed it will propagate it by changing dependenciesState to 2.
*
* By iterating over the dependencies in the same order that they were reported and
* stopping on the first change, all the recalculations are only called for ComputedValues
* that will be tracked by derivation. That is because we assume that if the first x
* dependencies of the derivation doesn't change then the derivation should run the same way
* up until accessing x-th dependency.
*/
function shouldCompute(derivation) {
	switch (derivation.dependenciesState) {
		case IDerivationState.UP_TO_DATE: return false;
		case IDerivationState.NOT_TRACKING:
		case IDerivationState.STALE: return true;
		case IDerivationState.POSSIBLY_STALE:
			var prevAllowStateReads = allowStateReadsStart(true);
			var prevUntracked = untrackedStart();
			var obs = derivation.observing, l = obs.length;
			for (var i = 0; i < l; i++) {
				var obj = obs[i];
				if (isComputedValue(obj)) {
					if (globalState.disableErrorBoundaries) obj.get();
					else try {
						obj.get();
					} catch (e) {
						untrackedEnd(prevUntracked);
						allowStateReadsEnd(prevAllowStateReads);
						return true;
					}
					if (derivation.dependenciesState === IDerivationState.STALE) {
						untrackedEnd(prevUntracked);
						allowStateReadsEnd(prevAllowStateReads);
						return true;
					}
				}
			}
			changeDependenciesStateTo0(derivation);
			untrackedEnd(prevUntracked);
			allowStateReadsEnd(prevAllowStateReads);
			return false;
	}
}
function checkIfStateModificationsAreAllowed(atom) {
	var hasObservers = atom.observers.size > 0;
	if (globalState.computationDepth > 0 && hasObservers) fail$2("Computed values are not allowed to cause side effects by changing observables that are already being observed. Tried to modify: " + atom.name);
	if (!globalState.allowStateChanges && (hasObservers || globalState.enforceActions === "strict")) fail$2((globalState.enforceActions ? "Since strict-mode is enabled, changing observed observable values outside actions is not allowed. Please wrap the code in an `action` if this change is intended. Tried to modify: " : "Side effects like changing state are not allowed at this point. Are you trying to modify state from, for example, the render function of a React component? Tried to modify: ") + atom.name);
}
function checkIfStateReadsAreAllowed(observable) {
	if (!globalState.allowStateReads && globalState.observableRequiresReaction) console.warn("[mobx] Observable " + observable.name + " being read outside a reactive context");
}
/**
* Executes the provided function `f` and tracks which observables are being accessed.
* The tracking information is stored on the `derivation` object and the derivation is registered
* as observer of any of the accessed observables.
*/
function trackDerivedFunction(derivation, f, context) {
	var prevAllowStateReads = allowStateReadsStart(true);
	changeDependenciesStateTo0(derivation);
	derivation.newObserving = new Array(derivation.observing.length + 100);
	derivation.unboundDepsCount = 0;
	derivation.runId = ++globalState.runId;
	var prevTracking = globalState.trackingDerivation;
	globalState.trackingDerivation = derivation;
	var result;
	if (globalState.disableErrorBoundaries === true) result = f.call(context);
	else try {
		result = f.call(context);
	} catch (e) {
		result = new CaughtException(e);
	}
	globalState.trackingDerivation = prevTracking;
	bindDependencies(derivation);
	warnAboutDerivationWithoutDependencies(derivation);
	allowStateReadsEnd(prevAllowStateReads);
	return result;
}
function warnAboutDerivationWithoutDependencies(derivation) {
	if (derivation.observing.length !== 0) return;
	if (globalState.reactionRequiresObservable || derivation.requiresObservable) console.warn("[mobx] Derivation " + derivation.name + " is created/updated without reading any observable value");
}
/**
* diffs newObserving with observing.
* update observing to be newObserving with unique observables
* notify observers that become observed/unobserved
*/
function bindDependencies(derivation) {
	var prevObserving = derivation.observing;
	var observing = derivation.observing = derivation.newObserving;
	var lowestNewObservingDerivationState = IDerivationState.UP_TO_DATE;
	var i0 = 0, l = derivation.unboundDepsCount;
	for (var i = 0; i < l; i++) {
		var dep = observing[i];
		if (dep.diffValue === 0) {
			dep.diffValue = 1;
			if (i0 !== i) observing[i0] = dep;
			i0++;
		}
		if (dep.dependenciesState > lowestNewObservingDerivationState) lowestNewObservingDerivationState = dep.dependenciesState;
	}
	observing.length = i0;
	derivation.newObserving = null;
	l = prevObserving.length;
	while (l--) {
		var dep = prevObserving[l];
		if (dep.diffValue === 0) removeObserver(dep, derivation);
		dep.diffValue = 0;
	}
	while (i0--) {
		var dep = observing[i0];
		if (dep.diffValue === 1) {
			dep.diffValue = 0;
			addObserver(dep, derivation);
		}
	}
	if (lowestNewObservingDerivationState !== IDerivationState.UP_TO_DATE) {
		derivation.dependenciesState = lowestNewObservingDerivationState;
		derivation.onBecomeStale();
	}
}
function clearObserving(derivation) {
	var obs = derivation.observing;
	derivation.observing = [];
	var i = obs.length;
	while (i--) removeObserver(obs[i], derivation);
	derivation.dependenciesState = IDerivationState.NOT_TRACKING;
}
function untracked(action) {
	var prev = untrackedStart();
	try {
		return action();
	} finally {
		untrackedEnd(prev);
	}
}
function untrackedStart() {
	var prev = globalState.trackingDerivation;
	globalState.trackingDerivation = null;
	return prev;
}
function untrackedEnd(prev) {
	globalState.trackingDerivation = prev;
}
function allowStateReadsStart(allowStateReads) {
	var prev = globalState.allowStateReads;
	globalState.allowStateReads = allowStateReads;
	return prev;
}
function allowStateReadsEnd(prev) {
	globalState.allowStateReads = prev;
}
/**
* needed to keep `lowestObserverState` correct. when changing from (2 or 1) to 0
*
*/
function changeDependenciesStateTo0(derivation) {
	if (derivation.dependenciesState === IDerivationState.UP_TO_DATE) return;
	derivation.dependenciesState = IDerivationState.UP_TO_DATE;
	var obs = derivation.observing;
	var i = obs.length;
	while (i--) obs[i].lowestObserverState = IDerivationState.UP_TO_DATE;
}
var currentActionId = 0;
var nextActionId = 1;
var functionNameDescriptor = Object.getOwnPropertyDescriptor(function() {}, "name");
var isFunctionNameConfigurable = functionNameDescriptor && functionNameDescriptor.configurable;
function createAction(actionName, fn, ref) {
	invariant$1(typeof fn === "function", "`action` can only be invoked on functions");
	if (typeof actionName !== "string" || !actionName) fail$2("actions should have valid names, got: '" + actionName + "'");
	var res = function() {
		return executeAction(actionName, fn, ref || this, arguments);
	};
	res.isMobxAction = true;
	if (isFunctionNameConfigurable) Object.defineProperty(res, "name", { value: actionName });
	return res;
}
function executeAction(actionName, fn, scope, args) {
	var runInfo = _startAction(actionName, scope, args);
	try {
		return fn.apply(scope, args);
	} catch (err) {
		runInfo.error = err;
		throw err;
	} finally {
		_endAction(runInfo);
	}
}
function _startAction(actionName, scope, args) {
	var notifySpy = isSpyEnabled() && !!actionName;
	var startTime = 0;
	if (notifySpy && true) {
		startTime = Date.now();
		var l = args && args.length || 0;
		var flattendArgs = new Array(l);
		if (l > 0) for (var i = 0; i < l; i++) flattendArgs[i] = args[i];
		spyReportStart({
			type: "action",
			name: actionName,
			object: scope,
			arguments: flattendArgs
		});
	}
	var prevDerivation = untrackedStart();
	startBatch();
	var runInfo = {
		prevDerivation,
		prevAllowStateChanges: allowStateChangesStart$1(true),
		prevAllowStateReads: allowStateReadsStart(true),
		notifySpy,
		startTime,
		actionId: nextActionId++,
		parentActionId: currentActionId
	};
	currentActionId = runInfo.actionId;
	return runInfo;
}
function _endAction(runInfo) {
	if (currentActionId !== runInfo.actionId) fail$2("invalid action stack. did you forget to finish an action?");
	currentActionId = runInfo.parentActionId;
	if (runInfo.error !== void 0) globalState.suppressReactionErrors = true;
	allowStateChangesEnd$1(runInfo.prevAllowStateChanges);
	allowStateReadsEnd(runInfo.prevAllowStateReads);
	endBatch();
	untrackedEnd(runInfo.prevDerivation);
	if (runInfo.notifySpy && true) spyReportEnd({ time: Date.now() - runInfo.startTime });
	globalState.suppressReactionErrors = false;
}
function allowStateChangesStart$1(allowStateChanges) {
	var prev = globalState.allowStateChanges;
	globalState.allowStateChanges = allowStateChanges;
	return prev;
}
function allowStateChangesEnd$1(prev) {
	globalState.allowStateChanges = prev;
}
var ObservableValue = function(_super) {
	__extends$44(ObservableValue, _super);
	function ObservableValue(value, enhancer, name, notifySpy, equals) {
		if (name === void 0) name = "ObservableValue@" + getNextId();
		if (notifySpy === void 0) notifySpy = true;
		if (equals === void 0) equals = comparer.default;
		var _this = _super.call(this, name) || this;
		_this.enhancer = enhancer;
		_this.name = name;
		_this.equals = equals;
		_this.hasUnreportedChange = false;
		_this.value = enhancer(value, void 0, name);
		if (notifySpy && isSpyEnabled() && true) spyReport({
			type: "create",
			name: _this.name,
			newValue: "" + _this.value
		});
		return _this;
	}
	ObservableValue.prototype.dehanceValue = function(value) {
		if (this.dehancer !== void 0) return this.dehancer(value);
		return value;
	};
	ObservableValue.prototype.set = function(newValue) {
		var oldValue = this.value;
		newValue = this.prepareNewValue(newValue);
		if (newValue !== globalState.UNCHANGED) {
			var notifySpy = isSpyEnabled();
			if (notifySpy && true) spyReportStart({
				type: "update",
				name: this.name,
				newValue,
				oldValue
			});
			this.setNewValue(newValue);
			if (notifySpy && true) spyReportEnd();
		}
	};
	ObservableValue.prototype.prepareNewValue = function(newValue) {
		checkIfStateModificationsAreAllowed(this);
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				object: this,
				type: "update",
				newValue
			});
			if (!change) return globalState.UNCHANGED;
			newValue = change.newValue;
		}
		newValue = this.enhancer(newValue, this.value, this.name);
		return this.equals(this.value, newValue) ? globalState.UNCHANGED : newValue;
	};
	ObservableValue.prototype.setNewValue = function(newValue) {
		var oldValue = this.value;
		this.value = newValue;
		this.reportChanged();
		if (hasListeners(this)) notifyListeners(this, {
			type: "update",
			object: this,
			newValue,
			oldValue
		});
	};
	ObservableValue.prototype.get = function() {
		this.reportObserved();
		return this.dehanceValue(this.value);
	};
	ObservableValue.prototype.intercept = function(handler) {
		return registerInterceptor(this, handler);
	};
	ObservableValue.prototype.observe = function(listener, fireImmediately) {
		if (fireImmediately) listener({
			object: this,
			type: "update",
			newValue: this.value,
			oldValue: void 0
		});
		return registerListener(this, listener);
	};
	ObservableValue.prototype.toJSON = function() {
		return this.get();
	};
	ObservableValue.prototype.toString = function() {
		return this.name + "[" + this.value + "]";
	};
	ObservableValue.prototype.valueOf = function() {
		return toPrimitive(this.get());
	};
	ObservableValue.prototype[Symbol.toPrimitive] = function() {
		return this.valueOf();
	};
	return ObservableValue;
}(Atom);
var isObservableValue = createInstanceofPredicate("ObservableValue", ObservableValue);
/**
* A node in the state dependency root that observes other nodes, and can be observed itself.
*
* ComputedValue will remember the result of the computation for the duration of the batch, or
* while being observed.
*
* During this time it will recompute only when one of its direct dependencies changed,
* but only when it is being accessed with `ComputedValue.get()`.
*
* Implementation description:
* 1. First time it's being accessed it will compute and remember result
*    give back remembered result until 2. happens
* 2. First time any deep dependency change, propagate POSSIBLY_STALE to all observers, wait for 3.
* 3. When it's being accessed, recompute if any shallow dependency changed.
*    if result changed: propagate STALE to all observers, that were POSSIBLY_STALE from the last step.
*    go to step 2. either way
*
* If at any point it's outside batch and it isn't observed: reset everything and go to 1.
*/
var ComputedValue = function() {
	/**
	* Create a new computed value based on a function expression.
	*
	* The `name` property is for debug purposes only.
	*
	* The `equals` property specifies the comparer function to use to determine if a newly produced
	* value differs from the previous value. Two comparers are provided in the library; `defaultComparer`
	* compares based on identity comparison (===), and `structualComparer` deeply compares the structure.
	* Structural comparison can be convenient if you always produce a new aggregated object and
	* don't want to notify observers if it is structurally the same.
	* This is useful for working with vectors, mouse coordinates etc.
	*/
	function ComputedValue(options) {
		this.dependenciesState = IDerivationState.NOT_TRACKING;
		this.observing = [];
		this.newObserving = null;
		this.isBeingObserved = false;
		this.isPendingUnobservation = false;
		this.observers = /* @__PURE__ */ new Set();
		this.diffValue = 0;
		this.runId = 0;
		this.lastAccessedBy = 0;
		this.lowestObserverState = IDerivationState.UP_TO_DATE;
		this.unboundDepsCount = 0;
		this.__mapid = "#" + getNextId();
		this.value = new CaughtException(null);
		this.isComputing = false;
		this.isRunningSetter = false;
		this.isTracing = TraceMode.NONE;
		invariant$1(options.get, "missing option for computed: get");
		this.derivation = options.get;
		this.name = options.name || "ComputedValue@" + getNextId();
		if (options.set) this.setter = createAction(this.name + "-setter", options.set);
		this.equals = options.equals || (options.compareStructural || options.struct ? comparer.structural : comparer.default);
		this.scope = options.context;
		this.requiresReaction = !!options.requiresReaction;
		this.keepAlive = !!options.keepAlive;
	}
	ComputedValue.prototype.onBecomeStale = function() {
		propagateMaybeChanged(this);
	};
	ComputedValue.prototype.onBecomeObserved = function() {
		if (this.onBecomeObservedListeners) this.onBecomeObservedListeners.forEach(function(listener) {
			return listener();
		});
	};
	ComputedValue.prototype.onBecomeUnobserved = function() {
		if (this.onBecomeUnobservedListeners) this.onBecomeUnobservedListeners.forEach(function(listener) {
			return listener();
		});
	};
	/**
	* Returns the current value of this computed value.
	* Will evaluate its computation first if needed.
	*/
	ComputedValue.prototype.get = function() {
		if (this.isComputing) fail$2("Cycle detected in computation " + this.name + ": " + this.derivation);
		if (globalState.inBatch === 0 && this.observers.size === 0 && !this.keepAlive) {
			if (shouldCompute(this)) {
				this.warnAboutUntrackedRead();
				startBatch();
				this.value = this.computeValue(false);
				endBatch();
			}
		} else {
			reportObserved(this);
			if (shouldCompute(this)) {
				if (this.trackAndCompute()) propagateChangeConfirmed(this);
			}
		}
		var result = this.value;
		if (isCaughtException(result)) throw result.cause;
		return result;
	};
	ComputedValue.prototype.peek = function() {
		var res = this.computeValue(false);
		if (isCaughtException(res)) throw res.cause;
		return res;
	};
	ComputedValue.prototype.set = function(value) {
		if (this.setter) {
			invariant$1(!this.isRunningSetter, "The setter of computed value '" + this.name + "' is trying to update itself. Did you intend to update an _observable_ value, instead of the computed property?");
			this.isRunningSetter = true;
			try {
				this.setter.call(this.scope, value);
			} finally {
				this.isRunningSetter = false;
			}
		} else invariant$1(false, "[ComputedValue '" + this.name + "'] It is not possible to assign a new value to a computed value.");
	};
	ComputedValue.prototype.trackAndCompute = function() {
		if (isSpyEnabled() && true) spyReport({
			object: this.scope,
			type: "compute",
			name: this.name
		});
		var oldValue = this.value;
		var wasSuspended = this.dependenciesState === IDerivationState.NOT_TRACKING;
		var newValue = this.computeValue(true);
		var changed = wasSuspended || isCaughtException(oldValue) || isCaughtException(newValue) || !this.equals(oldValue, newValue);
		if (changed) this.value = newValue;
		return changed;
	};
	ComputedValue.prototype.computeValue = function(track) {
		this.isComputing = true;
		globalState.computationDepth++;
		var res;
		if (track) res = trackDerivedFunction(this, this.derivation, this.scope);
		else if (globalState.disableErrorBoundaries === true) res = this.derivation.call(this.scope);
		else try {
			res = this.derivation.call(this.scope);
		} catch (e) {
			res = new CaughtException(e);
		}
		globalState.computationDepth--;
		this.isComputing = false;
		return res;
	};
	ComputedValue.prototype.suspend = function() {
		if (!this.keepAlive) {
			clearObserving(this);
			this.value = void 0;
		}
	};
	ComputedValue.prototype.observe = function(listener, fireImmediately) {
		var _this = this;
		var firstTime = true;
		var prevValue = void 0;
		return autorun(function() {
			var newValue = _this.get();
			if (!firstTime || fireImmediately) {
				var prevU = untrackedStart();
				listener({
					type: "update",
					object: _this,
					newValue,
					oldValue: prevValue
				});
				untrackedEnd(prevU);
			}
			firstTime = false;
			prevValue = newValue;
		});
	};
	ComputedValue.prototype.warnAboutUntrackedRead = function() {
		if (this.requiresReaction === true) fail$2("[mobx] Computed value " + this.name + " is read outside a reactive context");
		if (this.isTracing !== TraceMode.NONE) console.log("[mobx.trace] '" + this.name + "' is being read outside a reactive context. Doing a full recompute");
		if (globalState.computedRequiresReaction) console.warn("[mobx] Computed value " + this.name + " is being read outside a reactive context. Doing a full recompute");
	};
	ComputedValue.prototype.toJSON = function() {
		return this.get();
	};
	ComputedValue.prototype.toString = function() {
		return this.name + "[" + this.derivation.toString() + "]";
	};
	ComputedValue.prototype.valueOf = function() {
		return toPrimitive(this.get());
	};
	ComputedValue.prototype[Symbol.toPrimitive] = function() {
		return this.valueOf();
	};
	return ComputedValue;
}();
var isComputedValue = createInstanceofPredicate("ComputedValue", ComputedValue);
var MobXGlobals = function() {
	function MobXGlobals() {
		/**
		* MobXGlobals version.
		* MobX compatiblity with other versions loaded in memory as long as this version matches.
		* It indicates that the global state still stores similar information
		*
		* N.B: this version is unrelated to the package version of MobX, and is only the version of the
		* internal state storage of MobX, and can be the same across many different package versions
		*/
		this.version = 5;
		/**
		* globally unique token to signal unchanged
		*/
		this.UNCHANGED = {};
		/**
		* Currently running derivation
		*/
		this.trackingDerivation = null;
		/**
		* Are we running a computation currently? (not a reaction)
		*/
		this.computationDepth = 0;
		/**
		* Each time a derivation is tracked, it is assigned a unique run-id
		*/
		this.runId = 0;
		/**
		* 'guid' for general purpose. Will be persisted amongst resets.
		*/
		this.mobxGuid = 0;
		/**
		* Are we in a batch block? (and how many of them)
		*/
		this.inBatch = 0;
		/**
		* Observables that don't have observers anymore, and are about to be
		* suspended, unless somebody else accesses it in the same batch
		*
		* @type {IObservable[]}
		*/
		this.pendingUnobservations = [];
		/**
		* List of scheduled, not yet executed, reactions.
		*/
		this.pendingReactions = [];
		/**
		* Are we currently processing reactions?
		*/
		this.isRunningReactions = false;
		/**
		* Is it allowed to change observables at this point?
		* In general, MobX doesn't allow that when running computations and React.render.
		* To ensure that those functions stay pure.
		*/
		this.allowStateChanges = true;
		/**
		* Is it allowed to read observables at this point?
		* Used to hold the state needed for `observableRequiresReaction`
		*/
		this.allowStateReads = true;
		/**
		* If strict mode is enabled, state changes are by default not allowed
		*/
		this.enforceActions = false;
		/**
		* Spy callbacks
		*/
		this.spyListeners = [];
		/**
		* Globally attached error handlers that react specifically to errors in reactions
		*/
		this.globalReactionErrorHandlers = [];
		/**
		* Warn if computed values are accessed outside a reactive context
		*/
		this.computedRequiresReaction = false;
		/**
		* (Experimental)
		* Warn if you try to create to derivation / reactive context without accessing any observable.
		*/
		this.reactionRequiresObservable = false;
		/**
		* (Experimental)
		* Warn if observables are accessed outside a reactive context
		*/
		this.observableRequiresReaction = false;
		/**
		* Allows overwriting of computed properties, useful in tests but not prod as it can cause
		* memory leaks. See https://github.com/mobxjs/mobx/issues/1867
		*/
		this.computedConfigurable = false;
		this.disableErrorBoundaries = false;
		this.suppressReactionErrors = false;
	}
	return MobXGlobals;
}();
var mockGlobal = {};
function getGlobal() {
	if (typeof window !== "undefined") return window;
	if (typeof global !== "undefined") return global;
	if (typeof self !== "undefined") return self;
	return mockGlobal;
}
var canMergeGlobalState = true;
var isolateCalled = false;
var globalState = (function() {
	var global = getGlobal();
	if (global.__mobxInstanceCount > 0 && !global.__mobxGlobals) canMergeGlobalState = false;
	if (global.__mobxGlobals && global.__mobxGlobals.version !== new MobXGlobals().version) canMergeGlobalState = false;
	if (!canMergeGlobalState) {
		setTimeout(function() {
			if (!isolateCalled) fail$2("There are multiple, different versions of MobX active. Make sure MobX is loaded only once or use `configure({ isolateGlobalState: true })`");
		}, 1);
		return new MobXGlobals();
	} else if (global.__mobxGlobals) {
		global.__mobxInstanceCount += 1;
		if (!global.__mobxGlobals.UNCHANGED) global.__mobxGlobals.UNCHANGED = {};
		return global.__mobxGlobals;
	} else {
		global.__mobxInstanceCount = 1;
		return global.__mobxGlobals = new MobXGlobals();
	}
})();
function isolateGlobalState() {
	if (globalState.pendingReactions.length || globalState.inBatch || globalState.isRunningReactions) fail$2("isolateGlobalState should be called before MobX is running any reactions");
	isolateCalled = true;
	if (canMergeGlobalState) {
		if (--getGlobal().__mobxInstanceCount === 0) getGlobal().__mobxGlobals = void 0;
		globalState = new MobXGlobals();
	}
}
function getGlobalState() {
	return globalState;
}
function addObserver(observable, node) {
	observable.observers.add(node);
	if (observable.lowestObserverState > node.dependenciesState) observable.lowestObserverState = node.dependenciesState;
}
function removeObserver(observable, node) {
	observable.observers.delete(node);
	if (observable.observers.size === 0) queueForUnobservation(observable);
}
function queueForUnobservation(observable) {
	if (observable.isPendingUnobservation === false) {
		observable.isPendingUnobservation = true;
		globalState.pendingUnobservations.push(observable);
	}
}
/**
* Batch starts a transaction, at least for purposes of memoizing ComputedValues when nothing else does.
* During a batch `onBecomeUnobserved` will be called at most once per observable.
* Avoids unnecessary recalculations.
*/
function startBatch() {
	globalState.inBatch++;
}
function endBatch() {
	if (--globalState.inBatch === 0) {
		runReactions();
		var list = globalState.pendingUnobservations;
		for (var i = 0; i < list.length; i++) {
			var observable = list[i];
			observable.isPendingUnobservation = false;
			if (observable.observers.size === 0) {
				if (observable.isBeingObserved) {
					observable.isBeingObserved = false;
					observable.onBecomeUnobserved();
				}
				if (observable instanceof ComputedValue) observable.suspend();
			}
		}
		globalState.pendingUnobservations = [];
	}
}
function reportObserved(observable) {
	checkIfStateReadsAreAllowed(observable);
	var derivation = globalState.trackingDerivation;
	if (derivation !== null) {
		/**
		* Simple optimization, give each derivation run an unique id (runId)
		* Check if last time this observable was accessed the same runId is used
		* if this is the case, the relation is already known
		*/
		if (derivation.runId !== observable.lastAccessedBy) {
			observable.lastAccessedBy = derivation.runId;
			derivation.newObserving[derivation.unboundDepsCount++] = observable;
			if (!observable.isBeingObserved) {
				observable.isBeingObserved = true;
				observable.onBecomeObserved();
			}
		}
		return true;
	} else if (observable.observers.size === 0 && globalState.inBatch > 0) queueForUnobservation(observable);
	return false;
}
/**
* NOTE: current propagation mechanism will in case of self reruning autoruns behave unexpectedly
* It will propagate changes to observers from previous run
* It's hard or maybe impossible (with reasonable perf) to get it right with current approach
* Hopefully self reruning autoruns aren't a feature people should depend on
* Also most basic use cases should be ok
*/
function propagateChanged(observable) {
	if (observable.lowestObserverState === IDerivationState.STALE) return;
	observable.lowestObserverState = IDerivationState.STALE;
	observable.observers.forEach(function(d) {
		if (d.dependenciesState === IDerivationState.UP_TO_DATE) {
			if (d.isTracing !== TraceMode.NONE) logTraceInfo(d, observable);
			d.onBecomeStale();
		}
		d.dependenciesState = IDerivationState.STALE;
	});
}
function propagateChangeConfirmed(observable) {
	if (observable.lowestObserverState === IDerivationState.STALE) return;
	observable.lowestObserverState = IDerivationState.STALE;
	observable.observers.forEach(function(d) {
		if (d.dependenciesState === IDerivationState.POSSIBLY_STALE) d.dependenciesState = IDerivationState.STALE;
		else if (d.dependenciesState === IDerivationState.UP_TO_DATE) observable.lowestObserverState = IDerivationState.UP_TO_DATE;
	});
}
function propagateMaybeChanged(observable) {
	if (observable.lowestObserverState !== IDerivationState.UP_TO_DATE) return;
	observable.lowestObserverState = IDerivationState.POSSIBLY_STALE;
	observable.observers.forEach(function(d) {
		if (d.dependenciesState === IDerivationState.UP_TO_DATE) {
			d.dependenciesState = IDerivationState.POSSIBLY_STALE;
			if (d.isTracing !== TraceMode.NONE) logTraceInfo(d, observable);
			d.onBecomeStale();
		}
	});
}
function logTraceInfo(derivation, observable) {
	console.log("[mobx.trace] '" + derivation.name + "' is invalidated due to a change in: '" + observable.name + "'");
	if (derivation.isTracing === TraceMode.BREAK) {
		var lines = [];
		printDepTree(getDependencyTree(derivation), lines, 1);
		new Function("debugger;\n/*\nTracing '" + derivation.name + "'\n\nYou are entering this break point because derivation '" + derivation.name + "' is being traced and '" + observable.name + "' is now forcing it to update.\nJust follow the stacktrace you should now see in the devtools to see precisely what piece of your code is causing this update\nThe stackframe you are looking for is at least ~6-8 stack-frames up.\n\n" + (derivation instanceof ComputedValue ? derivation.derivation.toString().replace(/[*]\//g, "/") : "") + "\n\nThe dependencies for this derivation are:\n\n" + lines.join("\n") + "\n*/\n    ")();
	}
}
function printDepTree(tree, lines, depth) {
	if (lines.length >= 1e3) {
		lines.push("(and many more)");
		return;
	}
	lines.push("" + new Array(depth).join("	") + tree.name);
	if (tree.dependencies) tree.dependencies.forEach(function(child) {
		return printDepTree(child, lines, depth + 1);
	});
}
var Reaction = function() {
	function Reaction(name, onInvalidate, errorHandler, requiresObservable) {
		if (name === void 0) name = "Reaction@" + getNextId();
		if (requiresObservable === void 0) requiresObservable = false;
		this.name = name;
		this.onInvalidate = onInvalidate;
		this.errorHandler = errorHandler;
		this.requiresObservable = requiresObservable;
		this.observing = [];
		this.newObserving = [];
		this.dependenciesState = IDerivationState.NOT_TRACKING;
		this.diffValue = 0;
		this.runId = 0;
		this.unboundDepsCount = 0;
		this.__mapid = "#" + getNextId();
		this.isDisposed = false;
		this._isScheduled = false;
		this._isTrackPending = false;
		this._isRunning = false;
		this.isTracing = TraceMode.NONE;
	}
	Reaction.prototype.onBecomeStale = function() {
		this.schedule();
	};
	Reaction.prototype.schedule = function() {
		if (!this._isScheduled) {
			this._isScheduled = true;
			globalState.pendingReactions.push(this);
			runReactions();
		}
	};
	Reaction.prototype.isScheduled = function() {
		return this._isScheduled;
	};
	/**
	* internal, use schedule() if you intend to kick off a reaction
	*/
	Reaction.prototype.runReaction = function() {
		if (!this.isDisposed) {
			startBatch();
			this._isScheduled = false;
			if (shouldCompute(this)) {
				this._isTrackPending = true;
				try {
					this.onInvalidate();
					if (this._isTrackPending && isSpyEnabled() && true) spyReport({
						name: this.name,
						type: "scheduled-reaction"
					});
				} catch (e) {
					this.reportExceptionInDerivation(e);
				}
			}
			endBatch();
		}
	};
	Reaction.prototype.track = function(fn) {
		if (this.isDisposed) return;
		startBatch();
		var notify = isSpyEnabled();
		var startTime;
		if (notify && true) {
			startTime = Date.now();
			spyReportStart({
				name: this.name,
				type: "reaction"
			});
		}
		this._isRunning = true;
		var result = trackDerivedFunction(this, fn, void 0);
		this._isRunning = false;
		this._isTrackPending = false;
		if (this.isDisposed) clearObserving(this);
		if (isCaughtException(result)) this.reportExceptionInDerivation(result.cause);
		if (notify && true) spyReportEnd({ time: Date.now() - startTime });
		endBatch();
	};
	Reaction.prototype.reportExceptionInDerivation = function(error) {
		var _this = this;
		if (this.errorHandler) {
			this.errorHandler(error, this);
			return;
		}
		if (globalState.disableErrorBoundaries) throw error;
		var message = "[mobx] Encountered an uncaught exception that was thrown by a reaction or observer component, in: '" + this + "'";
		if (globalState.suppressReactionErrors) console.warn("[mobx] (error in reaction '" + this.name + "' suppressed, fix error of causing action below)");
		else console.error(message, error);
		if (isSpyEnabled()) spyReport({
			type: "error",
			name: this.name,
			message,
			error: "" + error
		});
		globalState.globalReactionErrorHandlers.forEach(function(f) {
			return f(error, _this);
		});
	};
	Reaction.prototype.dispose = function() {
		if (!this.isDisposed) {
			this.isDisposed = true;
			if (!this._isRunning) {
				startBatch();
				clearObserving(this);
				endBatch();
			}
		}
	};
	Reaction.prototype.getDisposer = function() {
		var r = this.dispose.bind(this);
		r[$mobx] = this;
		return r;
	};
	Reaction.prototype.toString = function() {
		return "Reaction[" + this.name + "]";
	};
	Reaction.prototype.trace = function(enterBreakPoint) {
		if (enterBreakPoint === void 0) enterBreakPoint = false;
		trace(this, enterBreakPoint);
	};
	return Reaction;
}();
/**
* Magic number alert!
* Defines within how many times a reaction is allowed to re-trigger itself
* until it is assumed that this is gonna be a never ending loop...
*/
var MAX_REACTION_ITERATIONS = 100;
var reactionScheduler = function(f) {
	return f();
};
function runReactions() {
	if (globalState.inBatch > 0 || globalState.isRunningReactions) return;
	reactionScheduler(runReactionsHelper);
}
function runReactionsHelper() {
	globalState.isRunningReactions = true;
	var allReactions = globalState.pendingReactions;
	var iterations = 0;
	while (allReactions.length > 0) {
		if (++iterations === MAX_REACTION_ITERATIONS) {
			console.error("Reaction doesn't converge to a stable state after " + MAX_REACTION_ITERATIONS + " iterations." + (" Probably there is a cycle in the reactive function: " + allReactions[0]));
			allReactions.splice(0);
		}
		var remainingReactions = allReactions.splice(0);
		for (var i = 0, l = remainingReactions.length; i < l; i++) remainingReactions[i].runReaction();
	}
	globalState.isRunningReactions = false;
}
var isReaction = createInstanceofPredicate("Reaction", Reaction);
function setReactionScheduler(fn) {
	var baseScheduler = reactionScheduler;
	reactionScheduler = function(f) {
		return fn(function() {
			return baseScheduler(f);
		});
	};
}
function isSpyEnabled() {
	return !!globalState.spyListeners.length;
}
function spyReport(event) {
	if (!globalState.spyListeners.length) return;
	var listeners = globalState.spyListeners;
	for (var i = 0, l = listeners.length; i < l; i++) listeners[i](event);
}
function spyReportStart(event) {
	spyReport(__assign$53(__assign$53({}, event), { spyReportStart: true }));
}
var END_EVENT = { spyReportEnd: true };
function spyReportEnd(change) {
	if (change) spyReport(__assign$53(__assign$53({}, change), { spyReportEnd: true }));
	else spyReport(END_EVENT);
}
function spy(listener) {
	globalState.spyListeners.push(listener);
	return once(function() {
		globalState.spyListeners = globalState.spyListeners.filter(function(l) {
			return l !== listener;
		});
	});
}
function dontReassignFields() {
	fail$2("@action fields are not reassignable");
}
function namedActionDecorator(name) {
	return function(target, prop, descriptor) {
		if (descriptor) {
			if (descriptor.get !== void 0) return fail$2("@action cannot be used with getters");
			if (descriptor.value) return {
				value: createAction(name, descriptor.value),
				enumerable: false,
				configurable: true,
				writable: true
			};
			var initializer_1 = descriptor.initializer;
			return {
				enumerable: false,
				configurable: true,
				writable: true,
				initializer: function() {
					return createAction(name, initializer_1.call(this));
				}
			};
		}
		return actionFieldDecorator(name).apply(this, arguments);
	};
}
function actionFieldDecorator(name) {
	return function(target, prop, descriptor) {
		Object.defineProperty(target, prop, {
			configurable: true,
			enumerable: false,
			get: function() {},
			set: function(value) {
				addHiddenProp(this, prop, action(name, value));
			}
		});
	};
}
function boundActionDecorator(target, propertyName, descriptor, applyToInstance) {
	if (applyToInstance === true) {
		defineBoundAction(target, propertyName, descriptor.value);
		return null;
	}
	if (descriptor) return {
		configurable: true,
		enumerable: false,
		get: function() {
			defineBoundAction(this, propertyName, descriptor.value || descriptor.initializer.call(this));
			return this[propertyName];
		},
		set: dontReassignFields
	};
	return {
		enumerable: false,
		configurable: true,
		set: function(v) {
			defineBoundAction(this, propertyName, v);
		},
		get: function() {}
	};
}
var action = function action(arg1, arg2, arg3, arg4) {
	if (arguments.length === 1 && typeof arg1 === "function") return createAction(arg1.name || "<unnamed action>", arg1);
	if (arguments.length === 2 && typeof arg2 === "function") return createAction(arg1, arg2);
	if (arguments.length === 1 && typeof arg1 === "string") return namedActionDecorator(arg1);
	if (arg4 === true) addHiddenProp(arg1, arg2, createAction(arg1.name || arg2, arg3.value, this));
	else return namedActionDecorator(arg2).apply(null, arguments);
};
action.bound = boundActionDecorator;
function runInAction(arg1, arg2) {
	var actionName = typeof arg1 === "string" ? arg1 : arg1.name || "<unnamed action>";
	var fn = typeof arg1 === "function" ? arg1 : arg2;
	invariant$1(typeof fn === "function" && fn.length === 0, "`runInAction` expects a function without arguments");
	if (typeof actionName !== "string" || !actionName) fail$2("actions should have valid names, got: '" + actionName + "'");
	return executeAction(actionName, fn, this, void 0);
}
function isAction(thing) {
	return typeof thing === "function" && thing.isMobxAction === true;
}
function defineBoundAction(target, propertyName, fn) {
	addHiddenProp(target, propertyName, createAction(propertyName, fn.bind(target)));
}
/**
* Creates a named reactive view and keeps it alive, so that the view is always
* updated if one of the dependencies changes, even when the view is not further used by something else.
* @param view The reactive view
* @returns disposer function, which can be used to stop the view from being updated in the future.
*/
function autorun(view, opts) {
	if (opts === void 0) opts = EMPTY_OBJECT;
	invariant$1(typeof view === "function", "Autorun expects a function as first argument");
	invariant$1(isAction(view) === false, "Autorun does not accept actions since actions are untrackable");
	var name = opts && opts.name || view.name || "Autorun@" + getNextId();
	var runSync = !opts.scheduler && !opts.delay;
	var reaction;
	if (runSync) reaction = new Reaction(name, function() {
		this.track(reactionRunner);
	}, opts.onError, opts.requiresObservable);
	else {
		var scheduler_1 = createSchedulerFromOptions(opts);
		var isScheduled_1 = false;
		reaction = new Reaction(name, function() {
			if (!isScheduled_1) {
				isScheduled_1 = true;
				scheduler_1(function() {
					isScheduled_1 = false;
					if (!reaction.isDisposed) reaction.track(reactionRunner);
				});
			}
		}, opts.onError, opts.requiresObservable);
	}
	function reactionRunner() {
		view(reaction);
	}
	reaction.schedule();
	return reaction.getDisposer();
}
var run = function(f) {
	return f();
};
function createSchedulerFromOptions(opts) {
	return opts.scheduler ? opts.scheduler : opts.delay ? function(f) {
		return setTimeout(f, opts.delay);
	} : run;
}
function reaction(expression, effect, opts) {
	if (opts === void 0) opts = EMPTY_OBJECT;
	invariant$1(typeof expression === "function", "First argument to reaction should be a function");
	invariant$1(typeof opts === "object", "Third argument of reactions should be an object");
	var name = opts.name || "Reaction@" + getNextId();
	var effectAction = action(name, opts.onError ? wrapErrorHandler(opts.onError, effect) : effect);
	var runSync = !opts.scheduler && !opts.delay;
	var scheduler = createSchedulerFromOptions(opts);
	var firstTime = true;
	var isScheduled = false;
	var value;
	var equals = opts.compareStructural ? comparer.structural : opts.equals || comparer.default;
	var r = new Reaction(name, function() {
		if (firstTime || runSync) reactionRunner();
		else if (!isScheduled) {
			isScheduled = true;
			scheduler(reactionRunner);
		}
	}, opts.onError, opts.requiresObservable);
	function reactionRunner() {
		isScheduled = false;
		if (r.isDisposed) return;
		var changed = false;
		r.track(function() {
			var nextValue = expression(r);
			changed = firstTime || !equals(value, nextValue);
			value = nextValue;
		});
		if (firstTime && opts.fireImmediately) effectAction(value, r);
		if (!firstTime && changed === true) effectAction(value, r);
		if (firstTime) firstTime = false;
	}
	r.schedule();
	return r.getDisposer();
}
function wrapErrorHandler(errorHandler, baseFn) {
	return function() {
		try {
			return baseFn.apply(this, arguments);
		} catch (e) {
			errorHandler.call(this, e);
		}
	};
}
function onBecomeObserved(thing, arg2, arg3) {
	return interceptHook("onBecomeObserved", thing, arg2, arg3);
}
function onBecomeUnobserved(thing, arg2, arg3) {
	return interceptHook("onBecomeUnobserved", thing, arg2, arg3);
}
function interceptHook(hook, thing, arg2, arg3) {
	var atom = typeof arg3 === "function" ? getAtom(thing, arg2) : getAtom(thing);
	var cb = typeof arg3 === "function" ? arg3 : arg2;
	var listenersKey = hook + "Listeners";
	if (atom[listenersKey]) atom[listenersKey].add(cb);
	else atom[listenersKey] = new Set([cb]);
	if (typeof atom[hook] !== "function") return fail$2("Not an atom that can be (un)observed");
	return function() {
		var hookListeners = atom[listenersKey];
		if (hookListeners) {
			hookListeners.delete(cb);
			if (hookListeners.size === 0) delete atom[listenersKey];
		}
	};
}
function configure(options) {
	var enforceActions = options.enforceActions, computedRequiresReaction = options.computedRequiresReaction, computedConfigurable = options.computedConfigurable, disableErrorBoundaries = options.disableErrorBoundaries, reactionScheduler = options.reactionScheduler, reactionRequiresObservable = options.reactionRequiresObservable, observableRequiresReaction = options.observableRequiresReaction;
	if (options.isolateGlobalState === true) isolateGlobalState();
	if (enforceActions !== void 0) {
		if (typeof enforceActions === "boolean" || enforceActions === "strict") deprecated("Deprecated value for 'enforceActions', use 'false' => '\"never\"', 'true' => '\"observed\"', '\"strict\"' => \"'always'\" instead");
		var ea = void 0;
		switch (enforceActions) {
			case true:
			case "observed":
				ea = true;
				break;
			case false:
			case "never":
				ea = false;
				break;
			case "strict":
			case "always":
				ea = "strict";
				break;
			default: fail$2("Invalid value for 'enforceActions': '" + enforceActions + "', expected 'never', 'always' or 'observed'");
		}
		globalState.enforceActions = ea;
		globalState.allowStateChanges = ea === true || ea === "strict" ? false : true;
	}
	if (computedRequiresReaction !== void 0) globalState.computedRequiresReaction = !!computedRequiresReaction;
	if (reactionRequiresObservable !== void 0) globalState.reactionRequiresObservable = !!reactionRequiresObservable;
	if (observableRequiresReaction !== void 0) {
		globalState.observableRequiresReaction = !!observableRequiresReaction;
		globalState.allowStateReads = !globalState.observableRequiresReaction;
	}
	if (computedConfigurable !== void 0) globalState.computedConfigurable = !!computedConfigurable;
	if (disableErrorBoundaries !== void 0) {
		if (disableErrorBoundaries === true) console.warn("WARNING: Debug feature only. MobX will NOT recover from errors when `disableErrorBoundaries` is enabled.");
		globalState.disableErrorBoundaries = !!disableErrorBoundaries;
	}
	if (reactionScheduler) setReactionScheduler(reactionScheduler);
}
function extendObservable(target, properties, decorators, options) {
	invariant$1(arguments.length >= 2 && arguments.length <= 4, "'extendObservable' expected 2-4 arguments");
	invariant$1(typeof target === "object", "'extendObservable' expects an object as first argument");
	invariant$1(!isObservableMap(target), "'extendObservable' should not be used on maps, use map.merge instead");
	options = asCreateObservableOptions(options);
	var defaultDecorator = getDefaultDecoratorFromObjectOptions(options);
	initializeInstance(target);
	asObservableObject(target, options.name, defaultDecorator.enhancer);
	if (properties) extendObservableObjectWithProperties(target, properties, decorators, defaultDecorator);
	return target;
}
function getDefaultDecoratorFromObjectOptions(options) {
	return options.defaultDecorator || (options.deep === false ? refDecorator : deepDecorator);
}
function extendObservableObjectWithProperties(target, properties, decorators, defaultDecorator) {
	var e_1, _a, e_2, _b;
	invariant$1(!isObservable(properties), "Extending an object with another observable (object) is not supported. Please construct an explicit propertymap, using `toJS` if need. See issue #540");
	if (decorators) {
		var keys = getPlainObjectKeys(decorators);
		try {
			for (var keys_1 = __values$3(keys), keys_1_1 = keys_1.next(); !keys_1_1.done; keys_1_1 = keys_1.next()) {
				var key = keys_1_1.value;
				if (!(key in properties)) fail$2("Trying to declare a decorator for unspecified property '" + stringifyKey(key) + "'");
			}
		} catch (e_1_1) {
			e_1 = { error: e_1_1 };
		} finally {
			try {
				if (keys_1_1 && !keys_1_1.done && (_a = keys_1.return)) _a.call(keys_1);
			} finally {
				if (e_1) throw e_1.error;
			}
		}
	}
	startBatch();
	try {
		var keys = ownKeys(properties);
		try {
			for (var keys_2 = __values$3(keys), keys_2_1 = keys_2.next(); !keys_2_1.done; keys_2_1 = keys_2.next()) {
				var key = keys_2_1.value;
				var descriptor = Object.getOwnPropertyDescriptor(properties, key);
				if (!isPlainObject(properties)) fail$2("'extendObservable' only accepts plain objects as second argument");
				if (isComputed(descriptor.value)) fail$2("Passing a 'computed' as initial property value is no longer supported by extendObservable. Use a getter or decorator instead");
				var decorator = decorators && key in decorators ? decorators[key] : descriptor.get ? computedDecorator : defaultDecorator;
				if (typeof decorator !== "function") fail$2("Not a valid decorator for '" + stringifyKey(key) + "', got: " + decorator);
				var resultDescriptor = decorator(target, key, descriptor, true);
				if (resultDescriptor) Object.defineProperty(target, key, resultDescriptor);
			}
		} catch (e_2_1) {
			e_2 = { error: e_2_1 };
		} finally {
			try {
				if (keys_2_1 && !keys_2_1.done && (_b = keys_2.return)) _b.call(keys_2);
			} finally {
				if (e_2) throw e_2.error;
			}
		}
	} finally {
		endBatch();
	}
}
function getDependencyTree(thing, property) {
	return nodeToDependencyTree(getAtom(thing, property));
}
function nodeToDependencyTree(node) {
	var result = { name: node.name };
	if (node.observing && node.observing.length > 0) result.dependencies = unique(node.observing).map(nodeToDependencyTree);
	return result;
}
function FlowCancellationError() {
	this.message = "FLOW_CANCELLED";
}
FlowCancellationError.prototype = Object.create(Error.prototype);
function _isComputed(value, property) {
	if (value === null || value === void 0) return false;
	if (property !== void 0) {
		if (isObservableObject(value) === false) return false;
		if (!value[$mobx].values.has(property)) return false;
		return isComputedValue(getAtom(value, property));
	}
	return isComputedValue(value);
}
function isComputed(value) {
	if (arguments.length > 1) return fail$2("isComputed expects only 1 argument. Use isObservableProp to inspect the observability of a property");
	return _isComputed(value);
}
function isComputedProp(value, propName) {
	if (typeof propName !== "string") return fail$2("isComputed expected a property name as second argument");
	return _isComputed(value, propName);
}
function _isObservable(value, property) {
	if (value === null || value === void 0) return false;
	if (property !== void 0) {
		if (isObservableMap(value) || isObservableArray(value)) return fail$2("isObservable(object, propertyName) is not supported for arrays and maps. Use map.has or array.length instead.");
		if (isObservableObject(value)) return value[$mobx].values.has(property);
		return false;
	}
	return isObservableObject(value) || !!value[$mobx] || isAtom(value) || isReaction(value) || isComputedValue(value);
}
function isObservable(value) {
	if (arguments.length !== 1) fail$2("isObservable expects only 1 argument. Use isObservableProp to inspect the observability of a property");
	return _isObservable(value);
}
function keys(obj) {
	if (isObservableObject(obj)) return obj[$mobx].getKeys();
	if (isObservableMap(obj)) return Array.from(obj.keys());
	if (isObservableSet(obj)) return Array.from(obj.keys());
	if (isObservableArray(obj)) return obj.map(function(_, index) {
		return index;
	});
	return fail$2("'keys()' can only be used on observable objects, arrays, sets and maps");
}
function values(obj) {
	if (isObservableObject(obj)) return keys(obj).map(function(key) {
		return obj[key];
	});
	if (isObservableMap(obj)) return keys(obj).map(function(key) {
		return obj.get(key);
	});
	if (isObservableSet(obj)) return Array.from(obj.values());
	if (isObservableArray(obj)) return obj.slice();
	return fail$2("'values()' can only be used on observable objects, arrays, sets and maps");
}
function entries(obj) {
	if (isObservableObject(obj)) return keys(obj).map(function(key) {
		return [key, obj[key]];
	});
	if (isObservableMap(obj)) return keys(obj).map(function(key) {
		return [key, obj.get(key)];
	});
	if (isObservableSet(obj)) return Array.from(obj.entries());
	if (isObservableArray(obj)) return obj.map(function(key, index) {
		return [index, key];
	});
	return fail$2("'entries()' can only be used on observable objects, arrays and maps");
}
function set(obj, key, value) {
	if (arguments.length === 2 && !isObservableSet(obj)) {
		startBatch();
		var values_1 = key;
		try {
			for (var key_1 in values_1) set(obj, key_1, values_1[key_1]);
		} finally {
			endBatch();
		}
		return;
	}
	if (isObservableObject(obj)) {
		var adm = obj[$mobx];
		if (adm.values.get(key)) adm.write(key, value);
		else adm.addObservableProp(key, value, adm.defaultEnhancer);
	} else if (isObservableMap(obj)) obj.set(key, value);
	else if (isObservableSet(obj)) obj.add(key);
	else if (isObservableArray(obj)) {
		if (typeof key !== "number") key = parseInt(key, 10);
		invariant$1(key >= 0, "Not a valid index: '" + key + "'");
		startBatch();
		if (key >= obj.length) obj.length = key + 1;
		obj[key] = value;
		endBatch();
	} else return fail$2("'set()' can only be used on observable objects, arrays and maps");
}
function observe(thing, propOrCb, cbOrFire, fireImmediately) {
	if (typeof cbOrFire === "function") return observeObservableProperty(thing, propOrCb, cbOrFire, fireImmediately);
	else return observeObservable(thing, propOrCb, cbOrFire);
}
function observeObservable(thing, listener, fireImmediately) {
	return getAdministration(thing).observe(listener, fireImmediately);
}
function observeObservableProperty(thing, property, listener, fireImmediately) {
	return getAdministration(thing, property).observe(listener, fireImmediately);
}
var defaultOptions = {
	detectCycles: true,
	exportMapsAsObjects: true,
	recurseEverything: false
};
function cache(map, key, value, options) {
	if (options.detectCycles) map.set(key, value);
	return value;
}
function toJSHelper(source, options, __alreadySeen) {
	if (!options.recurseEverything && !isObservable(source)) return source;
	if (typeof source !== "object") return source;
	if (source === null) return null;
	if (source instanceof Date) return source;
	if (isObservableValue(source)) return toJSHelper(source.get(), options, __alreadySeen);
	if (isObservable(source)) keys(source);
	if (options.detectCycles === true && source !== null && __alreadySeen.has(source)) return __alreadySeen.get(source);
	if (isObservableArray(source) || Array.isArray(source)) {
		var res_1 = cache(__alreadySeen, source, [], options);
		var toAdd = source.map(function(value) {
			return toJSHelper(value, options, __alreadySeen);
		});
		res_1.length = toAdd.length;
		for (var i = 0, l = toAdd.length; i < l; i++) res_1[i] = toAdd[i];
		return res_1;
	}
	if (isObservableSet(source) || Object.getPrototypeOf(source) === Set.prototype) if (options.exportMapsAsObjects === false) {
		var res_2 = cache(__alreadySeen, source, /* @__PURE__ */ new Set(), options);
		source.forEach(function(value) {
			res_2.add(toJSHelper(value, options, __alreadySeen));
		});
		return res_2;
	} else {
		var res_3 = cache(__alreadySeen, source, [], options);
		source.forEach(function(value) {
			res_3.push(toJSHelper(value, options, __alreadySeen));
		});
		return res_3;
	}
	if (isObservableMap(source) || Object.getPrototypeOf(source) === Map.prototype) if (options.exportMapsAsObjects === false) {
		var res_4 = cache(__alreadySeen, source, /* @__PURE__ */ new Map(), options);
		source.forEach(function(value, key) {
			res_4.set(key, toJSHelper(value, options, __alreadySeen));
		});
		return res_4;
	} else {
		var res_5 = cache(__alreadySeen, source, {}, options);
		source.forEach(function(value, key) {
			res_5[key] = toJSHelper(value, options, __alreadySeen);
		});
		return res_5;
	}
	var res = cache(__alreadySeen, source, {}, options);
	getPlainObjectKeys(source).forEach(function(key) {
		res[key] = toJSHelper(source[key], options, __alreadySeen);
	});
	return res;
}
function toJS(source, options) {
	if (typeof options === "boolean") options = { detectCycles: options };
	if (!options) options = defaultOptions;
	options.detectCycles = options.detectCycles === void 0 ? options.recurseEverything === true : options.detectCycles === true;
	var __alreadySeen;
	if (options.detectCycles) __alreadySeen = /* @__PURE__ */ new Map();
	return toJSHelper(source, options, __alreadySeen);
}
function trace() {
	var args = [];
	for (var _i = 0; _i < arguments.length; _i++) args[_i] = arguments[_i];
	var enterBreakPoint = false;
	if (typeof args[args.length - 1] === "boolean") enterBreakPoint = args.pop();
	var derivation = getAtomFromArgs(args);
	if (!derivation) return fail$2("'trace(break?)' can only be used inside a tracked computed value or a Reaction. Consider passing in the computed value or reaction explicitly");
	if (derivation.isTracing === TraceMode.NONE) console.log("[mobx.trace] '" + derivation.name + "' tracing enabled");
	derivation.isTracing = enterBreakPoint ? TraceMode.BREAK : TraceMode.LOG;
}
function getAtomFromArgs(args) {
	switch (args.length) {
		case 0: return globalState.trackingDerivation;
		case 1: return getAtom(args[0]);
		case 2: return getAtom(args[0], args[1]);
	}
}
/**
* During a transaction no views are updated until the end of the transaction.
* The transaction will be run synchronously nonetheless.
*
* @param action a function that updates some reactive state
* @returns any value that was returned by the 'action' parameter.
*/
function transaction(action, thisArg) {
	if (thisArg === void 0) thisArg = void 0;
	startBatch();
	try {
		return action.apply(thisArg);
	} finally {
		endBatch();
	}
}
function getAdm(target) {
	return target[$mobx];
}
function isPropertyKey(val) {
	return typeof val === "string" || typeof val === "number" || typeof val === "symbol";
}
var objectProxyTraps = {
	has: function(target, name) {
		if (name === $mobx || name === "constructor" || name === mobxDidRunLazyInitializersSymbol) return true;
		var adm = getAdm(target);
		if (isPropertyKey(name)) return adm.has(name);
		return name in target;
	},
	get: function(target, name) {
		if (name === $mobx || name === "constructor" || name === mobxDidRunLazyInitializersSymbol) return target[name];
		var adm = getAdm(target);
		var observable = adm.values.get(name);
		if (observable instanceof Atom) {
			var result = observable.get();
			if (result === void 0) adm.has(name);
			return result;
		}
		if (isPropertyKey(name)) adm.has(name);
		return target[name];
	},
	set: function(target, name, value) {
		if (!isPropertyKey(name)) return false;
		set(target, name, value);
		return true;
	},
	deleteProperty: function(target, name) {
		if (!isPropertyKey(name)) return false;
		getAdm(target).remove(name);
		return true;
	},
	ownKeys: function(target) {
		getAdm(target).keysAtom.reportObserved();
		return Reflect.ownKeys(target);
	},
	preventExtensions: function(target) {
		fail$2("Dynamic observable objects cannot be frozen");
		return false;
	}
};
function createDynamicObservableObject(base) {
	var proxy = new Proxy(base, objectProxyTraps);
	base[$mobx].proxy = proxy;
	return proxy;
}
function hasInterceptors(interceptable) {
	return interceptable.interceptors !== void 0 && interceptable.interceptors.length > 0;
}
function registerInterceptor(interceptable, handler) {
	var interceptors = interceptable.interceptors || (interceptable.interceptors = []);
	interceptors.push(handler);
	return once(function() {
		var idx = interceptors.indexOf(handler);
		if (idx !== -1) interceptors.splice(idx, 1);
	});
}
function interceptChange(interceptable, change) {
	var prevU = untrackedStart();
	try {
		var interceptors = __spread(interceptable.interceptors || []);
		for (var i = 0, l = interceptors.length; i < l; i++) {
			change = interceptors[i](change);
			invariant$1(!change || change.type, "Intercept handlers should return nothing or a change object");
			if (!change) break;
		}
		return change;
	} finally {
		untrackedEnd(prevU);
	}
}
function hasListeners(listenable) {
	return listenable.changeListeners !== void 0 && listenable.changeListeners.length > 0;
}
function registerListener(listenable, handler) {
	var listeners = listenable.changeListeners || (listenable.changeListeners = []);
	listeners.push(handler);
	return once(function() {
		var idx = listeners.indexOf(handler);
		if (idx !== -1) listeners.splice(idx, 1);
	});
}
function notifyListeners(listenable, change) {
	var prevU = untrackedStart();
	var listeners = listenable.changeListeners;
	if (!listeners) return;
	listeners = listeners.slice();
	for (var i = 0, l = listeners.length; i < l; i++) listeners[i](change);
	untrackedEnd(prevU);
}
var MAX_SPLICE_SIZE = 1e4;
var arrayTraps = {
	get: function(target, name) {
		if (name === $mobx) return target[$mobx];
		if (name === "length") return target[$mobx].getArrayLength();
		if (typeof name === "number") return arrayExtensions.get.call(target, name);
		if (typeof name === "string" && !isNaN(name)) return arrayExtensions.get.call(target, parseInt(name));
		if (arrayExtensions.hasOwnProperty(name)) return arrayExtensions[name];
		return target[name];
	},
	set: function(target, name, value) {
		if (name === "length") target[$mobx].setArrayLength(value);
		if (typeof name === "number") arrayExtensions.set.call(target, name, value);
		if (typeof name === "symbol" || isNaN(name)) target[name] = value;
		else arrayExtensions.set.call(target, parseInt(name), value);
		return true;
	},
	preventExtensions: function(target) {
		fail$2("Observable arrays cannot be frozen");
		return false;
	}
};
function createObservableArray(initialValues, enhancer, name, owned) {
	if (name === void 0) name = "ObservableArray@" + getNextId();
	if (owned === void 0) owned = false;
	var adm = new ObservableArrayAdministration(name, enhancer, owned);
	addHiddenFinalProp(adm.values, $mobx, adm);
	var proxy = new Proxy(adm.values, arrayTraps);
	adm.proxy = proxy;
	if (initialValues && initialValues.length) {
		var prev = allowStateChangesStart$1(true);
		adm.spliceWithArray(0, 0, initialValues);
		allowStateChangesEnd$1(prev);
	}
	return proxy;
}
var ObservableArrayAdministration = function() {
	function ObservableArrayAdministration(name, enhancer, owned) {
		this.owned = owned;
		this.values = [];
		this.proxy = void 0;
		this.lastKnownLength = 0;
		this.atom = new Atom(name || "ObservableArray@" + getNextId());
		this.enhancer = function(newV, oldV) {
			return enhancer(newV, oldV, name + "[..]");
		};
	}
	ObservableArrayAdministration.prototype.dehanceValue = function(value) {
		if (this.dehancer !== void 0) return this.dehancer(value);
		return value;
	};
	ObservableArrayAdministration.prototype.dehanceValues = function(values) {
		if (this.dehancer !== void 0 && values.length > 0) return values.map(this.dehancer);
		return values;
	};
	ObservableArrayAdministration.prototype.intercept = function(handler) {
		return registerInterceptor(this, handler);
	};
	ObservableArrayAdministration.prototype.observe = function(listener, fireImmediately) {
		if (fireImmediately === void 0) fireImmediately = false;
		if (fireImmediately) listener({
			object: this.proxy,
			type: "splice",
			index: 0,
			added: this.values.slice(),
			addedCount: this.values.length,
			removed: [],
			removedCount: 0
		});
		return registerListener(this, listener);
	};
	ObservableArrayAdministration.prototype.getArrayLength = function() {
		this.atom.reportObserved();
		return this.values.length;
	};
	ObservableArrayAdministration.prototype.setArrayLength = function(newLength) {
		if (typeof newLength !== "number" || newLength < 0) throw new Error("[mobx.array] Out of range: " + newLength);
		var currentLength = this.values.length;
		if (newLength === currentLength) return;
		else if (newLength > currentLength) {
			var newItems = new Array(newLength - currentLength);
			for (var i = 0; i < newLength - currentLength; i++) newItems[i] = void 0;
			this.spliceWithArray(currentLength, 0, newItems);
		} else this.spliceWithArray(newLength, currentLength - newLength);
	};
	ObservableArrayAdministration.prototype.updateArrayLength = function(oldLength, delta) {
		if (oldLength !== this.lastKnownLength) throw new Error("[mobx] Modification exception: the internal structure of an observable array was changed.");
		this.lastKnownLength += delta;
	};
	ObservableArrayAdministration.prototype.spliceWithArray = function(index, deleteCount, newItems) {
		var _this = this;
		checkIfStateModificationsAreAllowed(this.atom);
		var length = this.values.length;
		if (index === void 0) index = 0;
		else if (index > length) index = length;
		else if (index < 0) index = Math.max(0, length + index);
		if (arguments.length === 1) deleteCount = length - index;
		else if (deleteCount === void 0 || deleteCount === null) deleteCount = 0;
		else deleteCount = Math.max(0, Math.min(deleteCount, length - index));
		if (newItems === void 0) newItems = EMPTY_ARRAY;
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				object: this.proxy,
				type: "splice",
				index,
				removedCount: deleteCount,
				added: newItems
			});
			if (!change) return EMPTY_ARRAY;
			deleteCount = change.removedCount;
			newItems = change.added;
		}
		newItems = newItems.length === 0 ? newItems : newItems.map(function(v) {
			return _this.enhancer(v, void 0);
		});
		var lengthDelta = newItems.length - deleteCount;
		this.updateArrayLength(length, lengthDelta);
		var res = this.spliceItemsIntoValues(index, deleteCount, newItems);
		if (deleteCount !== 0 || newItems.length !== 0) this.notifyArraySplice(index, newItems, res);
		return this.dehanceValues(res);
	};
	ObservableArrayAdministration.prototype.spliceItemsIntoValues = function(index, deleteCount, newItems) {
		var _a;
		if (newItems.length < MAX_SPLICE_SIZE) return (_a = this.values).splice.apply(_a, __spread([index, deleteCount], newItems));
		else {
			var res = this.values.slice(index, index + deleteCount);
			this.values = this.values.slice(0, index).concat(newItems, this.values.slice(index + deleteCount));
			return res;
		}
	};
	ObservableArrayAdministration.prototype.notifyArrayChildUpdate = function(index, newValue, oldValue) {
		var notifySpy = !this.owned && isSpyEnabled();
		var notify = hasListeners(this);
		var change = notify || notifySpy ? {
			object: this.proxy,
			type: "update",
			index,
			newValue,
			oldValue
		} : null;
		if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), { name: this.atom.name }));
		this.atom.reportChanged();
		if (notify) notifyListeners(this, change);
		if (notifySpy && true) spyReportEnd();
	};
	ObservableArrayAdministration.prototype.notifyArraySplice = function(index, added, removed) {
		var notifySpy = !this.owned && isSpyEnabled();
		var notify = hasListeners(this);
		var change = notify || notifySpy ? {
			object: this.proxy,
			type: "splice",
			index,
			removed,
			added,
			removedCount: removed.length,
			addedCount: added.length
		} : null;
		if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), { name: this.atom.name }));
		this.atom.reportChanged();
		if (notify) notifyListeners(this, change);
		if (notifySpy && true) spyReportEnd();
	};
	return ObservableArrayAdministration;
}();
var arrayExtensions = {
	intercept: function(handler) {
		return this[$mobx].intercept(handler);
	},
	observe: function(listener, fireImmediately) {
		if (fireImmediately === void 0) fireImmediately = false;
		return this[$mobx].observe(listener, fireImmediately);
	},
	clear: function() {
		return this.splice(0);
	},
	replace: function(newItems) {
		var adm = this[$mobx];
		return adm.spliceWithArray(0, adm.values.length, newItems);
	},
	/**
	* Converts this array back to a (shallow) javascript structure.
	* For a deep clone use mobx.toJS
	*/
	toJS: function() {
		return this.slice();
	},
	toJSON: function() {
		return this.toJS();
	},
	splice: function(index, deleteCount) {
		var newItems = [];
		for (var _i = 2; _i < arguments.length; _i++) newItems[_i - 2] = arguments[_i];
		var adm = this[$mobx];
		switch (arguments.length) {
			case 0: return [];
			case 1: return adm.spliceWithArray(index);
			case 2: return adm.spliceWithArray(index, deleteCount);
		}
		return adm.spliceWithArray(index, deleteCount, newItems);
	},
	spliceWithArray: function(index, deleteCount, newItems) {
		return this[$mobx].spliceWithArray(index, deleteCount, newItems);
	},
	push: function() {
		var items = [];
		for (var _i = 0; _i < arguments.length; _i++) items[_i] = arguments[_i];
		var adm = this[$mobx];
		adm.spliceWithArray(adm.values.length, 0, items);
		return adm.values.length;
	},
	pop: function() {
		return this.splice(Math.max(this[$mobx].values.length - 1, 0), 1)[0];
	},
	shift: function() {
		return this.splice(0, 1)[0];
	},
	unshift: function() {
		var items = [];
		for (var _i = 0; _i < arguments.length; _i++) items[_i] = arguments[_i];
		var adm = this[$mobx];
		adm.spliceWithArray(0, 0, items);
		return adm.values.length;
	},
	reverse: function() {
		console.warn("[mobx] `observableArray.reverse()` will not update the array in place. Use `observableArray.slice().reverse()` to suppress this warning and perform the operation on a copy, or `observableArray.replace(observableArray.slice().reverse())` to reverse & update in place");
		var clone = this.slice();
		return clone.reverse.apply(clone, arguments);
	},
	sort: function(compareFn) {
		console.warn("[mobx] `observableArray.sort()` will not update the array in place. Use `observableArray.slice().sort()` to suppress this warning and perform the operation on a copy, or `observableArray.replace(observableArray.slice().sort())` to sort & update in place");
		var clone = this.slice();
		return clone.sort.apply(clone, arguments);
	},
	remove: function(value) {
		var adm = this[$mobx];
		var idx = adm.dehanceValues(adm.values).indexOf(value);
		if (idx > -1) {
			this.splice(idx, 1);
			return true;
		}
		return false;
	},
	get: function(index) {
		var adm = this[$mobx];
		if (adm) {
			if (index < adm.values.length) {
				adm.atom.reportObserved();
				return adm.dehanceValue(adm.values[index]);
			}
			console.warn("[mobx.array] Attempt to read an array index (" + index + ") that is out of bounds (" + adm.values.length + "). Please check length first. Out of bound indices will not be tracked by MobX");
		}
	},
	set: function(index, newValue) {
		var adm = this[$mobx];
		var values = adm.values;
		if (index < values.length) {
			checkIfStateModificationsAreAllowed(adm.atom);
			var oldValue = values[index];
			if (hasInterceptors(adm)) {
				var change = interceptChange(adm, {
					type: "update",
					object: adm.proxy,
					index,
					newValue
				});
				if (!change) return;
				newValue = change.newValue;
			}
			newValue = adm.enhancer(newValue, oldValue);
			if (newValue !== oldValue) {
				values[index] = newValue;
				adm.notifyArrayChildUpdate(index, newValue, oldValue);
			}
		} else if (index === values.length) adm.spliceWithArray(index, 0, [newValue]);
		else throw new Error("[mobx.array] Index out of bounds, " + index + " is larger than " + values.length);
	}
};
[
	"concat",
	"flat",
	"includes",
	"indexOf",
	"join",
	"lastIndexOf",
	"slice",
	"toString",
	"toLocaleString"
].forEach(function(funcName) {
	if (typeof Array.prototype[funcName] !== "function") return;
	arrayExtensions[funcName] = function() {
		var adm = this[$mobx];
		adm.atom.reportObserved();
		var dehancedValues = adm.dehanceValues(adm.values);
		return dehancedValues[funcName].apply(dehancedValues, arguments);
	};
});
[
	"every",
	"filter",
	"find",
	"findIndex",
	"flatMap",
	"forEach",
	"map",
	"some"
].forEach(function(funcName) {
	if (typeof Array.prototype[funcName] !== "function") return;
	arrayExtensions[funcName] = function(callback, thisArg) {
		var _this = this;
		var adm = this[$mobx];
		adm.atom.reportObserved();
		return adm.dehanceValues(adm.values)[funcName](function(element, index) {
			return callback.call(thisArg, element, index, _this);
		}, thisArg);
	};
});
["reduce", "reduceRight"].forEach(function(funcName) {
	arrayExtensions[funcName] = function() {
		var _this = this;
		var adm = this[$mobx];
		adm.atom.reportObserved();
		var callback = arguments[0];
		arguments[0] = function(accumulator, currentValue, index) {
			currentValue = adm.dehanceValue(currentValue);
			return callback(accumulator, currentValue, index, _this);
		};
		return adm.values[funcName].apply(adm.values, arguments);
	};
});
var isObservableArrayAdministration = createInstanceofPredicate("ObservableArrayAdministration", ObservableArrayAdministration);
function isObservableArray(thing) {
	return isObject(thing) && isObservableArrayAdministration(thing[$mobx]);
}
var _a;
var ObservableMapMarker = {};
var ObservableMap = function() {
	function ObservableMap(initialData, enhancer, name) {
		if (enhancer === void 0) enhancer = deepEnhancer;
		if (name === void 0) name = "ObservableMap@" + getNextId();
		this.enhancer = enhancer;
		this.name = name;
		this[_a] = ObservableMapMarker;
		this._keysAtom = createAtom(this.name + ".keys()");
		this[Symbol.toStringTag] = "Map";
		if (typeof Map !== "function") throw new Error("mobx.map requires Map polyfill for the current browser. Check babel-polyfill or core-js/es6/map.js");
		this._data = /* @__PURE__ */ new Map();
		this._hasMap = /* @__PURE__ */ new Map();
		this.merge(initialData);
	}
	ObservableMap.prototype._has = function(key) {
		return this._data.has(key);
	};
	ObservableMap.prototype.has = function(key) {
		var _this = this;
		if (!globalState.trackingDerivation) return this._has(key);
		var entry = this._hasMap.get(key);
		if (!entry) {
			var newEntry = entry = new ObservableValue(this._has(key), referenceEnhancer, this.name + "." + stringifyKey(key) + "?", false);
			this._hasMap.set(key, newEntry);
			onBecomeUnobserved(newEntry, function() {
				return _this._hasMap.delete(key);
			});
		}
		return entry.get();
	};
	ObservableMap.prototype.set = function(key, value) {
		var hasKey = this._has(key);
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				type: hasKey ? "update" : "add",
				object: this,
				newValue: value,
				name: key
			});
			if (!change) return this;
			value = change.newValue;
		}
		if (hasKey) this._updateValue(key, value);
		else this._addValue(key, value);
		return this;
	};
	ObservableMap.prototype.delete = function(key) {
		var _this = this;
		checkIfStateModificationsAreAllowed(this._keysAtom);
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				type: "delete",
				object: this,
				name: key
			});
			if (!change) return false;
		}
		if (this._has(key)) {
			var notifySpy = isSpyEnabled();
			var notify = hasListeners(this);
			var change = notify || notifySpy ? {
				type: "delete",
				object: this,
				oldValue: this._data.get(key).value,
				name: key
			} : null;
			if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), {
				name: this.name,
				key
			}));
			transaction(function() {
				_this._keysAtom.reportChanged();
				_this._updateHasMapEntry(key, false);
				_this._data.get(key).setNewValue(void 0);
				_this._data.delete(key);
			});
			if (notify) notifyListeners(this, change);
			if (notifySpy && true) spyReportEnd();
			return true;
		}
		return false;
	};
	ObservableMap.prototype._updateHasMapEntry = function(key, value) {
		var entry = this._hasMap.get(key);
		if (entry) entry.setNewValue(value);
	};
	ObservableMap.prototype._updateValue = function(key, newValue) {
		var observable = this._data.get(key);
		newValue = observable.prepareNewValue(newValue);
		if (newValue !== globalState.UNCHANGED) {
			var notifySpy = isSpyEnabled();
			var notify = hasListeners(this);
			var change = notify || notifySpy ? {
				type: "update",
				object: this,
				oldValue: observable.value,
				name: key,
				newValue
			} : null;
			if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), {
				name: this.name,
				key
			}));
			observable.setNewValue(newValue);
			if (notify) notifyListeners(this, change);
			if (notifySpy && true) spyReportEnd();
		}
	};
	ObservableMap.prototype._addValue = function(key, newValue) {
		var _this = this;
		checkIfStateModificationsAreAllowed(this._keysAtom);
		transaction(function() {
			var observable = new ObservableValue(newValue, _this.enhancer, _this.name + "." + stringifyKey(key), false);
			_this._data.set(key, observable);
			newValue = observable.value;
			_this._updateHasMapEntry(key, true);
			_this._keysAtom.reportChanged();
		});
		var notifySpy = isSpyEnabled();
		var notify = hasListeners(this);
		var change = notify || notifySpy ? {
			type: "add",
			object: this,
			name: key,
			newValue
		} : null;
		if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), {
			name: this.name,
			key
		}));
		if (notify) notifyListeners(this, change);
		if (notifySpy && true) spyReportEnd();
	};
	ObservableMap.prototype.get = function(key) {
		if (this.has(key)) return this.dehanceValue(this._data.get(key).get());
		return this.dehanceValue(void 0);
	};
	ObservableMap.prototype.dehanceValue = function(value) {
		if (this.dehancer !== void 0) return this.dehancer(value);
		return value;
	};
	ObservableMap.prototype.keys = function() {
		this._keysAtom.reportObserved();
		return this._data.keys();
	};
	ObservableMap.prototype.values = function() {
		var self = this;
		var keys = this.keys();
		return makeIterable({ next: function() {
			var _b = keys.next(), done = _b.done, value = _b.value;
			return {
				done,
				value: done ? void 0 : self.get(value)
			};
		} });
	};
	ObservableMap.prototype.entries = function() {
		var self = this;
		var keys = this.keys();
		return makeIterable({ next: function() {
			var _b = keys.next(), done = _b.done, value = _b.value;
			return {
				done,
				value: done ? void 0 : [value, self.get(value)]
			};
		} });
	};
	ObservableMap.prototype[_a = $mobx, Symbol.iterator] = function() {
		return this.entries();
	};
	ObservableMap.prototype.forEach = function(callback, thisArg) {
		var e_1, _b;
		try {
			for (var _c = __values$3(this), _d = _c.next(); !_d.done; _d = _c.next()) {
				var _e = __read$39(_d.value, 2), key = _e[0], value = _e[1];
				callback.call(thisArg, value, key, this);
			}
		} catch (e_1_1) {
			e_1 = { error: e_1_1 };
		} finally {
			try {
				if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
			} finally {
				if (e_1) throw e_1.error;
			}
		}
	};
	/** Merge another object into this object, returns this. */
	ObservableMap.prototype.merge = function(other) {
		var _this = this;
		if (isObservableMap(other)) other = other.toJS();
		transaction(function() {
			var prev = allowStateChangesStart$1(true);
			try {
				if (isPlainObject(other)) getPlainObjectKeys(other).forEach(function(key) {
					return _this.set(key, other[key]);
				});
				else if (Array.isArray(other)) other.forEach(function(_b) {
					var _c = __read$39(_b, 2), key = _c[0], value = _c[1];
					return _this.set(key, value);
				});
				else if (isES6Map(other)) {
					if (other.constructor !== Map) fail$2("Cannot initialize from classes that inherit from Map: " + other.constructor.name);
					other.forEach(function(value, key) {
						return _this.set(key, value);
					});
				} else if (other !== null && other !== void 0) fail$2("Cannot initialize map from " + other);
			} finally {
				allowStateChangesEnd$1(prev);
			}
		});
		return this;
	};
	ObservableMap.prototype.clear = function() {
		var _this = this;
		transaction(function() {
			untracked(function() {
				var e_2, _b;
				try {
					for (var _c = __values$3(_this.keys()), _d = _c.next(); !_d.done; _d = _c.next()) {
						var key = _d.value;
						_this.delete(key);
					}
				} catch (e_2_1) {
					e_2 = { error: e_2_1 };
				} finally {
					try {
						if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
					} finally {
						if (e_2) throw e_2.error;
					}
				}
			});
		});
	};
	ObservableMap.prototype.replace = function(values) {
		var _this = this;
		transaction(function() {
			var e_3, _b, e_4, _c;
			var replacementMap = convertToMap(values);
			var orderedData = /* @__PURE__ */ new Map();
			var keysReportChangedCalled = false;
			try {
				for (var _d = __values$3(_this._data.keys()), _e = _d.next(); !_e.done; _e = _d.next()) {
					var key = _e.value;
					if (!replacementMap.has(key)) if (_this.delete(key)) keysReportChangedCalled = true;
					else {
						var value = _this._data.get(key);
						orderedData.set(key, value);
					}
				}
			} catch (e_3_1) {
				e_3 = { error: e_3_1 };
			} finally {
				try {
					if (_e && !_e.done && (_b = _d.return)) _b.call(_d);
				} finally {
					if (e_3) throw e_3.error;
				}
			}
			try {
				for (var _f = __values$3(replacementMap.entries()), _g = _f.next(); !_g.done; _g = _f.next()) {
					var _h = __read$39(_g.value, 2), key = _h[0], value = _h[1];
					var keyExisted = _this._data.has(key);
					_this.set(key, value);
					if (_this._data.has(key)) {
						var value_1 = _this._data.get(key);
						orderedData.set(key, value_1);
						if (!keyExisted) keysReportChangedCalled = true;
					}
				}
			} catch (e_4_1) {
				e_4 = { error: e_4_1 };
			} finally {
				try {
					if (_g && !_g.done && (_c = _f.return)) _c.call(_f);
				} finally {
					if (e_4) throw e_4.error;
				}
			}
			if (!keysReportChangedCalled) if (_this._data.size !== orderedData.size) _this._keysAtom.reportChanged();
			else {
				var iter1 = _this._data.keys();
				var iter2 = orderedData.keys();
				var next1 = iter1.next();
				var next2 = iter2.next();
				while (!next1.done) {
					if (next1.value !== next2.value) {
						_this._keysAtom.reportChanged();
						break;
					}
					next1 = iter1.next();
					next2 = iter2.next();
				}
			}
			_this._data = orderedData;
		});
		return this;
	};
	Object.defineProperty(ObservableMap.prototype, "size", {
		get: function() {
			this._keysAtom.reportObserved();
			return this._data.size;
		},
		enumerable: true,
		configurable: true
	});
	/**
	* Returns a plain object that represents this map.
	* Note that all the keys being stringified.
	* If there are duplicating keys after converting them to strings, behaviour is undetermined.
	*/
	ObservableMap.prototype.toPOJO = function() {
		var e_5, _b;
		var res = {};
		try {
			for (var _c = __values$3(this), _d = _c.next(); !_d.done; _d = _c.next()) {
				var _e = __read$39(_d.value, 2), key = _e[0], value = _e[1];
				res[typeof key === "symbol" ? key : stringifyKey(key)] = value;
			}
		} catch (e_5_1) {
			e_5 = { error: e_5_1 };
		} finally {
			try {
				if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
			} finally {
				if (e_5) throw e_5.error;
			}
		}
		return res;
	};
	/**
	* Returns a shallow non observable object clone of this map.
	* Note that the values migth still be observable. For a deep clone use mobx.toJS.
	*/
	ObservableMap.prototype.toJS = function() {
		return new Map(this);
	};
	ObservableMap.prototype.toJSON = function() {
		return this.toPOJO();
	};
	ObservableMap.prototype.toString = function() {
		var _this = this;
		return this.name + "[{ " + Array.from(this.keys()).map(function(key) {
			return stringifyKey(key) + ": " + ("" + _this.get(key));
		}).join(", ") + " }]";
	};
	/**
	* Observes this object. Triggers for the events 'add', 'update' and 'delete'.
	* See: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/observe
	* for callback details
	*/
	ObservableMap.prototype.observe = function(listener, fireImmediately) {
		invariant$1(fireImmediately !== true, "`observe` doesn't support fireImmediately=true in combination with maps.");
		return registerListener(this, listener);
	};
	ObservableMap.prototype.intercept = function(handler) {
		return registerInterceptor(this, handler);
	};
	return ObservableMap;
}();
var isObservableMap = createInstanceofPredicate("ObservableMap", ObservableMap);
var _a$1;
var ObservableSetMarker = {};
var ObservableSet = function() {
	function ObservableSet(initialData, enhancer, name) {
		if (enhancer === void 0) enhancer = deepEnhancer;
		if (name === void 0) name = "ObservableSet@" + getNextId();
		this.name = name;
		this[_a$1] = ObservableSetMarker;
		this._data = /* @__PURE__ */ new Set();
		this._atom = createAtom(this.name);
		this[Symbol.toStringTag] = "Set";
		if (typeof Set !== "function") throw new Error("mobx.set requires Set polyfill for the current browser. Check babel-polyfill or core-js/es6/set.js");
		this.enhancer = function(newV, oldV) {
			return enhancer(newV, oldV, name);
		};
		if (initialData) this.replace(initialData);
	}
	ObservableSet.prototype.dehanceValue = function(value) {
		if (this.dehancer !== void 0) return this.dehancer(value);
		return value;
	};
	ObservableSet.prototype.clear = function() {
		var _this = this;
		transaction(function() {
			untracked(function() {
				var e_1, _b;
				try {
					for (var _c = __values$3(_this._data.values()), _d = _c.next(); !_d.done; _d = _c.next()) {
						var value = _d.value;
						_this.delete(value);
					}
				} catch (e_1_1) {
					e_1 = { error: e_1_1 };
				} finally {
					try {
						if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
					} finally {
						if (e_1) throw e_1.error;
					}
				}
			});
		});
	};
	ObservableSet.prototype.forEach = function(callbackFn, thisArg) {
		var e_2, _b;
		try {
			for (var _c = __values$3(this), _d = _c.next(); !_d.done; _d = _c.next()) {
				var value = _d.value;
				callbackFn.call(thisArg, value, value, this);
			}
		} catch (e_2_1) {
			e_2 = { error: e_2_1 };
		} finally {
			try {
				if (_d && !_d.done && (_b = _c.return)) _b.call(_c);
			} finally {
				if (e_2) throw e_2.error;
			}
		}
	};
	Object.defineProperty(ObservableSet.prototype, "size", {
		get: function() {
			this._atom.reportObserved();
			return this._data.size;
		},
		enumerable: true,
		configurable: true
	});
	ObservableSet.prototype.add = function(value) {
		var _this = this;
		checkIfStateModificationsAreAllowed(this._atom);
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				type: "add",
				object: this,
				newValue: value
			});
			if (!change) return this;
		}
		if (!this.has(value)) {
			transaction(function() {
				_this._data.add(_this.enhancer(value, void 0));
				_this._atom.reportChanged();
			});
			var notifySpy = isSpyEnabled();
			var notify = hasListeners(this);
			var change = notify || notifySpy ? {
				type: "add",
				object: this,
				newValue: value
			} : null;
			if (notifySpy && true) spyReportStart(change);
			if (notify) notifyListeners(this, change);
			if (notifySpy && true) spyReportEnd();
		}
		return this;
	};
	ObservableSet.prototype.delete = function(value) {
		var _this = this;
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				type: "delete",
				object: this,
				oldValue: value
			});
			if (!change) return false;
		}
		if (this.has(value)) {
			var notifySpy = isSpyEnabled();
			var notify = hasListeners(this);
			var change = notify || notifySpy ? {
				type: "delete",
				object: this,
				oldValue: value
			} : null;
			if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), { name: this.name }));
			transaction(function() {
				_this._atom.reportChanged();
				_this._data.delete(value);
			});
			if (notify) notifyListeners(this, change);
			if (notifySpy && true) spyReportEnd();
			return true;
		}
		return false;
	};
	ObservableSet.prototype.has = function(value) {
		this._atom.reportObserved();
		return this._data.has(this.dehanceValue(value));
	};
	ObservableSet.prototype.entries = function() {
		var nextIndex = 0;
		var keys = Array.from(this.keys());
		var values = Array.from(this.values());
		return makeIterable({ next: function() {
			var index = nextIndex;
			nextIndex += 1;
			return index < values.length ? {
				value: [keys[index], values[index]],
				done: false
			} : { done: true };
		} });
	};
	ObservableSet.prototype.keys = function() {
		return this.values();
	};
	ObservableSet.prototype.values = function() {
		this._atom.reportObserved();
		var self = this;
		var nextIndex = 0;
		var observableValues = Array.from(this._data.values());
		return makeIterable({ next: function() {
			return nextIndex < observableValues.length ? {
				value: self.dehanceValue(observableValues[nextIndex++]),
				done: false
			} : { done: true };
		} });
	};
	ObservableSet.prototype.replace = function(other) {
		var _this = this;
		if (isObservableSet(other)) other = other.toJS();
		transaction(function() {
			var prev = allowStateChangesStart$1(true);
			try {
				if (Array.isArray(other)) {
					_this.clear();
					other.forEach(function(value) {
						return _this.add(value);
					});
				} else if (isES6Set(other)) {
					_this.clear();
					other.forEach(function(value) {
						return _this.add(value);
					});
				} else if (other !== null && other !== void 0) fail$2("Cannot initialize set from " + other);
			} finally {
				allowStateChangesEnd$1(prev);
			}
		});
		return this;
	};
	ObservableSet.prototype.observe = function(listener, fireImmediately) {
		invariant$1(fireImmediately !== true, "`observe` doesn't support fireImmediately=true in combination with sets.");
		return registerListener(this, listener);
	};
	ObservableSet.prototype.intercept = function(handler) {
		return registerInterceptor(this, handler);
	};
	ObservableSet.prototype.toJS = function() {
		return new Set(this);
	};
	ObservableSet.prototype.toString = function() {
		return this.name + "[ " + Array.from(this).join(", ") + " ]";
	};
	ObservableSet.prototype[_a$1 = $mobx, Symbol.iterator] = function() {
		return this.values();
	};
	return ObservableSet;
}();
var isObservableSet = createInstanceofPredicate("ObservableSet", ObservableSet);
var ObservableObjectAdministration = function() {
	function ObservableObjectAdministration(target, values, name, defaultEnhancer) {
		if (values === void 0) values = /* @__PURE__ */ new Map();
		this.target = target;
		this.values = values;
		this.name = name;
		this.defaultEnhancer = defaultEnhancer;
		this.keysAtom = new Atom(name + ".keys");
	}
	ObservableObjectAdministration.prototype.read = function(key) {
		return this.values.get(key).get();
	};
	ObservableObjectAdministration.prototype.write = function(key, newValue) {
		var instance = this.target;
		var observable = this.values.get(key);
		if (observable instanceof ComputedValue) {
			observable.set(newValue);
			return;
		}
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				type: "update",
				object: this.proxy || instance,
				name: key,
				newValue
			});
			if (!change) return;
			newValue = change.newValue;
		}
		newValue = observable.prepareNewValue(newValue);
		if (newValue !== globalState.UNCHANGED) {
			var notify = hasListeners(this);
			var notifySpy = isSpyEnabled();
			var change = notify || notifySpy ? {
				type: "update",
				object: this.proxy || instance,
				oldValue: observable.value,
				name: key,
				newValue
			} : null;
			if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), {
				name: this.name,
				key
			}));
			observable.setNewValue(newValue);
			if (notify) notifyListeners(this, change);
			if (notifySpy && true) spyReportEnd();
		}
	};
	ObservableObjectAdministration.prototype.has = function(key) {
		var map = this.pendingKeys || (this.pendingKeys = /* @__PURE__ */ new Map());
		var entry = map.get(key);
		if (entry) return entry.get();
		else {
			entry = new ObservableValue(!!this.values.get(key), referenceEnhancer, this.name + "." + stringifyKey(key) + "?", false);
			map.set(key, entry);
			return entry.get();
		}
	};
	ObservableObjectAdministration.prototype.addObservableProp = function(propName, newValue, enhancer) {
		if (enhancer === void 0) enhancer = this.defaultEnhancer;
		var target = this.target;
		assertPropertyConfigurable(target, propName);
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				object: this.proxy || target,
				name: propName,
				type: "add",
				newValue
			});
			if (!change) return;
			newValue = change.newValue;
		}
		var observable = new ObservableValue(newValue, enhancer, this.name + "." + stringifyKey(propName), false);
		this.values.set(propName, observable);
		newValue = observable.value;
		Object.defineProperty(target, propName, generateObservablePropConfig(propName));
		this.notifyPropertyAddition(propName, newValue);
	};
	ObservableObjectAdministration.prototype.addComputedProp = function(propertyOwner, propName, options) {
		var target = this.target;
		options.name = options.name || this.name + "." + stringifyKey(propName);
		this.values.set(propName, new ComputedValue(options));
		if (propertyOwner === target || isPropertyConfigurable(propertyOwner, propName)) Object.defineProperty(propertyOwner, propName, generateComputedPropConfig(propName));
	};
	ObservableObjectAdministration.prototype.remove = function(key) {
		if (!this.values.has(key)) return;
		var target = this.target;
		if (hasInterceptors(this)) {
			var change = interceptChange(this, {
				object: this.proxy || target,
				name: key,
				type: "remove"
			});
			if (!change) return;
		}
		try {
			startBatch();
			var notify = hasListeners(this);
			var notifySpy = isSpyEnabled();
			var oldObservable = this.values.get(key);
			var oldValue = oldObservable && oldObservable.get();
			oldObservable && oldObservable.set(void 0);
			this.keysAtom.reportChanged();
			this.values.delete(key);
			if (this.pendingKeys) {
				var entry = this.pendingKeys.get(key);
				if (entry) entry.set(false);
			}
			delete this.target[key];
			var change = notify || notifySpy ? {
				type: "remove",
				object: this.proxy || target,
				oldValue,
				name: key
			} : null;
			if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), {
				name: this.name,
				key
			}));
			if (notify) notifyListeners(this, change);
			if (notifySpy && true) spyReportEnd();
		} finally {
			endBatch();
		}
	};
	ObservableObjectAdministration.prototype.illegalAccess = function(owner, propName) {
		/**
		* This happens if a property is accessed through the prototype chain, but the property was
		* declared directly as own property on the prototype.
		*
		* E.g.:
		* class A {
		* }
		* extendObservable(A.prototype, { x: 1 })
		*
		* classB extens A {
		* }
		* console.log(new B().x)
		*
		* It is unclear whether the property should be considered 'static' or inherited.
		* Either use `console.log(A.x)`
		* or: decorate(A, { x: observable })
		*
		* When using decorate, the property will always be redeclared as own property on the actual instance
		*/
		console.warn("Property '" + propName + "' of '" + owner + "' was accessed through the prototype chain. Use 'decorate' instead to declare the prop or access it statically through it's owner");
	};
	/**
	* Observes this object. Triggers for the events 'add', 'update' and 'delete'.
	* See: https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Object/observe
	* for callback details
	*/
	ObservableObjectAdministration.prototype.observe = function(callback, fireImmediately) {
		invariant$1(fireImmediately !== true, "`observe` doesn't support the fire immediately property for observable objects.");
		return registerListener(this, callback);
	};
	ObservableObjectAdministration.prototype.intercept = function(handler) {
		return registerInterceptor(this, handler);
	};
	ObservableObjectAdministration.prototype.notifyPropertyAddition = function(key, newValue) {
		var notify = hasListeners(this);
		var notifySpy = isSpyEnabled();
		var change = notify || notifySpy ? {
			type: "add",
			object: this.proxy || this.target,
			name: key,
			newValue
		} : null;
		if (notifySpy && true) spyReportStart(__assign$53(__assign$53({}, change), {
			name: this.name,
			key
		}));
		if (notify) notifyListeners(this, change);
		if (notifySpy && true) spyReportEnd();
		if (this.pendingKeys) {
			var entry = this.pendingKeys.get(key);
			if (entry) entry.set(true);
		}
		this.keysAtom.reportChanged();
	};
	ObservableObjectAdministration.prototype.getKeys = function() {
		var e_1, _a;
		this.keysAtom.reportObserved();
		var res = [];
		try {
			for (var _b = __values$3(this.values), _c = _b.next(); !_c.done; _c = _b.next()) {
				var _d = __read$39(_c.value, 2), key = _d[0];
				if (_d[1] instanceof ObservableValue) res.push(key);
			}
		} catch (e_1_1) {
			e_1 = { error: e_1_1 };
		} finally {
			try {
				if (_c && !_c.done && (_a = _b.return)) _a.call(_b);
			} finally {
				if (e_1) throw e_1.error;
			}
		}
		return res;
	};
	return ObservableObjectAdministration;
}();
function asObservableObject(target, name, defaultEnhancer) {
	if (name === void 0) name = "";
	if (defaultEnhancer === void 0) defaultEnhancer = deepEnhancer;
	if (Object.prototype.hasOwnProperty.call(target, $mobx)) return target[$mobx];
	invariant$1(Object.isExtensible(target), "Cannot make the designated object observable; it is not extensible");
	if (!isPlainObject(target)) name = (target.constructor.name || "ObservableObject") + "@" + getNextId();
	if (!name) name = "ObservableObject@" + getNextId();
	var adm = new ObservableObjectAdministration(target, /* @__PURE__ */ new Map(), stringifyKey(name), defaultEnhancer);
	addHiddenProp(target, $mobx, adm);
	return adm;
}
var observablePropertyConfigs = Object.create(null);
var computedPropertyConfigs = Object.create(null);
function generateObservablePropConfig(propName) {
	return observablePropertyConfigs[propName] || (observablePropertyConfigs[propName] = {
		configurable: true,
		enumerable: true,
		get: function() {
			return this[$mobx].read(propName);
		},
		set: function(v) {
			this[$mobx].write(propName, v);
		}
	});
}
function getAdministrationForComputedPropOwner(owner) {
	var adm = owner[$mobx];
	if (!adm) {
		initializeInstance(owner);
		return owner[$mobx];
	}
	return adm;
}
function generateComputedPropConfig(propName) {
	return computedPropertyConfigs[propName] || (computedPropertyConfigs[propName] = {
		configurable: globalState.computedConfigurable,
		enumerable: false,
		get: function() {
			return getAdministrationForComputedPropOwner(this).read(propName);
		},
		set: function(v) {
			getAdministrationForComputedPropOwner(this).write(propName, v);
		}
	});
}
var isObservableObjectAdministration = createInstanceofPredicate("ObservableObjectAdministration", ObservableObjectAdministration);
function isObservableObject(thing) {
	if (isObject(thing)) {
		initializeInstance(thing);
		return isObservableObjectAdministration(thing[$mobx]);
	}
	return false;
}
function getAtom(thing, property) {
	if (typeof thing === "object" && thing !== null) {
		if (isObservableArray(thing)) {
			if (property !== void 0) fail$2("It is not possible to get index atoms from arrays");
			return thing[$mobx].atom;
		}
		if (isObservableSet(thing)) return thing[$mobx];
		if (isObservableMap(thing)) {
			var anyThing = thing;
			if (property === void 0) return anyThing._keysAtom;
			var observable = anyThing._data.get(property) || anyThing._hasMap.get(property);
			if (!observable) fail$2("the entry '" + property + "' does not exist in the observable map '" + getDebugName(thing) + "'");
			return observable;
		}
		initializeInstance(thing);
		if (property && !thing[$mobx]) thing[property];
		if (isObservableObject(thing)) {
			if (!property) return fail$2("please specify a property");
			var observable = thing[$mobx].values.get(property);
			if (!observable) fail$2("no observable property '" + property + "' found on the observable object '" + getDebugName(thing) + "'");
			return observable;
		}
		if (isAtom(thing) || isComputedValue(thing) || isReaction(thing)) return thing;
	} else if (typeof thing === "function") {
		if (isReaction(thing[$mobx])) return thing[$mobx];
	}
	return fail$2("Cannot obtain atom from " + thing);
}
function getAdministration(thing, property) {
	if (!thing) fail$2("Expecting some object");
	if (property !== void 0) return getAdministration(getAtom(thing, property));
	if (isAtom(thing) || isComputedValue(thing) || isReaction(thing)) return thing;
	if (isObservableMap(thing) || isObservableSet(thing)) return thing;
	initializeInstance(thing);
	if (thing[$mobx]) return thing[$mobx];
	fail$2("Cannot obtain administration from " + thing);
}
function getDebugName(thing, property) {
	var named;
	if (property !== void 0) named = getAtom(thing, property);
	else if (isObservableObject(thing) || isObservableMap(thing) || isObservableSet(thing)) named = getAdministration(thing);
	else named = getAtom(thing);
	return named.name;
}
var toString = Object.prototype.toString;
function deepEqual(a, b, depth) {
	if (depth === void 0) depth = -1;
	return eq(a, b, depth);
}
function eq(a, b, depth, aStack, bStack) {
	if (a === b) return a !== 0 || 1 / a === 1 / b;
	if (a == null || b == null) return false;
	if (a !== a) return b !== b;
	var type = typeof a;
	if (type !== "function" && type !== "object" && typeof b != "object") return false;
	var className = toString.call(a);
	if (className !== toString.call(b)) return false;
	switch (className) {
		case "[object RegExp]":
		case "[object String]": return "" + a === "" + b;
		case "[object Number]":
			if (+a !== +a) return +b !== +b;
			return +a === 0 ? 1 / +a === 1 / b : +a === +b;
		case "[object Date]":
		case "[object Boolean]": return +a === +b;
		case "[object Symbol]": return typeof Symbol !== "undefined" && Symbol.valueOf.call(a) === Symbol.valueOf.call(b);
		case "[object Map]":
		case "[object Set]":
			if (depth >= 0) depth++;
			break;
	}
	a = unwrap(a);
	b = unwrap(b);
	var areArrays = className === "[object Array]";
	if (!areArrays) {
		if (typeof a != "object" || typeof b != "object") return false;
		var aCtor = a.constructor, bCtor = b.constructor;
		if (aCtor !== bCtor && !(typeof aCtor === "function" && aCtor instanceof aCtor && typeof bCtor === "function" && bCtor instanceof bCtor) && "constructor" in a && "constructor" in b) return false;
	}
	if (depth === 0) return false;
	else if (depth < 0) depth = -1;
	aStack = aStack || [];
	bStack = bStack || [];
	var length = aStack.length;
	while (length--) if (aStack[length] === a) return bStack[length] === b;
	aStack.push(a);
	bStack.push(b);
	if (areArrays) {
		length = a.length;
		if (length !== b.length) return false;
		while (length--) if (!eq(a[length], b[length], depth - 1, aStack, bStack)) return false;
	} else {
		var keys = Object.keys(a);
		var key = void 0;
		length = keys.length;
		if (Object.keys(b).length !== length) return false;
		while (length--) {
			key = keys[length];
			if (!(has$1(b, key) && eq(a[key], b[key], depth - 1, aStack, bStack))) return false;
		}
	}
	aStack.pop();
	bStack.pop();
	return true;
}
function unwrap(a) {
	if (isObservableArray(a)) return a.slice();
	if (isES6Map(a) || isObservableMap(a)) return Array.from(a.entries());
	if (isES6Set(a) || isObservableSet(a)) return Array.from(a.entries());
	return a;
}
function has$1(a, key) {
	return Object.prototype.hasOwnProperty.call(a, key);
}
function makeIterable(iterator) {
	iterator[Symbol.iterator] = getSelf;
	return iterator;
}
function getSelf() {
	return this;
}
/**
* (c) Michel Weststrate 2015 - 2018
* MIT Licensed
*
* Welcome to the mobx sources! To get an global overview of how MobX internally works,
* this is a good place to start:
* https://medium.com/@mweststrate/becoming-fully-reactive-an-in-depth-explanation-of-mobservable-55995262a254#.xvbh6qd74
*
* Source folders:
* ===============
*
* - api/     Most of the public static methods exposed by the module can be found here.
* - core/    Implementation of the MobX algorithm; atoms, derivations, reactions, dependency trees, optimizations. Cool stuff can be found here.
* - types/   All the magic that is need to have observable objects, arrays and values is in this folder. Including the modifiers like `asFlat`.
* - utils/   Utility stuff.
*
*/
if (typeof Proxy === "undefined" || typeof Symbol === "undefined") throw new Error("[mobx] MobX 5+ requires Proxy and Symbol objects. If your environment doesn't support Symbol or Proxy objects, please downgrade to MobX 4. For React Native Android, consider upgrading JSCore.");
(function() {
	function testCodeMinification() {}
	if (testCodeMinification.name !== "testCodeMinification" && typeof process !== "undefined" && process.env.IGNORE_MOBX_MINIFY_WARNING !== "true") {
		var varName = [
			"process",
			"env",
			"NODE_ENV"
		].join(".");
		console.warn("[mobx] you are running a minified build, but '" + varName + "' was not set to 'production' in your bundler. This results in an unnecessarily large and slow bundle");
	}
})();
if (typeof __MOBX_DEVTOOLS_GLOBAL_HOOK__ === "object") __MOBX_DEVTOOLS_GLOBAL_HOOK__.injectMobx({
	spy,
	extras: { getDebugName },
	$mobx
});
//#endregion
//#region node_modules/mobx-preact/lib/index.module.js
function isStateless(component) {
	return !(component.prototype && component.prototype.render) && !C.isPrototypeOf(component);
}
function makeDisplayName(component) {
	var _ref = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {}, _ref$prefix = _ref.prefix, prefix = _ref$prefix === void 0 ? "" : _ref$prefix, _ref$suffix = _ref.suffix, suffix = _ref$suffix === void 0 ? "" : _ref$suffix;
	return prefix + (component.displayName || component.name || component.constructor && component.constructor.name || "<component>") + suffix;
}
var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function(obj) {
	return typeof obj;
} : function(obj) {
	return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
};
var classCallCheck = function(instance, Constructor) {
	if (!(instance instanceof Constructor)) throw new TypeError("Cannot call a class as a function");
};
var createClass = function() {
	function defineProperties(target, props) {
		for (var i = 0; i < props.length; i++) {
			var descriptor = props[i];
			descriptor.enumerable = descriptor.enumerable || false;
			descriptor.configurable = true;
			if ("value" in descriptor) descriptor.writable = true;
			Object.defineProperty(target, descriptor.key, descriptor);
		}
	}
	return function(Constructor, protoProps, staticProps) {
		if (protoProps) defineProperties(Constructor.prototype, protoProps);
		if (staticProps) defineProperties(Constructor, staticProps);
		return Constructor;
	};
}();
var inherits = function(subClass, superClass) {
	if (typeof superClass !== "function" && superClass !== null) throw new TypeError("Super expression must either be null or a function, not " + typeof superClass);
	subClass.prototype = Object.create(superClass && superClass.prototype, { constructor: {
		value: subClass,
		enumerable: false,
		writable: true,
		configurable: true
	} });
	if (superClass) Object.setPrototypeOf ? Object.setPrototypeOf(subClass, superClass) : subClass.__proto__ = superClass;
};
var possibleConstructorReturn = function(self, call) {
	if (!self) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
	return call && (typeof call === "object" || typeof call === "function") ? call : self;
};
var isUsingStaticRendering = false;
var logger = console;
/**
Workaround

allowStateChanges from mobX must be patched so that props, state and args are passed to the render() function
*/
function allowStateChangesStart(allowStateChanges) {
	var prev = getGlobalState().allowStateChanges;
	getGlobalState().allowStateChanges = allowStateChanges;
	return prev;
}
function allowStateChangesEnd(prev) {
	getGlobalState().allowStateChanges = prev;
}
function allowStateChanges(allowStateChanges, func, props, state, context) {
	var prev = allowStateChangesStart(allowStateChanges);
	var res = void 0;
	try {
		res = func(props, state, context);
	} finally {
		allowStateChangesEnd(prev);
	}
	return res;
}
/**
* Utilities
*/
function patch(target, funcName) {
	var runMixinFirst = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : false;
	var base = target[funcName];
	var mixinFunc = reactiveMixin[funcName];
	target[funcName] = !base ? mixinFunc : runMixinFirst === true ? function() {
		mixinFunc.apply(this, arguments);
		base.apply(this, arguments);
	} : function() {
		base.apply(this, arguments);
		mixinFunc.apply(this, arguments);
	};
}
function isObjectShallowModified(prev, next) {
	if (null == prev || null == next || (typeof prev === "undefined" ? "undefined" : _typeof(prev)) !== "object" || (typeof next === "undefined" ? "undefined" : _typeof(next)) !== "object") return prev !== next;
	var keys = Object.keys(prev);
	if (keys.length !== Object.keys(next).length) return true;
	var key = void 0;
	for (var i = keys.length - 1; key = keys[i]; i--) if (next[key] !== prev[key]) return true;
	return false;
}
/**
* ReactiveMixin
*/
var reactiveMixin = {
	componentWillMount: function componentWillMount() {
		var _this = this;
		if (isUsingStaticRendering === true) return;
		var initialName = makeDisplayName(this);
		/**
		* If props are shallowly modified, react will render anyway,
		* so atom.reportChanged() should not result in yet another re-render
		*/
		var skipRender = false;
		/**
		* forceUpdate will re-assign this.props. We don't want that to cause a loop,
		* so detect these changes
		*/
		var isForcingUpdate = false;
		function makePropertyObservableReference(propName) {
			var valueHolder = this[propName];
			var atom = createAtom("reactive " + propName);
			Object.defineProperty(this, propName, {
				configurable: true,
				enumerable: true,
				get: function get$$1() {
					atom.reportObserved();
					return valueHolder;
				},
				set: function set$$1(v) {
					if (!isForcingUpdate && isObjectShallowModified(valueHolder, v)) {
						valueHolder = v;
						skipRender = true;
						atom.reportChanged();
						skipRender = false;
					} else valueHolder = v;
				}
			});
		}
		makePropertyObservableReference.call(this, "props");
		makePropertyObservableReference.call(this, "state");
		var baseRender = this.render.bind(this);
		var reaction = null;
		var isRenderingPending = false;
		var initialRender = function initialRender() {
			reaction = new Reaction(initialName + ".render()", function() {
				if (!isRenderingPending) {
					isRenderingPending = true;
					if (typeof _this.componentWillReact === "function") _this.componentWillReact();
					if (_this.__$mobxIsUnmounted !== true) {
						var hasError = true;
						try {
							isForcingUpdate = true;
							if (!skipRender) C.prototype.forceUpdate.call(_this);
							hasError = false;
						} finally {
							isForcingUpdate = false;
							if (hasError) reaction.dispose();
						}
					}
				}
			});
			reaction.reactComponent = _this;
			reactiveRender.$mobx = reaction;
			_this.render = reactiveRender;
			return reactiveRender(_this.props, _this.state, _this.context);
		};
		var reactiveRender = function reactiveRender(props, state, context) {
			isRenderingPending = false;
			var exception = void 0;
			var rendering = void 0;
			reaction.track(function() {
				try {
					rendering = allowStateChanges(false, baseRender, props, state, context);
				} catch (e) {
					exception = e;
				}
			});
			if (exception) throw exception;
			return rendering;
		};
		this.render = initialRender;
	},
	componentWillUnmount: function componentWillUnmount() {
		if (isUsingStaticRendering === true) return;
		this.render.$mobx && this.render.$mobx.dispose();
		this.__$mobxIsUnmounted = true;
	},
	componentDidMount: function componentDidMount() {},
	componentDidUpdate: function componentDidUpdate() {},
	shouldComponentUpdate: function shouldComponentUpdate(nextProps, nextState) {
		if (isUsingStaticRendering) logger.warn("[mobx-preact] It seems that a re-rendering of a React component is triggered while in static (server-side) mode. Please make sure components are rendered only once server-side.");
		if (this.state !== nextState) return true;
		return isObjectShallowModified(this.props, nextProps);
	}
};
/**
* Observer function / decorator
*/
function observer$1(componentClass) {
	if (arguments.length > 1) logger.warn("Mobx observer: Using observer to inject stores is not supported. Use `@connect([\"store1\", \"store2\"]) ComponentClass instead or preferably, use `@inject(\"store1\", \"store2\") @observer ComponentClass` or `inject(\"store1\", \"store2\")(observer(componentClass))``");
	if (componentClass.isMobxInjector === true) logger.warn("Mobx observer: You are trying to use 'observer' on a component that already has 'inject'. Please apply 'observer' before applying 'inject'");
	if (isStateless(componentClass)) {
		var _class, _temp;
		return observer$1((_temp = _class = function(_Component) {
			inherits(_class, _Component);
			function _class() {
				classCallCheck(this, _class);
				return possibleConstructorReturn(this, (_class.__proto__ || Object.getPrototypeOf(_class)).apply(this, arguments));
			}
			createClass(_class, [{
				key: "render",
				value: function render() {
					return componentClass.call(this, this.props, this.context);
				}
			}]);
			return _class;
		}(C), _class.displayName = makeDisplayName(componentClass), _temp));
	}
	if (!componentClass) throw new Error("Please pass a valid component to 'observer'");
	mixinLifecycleEvents(componentClass.prototype || componentClass);
	componentClass.isMobXReactObserver = true;
	return componentClass;
}
function mixinLifecycleEvents(target) {
	patch(target, "componentWillMount", true);
	patch(target, "componentDidMount");
	if (!target.shouldComponentUpdate) target.shouldComponentUpdate = reactiveMixin.shouldComponentUpdate;
}
var Observer = observer$1(function(_ref) {
	return _ref.children[0]();
});
Observer.displayName = "Observer";
var commonjsGlobal = typeof window !== "undefined" ? window : typeof global !== "undefined" ? global : typeof self !== "undefined" ? self : {};
function createCommonjsModule(fn, module) {
	return module = { exports: {} }, fn(module, module.exports), module.exports;
}
createCommonjsModule(function(module, exports) {
	/**
	* Copyright 2015, Yahoo! Inc.
	* Copyrights licensed under the New BSD License. See the accompanying LICENSE file for terms.
	*/
	(function(global, factory) {
		module.exports = factory();
	})(commonjsGlobal, (function() {
		var REACT_STATICS = {
			childContextTypes: true,
			contextTypes: true,
			defaultProps: true,
			displayName: true,
			getDefaultProps: true,
			getDerivedStateFromProps: true,
			mixins: true,
			propTypes: true,
			type: true
		};
		var KNOWN_STATICS = {
			name: true,
			length: true,
			prototype: true,
			caller: true,
			callee: true,
			arguments: true,
			arity: true
		};
		var defineProperty = Object.defineProperty;
		var getOwnPropertyNames = Object.getOwnPropertyNames;
		var getOwnPropertySymbols = Object.getOwnPropertySymbols;
		var getOwnPropertyDescriptor = Object.getOwnPropertyDescriptor;
		var getPrototypeOf = Object.getPrototypeOf;
		var objectPrototype = getPrototypeOf && getPrototypeOf(Object);
		return function hoistNonReactStatics(targetComponent, sourceComponent, blacklist) {
			if (typeof sourceComponent !== "string") {
				if (objectPrototype) {
					var inheritedComponent = getPrototypeOf(sourceComponent);
					if (inheritedComponent && inheritedComponent !== objectPrototype) hoistNonReactStatics(targetComponent, inheritedComponent, blacklist);
				}
				var keys = getOwnPropertyNames(sourceComponent);
				if (getOwnPropertySymbols) keys = keys.concat(getOwnPropertySymbols(sourceComponent));
				for (var i = 0; i < keys.length; ++i) {
					var key = keys[i];
					if (!REACT_STATICS[key] && !KNOWN_STATICS[key] && (!blacklist || !blacklist[key])) {
						var descriptor = getOwnPropertyDescriptor(sourceComponent, key);
						try {
							defineProperty(targetComponent, key, descriptor);
						} catch (e) {}
					}
				}
				return targetComponent;
			}
			return targetComponent;
		};
	}));
});
var specialReactKeys = {
	children: true,
	key: true,
	ref: true
};
var logger$1 = console;
(function(_Component) {
	inherits(Provider, _Component);
	function Provider() {
		classCallCheck(this, Provider);
		return possibleConstructorReturn(this, (Provider.__proto__ || Object.getPrototypeOf(Provider)).apply(this, arguments));
	}
	createClass(Provider, [
		{
			key: "render",
			value: function render(_ref) {
				var children = _ref.children;
				return children.length > 1 ? k$1("div", null, " ", children, " ") : children[0];
			}
		},
		{
			key: "getChildContext",
			value: function getChildContext() {
				var stores = {};
				var baseStores = this.context.mobxStores;
				if (baseStores) for (var key in baseStores) stores[key] = baseStores[key];
				for (var _key in this.props) if (!specialReactKeys[_key] && _key !== "suppressChangedStoreWarning") stores[_key] = this.props[_key];
				return { mobxStores: stores };
			}
		},
		{
			key: "componentWillReceiveProps",
			value: function componentWillReceiveProps(nextProps) {
				if (Object.keys(nextProps).length !== Object.keys(this.props).length) logger$1.warn("MobX Provider: The set of provided stores has changed. Please avoid changing stores as the change might not propagate to all children");
				if (!nextProps.suppressChangedStoreWarning) {
					for (var key in nextProps) if (!specialReactKeys[key] && this.props[key] !== nextProps[key]) logger$1.warn("MobX Provider: Provided store '" + key + "' has changed. Please avoid replacing stores as the change might not propagate to all children");
				}
			}
		}
	]);
	return Provider;
})(C);
if (!C) throw new Error("mobx-preact requires Preact to be available");
//#endregion
//#region node_modules/preact/hooks/dist/hooks.module.js
var r, u$1, i$1, f$1 = [], c = l$1, e = c.__b, a = c.__r, v = c.diffed, l = c.__c, m = c.unmount, s = c.__;
function j$1() {
	for (var n; n = f$1.shift();) {
		var t = n.__H;
		if (n.__P && t) try {
			t.__h.some(z), t.__h.some(B$1), t.__h = [];
		} catch (r) {
			t.__h = [], c.__e(r, n.__v);
		}
	}
}
c.__b = function(n) {
	r = null, e && e(n);
}, c.__ = function(n, t) {
	n && t.__k && t.__k.__m && (n.__m = t.__k.__m), s && s(n, t);
}, c.__r = function(n) {
	a && a(n);
	var i = (r = n.__c).__H;
	i && (u$1 === r ? (i.__h = [], r.__h = [], i.__.some(function(n) {
		n.__N && (n.__ = n.__N), n.u = n.__N = void 0;
	})) : (i.__h.some(z), i.__h.some(B$1), i.__h = [])), u$1 = r;
}, c.diffed = function(n) {
	v && v(n);
	var t = n.__c;
	t && t.__H && (t.__H.__h.length && (1 !== f$1.push(t) && i$1 === c.requestAnimationFrame || ((i$1 = c.requestAnimationFrame) || w)(j$1)), t.__H.__.some(function(n) {
		n.u && (n.__H = n.u), n.u = void 0;
	})), u$1 = r = null;
}, c.__c = function(n, t) {
	t.some(function(n) {
		try {
			n.__h.some(z), n.__h = n.__h.filter(function(n) {
				return !n.__ || B$1(n);
			});
		} catch (r) {
			t.some(function(n) {
				n.__h && (n.__h = []);
			}), t = [], c.__e(r, n.__v);
		}
	}), l && l(n, t);
}, c.unmount = function(n) {
	m && m(n);
	var t, r = n.__c;
	r && r.__H && (r.__H.__.some(function(n) {
		try {
			z(n);
		} catch (n) {
			t = n;
		}
	}), r.__H = void 0, t && c.__e(t, r.__v));
};
var k = "function" == typeof requestAnimationFrame;
function w(n) {
	var t, r = function() {
		clearTimeout(u), k && cancelAnimationFrame(t), setTimeout(n);
	}, u = setTimeout(r, 35);
	k && (t = requestAnimationFrame(r));
}
function z(n) {
	var t = r, u = n.__c;
	"function" == typeof u && (n.__c = void 0, u()), r = t;
}
function B$1(n) {
	var t = r;
	n.__c = n.__(), r = t;
}
//#endregion
//#region node_modules/preact/compat/dist/compat.module.js
function g(n, t) {
	for (var e in t) n[e] = t[e];
	return n;
}
function E(n, t) {
	for (var e in n) if ("__source" !== e && !(e in t)) return !0;
	for (var r in t) if ("__source" !== r && n[r] !== t[r]) return !0;
	return !1;
}
function M(n, t) {
	this.props = n, this.context = t;
}
(M.prototype = new C()).isPureReactComponent = !0, M.prototype.shouldComponentUpdate = function(n, t) {
	return E(this.props, n) || E(this.state, t);
};
var T = l$1.__b;
l$1.__b = function(n) {
	n.type && n.type.__f && n.ref && (n.props.ref = n.ref, n.ref = null), T && T(n);
};
"undefined" != typeof Symbol && Symbol.for;
var O = l$1.__e;
l$1.__e = function(n, t, e, r) {
	if (n.then) {
		for (var u, o = t; o = o.__;) if ((u = o.__c) && u.__c) return t.__e ?? (t.__e = e.__e, t.__k = e.__k), u.__c(n, t);
	}
	O(n, t, e, r);
};
var U = l$1.unmount;
function V(n, t, e) {
	return n && (n.__c && n.__c.__H && (n.__c.__H.__.forEach(function(n) {
		"function" == typeof n.__c && n.__c();
	}), n.__c.__H = null), null != (n = g({}, n)).__c && (n.__c.__P === e && (n.__c.__P = t), n.__c.__e = !0, n.__c = null), n.__k = n.__k && n.__k.map(function(n) {
		return V(n, t, e);
	})), n;
}
function W(n, t, e) {
	return n && e && (n.__v = null, n.__k = n.__k && n.__k.map(function(n) {
		return W(n, t, e);
	}), n.__c && n.__c.__P === t && (n.__e && e.appendChild(n.__e), n.__c.__e = !0, n.__c.__P = e)), n;
}
function P() {
	this.__u = 0, this.o = null, this.__b = null;
}
function j(n) {
	var t = n.__ && n.__.__c;
	return t && t.__a && t.__a(n);
}
function B() {
	this.i = null, this.l = null;
}
l$1.unmount = function(n) {
	var t = n.__c;
	t && (t.__z = !0), t && t.__R && t.__R(), t && 32 & n.__u && (n.type = null), U && U(n);
}, (P.prototype = new C()).__c = function(n, t) {
	var e = t.__c, r = this;
	r.o ??= [], r.o.push(e);
	var u = j(r.__v), o = !1, i = function() {
		o || r.__z || (o = !0, e.__R = null, u ? u(c) : c());
	};
	e.__R = i;
	var l = e.__P;
	e.__P = null;
	var c = function() {
		if (!--r.__u) {
			if (r.state.__a) {
				var n = r.state.__a;
				r.__v.__k[0] = W(n, n.__c.__P, n.__c.__O);
			}
			var t;
			for (r.setState({ __a: r.__b = null }); t = r.o.pop();) t.__P = l, t.forceUpdate();
		}
	};
	r.__u++ || 32 & t.__u || r.setState({ __a: r.__b = r.__v.__k[0] }), n.then(i, i);
}, P.prototype.componentWillUnmount = function() {
	this.o = [];
}, P.prototype.render = function(n, e) {
	if (this.__b) {
		if (this.__v.__k) {
			var r = document.createElement("div"), o = this.__v.__k[0].__c;
			this.__v.__k[0] = V(this.__b, r, o.__O = o.__P);
		}
		this.__b = null;
	}
	var i = e.__a && k$1(S, null, n.fallback);
	return i && (i.__u &= -33), [k$1(S, null, e.__a ? null : n.children), i];
};
var H = function(n, t, e) {
	if (++e[1] === e[0] && n.l.delete(t), n.props.revealOrder && ("t" !== n.props.revealOrder[0] || !n.l.size)) for (e = n.i; e;) {
		for (; e.length > 3;) e.pop()();
		if (e[1] < e[0]) break;
		n.i = e = e[2];
	}
};
(B.prototype = new C()).__a = function(n) {
	var t = this, e = j(t.__v), r = t.l.get(n);
	return r[0]++, function(u) {
		var o = function() {
			t.props.revealOrder ? (r.push(u), H(t, n, r)) : u();
		};
		e ? e(o) : o();
	};
}, B.prototype.render = function(n) {
	this.i = null, this.l = /* @__PURE__ */ new Map();
	var t = F$1(n.children);
	n.revealOrder && "b" === n.revealOrder[0] && t.reverse();
	for (var e = t.length; e--;) this.l.set(t[e], this.i = [
		1,
		0,
		this.i
	]);
	return n.children;
}, B.prototype.componentDidUpdate = B.prototype.componentDidMount = function() {
	var n = this;
	this.l.forEach(function(t, e) {
		H(n, e, t);
	});
};
var q = "undefined" != typeof Symbol && Symbol.for && Symbol.for("react.element") || 60103, G = /^(?:accent|alignment|arabic|baseline|cap|clip(?!PathU)|color|dominant|fill|flood|font|glyph(?!R)|horiz|image(!S)|letter|lighting|marker(?!H|W|U)|overline|paint|pointer|shape|stop|strikethrough|stroke|text(?!L)|transform|underline|unicode|units|v|vector|vert|word|writing|x(?!C))[A-Z]/, J = /^on(Ani|Tra|Tou|BeforeInp|Compo)/, K = /[A-Z0-9]/g, Q = "undefined" != typeof document, X = function(n) {
	return ("undefined" != typeof Symbol && "symbol" == typeof Symbol() ? /fil|che|rad/ : /fil|che|ra/).test(n);
};
function nn(n, t, e) {
	return t.__k ?? (t.textContent = ""), R(n, t), "function" == typeof e && e(), n ? n.__c : null;
}
C.prototype.isReactComponent = !0, [
	"componentWillMount",
	"componentWillReceiveProps",
	"componentWillUpdate"
].forEach(function(t) {
	Object.defineProperty(C.prototype, t, {
		configurable: !0,
		get: function() {
			return this["UNSAFE_" + t];
		},
		set: function(n) {
			Object.defineProperty(this, t, {
				configurable: !0,
				writable: !0,
				value: n
			});
		}
	});
});
var en = l$1.event;
l$1.event = function(n) {
	return en && (n = en(n)), n.persist = function() {}, n.isPropagationStopped = function() {
		return this.cancelBubble;
	}, n.isDefaultPrevented = function() {
		return this.defaultPrevented;
	}, n.nativeEvent = n;
};
var un = {
	configurable: !0,
	get: function() {
		return this.class;
	}
}, on = l$1.vnode;
l$1.vnode = function(n) {
	"string" == typeof n.type && function(n) {
		var t = n.props, e = n.type, u = {}, o = -1 == e.indexOf("-");
		for (var i in t) {
			var l = t[i];
			if (!("value" === i && "defaultValue" in t && null == l || Q && "children" === i && "noscript" === e || "class" === i || "className" === i)) {
				var c = i.toLowerCase();
				"defaultValue" === i && "value" in t && null == t.value ? i = "value" : "download" === i && !0 === l ? l = "" : "translate" === c && "no" === l ? l = !1 : "o" === c[0] && "n" === c[1] ? "ondoubleclick" === c ? i = "ondblclick" : "onchange" !== c || "input" !== e && "textarea" !== e || X(t.type) ? "onfocus" === c ? i = "onfocusin" : "onblur" === c ? i = "onfocusout" : J.test(i) && (i = c) : c = i = "oninput" : o && G.test(i) ? i = i.replace(K, "-$&").toLowerCase() : null === l && (l = void 0), "oninput" === c && u[i = c] && (i = "oninputCapture"), u[i] = l;
			}
		}
		"select" == e && (u.multiple && Array.isArray(u.value) && (u.value = F$1(t.children).forEach(function(n) {
			n.props.selected = -1 != u.value.indexOf(n.props.value);
		})), null != u.defaultValue && (u.value = F$1(t.children).forEach(function(n) {
			n.props.selected = u.multiple ? -1 != u.defaultValue.indexOf(n.props.value) : u.defaultValue == n.props.value;
		}))), t.class && !t.className ? (u.class = t.class, Object.defineProperty(u, "className", un)) : t.className && (u.class = u.className = t.className), n.props = u;
	}(n), n.$$typeof = q, on && on(n);
};
var ln = l$1.__r;
l$1.__r = function(n) {
	ln && ln(n), n.__c;
};
var cn = l$1.diffed;
l$1.diffed = function(n) {
	cn && cn(n);
	var t = n.props, e = n.__e;
	null != e && "textarea" === n.type && "value" in t && t.value !== e.value && (e.value = null == t.value ? "" : t.value);
};
//#endregion
//#region node_modules/preact/jsx-runtime/dist/jsxRuntime.module.js
var f = 0;
Array.isArray;
function u(e, t, n, o, i, u) {
	t || (t = {});
	var a, c, p = t;
	if ("ref" in p) for (c in p = {}, t) "ref" == c ? a = t[c] : p[c] = t[c];
	var l = {
		type: e,
		props: p,
		key: n,
		ref: a,
		__k: null,
		__: null,
		__b: 0,
		__e: null,
		__c: null,
		constructor: void 0,
		__v: --f,
		__i: -1,
		__u: 0,
		__source: i,
		__self: u
	};
	if ("function" == typeof e && (a = e.defaultProps)) for (c in a) void 0 === p[c] && (p[c] = a[c]);
	return l$1.vnode && l$1.vnode(l), l;
}
//#endregion
//#region node_modules/lodash-es/assign.js
/** Used to check objects for own properties. */
var hasOwnProperty$2 = Object.prototype.hasOwnProperty;
/**
* Assigns own enumerable string keyed properties of source objects to the
* destination object. Source objects are applied from left to right.
* Subsequent sources overwrite property assignments of previous sources.
*
* **Note:** This method mutates `object` and is loosely based on
* [`Object.assign`](https://mdn.io/Object/assign).
*
* @static
* @memberOf _
* @since 0.10.0
* @category Object
* @param {Object} object The destination object.
* @param {...Object} [sources] The source objects.
* @returns {Object} Returns `object`.
* @see _.assignIn
* @example
*
* function Foo() {
*   this.a = 1;
* }
*
* function Bar() {
*   this.c = 3;
* }
*
* Foo.prototype.b = 2;
* Bar.prototype.d = 4;
*
* _.assign({ 'a': 0 }, new Foo, new Bar);
* // => { 'a': 1, 'c': 3 }
*/
var assign = createAssigner(function(object, source) {
	if (isPrototype(source) || isArrayLike(source)) {
		copyObject(source, keys$1(source), object);
		return;
	}
	for (var key in source) if (hasOwnProperty$2.call(source, key)) assignValue(object, key, source[key]);
});
//#endregion
//#region node_modules/lodash-es/_arrayReduce.js
/**
* A specialized version of `_.reduce` for arrays without support for
* iteratee shorthands.
*
* @private
* @param {Array} [array] The array to iterate over.
* @param {Function} iteratee The function invoked per iteration.
* @param {*} [accumulator] The initial value.
* @param {boolean} [initAccum] Specify using the first element of `array` as
*  the initial value.
* @returns {*} Returns the accumulated value.
*/
function arrayReduce(array, iteratee, accumulator, initAccum) {
	var index = -1, length = array == null ? 0 : array.length;
	if (initAccum && length) accumulator = array[++index];
	while (++index < length) accumulator = iteratee(accumulator, array[index], index, array);
	return accumulator;
}
//#endregion
//#region node_modules/lodash-es/_castFunction.js
/**
* Casts `value` to `identity` if it's not a function.
*
* @private
* @param {*} value The value to inspect.
* @returns {Function} Returns cast function.
*/
function castFunction(value) {
	return typeof value == "function" ? value : identity;
}
//#endregion
//#region node_modules/lodash-es/forEach.js
/**
* Iterates over elements of `collection` and invokes `iteratee` for each element.
* The iteratee is invoked with three arguments: (value, index|key, collection).
* Iteratee functions may exit iteration early by explicitly returning `false`.
*
* **Note:** As with other "Collections" methods, objects with a "length"
* property are iterated like arrays. To avoid this behavior use `_.forIn`
* or `_.forOwn` for object iteration.
*
* @static
* @memberOf _
* @since 0.1.0
* @alias each
* @category Collection
* @param {Array|Object} collection The collection to iterate over.
* @param {Function} [iteratee=_.identity] The function invoked per iteration.
* @returns {Array|Object} Returns `collection`.
* @see _.forEachRight
* @example
*
* _.forEach([1, 2], function(value) {
*   console.log(value);
* });
* // => Logs `1` then `2`.
*
* _.forEach({ 'a': 1, 'b': 2 }, function(value, key) {
*   console.log(key);
* });
* // => Logs 'a' then 'b' (iteration order is not guaranteed).
*/
function forEach(collection, iteratee) {
	return (isArray(collection) ? arrayEach : baseEach)(collection, castFunction(iteratee));
}
//#endregion
//#region node_modules/lodash-es/_baseToPairs.js
/**
* The base implementation of `_.toPairs` and `_.toPairsIn` which creates an array
* of key-value pairs for `object` corresponding to the property names of `props`.
*
* @private
* @param {Object} object The object to query.
* @param {Array} props The property names to get values for.
* @returns {Object} Returns the key-value pairs.
*/
function baseToPairs(object, props) {
	return arrayMap(props, function(key) {
		return [key, object[key]];
	});
}
//#endregion
//#region node_modules/lodash-es/_setToPairs.js
/**
* Converts `set` to its value-value pairs.
*
* @private
* @param {Object} set The set to convert.
* @returns {Array} Returns the value-value pairs.
*/
function setToPairs(set) {
	var index = -1, result = Array(set.size);
	set.forEach(function(value) {
		result[++index] = [value, value];
	});
	return result;
}
//#endregion
//#region node_modules/lodash-es/_createToPairs.js
/** `Object#toString` result references. */
var mapTag$1 = "[object Map]", setTag$1 = "[object Set]";
/**
* Creates a `_.toPairs` or `_.toPairsIn` function.
*
* @private
* @param {Function} keysFunc The function to get the keys of a given object.
* @returns {Function} Returns the new pairs function.
*/
function createToPairs(keysFunc) {
	return function(object) {
		var tag = _getTag_default(object);
		if (tag == mapTag$1) return mapToArray(object);
		if (tag == setTag$1) return setToPairs(object);
		return baseToPairs(object, keysFunc(object));
	};
}
//#endregion
//#region node_modules/lodash-es/toPairs.js
/**
* Creates an array of own enumerable string keyed-value pairs for `object`
* which can be consumed by `_.fromPairs`. If `object` is a map or set, its
* entries are returned.
*
* @static
* @memberOf _
* @since 4.0.0
* @alias entries
* @category Object
* @param {Object} object The object to query.
* @returns {Array} Returns the key-value pairs.
* @example
*
* function Foo() {
*   this.a = 1;
*   this.b = 2;
* }
*
* Foo.prototype.c = 3;
*
* _.toPairs(new Foo);
* // => [['a', 1], ['b', 2]] (iteration order is not guaranteed)
*/
var toPairs = createToPairs(keys$1);
//#endregion
//#region node_modules/lodash-es/_createFind.js
/**
* Creates a `_.find` or `_.findLast` function.
*
* @private
* @param {Function} findIndexFunc The function to find the collection index.
* @returns {Function} Returns the new find function.
*/
function createFind(findIndexFunc) {
	return function(collection, predicate, fromIndex) {
		var iterable = Object(collection);
		if (!isArrayLike(collection)) {
			var iteratee = baseIteratee(predicate, 3);
			collection = keys$1(collection);
			predicate = function(key) {
				return iteratee(iterable[key], key, iterable);
			};
		}
		var index = findIndexFunc(collection, predicate, fromIndex);
		return index > -1 ? iterable[iteratee ? collection[index] : index] : void 0;
	};
}
//#endregion
//#region node_modules/lodash-es/findIndex.js
var nativeMax$1 = Math.max;
/**
* This method is like `_.find` except that it returns the index of the first
* element `predicate` returns truthy for instead of the element itself.
*
* @static
* @memberOf _
* @since 1.1.0
* @category Array
* @param {Array} array The array to inspect.
* @param {Function} [predicate=_.identity] The function invoked per iteration.
* @param {number} [fromIndex=0] The index to search from.
* @returns {number} Returns the index of the found element, else `-1`.
* @example
*
* var users = [
*   { 'user': 'barney',  'active': false },
*   { 'user': 'fred',    'active': false },
*   { 'user': 'pebbles', 'active': true }
* ];
*
* _.findIndex(users, function(o) { return o.user == 'barney'; });
* // => 0
*
* // The `_.matches` iteratee shorthand.
* _.findIndex(users, { 'user': 'fred', 'active': false });
* // => 1
*
* // The `_.matchesProperty` iteratee shorthand.
* _.findIndex(users, ['active', false]);
* // => 0
*
* // The `_.property` iteratee shorthand.
* _.findIndex(users, 'active');
* // => 2
*/
function findIndex(array, predicate, fromIndex) {
	var length = array == null ? 0 : array.length;
	if (!length) return -1;
	var index = fromIndex == null ? 0 : toInteger(fromIndex);
	if (index < 0) index = nativeMax$1(length + index, 0);
	return baseFindIndex(array, baseIteratee(predicate, 3), index);
}
//#endregion
//#region node_modules/lodash-es/find.js
/**
* Iterates over elements of `collection`, returning the first element
* `predicate` returns truthy for. The predicate is invoked with three
* arguments: (value, index|key, collection).
*
* @static
* @memberOf _
* @since 0.1.0
* @category Collection
* @param {Array|Object} collection The collection to inspect.
* @param {Function} [predicate=_.identity] The function invoked per iteration.
* @param {number} [fromIndex=0] The index to search from.
* @returns {*} Returns the matched element, else `undefined`.
* @example
*
* var users = [
*   { 'user': 'barney',  'age': 36, 'active': true },
*   { 'user': 'fred',    'age': 40, 'active': false },
*   { 'user': 'pebbles', 'age': 1,  'active': true }
* ];
*
* _.find(users, function(o) { return o.age < 40; });
* // => object for 'barney'
*
* // The `_.matches` iteratee shorthand.
* _.find(users, { 'age': 1, 'active': true });
* // => object for 'pebbles'
*
* // The `_.matchesProperty` iteratee shorthand.
* _.find(users, ['active', false]);
* // => object for 'fred'
*
* // The `_.property` iteratee shorthand.
* _.find(users, 'active');
* // => object for 'barney'
*/
var find = createFind(findIndex);
//#endregion
//#region node_modules/lodash-es/_baseHas.js
/** Used to check objects for own properties. */
var hasOwnProperty$1 = Object.prototype.hasOwnProperty;
/**
* The base implementation of `_.has` without support for deep paths.
*
* @private
* @param {Object} [object] The object to query.
* @param {Array|string} key The key to check.
* @returns {boolean} Returns `true` if `key` exists, else `false`.
*/
function baseHas(object, key) {
	return object != null && hasOwnProperty$1.call(object, key);
}
//#endregion
//#region node_modules/lodash-es/has.js
/**
* Checks if `path` is a direct property of `object`.
*
* @static
* @since 0.1.0
* @memberOf _
* @category Object
* @param {Object} object The object to query.
* @param {Array|string} path The path to check.
* @returns {boolean} Returns `true` if `path` exists, else `false`.
* @example
*
* var object = { 'a': { 'b': 2 } };
* var other = _.create({ 'a': _.create({ 'b': 2 }) });
*
* _.has(object, 'a');
* // => true
*
* _.has(object, 'a.b');
* // => true
*
* _.has(object, ['a', 'b']);
* // => true
*
* _.has(other, 'a');
* // => false
*/
function has(object, path) {
	return object != null && hasPath(object, path, baseHas);
}
//#endregion
//#region node_modules/lodash-es/indexOf.js
var nativeMax = Math.max;
/**
* Gets the index at which the first occurrence of `value` is found in `array`
* using [`SameValueZero`](http://ecma-international.org/ecma-262/7.0/#sec-samevaluezero)
* for equality comparisons. If `fromIndex` is negative, it's used as the
* offset from the end of `array`.
*
* @static
* @memberOf _
* @since 0.1.0
* @category Array
* @param {Array} array The array to inspect.
* @param {*} value The value to search for.
* @param {number} [fromIndex=0] The index to search from.
* @returns {number} Returns the index of the matched value, else `-1`.
* @example
*
* _.indexOf([1, 2, 1, 2], 2);
* // => 1
*
* // Search from the `fromIndex`.
* _.indexOf([1, 2, 1, 2], 2, 2);
* // => 3
*/
function indexOf(array, value, fromIndex) {
	var length = array == null ? 0 : array.length;
	if (!length) return -1;
	var index = fromIndex == null ? 0 : toInteger(fromIndex);
	if (index < 0) index = nativeMax(length + index, 0);
	return baseIndexOf(array, value, index);
}
//#endregion
//#region node_modules/lodash-es/isBoolean.js
/** `Object#toString` result references. */
var boolTag = "[object Boolean]";
/**
* Checks if `value` is classified as a boolean primitive or object.
*
* @static
* @memberOf _
* @since 0.1.0
* @category Lang
* @param {*} value The value to check.
* @returns {boolean} Returns `true` if `value` is a boolean, else `false`.
* @example
*
* _.isBoolean(false);
* // => true
*
* _.isBoolean(null);
* // => false
*/
function isBoolean(value) {
	return value === true || value === false || isObjectLike(value) && baseGetTag(value) == boolTag;
}
//#endregion
//#region node_modules/lodash-es/isEmpty.js
/** `Object#toString` result references. */
var mapTag = "[object Map]", setTag = "[object Set]";
/** Used to check objects for own properties. */
var hasOwnProperty = Object.prototype.hasOwnProperty;
/**
* Checks if `value` is an empty object, collection, map, or set.
*
* Objects are considered empty if they have no own enumerable string keyed
* properties.
*
* Array-like values such as `arguments` objects, arrays, buffers, strings, or
* jQuery-like collections are considered empty if they have a `length` of `0`.
* Similarly, maps and sets are considered empty if they have a `size` of `0`.
*
* @static
* @memberOf _
* @since 0.1.0
* @category Lang
* @param {*} value The value to check.
* @returns {boolean} Returns `true` if `value` is empty, else `false`.
* @example
*
* _.isEmpty(null);
* // => true
*
* _.isEmpty(true);
* // => true
*
* _.isEmpty(1);
* // => true
*
* _.isEmpty([1, 2, 3]);
* // => false
*
* _.isEmpty({ 'a': 1 });
* // => false
*/
function isEmpty(value) {
	if (value == null) return true;
	if (isArrayLike(value) && (isArray(value) || typeof value == "string" || typeof value.splice == "function" || isBuffer(value) || isTypedArray(value) || isArguments(value))) return !value.length;
	var tag = _getTag_default(value);
	if (tag == mapTag || tag == setTag) return !value.size;
	if (isPrototype(value)) return !baseKeys(value).length;
	for (var key in value) if (hasOwnProperty.call(value, key)) return false;
	return true;
}
//#endregion
//#region node_modules/lodash-es/mapKeys.js
/**
* The opposite of `_.mapValues`; this method creates an object with the
* same values as `object` and keys generated by running each own enumerable
* string keyed property of `object` thru `iteratee`. The iteratee is invoked
* with three arguments: (value, key, object).
*
* @static
* @memberOf _
* @since 3.8.0
* @category Object
* @param {Object} object The object to iterate over.
* @param {Function} [iteratee=_.identity] The function invoked per iteration.
* @returns {Object} Returns the new mapped object.
* @see _.mapValues
* @example
*
* _.mapKeys({ 'a': 1, 'b': 2 }, function(value, key) {
*   return key + value;
* });
* // => { 'a1': 1, 'b2': 2 }
*/
function mapKeys(object, iteratee) {
	var result = {};
	iteratee = baseIteratee(iteratee, 3);
	baseForOwn(object, function(value, key, object) {
		baseAssignValue(result, iteratee(value, key, object), value);
	});
	return result;
}
//#endregion
//#region node_modules/lodash-es/_baseReduce.js
/**
* The base implementation of `_.reduce` and `_.reduceRight`, without support
* for iteratee shorthands, which iterates over `collection` using `eachFunc`.
*
* @private
* @param {Array|Object} collection The collection to iterate over.
* @param {Function} iteratee The function invoked per iteration.
* @param {*} accumulator The initial value.
* @param {boolean} initAccum Specify using the first or last element of
*  `collection` as the initial value.
* @param {Function} eachFunc The function to iterate over `collection`.
* @returns {*} Returns the accumulated value.
*/
function baseReduce(collection, iteratee, accumulator, initAccum, eachFunc) {
	eachFunc(collection, function(value, index, collection) {
		accumulator = initAccum ? (initAccum = false, value) : iteratee(accumulator, value, index, collection);
	});
	return accumulator;
}
//#endregion
//#region node_modules/lodash-es/reduce.js
/**
* Reduces `collection` to a value which is the accumulated result of running
* each element in `collection` thru `iteratee`, where each successive
* invocation is supplied the return value of the previous. If `accumulator`
* is not given, the first element of `collection` is used as the initial
* value. The iteratee is invoked with four arguments:
* (accumulator, value, index|key, collection).
*
* Many lodash methods are guarded to work as iteratees for methods like
* `_.reduce`, `_.reduceRight`, and `_.transform`.
*
* The guarded methods are:
* `assign`, `defaults`, `defaultsDeep`, `includes`, `merge`, `orderBy`,
* and `sortBy`
*
* @static
* @memberOf _
* @since 0.1.0
* @category Collection
* @param {Array|Object} collection The collection to iterate over.
* @param {Function} [iteratee=_.identity] The function invoked per iteration.
* @param {*} [accumulator] The initial value.
* @returns {*} Returns the accumulated value.
* @see _.reduceRight
* @example
*
* _.reduce([1, 2], function(sum, n) {
*   return sum + n;
* }, 0);
* // => 3
*
* _.reduce({ 'a': 1, 'b': 2, 'c': 1 }, function(result, value, key) {
*   (result[value] || (result[value] = [])).push(key);
*   return result;
* }, {});
* // => { '1': ['a', 'c'], '2': ['b'] } (iteration order is not guaranteed)
*/
function reduce(collection, iteratee, accumulator) {
	var func = isArray(collection) ? arrayReduce : baseReduce, initAccum = arguments.length < 3;
	return func(collection, baseIteratee(iteratee, 4), accumulator, initAccum, baseEach);
}
//#endregion
//#region node_modules/@logicflow/core/es/options.js
var __rest$4 = function(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
};
var Options;
(function(Options) {
	function get(options) {
		var others = __rest$4(options, []);
		if (!options.container) throw new Error("Ensure the container of LogicFlow is specified and valid.");
		return assign({}, Options.defaults, others);
	}
	Options.get = get;
})(Options || (Options = {}));
(function(Options) {
	Options.defaults = {
		background: false,
		grid: false,
		textEdit: true,
		snapline: true,
		outline: false,
		disabledTools: []
	};
})(Options || (Options = {}));
//#endregion
//#region node_modules/@logicflow/core/es/util/animation.js
var defaultAnimationOffConfig = {
	node: false,
	edge: false
};
var defaultAnimationOnConfig = {
	node: true,
	edge: true
};
var setupAnimation = function(config) {
	if (!config || typeof config === "boolean") return config === true ? cloneDeep(defaultAnimationOnConfig) : cloneDeep(defaultAnimationOffConfig);
	return merge(cloneDeep(defaultAnimationOffConfig), config);
};
var updateAnimation = setupAnimation;
//#endregion
//#region node_modules/@logicflow/core/es/util/browser.js
var isIe = function() {
	return get(window, "navigator.userAgent", "").match(/MSIE|Trident/) !== null;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/compatible.js
/**
* 对数据实现兼容处理。
*
* Vue 中的 data 会进行 Observe，深拷贝的原始数据对象
*/
function formatData(data) {
	try {
		return JSON.parse(JSON.stringify(data));
	} catch (_a) {
		return data;
	}
}
//#endregion
//#region node_modules/@logicflow/core/es/constant/theme.js
var DEFAULT_GRID_SIZE = 10;
var defaultTheme = {
	baseNode: {
		fill: "#fff",
		stroke: "#474747",
		strokeWidth: 2,
		radius: 12
	},
	baseEdge: {
		stroke: "#474747",
		strokeWidth: 2,
		radius: 12
	},
	rect: { shadow: {
		dx: 0,
		dy: 0,
		stdDeviation: 4,
		floodColor: "#9CA1AA55"
	} },
	circle: {},
	diamond: {},
	ellipse: {},
	polygon: {},
	html: {
		fill: "transparent",
		stroke: "transparent",
		shadow: {
			dx: 0,
			dy: 0,
			stdDeviation: 4,
			floodColor: "#9CA1AA55"
		}
	},
	text: {
		color: "#474747",
		stroke: "none",
		fontSize: 12,
		background: { fill: "transparent" }
	},
	nodeText: {
		color: "#474747",
		overflowMode: "default",
		fontSize: 12,
		lineHeight: 1.2
	},
	anchor: {
		stroke: "#474747",
		fill: "#fff",
		r: 4,
		hover: {
			r: 8,
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	anchorLine: {
		stroke: "#474747",
		strokeWidth: 2,
		strokeDasharray: "3,2"
	},
	line: {},
	polyline: {},
	bezier: {
		fill: "none",
		adjustLine: { stroke: "#949494" },
		adjustAnchor: {
			r: 4,
			fill: "#949494",
			fillOpacity: 1,
			stroke: "#949494"
		}
	},
	edgeText: {
		textWidth: 100,
		overflowMode: "default",
		fontSize: 12,
		background: { fill: "#fff" }
	},
	arrow: {
		strokeLinecap: "round",
		strokeLinejoin: "round",
		offset: 10,
		verticalLength: 5
	},
	snapline: {
		strokeLinecap: "round",
		strokeLinejoin: "round",
		stroke: "#AAAAAA",
		strokeWidth: 1,
		strokeDasharray: "3,3"
	},
	edgeAdjust: {
		r: 4,
		fill: "#fff",
		stroke: "#474747",
		strokeWidth: 1
	},
	outline: {
		fill: "none",
		stroke: "transparent",
		strokeWidth: 0
	},
	edgeOutline: {
		fill: "transparent",
		stroke: "#757575",
		strokeWidth: 1.5,
		strokeDasharray: "4,4",
		radius: 8,
		hover: { stroke: "#4271DF" }
	},
	edgeAnimation: {
		stroke: "#4271DF",
		strokeDasharray: "12,4,6,4",
		strokeDashoffset: "100%",
		animationName: "lf_animate_dash",
		animationDuration: "20s",
		animationIterationCount: "infinite",
		animationTimingFunction: "linear",
		animationDirection: "normal"
	},
	rotateControl: {
		stroke: "#474747",
		fill: "#fff",
		strokeWidth: 1,
		hover: {
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	resizeControl: {
		width: 8,
		height: 8,
		radius: 2,
		fill: "#fff",
		stroke: "#474747",
		hover: {
			width: 12,
			height: 12,
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	resizeOutline: {
		fill: "transparent",
		stroke: "#757575",
		strokeWidth: 1.5,
		strokeDasharray: "4,4",
		radius: 8,
		hover: { stroke: "#4271DF" }
	}
};
var retroTheme = {
	baseNode: {
		fill: "#fff",
		stroke: "#000",
		strokeWidth: 2
	},
	baseEdge: {
		stroke: "#000",
		strokeWidth: 2
	},
	rect: {},
	circle: {},
	diamond: {},
	ellipse: {},
	polygon: {},
	html: {
		fill: "transparent",
		stroke: "transparent"
	},
	text: {
		color: "#000",
		stroke: "none",
		fontSize: 12,
		background: { fill: "transparent" }
	},
	anchor: {
		stroke: "#000",
		fill: "#fff",
		r: 4,
		hover: {
			r: 10,
			fill: "#949494",
			fillOpacity: .5,
			stroke: "#949494"
		}
	},
	anchorLine: {
		stroke: "#000",
		strokeWidth: 2,
		strokeDasharray: "3,2"
	},
	nodeText: {
		color: "#000",
		overflowMode: "default",
		fontSize: 12,
		lineHeight: 1.2
	},
	edgeText: {
		textWidth: 100,
		overflowMode: "default",
		fontSize: 12,
		background: { fill: "#fff" }
	},
	line: {},
	polyline: {},
	bezier: {
		fill: "none",
		adjustLine: { stroke: "#949494" },
		adjustAnchor: {
			r: 4,
			fill: "#949494",
			fillOpacity: 1,
			stroke: "#949494"
		}
	},
	arrow: {
		offset: 10,
		verticalLength: 5
	},
	snapline: {
		stroke: "#949494",
		strokeWidth: 1
	},
	edgeAdjust: {
		r: 4,
		fill: "#fff",
		stroke: "#949494",
		strokeWidth: 2
	},
	outline: {
		fill: "transparent",
		stroke: "#949494",
		strokeDasharray: "3,3",
		hover: { stroke: "#949494" }
	},
	edgeOutline: {
		fill: "transparent",
		stroke: "#949494",
		strokeDasharray: "3,3",
		hover: { stroke: "#949494" }
	},
	edgeAnimation: {
		stroke: "red",
		strokeDasharray: "10,10",
		strokeDashoffset: "100%",
		animationName: "lf_animate_dash",
		animationDuration: "20s",
		animationIterationCount: "infinite",
		animationTimingFunction: "linear",
		animationDirection: "normal"
	},
	rotateControl: {
		stroke: "#000",
		fill: "#fff",
		strokeWidth: 1.5
	},
	resizeControl: {
		width: 7,
		height: 7,
		fill: "#fff",
		stroke: "#000"
	},
	resizeOutline: {
		fill: "none",
		stroke: "transparent",
		strokeWidth: 1,
		strokeDasharray: "3,3"
	}
};
var darkTheme = {
	baseNode: {
		fill: "#33353A",
		stroke: "#EAEAEA",
		strokeWidth: 2,
		radius: 12
	},
	baseEdge: {
		stroke: "#EAEAEA",
		strokeWidth: 2,
		radius: 12
	},
	rect: {},
	circle: {},
	diamond: {},
	ellipse: {},
	polygon: {},
	html: {
		fill: "#33353A",
		stroke: "transparent",
		shadow: {
			dx: 0,
			dy: 0,
			stdDeviation: 4,
			floodColor: "#9CA1AA55"
		}
	},
	text: {
		color: "#EAEAEA",
		stroke: "none",
		fontSize: 12,
		background: { fill: "transparent" }
	},
	nodeText: {
		color: "#EAEAEA",
		overflowMode: "default",
		fontSize: 12,
		lineHeight: 1.2
	},
	anchor: {
		stroke: "#EAEAEA",
		fill: "#33353A",
		r: 4,
		hover: {
			r: 8,
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	anchorLine: {
		stroke: "#EAEAEA",
		strokeWidth: 2,
		strokeDasharray: "3,2"
	},
	line: {},
	polyline: {},
	bezier: {
		fill: "none",
		adjustLine: { stroke: "#EAEAEA" },
		adjustAnchor: {
			r: 4,
			fill: "#33353A",
			fillOpacity: 1,
			stroke: "#EAEAEA"
		}
	},
	edgeText: {
		textWidth: 100,
		overflowMode: "default",
		fontSize: 12,
		background: { fill: "#EAEAEA" }
	},
	arrow: {
		strokeLinecap: "round",
		strokeLinejoin: "round",
		offset: 10,
		verticalLength: 5
	},
	snapline: {
		strokeLinecap: "round",
		strokeLinejoin: "round",
		stroke: "#949494",
		strokeWidth: 1
	},
	edgeAdjust: {
		r: 4,
		fill: "#33353A",
		stroke: "#EAEAEA",
		strokeWidth: 1
	},
	outline: {
		fill: "none",
		stroke: "transparent",
		strokeWidth: 0
	},
	edgeOutline: {
		fill: "transparent",
		stroke: "#EAEAEA",
		strokeWidth: 1.5,
		strokeDasharray: "4,4",
		radius: 8,
		hover: { stroke: "#4271DF" }
	},
	edgeAnimation: {
		stroke: "#4271DF",
		strokeDasharray: "12,4,6,4",
		strokeDashoffset: "100%",
		animationName: "lf_animate_dash",
		animationDuration: "20s",
		animationIterationCount: "infinite",
		animationTimingFunction: "linear",
		animationDirection: "normal"
	},
	rotateControl: {
		stroke: "#EAEAEA",
		fill: "#33353A",
		strokeWidth: 1,
		hover: {
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	resizeControl: {
		width: 8,
		height: 8,
		radius: 2,
		fill: "#33353A",
		stroke: "#EAEAEA",
		hover: {
			width: 12,
			height: 12,
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	resizeOutline: {
		fill: "transparent",
		stroke: "#EAEAEA",
		strokeWidth: 1.5,
		strokeDasharray: "4,4",
		radius: 8,
		hover: { stroke: "#4271DF" }
	}
};
var colorfulTheme = {
	baseNode: {
		fill: "#fff",
		stroke: "#474747",
		strokeWidth: 2,
		radius: 12
	},
	baseEdge: {
		stroke: "#474747",
		strokeWidth: 2,
		radius: 12
	},
	rect: {
		fill: "#72CBFF",
		stroke: "#3ABDF9",
		radius: 8
	},
	circle: {
		fill: "#FFE075",
		stroke: "#F9CE3A",
		radius: 8
	},
	ellipse: {
		fill: "#FFA8A8",
		stroke: "#FF6B66",
		radius: 8
	},
	text: {
		fill: "#72CBFF",
		radius: 8,
		fontSize: 12
	},
	diamond: {
		fill: "#96F7AF",
		stroke: "#40EF7E",
		radius: 8
	},
	polygon: {
		fill: "#E0A8FF",
		stroke: "#C271FF",
		radius: 8
	},
	nodeText: {
		color: "#474747",
		overflowMode: "default",
		fontSize: 12,
		lineHeight: 1.2
	},
	edgeText: {
		textWidth: 100,
		overflowMode: "default",
		fontSize: 12,
		background: { fill: "#fff" }
	},
	html: {
		fill: "#fff",
		stroke: "transparent",
		strokeWidth: 0
	},
	anchor: {
		stroke: "#474747",
		fill: "#fff",
		r: 4,
		hover: {
			r: 8,
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	anchorLine: {
		stroke: "#474747",
		strokeWidth: 2,
		strokeDasharray: "3,2"
	},
	line: {},
	polyline: { radius: 8 },
	bezier: {
		fill: "none",
		adjustLine: { stroke: "#3ABDF9" },
		adjustAnchor: {
			r: 4,
			fill: "#3ABDF9",
			fillOpacity: 1,
			stroke: "#3ABDF9"
		}
	},
	edgeAdjust: {
		r: 4,
		fill: "#fff",
		stroke: "#474747",
		strokeWidth: 1
	},
	arrow: {
		strokeLinecap: "round",
		strokeLinejoin: "round",
		offset: 10,
		verticalLength: 5
	},
	snapline: {
		strokeLinecap: "round",
		strokeLinejoin: "round",
		stroke: "#949494",
		strokeWidth: 1
	},
	outline: {
		radius: 8,
		fill: "transparent",
		stroke: "#949494",
		strokeDasharray: "3,3",
		hover: { stroke: "#949494" }
	},
	edgeOutline: {
		fill: "transparent",
		stroke: "#757575",
		strokeWidth: 1.5,
		strokeDasharray: "4,4",
		radius: 8,
		hover: { stroke: "#4271DF" }
	},
	edgeAnimation: {
		stroke: "#4271DF",
		strokeDasharray: "12,4,6,4",
		strokeDashoffset: "100%",
		animationName: "lf_animate_dash",
		animationDuration: "20s",
		animationIterationCount: "infinite",
		animationTimingFunction: "linear",
		animationDirection: "normal"
	},
	rotateControl: {
		stroke: "#474747",
		fill: "#fff",
		strokeWidth: 1,
		hover: {
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	resizeControl: {
		width: 8,
		height: 8,
		radius: 2,
		fill: "#fff",
		stroke: "#474747",
		hover: {
			width: 12,
			height: 12,
			fill: "#707070",
			fillOpacity: .15,
			stroke: "transparent"
		}
	},
	resizeOutline: {
		fill: "transparent",
		stroke: "#757575",
		strokeWidth: 1.5,
		strokeDasharray: "4,4",
		radius: 8,
		hover: { stroke: "#4271DF" }
	}
};
var themeModeMap = {
	default: defaultTheme,
	colorful: colorfulTheme,
	dark: darkTheme,
	retro: retroTheme
};
var darkBackground = { background: "#33353A" };
var colorfulBackground = { background: "#fefeff" };
var defaultBackground = { background: "#FBFCFE" };
var retroBackground = { background: "#ffffff" };
var backgroundModeMap = {
	colorful: colorfulBackground,
	dark: darkBackground,
	default: defaultBackground,
	retro: retroBackground
};
var darkGrid = {
	size: 10,
	visible: true,
	type: "mesh",
	config: {
		color: "#83838377",
		thickness: 2
	},
	majorBold: true
};
var colorfulGrid = {
	size: 10,
	visible: true,
	type: "dot",
	config: {
		color: "#b7c2d9",
		thickness: 2
	},
	majorBold: false
};
var defaultGrid = {
	size: 10,
	visible: true,
	type: "mesh",
	config: {
		color: "#D7DEEB",
		thickness: 1
	},
	majorBold: {
		opacity: .6,
		boldIndices: [5],
		dashArrayConfig: { pattern: [] },
		customBoldWidth: 2
	}
};
var retroGrid = {
	size: 10,
	visible: true,
	type: "dot",
	config: {
		color: "#ababab",
		thickness: 1
	},
	majorBold: false
};
var gridModeMap = {
	colorful: colorfulGrid,
	dark: darkGrid,
	retro: retroGrid,
	default: defaultGrid
};
//#endregion
//#region node_modules/@logicflow/core/es/constant/index.js
var DEFAULT_VISIBLE_SPACE = 200;
var ELEMENT_MAX_Z_INDEX = 9999;
var ElementState;
(function(ElementState) {
	ElementState[ElementState["DEFAULT"] = 1] = "DEFAULT";
	ElementState[ElementState["TEXT_EDIT"] = 2] = "TEXT_EDIT";
	ElementState[ElementState["SHOW_MENU"] = 3] = "SHOW_MENU";
	ElementState[ElementState["ALLOW_CONNECT"] = 4] = "ALLOW_CONNECT";
	ElementState[ElementState["NOT_ALLOW_CONNECT"] = 5] = "NOT_ALLOW_CONNECT";
})(ElementState || (ElementState = {}));
var ElementType;
(function(ElementType) {
	ElementType["NODE"] = "node";
	ElementType["EDGE"] = "edge";
	ElementType["GRAPH"] = "graph";
})(ElementType || (ElementType = {}));
var ModelType;
(function(ModelType) {
	ModelType["NODE"] = "node";
	ModelType["CIRCLE_NODE"] = "circle-node";
	ModelType["POLYGON_NODE"] = "polygon-node";
	ModelType["RECT_NODE"] = "rect-node";
	ModelType["TEXT_NODE"] = "text-node";
	ModelType["ELLIPSE_NODE"] = "ellipse-node";
	ModelType["DIAMOND_NODE"] = "diamond-node";
	ModelType["HTML_NODE"] = "html-node";
	ModelType["CUSTOM_HTML_NODE"] = "custom-html-node";
	ModelType["EDGE"] = "edge";
	ModelType["LINE_EDGE"] = "line-edge";
	ModelType["POLYLINE_EDGE"] = "polyline-edge";
	ModelType["BEZIER_EDGE"] = "bezier-edge";
	ModelType["GRAPH"] = "graph";
})(ModelType || (ModelType = {}));
var EventType;
(function(EventType) {
	EventType["ELEMENT_CLICK"] = "element:click";
	EventType["NODE_ADD"] = "node:add";
	EventType["NODE_DELETE"] = "node:delete";
	EventType["NODE_CLICK"] = "node:click";
	EventType["NODE_DBCLICK"] = "node:dbclick";
	EventType["NODE_GROUP_COPY"] = "node:group-copy-add";
	EventType["NODE_DND_ADD"] = "node:dnd-add";
	EventType["NODE_DND_DRAG"] = "node:dnd-drag";
	EventType["NODE_MOUSEDOWN"] = "node:mousedown";
	EventType["NODE_DRAGSTART"] = "node:dragstart";
	EventType["NODE_DRAG"] = "node:drag";
	EventType["NODE_DROP"] = "node:drop";
	EventType["NODE_MOUSEUP"] = "node:mouseup";
	EventType["NODE_MOUSEMOVE"] = "node:mousemove";
	EventType["NODE_MOUSEENTER"] = "node:mouseenter";
	EventType["NODE_MOUSELEAVE"] = "node:mouseleave";
	EventType["NODE_CONTEXTMENU"] = "node:contextmenu";
	EventType["NODE_ROTATE"] = "node:rotate";
	EventType["NODE_RESIZE"] = "node:resize";
	EventType["NODE_FOCUS"] = "node:focus";
	EventType["NODE_BLUR"] = "node:blur";
	EventType["NODE_PROPERTIES_CHANGE"] = "node:properties-change";
	EventType["NODE_PROPERTIES_DELETE"] = "node:properties-delete";
	EventType["EDGE_ADD"] = "edge:add";
	EventType["EDGE_DELETE"] = "edge:delete";
	EventType["EDGE_CLICK"] = "edge:click";
	EventType["EDGE_DBCLICK"] = "edge:dbclick";
	EventType["EDGE_FOCUS"] = "edge:focus";
	EventType["EDGE_BLUR"] = "edge:blur";
	EventType["EDGE_MOUSEENTER"] = "edge:mouseenter";
	EventType["EDGE_MOUSELEAVE"] = "edge:mouseleave";
	EventType["EDGE_CONTEXTMENU"] = "edge:contextmenu";
	EventType["EDGE_ADJUST"] = "edge:adjust";
	EventType["EDGE_EXCHANGE_NODE"] = "edge:exchange-node";
	EventType["ANCHOR_CLICK"] = "anchor:click";
	EventType["ANCHOR_MOUSEDOWN"] = "anchor:mousedown";
	EventType["ANCHOR_DRAGSTART"] = "anchor:dragstart";
	EventType["ANCHOR_DRAG"] = "anchor:drag";
	EventType["ANCHOR_DROP"] = "anchor:drop";
	EventType["ANCHOR_DRAGEND"] = "anchor:dragend";
	EventType["ADJUST_POINT_MOUSEDOWN"] = "adjustPoint:mousedown";
	EventType["ADJUST_POINT_MOUSEUP"] = "adjustPoint:mouseup";
	EventType["ADJUST_POINT_MOUSEMOVE"] = "adjustPoint:mousemove";
	EventType["ADJUST_POINT_DRAGSTART"] = "adjustPoint:dragstart";
	EventType["ADJUST_POINT_DRAG"] = "adjustPoint:drag";
	EventType["ADJUST_POINT_DROP"] = "adjustPoint:drop";
	EventType["ADJUST_POINT_DRAGEND"] = "adjustPoint:dragend";
	EventType["BLANK_MOUSEDOWN"] = "blank:mousedown";
	EventType["BLANK_DRAGSTART"] = "blank:dragstart";
	EventType["BLANK_DRAG"] = "blank:drag";
	EventType["BLANK_DROP"] = "blank:drop";
	EventType["BLANK_MOUSEMOVE"] = "blank:mousemove";
	EventType["BLANK_CANVAS_MOUSEMOVE"] = "blank:canvas-mousemove";
	EventType["BLANK_MOUSEUP"] = "blank:mouseup";
	EventType["BLANK_CLICK"] = "blank:click";
	EventType["BLANK_CONTEXTMENU"] = "blank:contextmenu";
	EventType["SELECTION_MOUSEDOWN"] = "selection:mousedown";
	EventType["SELECTION_DRAGSTART"] = "selection:dragstart";
	EventType["SELECTION_DRAG"] = "selection:drag";
	EventType["SELECTION_DROP"] = "selection:drop";
	EventType["SELECTION_MOUSEMOVE"] = "selection:mousemove";
	EventType["SELECTION_MOUSEUP"] = "selection:mouseup";
	EventType["SELECTION_CONTEXTMENU"] = "selection:contextmenu";
	EventType["CONNECTION_NOT_ALLOWED"] = "connection:not-allowed";
	EventType["TEXT_MOUSEDOWN"] = "text:mousedown";
	EventType["TEXT_DRAGSTART"] = "text:dragstart";
	EventType["TEXT_DRAG"] = "text:drag";
	EventType["TEXT_DROP"] = "text:drop";
	EventType["TEXT_CLICK"] = "text:click";
	EventType["TEXT_DBCLICK"] = "text:dbclick";
	EventType["TEXT_BLUR"] = "text:blur";
	EventType["TEXT_MOUSEMOVE"] = "text:mousemove";
	EventType["TEXT_MOUSEUP"] = "text:mouseup";
	EventType["TEXT_FOCUS"] = "text:focus";
	EventType["TEXT_ADD"] = "text:add";
	EventType["TEXT_UPDATE"] = "text:update";
	EventType["TEXT_CLEAR"] = "text:clear";
	EventType["LABEL_MOUSEDOWN"] = "label:mousedown";
	EventType["LABEL_DRAGSTART"] = "label:dragstart";
	EventType["LABEL_DRAG"] = "label:drag";
	EventType["LABEL_DROP"] = "label:drop";
	EventType["LABEL_CLICK"] = "label:click";
	EventType["LABEL_DBCLICK"] = "label:dbclick";
	EventType["LABEL_BLUR"] = "label:blur";
	EventType["LABEL_MOUSEMOVE"] = "label:mousemove";
	EventType["LABEL_MOUSEUP"] = "label:mouseup";
	EventType["LABEL_FOCUS"] = "label:focus";
	EventType["LABEL_ADD"] = "label:add";
	EventType["LABEL_UPDATE"] = "label:update";
	EventType["LABEL_CLEAR"] = "label:clear";
	EventType["LABEL_DELETE"] = "label:delete";
	EventType["LABEL_SHOULD_ADD"] = "label:should-add";
	EventType["LABEL_BATCH_ADD"] = "label:batch-add";
	EventType["LABEL_SHOULD_UPDATE"] = "label:should-update";
	EventType["LABEL_SHOULD_DELETE"] = "label:should-delete";
	EventType["LABEL_BATCH_DELETE"] = "label:batch-delete";
	EventType["LABEL_NOT_ALLOWED_ADD"] = "label:not-allowed-add";
	EventType["HISTORY_CHANGE"] = "history:change";
	EventType["GRAPH_TRANSFORM"] = "graph:transform";
	EventType["GRAPH_RENDERED"] = "graph:rendered";
	EventType["GRAPH_UPDATED"] = "graph:updated";
	EventType["EDIT_CONFIG_CHANGED"] = "editConfig:changed";
})(EventType || (EventType = {}));
var OverlapMode;
(function(OverlapMode) {
	OverlapMode[OverlapMode["STATIC"] = -1] = "STATIC";
	OverlapMode[OverlapMode["DEFAULT"] = 0] = "DEFAULT";
	OverlapMode[OverlapMode["INCREASE"] = 1] = "INCREASE";
	OverlapMode[OverlapMode["EDGE_TOP"] = 2] = "EDGE_TOP";
})(OverlapMode || (OverlapMode = {}));
var SegmentDirection;
(function(SegmentDirection) {
	SegmentDirection["HORIZONTAL"] = "horizontal";
	SegmentDirection["VERTICAL"] = "vertical";
})(SegmentDirection || (SegmentDirection = {}));
var TextMode;
(function(TextMode) {
	TextMode["TEXT"] = "text";
	TextMode["LABEL"] = "label";
})(TextMode || (TextMode = {}));
//#endregion
//#region node_modules/@logicflow/core/es/util/drag.js
var LEFT_MOUSE_BUTTON_CODE = 0;
var StepDrag = function() {
	function StepDrag(_a) {
		var _b = _a.onDragStart, onDragStart = _b === void 0 ? noop$1 : _b, _c = _a.onDragging, onDragging = _c === void 0 ? noop$1 : _c, _d = _a.onDragEnd, onDragEnd = _d === void 0 ? noop$1 : _d, _e = _a.eventType, eventType = _e === void 0 ? "" : _e, eventCenter = _a.eventCenter, _f = _a.step, step = _f === void 0 ? 1 : _f, _g = _a.isStopPropagation, isStopPropagation = _g === void 0 ? true : _g, model = _a.model, data = _a.data;
		var _this = this;
		this.isDragging = false;
		this.isStartDragging = false;
		this.startX = 0;
		this.startY = 0;
		this.sumDeltaX = 0;
		this.sumDeltaY = 0;
		this.handleMouseDown = function(e) {
			var _a, _b;
			var DOC = window === null || window === void 0 ? void 0 : window.document;
			if (e.button !== LEFT_MOUSE_BUTTON_CODE) return;
			if (_this.isStopPropagation) e.stopPropagation();
			_this.isStartDragging = true;
			_this.startX = e.clientX;
			_this.startY = e.clientY;
			DOC.addEventListener("pointermove", _this.handleMouseMove, false);
			DOC.addEventListener("pointerup", _this.handleMouseUp, false);
			var elementData = (_a = _this.model) === null || _a === void 0 ? void 0 : _a.getData();
			(_b = _this.eventCenter) === null || _b === void 0 || _b.emit(EventType["".concat(_this.eventType, "_MOUSEDOWN")], {
				e,
				data: _this.data || elementData
			});
			_this.startTime = (/* @__PURE__ */ new Date()).getTime();
		};
		this.handleMouseMove = function(e) {
			var _a, _b;
			if (_this.isStopPropagation) e.stopPropagation();
			e.preventDefault();
			if (!_this.isStartDragging) return;
			_this.sumDeltaX += e.clientX - _this.startX;
			_this.sumDeltaY += e.clientY - _this.startY;
			_this.startX = e.clientX;
			_this.startY = e.clientY;
			if (_this.step <= 1 || Math.abs(_this.sumDeltaX) > _this.step || Math.abs(_this.sumDeltaY) > _this.step) {
				var remainderX = _this.sumDeltaX % _this.step;
				var remainderY = _this.sumDeltaY % _this.step;
				var deltaX_1 = _this.sumDeltaX - remainderX;
				var deltaY_1 = _this.sumDeltaY - remainderY;
				_this.sumDeltaX = remainderX;
				_this.sumDeltaY = remainderY;
				var elementData_1 = (_a = _this.model) === null || _a === void 0 ? void 0 : _a.getData();
				/**
				* 为了区分点击和拖动，在鼠标没有拖动时，不触发dragstart。
				*/
				if (!_this.isDragging) {
					(_b = _this.eventCenter) === null || _b === void 0 || _b.emit(EventType["".concat(_this.eventType, "_DRAGSTART")], {
						e,
						data: _this.data || elementData_1
					});
					_this.onDragStart({ event: e });
				}
				_this.isDragging = true;
				Promise.resolve().then(function() {
					var _a, _b;
					_this.onDragging({
						deltaX: deltaX_1,
						deltaY: deltaY_1,
						event: e
					});
					(_a = _this.eventCenter) === null || _a === void 0 || _a.emit(EventType["".concat(_this.eventType, "_MOUSEMOVE")], {
						deltaX: deltaX_1,
						deltaY: deltaY_1,
						e,
						data: _this.data || elementData_1
					});
					(_b = _this.eventCenter) === null || _b === void 0 || _b.emit(EventType["".concat(_this.eventType, "_DRAG")], {
						e,
						data: _this.data || elementData_1
					});
				});
			}
		};
		this.handleMouseUp = function(e) {
			var DOC = window.document;
			_this.isStartDragging = false;
			if (_this.isStopPropagation) e.stopPropagation();
			var target = e.target;
			if (target && typeof target.releasePointerCapture === "function") target.releasePointerCapture(e.pointerId);
			Promise.resolve().then(function() {
				var _a, _b, _c;
				DOC.removeEventListener("pointermove", _this.handleMouseMove, false);
				DOC.removeEventListener("pointerup", _this.handleMouseUp, false);
				var elementData = (_a = _this.model) === null || _a === void 0 ? void 0 : _a.getData();
				(_b = _this.eventCenter) === null || _b === void 0 || _b.emit(EventType["".concat(_this.eventType, "_MOUSEUP")], {
					e,
					data: _this.data || elementData
				});
				if (!_this.isDragging) return;
				_this.isDragging = false;
				_this.onDragEnd({ event: e });
				(_c = _this.eventCenter) === null || _c === void 0 || _c.emit(EventType["".concat(_this.eventType, "_DROP")], {
					e,
					data: _this.data || elementData
				});
			});
		};
		this.cancelDrag = function() {
			var DOC = window === null || window === void 0 ? void 0 : window.document;
			DOC.removeEventListener("pointermove", _this.handleMouseMove, false);
			DOC.removeEventListener("pointerup", _this.handleMouseUp, false);
			_this.onDragEnd({ event: void 0 });
			_this.isDragging = false;
		};
		this.destroy = function() {
			if (_this.isStartDragging) _this.cancelDrag();
		};
		this.onDragStart = onDragStart;
		this.onDragging = onDragging;
		this.onDragEnd = onDragEnd;
		this.step = step;
		this.isStopPropagation = isStopPropagation;
		this.eventType = eventType;
		this.eventCenter = eventCenter;
		this.model = model;
		this.data = data;
	}
	StepDrag.prototype.setStep = function(step) {
		this.step = step;
	};
	StepDrag.prototype.setModel = function(model) {
		this.model = model;
	};
	return StepDrag;
}();
//#endregion
//#region node_modules/@logicflow/core/es/algorithm/outline.js
var getNodeOutline = function(node) {
	var x = node.x, y = node.y, width = node.width, height = node.height;
	return {
		x: x - width / 2,
		y: y - height / 2,
		x1: x + width / 2,
		y1: y + height / 2
	};
};
var getLineOutline = function(edge) {
	var startPoint = edge.startPoint, endPoint = edge.endPoint;
	var x = (startPoint.x + endPoint.x) / 2;
	var y = (startPoint.y + endPoint.y) / 2;
	var width = Math.abs(startPoint.x - endPoint.x) + 10;
	var height = Math.abs(startPoint.y - endPoint.y) + 10;
	return {
		x: x - width / 2,
		y: y - height / 2,
		x1: x + width / 2,
		y1: y + height / 2
	};
};
var getPolylineOutline = function(edge) {
	var points = edge.points;
	var bbox = getBBoxOfPoints(points2PointsList(points), 8);
	var x = bbox.x, y = bbox.y, width = bbox.width, height = bbox.height;
	return {
		x: x - width / 2,
		y: y - height / 2,
		x1: x + width / 2,
		y1: y + height / 2
	};
};
var getBezierOutline = function(edge) {
	var path = edge.path;
	var bbox = getBBoxOfPoints(getBezierPoints(path), 8);
	var x = bbox.x, y = bbox.y, width = bbox.width, height = bbox.height;
	return {
		x: x - width / 2,
		y: y - height / 2,
		x1: x + width / 2,
		y1: y + height / 2
	};
};
var getEdgeOutline = function(edge) {
	if (edge.modelType === ModelType.LINE_EDGE) return getLineOutline(edge);
	if (edge.modelType === ModelType.POLYLINE_EDGE) return getPolylineOutline(edge);
	if (edge.modelType === ModelType.BEZIER_EDGE) return getBezierOutline(edge);
};
//#endregion
//#region node_modules/@logicflow/core/es/algorithm/edge.js
/**
* 求两个线段交点入参：线段1端点: ab, 线段2端点： cd,
* @param a
* @param b
* @param c
* @param d
* @return：有交点返回交点 Point，无交点返回false
*/
var getCrossPointOfLine = function(a, b, c, d) {
	/** 1 解线性方程组, 求线段交点. * */
	var denominator = (b.y - a.y) * (d.x - c.x) - (a.x - b.x) * (c.y - d.y);
	if (denominator === 0) return false;
	var x = ((b.x - a.x) * (d.x - c.x) * (c.y - a.y) + (b.y - a.y) * (d.x - c.x) * a.x - (d.y - c.y) * (b.x - a.x) * c.x) / denominator;
	var y = -((b.y - a.y) * (d.y - c.y) * (c.x - a.x) + (b.x - a.x) * (d.y - c.y) * a.y - (d.x - c.x) * (b.y - a.y) * c.y) / denominator;
	/** 2 判断交点是否在两条线段上 * */
	if ((x - a.x) * (x - b.x) <= 0 && (y - a.y) * (y - b.y) <= 0 && (x - c.x) * (x - d.x) <= 0 && (y - c.y) * (y - d.y) <= 0) return {
		x,
		y
	};
	return false;
};
var isInSegment = function(point, start, end) {
	var x = point.x, y = point.y;
	var startX = start.x, startY = start.y;
	var endX = end.x, endY = end.y;
	var k = (endY - startY) / (endX - startX);
	var b = startY - k * startX;
	return (x >= startX && x <= endX || x <= startX && x >= endX) && (y >= startY && y <= endY || y <= startY && y >= endY) && Math.abs(y - k * x - b) < Number.EPSILON;
};
//#endregion
//#region node_modules/@logicflow/core/es/algorithm/index.js
var getVerticalPointOfLine = function(config) {
	var start = config.start, end = config.end, offset = config.offset, verticalLength = config.verticalLength, type = config.type;
	var pointPosition = {
		leftX: 0,
		leftY: 0,
		rightX: 0,
		rightY: 0
	};
	var angleOfHorizontal = Math.atan((end.y - start.y) / (end.x - start.x));
	var angleOfPoints = Math.atan(offset / verticalLength);
	var len = Math.sqrt(verticalLength * verticalLength + offset * offset);
	if (type === "start") if (end.x >= start.x) {
		pointPosition.leftX = start.x + len * Math.sin(angleOfHorizontal + angleOfPoints);
		pointPosition.leftY = start.y - len * Math.cos(angleOfHorizontal + angleOfPoints);
		pointPosition.rightX = start.x - len * Math.sin(angleOfHorizontal - angleOfPoints);
		pointPosition.rightY = start.y + len * Math.cos(angleOfHorizontal - angleOfPoints);
	} else {
		pointPosition.leftX = start.x - len * Math.sin(angleOfHorizontal + angleOfPoints);
		pointPosition.leftY = start.y + len * Math.cos(angleOfHorizontal + angleOfPoints);
		pointPosition.rightX = start.x + len * Math.sin(angleOfHorizontal - angleOfPoints);
		pointPosition.rightY = start.y - len * Math.cos(angleOfHorizontal - angleOfPoints);
	}
	else if (type === "end") if (end.x >= start.x) {
		pointPosition.leftX = end.x + len * Math.sin(angleOfHorizontal - angleOfPoints);
		pointPosition.leftY = end.y - len * Math.cos(angleOfHorizontal - angleOfPoints);
		pointPosition.rightX = end.x - len * Math.sin(angleOfHorizontal + angleOfPoints);
		pointPosition.rightY = end.y + len * Math.cos(angleOfHorizontal + angleOfPoints);
	} else {
		pointPosition.leftX = end.x - len * Math.sin(angleOfHorizontal - angleOfPoints);
		pointPosition.leftY = end.y + len * Math.cos(angleOfHorizontal - angleOfPoints);
		pointPosition.rightX = end.x + len * Math.sin(angleOfHorizontal + angleOfPoints);
		pointPosition.rightY = end.y - len * Math.cos(angleOfHorizontal + angleOfPoints);
	}
	return pointPosition;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/edge.js
var __assign$52 = function() {
	__assign$52 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$52.apply(this, arguments);
};
var __read$38 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$11 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var setupEdgeModel = function(edge, graphModel) {
	var model;
	switch (edge.type) {
		case "line":
			model = new LineEdgeModel(edge, graphModel);
			break;
		case "polyline":
			model = new PolylineEdgeModel(edge, graphModel);
			break;
		default:
			model = new LineEdgeModel(edge, graphModel);
			break;
	}
	return model;
};
var isBboxOverLapping = function(b1, b2) {
	return Math.abs(b1.centerX - b2.centerX) * 2 < b1.width + b2.width && Math.abs(b1.centerY - b2.centerY) * 2 < b1.height + b2.height;
};
var filterRepeatPoints = function(points) {
	var result = [];
	var pointsMap = {};
	points.forEach(function(p) {
		var id = "".concat(p.x, "-").concat(p.y);
		p.id = id;
		pointsMap[id] = p;
	});
	Object.keys(pointsMap).forEach(function(p) {
		result.push(pointsMap[p]);
	});
	return result;
};
var getSimplePolyline = function(sPoint, tPoint) {
	return filterRepeatPoints([
		sPoint,
		{
			x: sPoint.x,
			y: tPoint.y
		},
		tPoint
	]);
};
var getExpandedBBox = function(bbox, offset) {
	if (bbox.width === 0 && bbox.height === 0) return bbox;
	return {
		x: bbox.x,
		y: bbox.y,
		centerX: bbox.centerX,
		centerY: bbox.centerY,
		minX: bbox.minX - offset,
		minY: bbox.minY - offset,
		maxX: bbox.maxX + offset,
		maxY: bbox.maxY + offset,
		height: bbox.height + 2 * offset,
		width: bbox.width + 2 * offset
	};
};
var pointDirection = function(point, bbox) {
	var dx = Math.abs(point.x - bbox.centerX);
	var dy = Math.abs(point.y - bbox.centerY);
	return dx / bbox.width > dy / bbox.height ? SegmentDirection.HORIZONTAL : SegmentDirection.VERTICAL;
};
/**
* 计算扩展包围盒上的相邻点（起点或终点的下一个/上一个拐点）
* - 使用原始节点 bbox 来判定点相对中心的方向，避免 offset 扩展后宽高改变导致方向误判
* - 若 start 相对中心为水平方向，则返回扩展盒在 x 上的边界，y 保持不变
* - 若为垂直方向，则返回扩展盒在 y 上的边界，x 保持不变
* @param expendBBox 扩展后的包围盒（包含 offset）
* @param bbox 原始节点包围盒（用于正确的方向判定）
* @param point 起点或终点坐标
*/
var getExpandedBBoxPoint = function(expendBBox, bbox, point) {
	if (pointDirection(point, bbox) === SegmentDirection.HORIZONTAL) return {
		x: point.x > expendBBox.centerX ? expendBBox.maxX : expendBBox.minX,
		y: point.y
	};
	return {
		x: point.x,
		y: point.y > expendBBox.centerY ? expendBBox.maxY : expendBBox.minY
	};
};
var mergeBBox = function(b1, b2) {
	var minX = Math.min(b1.minX, b2.minX);
	var minY = Math.min(b1.minY, b2.minY);
	var maxX = Math.max(b1.maxX, b2.maxX);
	var maxY = Math.max(b1.maxY, b2.maxY);
	return {
		x: (minX + maxX) / 2,
		y: (minY + maxY) / 2,
		centerX: (minX + maxX) / 2,
		centerY: (minY + maxY) / 2,
		minX,
		minY,
		maxX,
		maxY,
		height: maxY - minY,
		width: maxX - minX
	};
};
var getBBoxOfPoints = function(points, offset, heightOffset) {
	if (points === void 0) points = [];
	var xList = [];
	var yList = [];
	points.forEach(function(p) {
		xList.push(p.x);
		yList.push(p.y);
	});
	var minX = Math.min.apply(Math, __spreadArray$11([], __read$38(xList), false));
	var maxX = Math.max.apply(Math, __spreadArray$11([], __read$38(xList), false));
	var minY = Math.min.apply(Math, __spreadArray$11([], __read$38(yList), false));
	var maxY = Math.max.apply(Math, __spreadArray$11([], __read$38(yList), false));
	var width = maxX - minX;
	var height = maxY - minY;
	if (offset) {
		width += offset;
		height += heightOffset || offset;
	}
	return {
		centerX: (minX + maxX) / 2,
		centerY: (minY + maxY) / 2,
		maxX,
		maxY,
		minX,
		minY,
		x: (minX + maxX) / 2,
		y: (minY + maxY) / 2,
		height,
		width
	};
};
var getPointsFromBBox = function(bbox) {
	var minX = bbox.minX, minY = bbox.minY, maxX = bbox.maxX, maxY = bbox.maxY;
	return [
		{
			x: minX,
			y: minY
		},
		{
			x: maxX,
			y: minY
		},
		{
			x: maxX,
			y: maxY
		},
		{
			x: minX,
			y: maxY
		}
	];
};
var isPointOutsideBBox = function(point, bbox) {
	var x = point.x, y = point.y;
	return x < bbox.minX || x > bbox.maxX || y < bbox.minY || y > bbox.maxY;
};
var getBBoxXCrossPoints = function(bbox, x) {
	if (x < bbox.minX || x > bbox.maxX) return [];
	return [{
		x,
		y: bbox.minY
	}, {
		x,
		y: bbox.maxY
	}];
};
var getBBoxYCrossPoints = function(bbox, y) {
	if (y < bbox.minY || y > bbox.maxY) return [];
	return [{
		x: bbox.minX,
		y
	}, {
		x: bbox.maxX,
		y
	}];
};
var getBBoxCrossPointsByPoint = function(bbox, point) {
	return __spreadArray$11(__spreadArray$11([], __read$38(getBBoxXCrossPoints(bbox, point.x)), false), __read$38(getBBoxYCrossPoints(bbox, point.y)), false);
};
var estimateDistance = function(p1, p2) {
	return Math.abs(p1.x - p2.x) + Math.abs(p1.y - p2.y);
};
var costByPoints = function(p, points) {
	var offset = -2;
	var result = 0;
	points.forEach(function(point) {
		if (point) {
			if (p.x === point.x) result += offset;
			if (p.y === point.y) result += offset;
		}
	});
	return result;
};
var heuristicCostEstimate = function(p, ps, pt, source, target) {
	return estimateDistance(p, ps) + estimateDistance(p, pt) + costByPoints(p, [
		ps,
		pt,
		source,
		target
	]);
};
var rebuildPath = function(pathPoints, pointById, cameFrom, currentId, iterator) {
	if (!iterator) iterator = 0;
	pathPoints.unshift(pointById[currentId]);
	if (cameFrom[currentId] && cameFrom[currentId] !== currentId && iterator <= 100) rebuildPath(pathPoints, pointById, cameFrom, cameFrom[currentId], iterator + 1);
};
var removeClosePointFromOpenList = function(arr, item) {
	var index = arr.indexOf(item);
	if (index > -1) arr.splice(index, 1);
};
var isSegmentsIntersected = function(p0, p1, p2, p3) {
	var s1x = p1.x - p0.x;
	var s1y = p1.y - p0.y;
	var s2x = p3.x - p2.x;
	var s2y = p3.y - p2.y;
	var s = (-s1y * (p0.x - p2.x) + s1x * (p0.y - p2.y)) / (-s2x * s1y + s1x * s2y);
	var t = (s2x * (p0.y - p2.y) - s2y * (p0.x - p2.x)) / (-s2x * s1y + s1x * s2y);
	return s >= 0 && s <= 1 && t >= 0 && t <= 1;
};
var isSegmentCrossingBBox = function(p1, p2, bbox) {
	if (bbox.width === 0 && bbox.height === 0) return false;
	var _a = __read$38(getPointsFromBBox(bbox), 4), pa = _a[0], pb = _a[1], pc = _a[2], pd = _a[3];
	return isSegmentsIntersected(p1, p2, pa, pb) || isSegmentsIntersected(p1, p2, pa, pd) || isSegmentsIntersected(p1, p2, pb, pc) || isSegmentsIntersected(p1, p2, pc, pd);
};
/**
* 基于轴对齐规则获取某点的相邻可连通点（不穿越节点）
* - 仅考虑 x 或 y 相同的候选点，保证严格水平/垂直
* - 使用 isSegmentCrossingBBox 校验线段不穿越源/目标节点
*/
var getNextNeighborPoints = function(points, point, bbox1, bbox2) {
	var neighbors = [];
	points.forEach(function(p) {
		if (p !== point) {
			if (p.x === point.x || p.y === point.y) {
				if (!isSegmentCrossingBBox(p, point, bbox1) && !isSegmentCrossingBBox(p, point, bbox2)) neighbors.push(p);
			}
		}
	});
	return filterRepeatPoints(neighbors);
};
/**
* 使用 A* + 曼哈顿启发式在候选点图上查找正交路径
* - 开放集/关闭集管理遍历
* - gScore 为累计实际代价，fScore = gScore + 启发式
* - 邻居仅为与当前点 x 或 y 相同且不穿越节点的点
* 参考：https://zh.wikipedia.org/wiki/A*%E6%90%9C%E5%B0%8B%E6%BC%94%E7%AE%97%E6%B3%95
*/
var pathFinder = function(points, start, goal, sBBox, tBBox, os, ot) {
	var closedSet = [];
	var openSet = [start];
	var cameFrom = {};
	var gScore = {};
	var fScore = {};
	if (start.id) {
		gScore[start.id] = 0;
		fScore[start.id] = heuristicCostEstimate(start, goal, start);
	}
	var pointById = {};
	points.forEach(function(p) {
		if (p.id) pointById[p.id] = p;
	});
	var _loop_1 = function() {
		var current;
		var lowestFScore = Infinity;
		openSet.forEach(function(p) {
			if (p.id && fScore[p.id] < lowestFScore) {
				lowestFScore = fScore[p.id];
				current = p;
			}
		});
		if (current === goal && goal.id) {
			var pathPoints = [];
			rebuildPath(pathPoints, pointById, cameFrom, goal.id);
			return { value: pathPoints };
		}
		if (!current) return { value: [start, goal] };
		removeClosePointFromOpenList(openSet, current);
		closedSet.push(current);
		getNextNeighborPoints(points, current, sBBox, tBBox).forEach(function(neighbor) {
			var _a;
			if (closedSet.indexOf(neighbor) !== -1) return;
			if (openSet.indexOf(neighbor) === -1) openSet.push(neighbor);
			if ((current === null || current === void 0 ? void 0 : current.id) && (neighbor === null || neighbor === void 0 ? void 0 : neighbor.id)) {
				var tentativeGScore = ((_a = gScore[current.id]) !== null && _a !== void 0 ? _a : 0) + estimateDistance(current, neighbor);
				if (gScore[neighbor.id] && tentativeGScore >= gScore[neighbor.id]) return;
				cameFrom[neighbor.id] = current.id;
				gScore[neighbor.id] = tentativeGScore;
				fScore[neighbor.id] = gScore[neighbor.id] + heuristicCostEstimate(neighbor, goal, start, os, ot);
			}
		});
	};
	while (openSet.length) {
		var state_1 = _loop_1();
		if (typeof state_1 === "object") return state_1.value;
	}
	return [start, goal];
};
var getBoxByOriginNode = function(node) {
	return getNodeBBox(node);
};
/**
* 去除共线冗余中间点，保持每条直线段仅保留两端点
* - 若三点在同一水平线或同一垂直线，移除中间点
*/
var pointFilter = function(points) {
	var i = 1;
	while (i < points.length - 1) {
		var pre = points[i - 1];
		var current = points[i];
		var next = points[i + 1];
		if (pre.x === current.x && current.x === next.x || pre.y === current.y && current.y === next.y) points.splice(i, 1);
		else i++;
	}
	return points;
};
/**
* 计算折线点（正交候选点 + A* 路径）
* 步骤：
* 1) 取源/目标节点的扩展包围盒与相邻点 sPoint/tPoint
* 2) 若两个扩展盒重合，使用简单路径 getSimplePoints
* 3) 构造 lineBBox/sMixBBox/tMixBBox，并收集其角点与中心交点
* 4) 过滤掉落在两个扩展盒内部的点，形成 connectPoints
* 5) 以 sPoint/tPoint 为起止，用 A* 查找路径
* 6) 拼入原始 start/end，并用 pointFilter 去除冗余共线点
*/
var getPolylinePoints = function(start, end, sNode, tNode, offset) {
	var sBBox = getBoxByOriginNode(sNode);
	var tBBox = getBoxByOriginNode(tNode);
	var sxBBox = getExpandedBBox(sBBox, offset);
	var txBBox = getExpandedBBox(tBBox, offset);
	var sPoint = getExpandedBBoxPoint(sxBBox, sBBox, start);
	var tPoint = getExpandedBBoxPoint(txBBox, tBBox, end);
	if (isBboxOverLapping(sxBBox, txBBox)) {
		var points = getSimplePoints(start, end, sPoint, tPoint);
		return __spreadArray$11(__spreadArray$11([start, sPoint], __read$38(points), false), [tPoint, end], false);
	}
	var lineBBox = getBBoxOfPoints([sPoint, tPoint]);
	var sMixBBox = mergeBBox(sxBBox, lineBBox);
	var tMixBBox = mergeBBox(txBBox, lineBBox);
	var connectPoints = [];
	connectPoints = connectPoints.concat(getPointsFromBBox(sMixBBox));
	connectPoints = connectPoints.concat(getPointsFromBBox(tMixBBox));
	var centerPoint = {
		x: (start.x + end.x) / 2,
		y: (start.y + end.y) / 2
	};
	[
		lineBBox,
		sMixBBox,
		tMixBBox
	].forEach(function(bbox) {
		connectPoints = connectPoints.concat(getBBoxCrossPointsByPoint(bbox, centerPoint).filter(function(p) {
			return isPointOutsideBBox(p, sxBBox) && isPointOutsideBBox(p, txBBox);
		}));
	});
	[{
		x: sPoint.x,
		y: tPoint.y
	}, {
		x: tPoint.x,
		y: sPoint.y
	}].forEach(function(p) {
		if (isPointOutsideBBox(p, sxBBox) && isPointOutsideBBox(p, txBBox)) connectPoints.push(p);
	});
	connectPoints.unshift(sPoint);
	connectPoints.push(tPoint);
	connectPoints = filterRepeatPoints(connectPoints);
	var pathPoints = pathFinder(connectPoints, sPoint, tPoint, sBBox, tBBox, start, end);
	pathPoints.unshift(start);
	pathPoints.push(end);
	if (pathPoints.length > 2) pathPoints = pointFilter(pathPoints);
	return filterRepeatPoints(pathPoints);
};
/**
* 获取折线中最长的一个线
* @param pointsList 多个点组成的数组
*/
var getLongestEdge = function(pointsList) {
	if (pointsList.length === 1) {
		var point = __read$38(pointsList, 1)[0];
		return [point, point];
	} else {
		var point1 = pointsList[0];
		var point2 = pointsList[1];
		var edgeLength = distance(point1.x, point1.y, point2.x, point2.y);
		for (var i = 1; i < pointsList.length - 1; i++) {
			var newPoint1 = pointsList[i];
			var newPoint2 = pointsList[i + 1];
			var newEdgeLength = distance(newPoint1.x, newPoint1.y, newPoint2.x, newPoint2.y);
			if (newEdgeLength > edgeLength) {
				edgeLength = newEdgeLength;
				point1 = newPoint1;
				point2 = newPoint2;
			}
		}
		return [point1, point2];
	}
};
var isSegmentsInNode = function(start, end, node) {
	var startInNode = isInNode(start, node);
	var endInNode = isInNode(end, node);
	return startInNode && endInNode;
};
var isSegmentsCrossNode = function(start, end, node) {
	var startInNode = isInNode(start, node);
	var endInNode = isInNode(end, node);
	return !(startInNode && endInNode) && (startInNode || endInNode);
};
var getCrossPointInRect = function(start, end, node) {
	var crossSegments = void 0;
	var points = getPointsFromBBox(getNodeBBox(node));
	for (var i = 0; i < points.length; i++) if (isSegmentsIntersected(start, end, points[i], points[(i + 1) % points.length])) crossSegments = [points[i], points[(i + 1) % points.length]];
	if (crossSegments) return getCrossPointOfLine(start, end, crossSegments[0], crossSegments[1]);
};
var segmentDirection = function(start, end) {
	var direction = void 0;
	if (start.x === end.x) direction = SegmentDirection.VERTICAL;
	else if (start.y === end.y) direction = SegmentDirection.HORIZONTAL;
	return direction;
};
var points2PointsList = function(points) {
	var currentPositionList = points.split(" ");
	var pointsList = [];
	currentPositionList && currentPositionList.forEach(function(item) {
		var _a = __read$38(item.split(","), 2), x = _a[0], y = _a[1];
		pointsList.push({
			x: Number(x),
			y: Number(y)
		});
	});
	return pointsList;
};
/**
* 当扩展 bbox 重合时的简化拐点计算
* - 根据起止段的方向（水平/垂直）插入 1~2 个中间点，避免折线重合与穿越
*/
var getSimplePoints = function(start, end, sPoint, tPoint) {
	var points = [];
	if (segmentDirection(start, sPoint) === segmentDirection(end, tPoint)) if (start.y === sPoint.y) {
		points.push({
			x: sPoint.x,
			y: (sPoint.y + tPoint.y) / 2
		});
		points.push({
			x: tPoint.x,
			y: (sPoint.y + tPoint.y) / 2
		});
	} else {
		points.push({
			x: (sPoint.x + tPoint.x) / 2,
			y: sPoint.y
		});
		points.push({
			x: (sPoint.x + tPoint.x) / 2,
			y: tPoint.y
		});
	}
	else {
		var point = {
			x: sPoint.x,
			y: tPoint.y
		};
		var inStart = isInSegment(point, start, sPoint);
		var inEnd = isInSegment(point, end, tPoint);
		if (inStart || inEnd) point = {
			x: tPoint.x,
			y: sPoint.y
		};
		else {
			var onStart = isOnLine(point, start, sPoint);
			var onEnd = isOnLine(point, end, tPoint);
			if (onStart && onEnd) point = {
				x: tPoint.x,
				y: sPoint.y
			};
		}
		points.push(point);
	}
	return points;
};
var isOnLine = function(point, start, end) {
	return point.x === start.x && point.x === end.x || point.y === start.y && point.y === end.y;
};
var getBytesLength = function(word) {
	if (!word) return 0;
	var totalLength = 0;
	for (var i = 0; i < word.length; i++) {
		var c = word.charCodeAt(i);
		if (word.match(/[A-Z]/)) totalLength += 1.5;
		else if (c >= 1 && c <= 126 || c >= 65376 && c <= 65439) totalLength += 1;
		else totalLength += 2;
	}
	return totalLength;
};
/**
* Uses canvas.measureText to compute
* and return the width of the given text of given font in pixels.
* @param {String} text The text to be rendered.
* @param {String} font The css font descriptor
* that text is to be rendered with (e.g. "bold 14px verdana").
* @see https://stackoverflow.com/questions/118241/calculate-text-width-with-javascript/21015393#21015393
*/
var canvas = void 0;
var getTextWidth = function(text, font) {
	if (!canvas) canvas = document.createElement("canvas");
	var context = canvas.getContext("2d");
	context.font = font;
	return context.measureText(text).width;
};
var getAppendAttributes = function(appendInfo) {
	var start = appendInfo.start, end = appendInfo.end;
	var d;
	if (start.x === end.x && start.y === end.y) d = "";
	else {
		var config = {
			start,
			end,
			offset: 10,
			verticalLength: 5
		};
		var startPosition = getVerticalPointOfLine(__assign$52(__assign$52({}, config), { type: "start" }));
		var endPosition = getVerticalPointOfLine(__assign$52(__assign$52({}, config), { type: "end" }));
		d = "M".concat(startPosition.leftX, " ").concat(startPosition.leftY, "\n    L").concat(startPosition.rightX, " ").concat(startPosition.rightY, "\n    L").concat(endPosition.rightX, " ").concat(endPosition.rightY, "\n    L").concat(endPosition.leftX, " ").concat(endPosition.leftY, " z");
	}
	return {
		d,
		fill: "transparent",
		stroke: "transparent",
		strokeWidth: 1,
		strokeDasharray: "4, 4"
	};
};
var getBezierControlPoints = function(_a) {
	var start = _a.start, end = _a.end, sourceNode = _a.sourceNode, targetNode = _a.targetNode, offset = _a.offset;
	var sBBox = getNodeBBox(sourceNode);
	var tBBox = getNodeBBox(targetNode);
	var sExpendBBox = getExpandedBBox(sBBox, offset);
	var tExpendBBox = getExpandedBBox(tBBox, offset);
	return {
		sNext: getExpandedBBoxPoint(sExpendBBox, sBBox, start),
		ePre: getExpandedBBoxPoint(tExpendBBox, tBBox, end)
	};
};
var getBezierPoints = function(path) {
	var list = path.replace(/M/g, "").replace(/C/g, ",").split(",");
	return [
		getBezierPoint(list[0]),
		getBezierPoint(list[1]),
		getBezierPoint(list[2]),
		getBezierPoint(list[3])
	];
};
var getBezierPoint = function(positionStr) {
	var _a = __read$38(positionStr.replace(/(^\s*)/g, "").split(" "), 2), x = _a[0], y = _a[1];
	return {
		x: +x,
		y: +y
	};
};
var getEndTangent = function(pointsList, offset) {
	var _a = __read$38(pointsList, 4), p1 = _a[0], cp1 = _a[1], cp2 = _a[2], p2 = _a[3];
	return [sampleCubic(p1, cp1, cp2, p2, offset), pointsList[3]];
};
/**
* 获取移动边后，文本位置距离边上的最近的一点
* @param point 边上文本的位置
* @param points 边的各个拐点
* TODO: Label实验没问题后统一改成新的计算方式，把这个方法废弃
*/
var getClosestPointOfPolyline = function(point, points) {
	var x = point.x, y = point.y;
	var pointsPosition = points2PointsList(points);
	var minDistance = Number.MAX_SAFE_INTEGER;
	var crossPoint;
	var segments = [];
	for (var i = 0; i < pointsPosition.length; i++) segments.push({
		start: pointsPosition[i],
		end: pointsPosition[(i + 1) % pointsPosition.length]
	});
	segments.forEach(function(item) {
		var start = item.start, end = item.end;
		if (start.x === end.x) {
			var pointXY = {
				x: start.x,
				y
			};
			var inSegment = isInSegment(pointXY, start, end);
			if (inSegment) {
				var currentDistance = Math.abs(start.x - x);
				if (currentDistance < minDistance) {
					minDistance = currentDistance;
					crossPoint = pointXY;
				}
			}
		} else if (start.y === end.y) {
			var pointXY = {
				x,
				y: start.y
			};
			var inSegment = isInSegment(pointXY, start, end);
			if (inSegment) {
				var currentDistance = Math.abs(start.y - y);
				if (currentDistance < minDistance) {
					minDistance = currentDistance;
					crossPoint = pointXY;
				}
			}
		}
	});
	if (!crossPoint) {
		var _a = segments[0], start = _a.start, end = _a.end;
		crossPoint = {
			x: start.x + (end.x - start.x) / 2,
			y: start.y + (end.y - start.y) / 2
		};
	}
	return crossPoint;
};
var pickEdgeConfig = function(data) {
	return pick(data, [
		"id",
		"type",
		"sourceNodeId",
		"sourceAnchorId",
		"targetNodeId",
		"targetAnchorId",
		"pointsList",
		"startPoint",
		"endPoint",
		"properties"
	]);
};
var twoPointDistance = function(source, target) {
	return Math.sqrt(Math.pow(source.x - target.x, 2) + Math.pow(source.y - target.y, 2));
};
/**
* 包装边生成函数
* @param graphModel graph model
* @param generator 用户自定义的边生成函数
*/
function createEdgeGenerator(graphModel, generator) {
	if (typeof generator !== "function") return function(_sourceNode, _targetNode, currentEdge) {
		return Object.assign({ type: graphModel.edgeType }, currentEdge);
	};
	return function(sourceNode, targetNode, currentEdge) {
		var result = generator(sourceNode, targetNode, currentEdge);
		if (!result) return { type: graphModel.edgeType };
		if (typeof result === "string") return Object.assign({}, currentEdge, { type: result });
		return Object.assign({ type: result }, currentEdge);
	};
}
var getSvgTextSize = function(_a) {
	var rows = _a.rows, rowsLength = _a.rowsLength, fontSize = _a.fontSize;
	var longestBytes = 0;
	forEach(rows, function(row) {
		var rowBytesLength = getBytesLength(row);
		longestBytes = rowBytesLength > longestBytes ? rowBytesLength : longestBytes;
	});
	return {
		width: Math.ceil(longestBytes / 2) * fontSize + fontSize / 4,
		height: rowsLength * (fontSize + 2) + fontSize / 4
	};
};
//#endregion
//#region node_modules/@logicflow/core/es/util/geometry.js
var __read$37 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$10 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
function snapToGrid(point, gridSize, snapGrid) {
	if (!snapGrid) return point;
	return gridSize * Math.round(point / gridSize) || point;
}
function getGridOffset(distance, gridSize) {
	return distance % gridSize;
}
/**
* 多边形设置 points 后，坐标平移至原点 并 根据 width、height 缩放
* @param points
* @param width
* @param height
*/
function normalizePolygon(points, width, height) {
	if (!points) return [];
	var minX = Math.min.apply(Math, __spreadArray$10([], __read$37(points.map(function(p) {
		return p[0];
	})), false));
	var maxX = Math.max.apply(Math, __spreadArray$10([], __read$37(points.map(function(p) {
		return p[0];
	})), false));
	var minY = Math.min.apply(Math, __spreadArray$10([], __read$37(points.map(function(p) {
		return p[1];
	})), false));
	var maxY = Math.max.apply(Math, __spreadArray$10([], __read$37(points.map(function(p) {
		return p[1];
	})), false));
	var dx = -minX;
	var dy = -minY;
	var translatedPoints = points.map(function(_a) {
		var _b = __read$37(_a, 2), x = _b[0], y = _b[1];
		return [x + dx, y + dy];
	});
	var bboxWidth = maxX - minX;
	var bboxHeight = maxY - minY;
	var scaleX = width ? width / bboxWidth : 1;
	var scaleY = height ? height / bboxHeight : 1;
	var scaleFactor = Math.min(scaleX, scaleY);
	return translatedPoints.map(function(_a) {
		var _b = __read$37(_a, 2), x = _b[0], y = _b[1];
		return [x * scaleFactor, y * scaleFactor];
	});
}
/**
* 通用圆角生成：为菱形、多边形、折线在转折处生成与矩形视觉一致的圆角
* - 圆角基于角平分线，切点距顶点的距离 t = r * tan(theta/2)
* - 半径会根据相邻边长度进行钳制，避免超过边长造成断裂
* - 多边形/菱形保持闭合；折线保持开口
*/
var generateRoundedCorners = function(points, radius, isClosedShape) {
	var n = points.length;
	if (n < 2 || radius <= 0) return points.slice();
	var toVec = function(a, b) {
		return {
			x: b.x - a.x,
			y: b.y - a.y
		};
	};
	var len = function(v) {
		return Math.hypot(v.x, v.y);
	};
	var norm = function(v) {
		var l = len(v) || 1;
		return {
			x: v.x / l,
			y: v.y / l
		};
	};
	var result = [];
	var makeRoundCorner = function(prev, curr, next) {
		var vPrev = toVec(curr, prev);
		var vNext = toVec(curr, next);
		var dPrev = len(vPrev);
		var dNext = len(vNext);
		if (dPrev < 1e-6 || dNext < 1e-6) return [curr];
		var uPrev = norm(vPrev);
		var uNext = norm(vNext);
		var t = Math.min(radius, dPrev * .45, dNext * .45);
		var start = {
			x: curr.x + uPrev.x * t,
			y: curr.y + uPrev.y * t
		};
		var end = {
			x: curr.x + uNext.x * t,
			y: curr.y + uNext.y * t
		};
		var steps = 10;
		var pts = [start];
		for (var k = 1; k < steps; k++) {
			var s = k / steps;
			var a = 1 - s;
			pts.push({
				x: a * a * start.x + 2 * a * s * curr.x + s * s * end.x,
				y: a * a * start.y + 2 * a * s * curr.y + s * s * end.y
			});
		}
		pts.push(end);
		return pts;
	};
	for (var i = 0; i < n; i++) {
		var prevIdx = i === 0 ? isClosedShape ? n - 1 : 0 : i - 1;
		var nextIdx = i === n - 1 ? isClosedShape ? 0 : n - 1 : i + 1;
		var prev = points[prevIdx];
		var curr = points[i];
		var next = points[nextIdx];
		if (!isClosedShape && (i === 0 || i === n - 1)) result.push(curr);
		else makeRoundCorner(prev, curr, next).forEach(function(p) {
			return result.push(p);
		});
	}
	var dedup = [];
	for (var i = 0; i < result.length; i++) {
		var p = result[i];
		if (dedup.length === 0 || Math.hypot(p.x - dedup[dedup.length - 1].x, p.y - dedup[dedup.length - 1].y) > 1e-6) dedup.push(p);
	}
	if (isClosedShape && dedup.length > 1) {
		var first = dedup[0];
		var last = dedup[dedup.length - 1];
		if (Math.hypot(first.x - last.x, first.y - last.y) < 1e-6) dedup.pop();
	}
	return dedup;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/graph.js
var __read$36 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
/**
* 判断一个点是否在指定区域
* @param point 当前点
* @param leftTopPoint 区域左上角点
* @param rightBottomPoint 区域的右下角点
*/
var isPointInArea = function(_a, _b, _c) {
	var _d = __read$36(_a, 2), x = _d[0], y = _d[1];
	var _e = __read$36(_b, 2), leftTopX = _e[0], leftTopY = _e[1];
	var _f = __read$36(_c, 2), rightBottomX = _f[0], rightBottomY = _f[1];
	return x > leftTopX && x < rightBottomX && y > leftTopY && y < rightBottomY;
};
/**
* 判断鼠标点击选中元素的时候，是否为多选
*/
var isMultipleSelect = function(e, editConfigModel) {
	var multipleSelectKey = editConfigModel.multipleSelectKey;
	var isMultiple = false;
	switch (multipleSelectKey) {
		case "meta":
			isMultiple = e.metaKey;
			break;
		case "alt":
			isMultiple = e.altKey;
			break;
		case "shift":
			isMultiple = e.shiftKey;
			break;
		case "ctrl":
			isMultiple = e.ctrlKey;
			break;
		default:
			isMultiple = false;
			break;
	}
	return isMultiple;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/vector.js
var __extends$43 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
function isVector(a1, a2) {
	return !!(a1 instanceof Vector && a2 instanceof Vector);
}
var Base = function(_super) {
	__extends$43(Base, _super);
	function Base(x, y, z) {
		var _this = _super.call(this, 3) || this;
		_this[0] = x;
		_this[1] = y;
		_this[2] = z;
		_this.x = x;
		_this.y = y;
		_this.z = z;
		Object.setPrototypeOf(_this, Base.prototype);
		return _this;
	}
	Base.prototype.add = function(v1) {
		if (isVector(this, v1)) return new Vector(this.x + v1.x, this.y + v1.y);
		var z = this.z + v1.z;
		return new Point((this.x + v1.x) / z, (this.y + v1.y) / z);
	};
	Base.prototype.subtract = function(v1) {
		if (isVector(this, v1)) return new Vector(this.x - v1.x, this.y - v1.y);
		var z = this.z - v1.z;
		return z === 0 ? new Vector(this.x - v1.x, this.y - v1.y) : new Point((this.x - v1.x) / z, (this.y - v1.y) / z);
	};
	return Base;
}(Array);
var Vector = function(_super) {
	__extends$43(Vector, _super);
	function Vector(x, y, z) {
		var _this = _super.call(this, x, y, z !== null && z !== void 0 ? z : 0) || this;
		Object.setPrototypeOf(_this, Vector.prototype);
		return _this;
	}
	Vector.prototype.toString = function() {
		return "Vector";
	};
	Vector.prototype.dot = function(v1) {
		var _this = this;
		return v1.reduce(function(prev, cur, index) {
			return prev + cur * _this[index];
		});
	};
	Vector.prototype.cross = function(v1) {
		return new Vector(this.y * v1.z - this.z * v1.y, this.z * v1.x - this.x * v1.z, this.x * v1.y - this.y * v1.x);
	};
	Vector.prototype.getLength = function() {
		return Math.hypot(this.x, this.y);
	};
	Vector.prototype.normalize = function() {
		var len = this.getLength();
		return new Vector(this.x / len, this.y / len);
	};
	Vector.prototype.crossZ = function(v1) {
		return this.x * v1.y - this.y * v1.x;
	};
	Vector.prototype.angle = function(v1) {
		var negative = this.crossZ(v1);
		var r = Math.acos(this.normalize().dot(v1.normalize()));
		return negative >= 0 ? r : -r;
	};
	return Vector;
}(Base);
var Point = function(_super) {
	__extends$43(Point, _super);
	function Point(x, y) {
		var _this = _super.call(this, x, y, 1) || this;
		Object.setPrototypeOf(_this, Point.prototype);
		return _this;
	}
	Point.prototype.toString = function() {
		return "Point";
	};
	return Point;
}(Base);
//#endregion
//#region node_modules/@logicflow/core/es/util/matrix.js
var __extends$42 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __read$35 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$9 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var Matrix = function(_super) {
	__extends$42(Matrix, _super);
	function Matrix() {
		var vectors = [];
		for (var _i = 0; _i < arguments.length; _i++) vectors[_i] = arguments[_i];
		var _this = _super.call(this, vectors.length) || this;
		_this.fill(new Array(3));
		vectors.forEach(function(v, index) {
			_this[index] = v;
		});
		_this.columns = vectors[0].length;
		_this.rows = vectors.length;
		Object.setPrototypeOf(_this, Matrix.prototype);
		return _this;
	}
	Matrix.prototype.getRow = function(index) {
		return this[index];
	};
	Matrix.prototype.getColumn = function(index) {
		return __spreadArray$9([], __read$35(this.map(function(row) {
			return row[index];
		})), false);
	};
	Matrix.prototype.transpose = function() {
		var vectors = [];
		for (var i = 0; i < this.columns; i++) vectors.push(this.getColumn(i));
		return new (Matrix.bind.apply(Matrix, __spreadArray$9([void 0], __read$35(vectors), false)))();
	};
	Matrix.prototype.cross = function(m1) {
		var arr = new Array(this.rows).fill("").map(function() {
			return [];
		});
		if (this.columns === m1.rows) for (var i = 0; i < this.rows; i++) {
			var row = this.getRow(i);
			var _loop_1 = function(j) {
				var column = m1.getColumn(j);
				arr[i][j] = row.reduce(function(prev, r, index) {
					return prev + r * column[index];
				}, 0);
			};
			for (var j = 0; j < m1.columns; j++) _loop_1(j);
		}
		return new (Matrix.bind.apply(Matrix, __spreadArray$9([void 0], __read$35(arr), false)))();
	};
	Matrix.prototype.to2D = function() {
		return this.map(function(item) {
			return [item[0], item[1]];
		});
	};
	Matrix.prototype.toPoints = function() {
		return this.map(function(item) {
			return new Point(item[0], item[1]);
		});
	};
	Matrix.prototype.toString = function() {
		var _a = __read$35(this[0], 2), a = _a[0], b = _a[1];
		var _b = __read$35(this[1], 2), c = _b[0], d = _b[1];
		var _c = __read$35(this[2], 2), e = _c[0], f = _c[1];
		return "matrix(".concat(a, " ").concat(b, " ").concat(c, " ").concat(d, " ").concat(e, " ").concat(f, ")");
	};
	Matrix.prototype.translate = function(tx, ty) {
		return this.cross(new TranslateMatrix(tx, ty));
	};
	Matrix.prototype.rotate = function(angle) {
		return this.cross(new RotateMatrix(angle));
	};
	Matrix.prototype.scale = function(sx, sy) {
		return this.cross(new ScaleMatrix(sx, sy));
	};
	return Matrix;
}(Array);
var RotateMatrix = function(_super) {
	__extends$42(RotateMatrix, _super);
	function RotateMatrix(theta) {
		var _this = _super.call(this, new Vector(+Math.cos(theta).toFixed(2), +Math.sin(theta).toFixed(2), 0), new Vector(-Math.sin(theta).toFixed(2), +Math.cos(theta).toFixed(2), 0), new Vector(0, 0, 1)) || this;
		Object.setPrototypeOf(_this, RotateMatrix.prototype);
		return _this;
	}
	RotateMatrix.prototype.inverse = function() {
		return this.transpose();
	};
	return RotateMatrix;
}(Matrix);
var ScaleMatrix = function(_super) {
	__extends$42(ScaleMatrix, _super);
	function ScaleMatrix(sx, sy) {
		var _this = _super.call(this, new Vector(sx, 0, 0), new Vector(0, sy, 0), new Vector(0, 0, 1)) || this;
		_this.sx = sx;
		_this.sy = sy;
		Object.setPrototypeOf(_this, ScaleMatrix.prototype);
		return _this;
	}
	ScaleMatrix.prototype.inverse = function() {
		return new ScaleMatrix(1 / this.sx, 1 / this.sy);
	};
	return ScaleMatrix;
}(Matrix);
var TranslateMatrix = function(_super) {
	__extends$42(TranslateMatrix, _super);
	function TranslateMatrix(tx, ty) {
		var _this = _super.call(this, new Vector(1, 0, 0), new Vector(0, 1, 0), new Vector(tx, ty, 1)) || this;
		_this.tx = tx;
		_this.ty = ty;
		Object.setPrototypeOf(_this, TranslateMatrix.prototype);
		return _this;
	}
	TranslateMatrix.prototype.inverse = function() {
		return new TranslateMatrix(-this.tx, -this.ty);
	};
	return TranslateMatrix;
}(Matrix);
//#endregion
//#region node_modules/@logicflow/core/es/util/mobx.js
configure({ isolateGlobalState: true });
//#endregion
//#region node_modules/@logicflow/core/es/util/node.js
var __assign$51 = function() {
	__assign$51 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$51.apply(this, arguments);
};
var __read$34 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var getAnchors = function(data) {
	return data.anchors;
};
var targetNodeInfo = function(position, graphModel) {
	var nodes = graphModel.nodes;
	var nodeInfo;
	for (var i = nodes.length - 1; i >= 0; i--) {
		var targetNode = nodes[i];
		if (isInNodeBbox(position, targetNode)) {
			var anchorInfo = targetNode.getTargetAnchor(position);
			if (anchorInfo) {
				var currentNodeInfo = {
					node: targetNode,
					anchorIndex: anchorInfo.index,
					anchor: anchorInfo.anchor
				};
				if (!nodeInfo || isNodeHigher(targetNode, nodeInfo.node, graphModel)) nodeInfo = currentNodeInfo;
			}
		}
	}
	return nodeInfo;
};
/**
* 比较两个节点
*/
var isNodeHigher = function(node1, node2, graphModel) {
	if (node1.zIndex > node2.zIndex) return true;
	return graphModel.nodesMap[node1.id].index > graphModel.nodesMap[node2.id].index;
};
var getClosestAnchor = function(position, node) {
	var anchors = getAnchors(node);
	var closest;
	var minDistance = Number.MAX_SAFE_INTEGER;
	for (var i = 0; i < anchors.length; i++) {
		var len = distance(position.x, position.y, anchors[i].x, anchors[i].y);
		if (len < minDistance) {
			minDistance = len;
			closest = {
				index: i,
				anchor: __assign$51(__assign$51({}, anchors[i]), {
					x: anchors[i].x,
					y: anchors[i].y,
					id: anchors[i].id
				})
			};
		}
	}
	return closest;
};
var distance = function(x1, y1, x2, y2) {
	return Math.hypot(x1 - x2, y1 - y2);
};
var isInNode = function(position, node, offset) {
	if (offset === void 0) offset = 0;
	var inNode = false;
	var bBox = getNodeBBox(node);
	var _a = __read$34(new Matrix([
		position.x,
		position.y,
		1
	]).translate(-node.x, -node.y).rotate(-node.rotate).translate(node.x, node.y)[0], 2);
	var reverseRotatedPosition = {
		x: _a[0],
		y: _a[1]
	};
	if (reverseRotatedPosition.x >= bBox.minX - offset && reverseRotatedPosition.x <= bBox.maxX + offset && reverseRotatedPosition.y >= bBox.minY - offset && reverseRotatedPosition.y <= bBox.maxY + offset) inNode = true;
	return inNode;
};
var isInNodeBbox = function(position, node) {
	return isInNode(position, node, 5);
};
var getNodeBBox = function(node) {
	var x = node.x, y = node.y, width = node.width, height = node.height;
	return {
		minX: x - width / 2,
		minY: y - height / 2,
		maxX: x + width / 2,
		maxY: y + height / 2,
		x,
		y,
		width,
		height,
		centerX: x,
		centerY: y
	};
};
var getRectRadiusCircle = function(node) {
	var _a = node, x = _a.x, y = _a.y, width = _a.width, height = _a.height, radius = _a.radius;
	return [
		{
			x: x - width / 2 + radius,
			y: y - height / 2 + radius,
			r: radius
		},
		{
			x: x + width / 2 - radius,
			y: y - height / 2 + radius,
			r: radius
		},
		{
			x: x - width / 2 + radius,
			y: y + height / 2 - radius,
			r: radius
		},
		{
			x: x + width / 2 - radius,
			y: y + height / 2 - radius,
			r: radius
		}
	];
};
var getClosestRadiusCenter = function(point, direction, node) {
	var radiusCenter = getRectRadiusCircle(node);
	var closestRadiusPoint;
	var minDistance = Number.MAX_SAFE_INTEGER;
	radiusCenter.forEach(function(item) {
		var radiusDistance = distance(point.x, point.y, item.x, item.y);
		if (radiusDistance < minDistance) {
			minDistance = radiusDistance;
			closestRadiusPoint = item;
		}
	});
	return getCrossPointWithCircle(point, direction, closestRadiusPoint);
};
var getCrossPointWithCircle = function(point, direction, node) {
	var crossPoint;
	var x = node.x, y = node.y, r = node.r;
	if (direction === SegmentDirection.HORIZONTAL) {
		var crossLeft = x - Math.sqrt(r * r - (point.y - y) * (point.y - y));
		var crossRight = x + Math.sqrt(r * r - (point.y - y) * (point.y - y));
		crossPoint = {
			x: Math.abs(crossLeft - point.x) < Math.abs(crossRight - point.x) ? crossLeft : crossRight,
			y: point.y
		};
	} else if (direction === SegmentDirection.VERTICAL) {
		var crossTop = y - Math.sqrt(r * r - (point.x - x) * (point.x - x));
		var crossBottom = y + Math.sqrt(r * r - (point.x - x) * (point.x - x));
		var crossY = Math.abs(crossTop - point.y) < Math.abs(crossBottom - point.y) ? crossTop : crossBottom;
		crossPoint = {
			x: point.x,
			y: crossY
		};
	}
	return crossPoint;
};
var pointEdgeDirection = function(point, node) {
	var dx = Math.abs(point.x - node.x);
	var dy = Math.abs(point.y - node.y);
	return dx / node.width > dy / node.height ? SegmentDirection.VERTICAL : SegmentDirection.HORIZONTAL;
};
var inStraightLineOfRect = function(point, node) {
	var rect = node;
	var isInStraight = false;
	var x = rect.x, y = rect.y, width = rect.width, height = rect.height, radius = rect.radius;
	var rectBox = {
		minX: x - width / 2 + radius,
		maxX: x + width / 2 - radius,
		minY: y - height / 2 + radius,
		maxY: y + height / 2 - radius
	};
	if (point.y === y + height / 2 || point.y === y - height / 2) isInStraight = point.x > rectBox.minX && point.x < rectBox.maxX;
	else if (point.x === x + width / 2 || point.x === x - width / 2) isInStraight = point.y > rectBox.minY && point.y < rectBox.maxY;
	return isInStraight;
};
var getCrossPointWithEllipse = function(point, direction, node) {
	var crossPoint;
	var _a = node, x = _a.x, y = _a.y, rx = _a.rx, ry = _a.ry;
	if (direction === SegmentDirection.HORIZONTAL) {
		var crossLeft = x - Math.sqrt(rx * rx - (point.y - y) * (point.y - y) * rx * rx / (ry * ry));
		var crossRight = x + Math.sqrt(rx * rx - (point.y - y) * (point.y - y) * rx * rx / (ry * ry));
		crossPoint = {
			x: Math.abs(crossLeft - point.x) < Math.abs(crossRight - point.x) ? crossLeft : crossRight,
			y: point.y
		};
	} else if (direction === SegmentDirection.VERTICAL) {
		var crossTop = y - Math.sqrt(ry * ry - (point.x - x) * (point.x - x) * ry * ry / (rx * rx));
		var crossBottom = y + Math.sqrt(ry * ry - (point.x - x) * (point.x - x) * ry * ry / (rx * rx));
		var crossY = Math.abs(crossTop - point.y) < Math.abs(crossBottom - point.y) ? crossTop : crossBottom;
		crossPoint = {
			x: point.x,
			y: crossY
		};
	}
	return crossPoint;
};
var getCrossPointWithPolygon = function(point, direction, node) {
	var pointsPosition = node.pointsPosition;
	var minDistance = Number.MAX_SAFE_INTEGER;
	var crossPoint;
	var segments = [];
	for (var i = 0; i < pointsPosition.length; i++) segments.push({
		start: pointsPosition[i],
		end: pointsPosition[(i + 1) % pointsPosition.length]
	});
	segments.forEach(function(item) {
		var start = item.start, end = item.end;
		var a = start;
		var b = end;
		if (start.x > end.x) {
			a = end;
			b = start;
		}
		var pointXY = {
			x: point.x,
			y: point.y
		};
		if (a.x === b.x && direction === SegmentDirection.HORIZONTAL) pointXY = {
			x: a.x,
			y: point.y
		};
		if (a.y === b.y && direction === SegmentDirection.VERTICAL) pointXY = {
			x: point.x,
			y: a.y
		};
		if (a.x !== b.x && a.y !== b.y) {
			var k = (b.y - a.y) / (b.x - a.x);
			var m = (a.x * b.y - b.x * a.y) / (a.x - b.x);
			if (direction === SegmentDirection.HORIZONTAL) pointXY = {
				x: (point.y - m) / k,
				y: point.y
			};
			else if (direction === SegmentDirection.VERTICAL) pointXY = {
				x: point.x,
				y: k * point.x + m
			};
		}
		if (isInSegment(pointXY, start, end)) {
			var currentDistance = distance(pointXY.x, pointXY.y, point.x, point.y);
			if (currentDistance < minDistance) {
				minDistance = currentDistance;
				crossPoint = pointXY;
			}
		}
	});
	return crossPoint;
};
var pickNodeConfig = function(data) {
	return pick(data, [
		"id",
		"type",
		"x",
		"y",
		"text",
		"label",
		"properties",
		"virtual",
		"rotate",
		"resizable",
		"rotatable"
	]);
};
/**
* 基于节点的边，重新获取新的节点
*/
var getNodeAnchorPosition = function(center, point, width, height) {
	var x = center.x, y = center.y;
	if (point.x > center.x) x = center.x + width / 2;
	else if (point.x < center.x) x = center.x - width / 2;
	if (point.y > center.y) y = center.y + height / 2;
	else if (point.y < center.y) y = center.y - height / 2;
	return {
		x,
		y
	};
};
/*********************************************************
* Text 节点文本相关工具函数
********************************************************/
var getHtmlTextHeight = function(_a) {
	var rows = _a.rows, style = _a.style, rowsLength = _a.rowsLength, className = _a.className;
	var dom = document.createElement("div");
	dom.className = className;
	dom.style.fontSize = "".concat(style.fontSize);
	dom.style.width = "".concat(style.width);
	dom.style.lineHeight = "".concat(style.lineHeight);
	dom.style.padding = "".concat(style.padding);
	if (style.fontFamily) dom.style.fontFamily = "".concat(style.fontFamily);
	if (rowsLength > 1) rows.forEach(function(row) {
		var rowDom = document.createElement("div");
		rowDom.textContent = row;
		dom.appendChild(rowDom);
	});
	else dom.textContent = rows[0];
	document.body.appendChild(dom);
	var height = dom.clientHeight;
	document.body.removeChild(dom);
	return height;
};
var getSvgTextWidthHeight = function(_a) {
	var rows = _a.rows, rowsLength = _a.rowsLength, fontSize = _a.fontSize;
	var longestBytes = 0;
	rows && rows.forEach(function(item) {
		var rowByteLength = getBytesLength(item);
		longestBytes = rowByteLength > longestBytes ? rowByteLength : longestBytes;
	});
	return {
		width: Math.ceil(longestBytes / 2) * fontSize + fontSize / 4,
		height: rowsLength * (fontSize + 2) + fontSize / 4
	};
};
/**
* @description 格式化边校验信息
*/
var formatAnchorConnectValidateData = function(data) {
	if (typeof data !== "object") return {
		isAllPass: !!data,
		msg: data ? "" : "不允许连接"
	};
	return data;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/uuid.js
var createUuid = function() {
	return v4();
};
/**
* 重新刷新流程图的所有id
*/
var refreshGraphId = function(graphData, prefix) {
	if (prefix === void 0) prefix = "";
	var nodeIdMap = graphData.nodes.reduce(function(nMap, node) {
		nMap[node.id] = prefix + v4();
		node.id = nMap[node.id];
		return nMap;
	}, {});
	graphData.edges.forEach(function(edge) {
		edge.id = prefix + v4();
		edge.sourceNodeId = nodeIdMap[edge.sourceNodeId];
		edge.targetNodeId = nodeIdMap[edge.targetNodeId];
	});
	return graphData;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/raf.js
var rafIdMap = /* @__PURE__ */ new Map();
var createRaf = function(callback) {
	var rafId = createUuid();
	function run() {
		callback();
		if (rafIdMap.get(rafId)) {
			var nId = window.requestAnimationFrame(run);
			rafIdMap.set(rafId, nId);
		}
	}
	var id = window.requestAnimationFrame(run);
	rafIdMap.set(rafId, id);
	return rafId;
};
var cancelRaf = function(rafId) {
	var eId = rafIdMap.get(rafId);
	if (eId) {
		window.cancelAnimationFrame(eId);
		rafIdMap.delete(rafId);
	}
};
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Text.js
var __assign$50 = function() {
	__assign$50 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$50.apply(this, arguments);
};
var __read$33 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Text(props) {
	var _a = props.x, x = _a === void 0 ? 0 : _a, _b = props.y, y = _b === void 0 ? 0 : _b, value = props.value, _c = props.fontSize, fontSize = _c === void 0 ? 12 : _c, _d = props.fill, fill = _d === void 0 ? "currentColor" : _d, _e = props.overflowMode, overflowMode = _e === void 0 ? "default" : _e, _f = props.textWidth, textWidth = _f === void 0 ? void 0 : _f, model = props.model;
	var attrs = {
		x,
		y,
		fill,
		fontSize,
		textAnchor: "middle",
		dominantBaseline: "central"
	};
	forEach(toPairs(props), function(_a) {
		var _b = __read$33(_a, 2), k = _b[0], v = _b[1];
		if (typeof v !== "object") attrs[k] = v;
	});
	if (value) {
		var rows = String(value).split(/[\r\n]/g);
		var rowsLength_1 = rows.length;
		if (overflowMode !== "default") {
			var BaseType = model.BaseType, modelType = model.modelType;
			if (BaseType === ElementType.NODE && modelType !== ModelType.TEXT_NODE || BaseType === ElementType.EDGE && textWidth) return renderHtmlText(props);
		}
		if (rowsLength_1 > 1) {
			var tSpans = rows.map(function(row, idx) {
				var tSpanLineHeight = fontSize + 2;
				return u("tspan", {
					className: "lf-text-tspan",
					x,
					y: y + (idx - (rowsLength_1 - 1) / 2) * tSpanLineHeight,
					children: row
				});
			});
			return u("text", __assign$50({}, attrs, { children: tSpans }));
		}
		return u("text", __assign$50({}, attrs, { children: value }));
	}
	return null;
}
function renderHtmlText(props) {
	var x = props.x, y = props.y, value = props.value, model = props.model, textWidth = props.textWidth, _a = props.fontSize, fontSize = _a === void 0 ? 12 : _a, lineHeight = props.lineHeight, _b = props.fontFamily, fontFamily = _b === void 0 ? "" : _b, _c = props.wrapPadding, wrapPadding = _c === void 0 ? "0, 0" : _c, overflowMode = props.overflowMode;
	var width = model.width, height = model.height, textHeight = model.textHeight;
	var textRealWidth = textWidth || width;
	var rows = String(value).split(/[\r\n]/g);
	var rowsLength = rows.length;
	var textRealHeight = getHtmlTextHeight({
		rows,
		style: {
			fontSize: "".concat(fontSize, "px"),
			width: "".concat(textRealWidth, "px"),
			fontFamily,
			lineHeight,
			padding: wrapPadding
		},
		rowsLength,
		className: "lf-get-text-height"
	});
	var foreignObjectHeight = height > textRealHeight ? height : textRealHeight;
	if (textHeight) foreignObjectHeight = textHeight;
	var isEllipsis = overflowMode === "ellipsis";
	if (isEllipsis) foreignObjectHeight = fontSize + 2;
	return u("g", { children: u("foreignObject", {
		width: textRealWidth,
		height: foreignObjectHeight,
		x: x - textRealWidth / 2,
		y: y - foreignObjectHeight / 2,
		style: {
			overflow: "visible",
			textAlign: "left"
		},
		children: u("div", {
			className: "lf-node-text-auto-wrap",
			style: {
				minHeight: foreignObjectHeight,
				width: textRealWidth,
				padding: wrapPadding
			},
			children: u("div", {
				className: isEllipsis ? "lf-node-text-ellipsis-content" : "lf-node-text-auto-wrap-content",
				title: isEllipsis ? rows.join("") : "",
				style: __assign$50({}, props),
				children: rows.map(function(row) {
					return u("div", {
						className: "lf-node-text--auto-wrap-inner",
						children: row
					});
				})
			})
		})
	}) });
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Line.js
var __assign$49 = function() {
	__assign$49 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$49.apply(this, arguments);
};
var __read$32 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Line(props) {
	var attrs = {
		x1: 10,
		y1: 10,
		x2: 20,
		y2: 20,
		stroke: "black"
	};
	forEach(toPairs(props), function(_a) {
		var _b = __read$32(_a, 2), k = _b[0], v = _b[1];
		if (k === "style") attrs[k] = v;
		else if (typeof v !== "object") attrs[k] = v;
	});
	return u("line", __assign$49({}, attrs));
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Rect.js
var __assign$48 = function() {
	__assign$48 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$48.apply(this, arguments);
};
var __read$31 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Rect(props) {
	var x = props.x, y = props.y, width = props.width, height = props.height, className = props.className, strokeWidth = props.strokeWidth, _a = props.radius, radius = _a === void 0 ? 0 : _a;
	var leftTopX = x - width / 2;
	var leftTopY = y - height / 2;
	var attrs = {};
	attrs["stroke-width"] = strokeWidth;
	forEach(toPairs(props), function(_a) {
		var _b = __read$31(_a, 2), k = _b[0], v = _b[1];
		if (typeof v !== "object") attrs[k] = v;
	});
	if (className) attrs.className = "lf-basic-shape ".concat(className);
	else attrs.className = "lf-basic-shape";
	if (radius) {
		attrs.rx = radius;
		attrs.ry = radius;
	}
	attrs.x = leftTopX;
	attrs.y = leftTopY;
	return u("rect", __assign$48({}, attrs));
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Path.js
var __assign$47 = function() {
	__assign$47 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$47.apply(this, arguments);
};
var __read$30 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Path(props) {
	var attrs = { d: "" };
	forEach(toPairs(props), function(_a) {
		var _b = __read$30(_a, 2), k = _b[0], v = _b[1];
		if (k === "style" || typeof v !== "object") attrs[k] = v;
	});
	return u("path", __assign$47({}, attrs));
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Circle.js
var __assign$46 = function() {
	__assign$46 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$46.apply(this, arguments);
};
var __read$29 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Circle(props) {
	var _a = props.x, x = _a === void 0 ? 0 : _a, _b = props.y, y = _b === void 0 ? 0 : _b, _c = props.r, r = _c === void 0 ? 4 : _c, className = props.className;
	var attrs = {
		cx: x,
		cy: y,
		r,
		fill: "transparent",
		fillOpacity: 1,
		strokeWidth: 1,
		stroke: "#000",
		strokeOpacity: 1
	};
	forEach(toPairs(props), function(_a) {
		var _b = __read$29(_a, 2), k = _b[0], v = _b[1];
		if (typeof v !== "object") attrs[k] = v;
	});
	if (className) attrs.className = "lf-basic-shape ".concat(className);
	else attrs.className = "lf-basic-shape";
	return u("circle", __assign$46({}, attrs));
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Ellipse.js
var __assign$45 = function() {
	__assign$45 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$45.apply(this, arguments);
};
var __read$28 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Ellipse(props) {
	var _a = props.x, x = _a === void 0 ? 0 : _a, _b = props.y, y = _b === void 0 ? 0 : _b, _c = props.rx, rx = _c === void 0 ? 4 : _c, _d = props.ry, ry = _d === void 0 ? 4 : _d, className = props.className;
	var attrs = {
		cx: x,
		cy: y,
		rx,
		ry,
		fill: "transparent",
		fillOpacity: 1,
		strokeWidth: 1,
		stroke: "#000",
		strokeOpacity: 1
	};
	forEach(toPairs(props), function(_a) {
		var _b = __read$28(_a, 2), k = _b[0], v = _b[1];
		if (typeof v !== "object") attrs[k] = v;
	});
	if (className) attrs.className = "lf-basic-shape ".concat(className);
	else attrs.className = "lf-basic-shape";
	return u("ellipse", __assign$45({}, attrs));
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Polygon.js
var __assign$44 = function() {
	__assign$44 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$44.apply(this, arguments);
};
var __read$27 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Polygon(props) {
	var _a = props.points, points = _a === void 0 ? [] : _a, className = props.className, radius = props.radius;
	var attrs = {
		fill: "transparent",
		fillOpacity: 1,
		strokeWidth: 1,
		stroke: "#000",
		strokeOpacity: 1,
		points: ""
	};
	forEach(toPairs(props), function(_a) {
		var _b = __read$27(_a, 2), k = _b[0], v = _b[1];
		if (typeof v !== "object") attrs[k] = v;
	});
	if (className) attrs.className = "lf-basic-shape ".concat(className);
	else attrs.className = "lf-basic-shape";
	if (radius) {
		var rounded = generateRoundedCorners(points.map(function(point) {
			return {
				x: point[0],
				y: point[1]
			};
		}), radius, true);
		attrs.d = rounded.length ? "M ".concat(rounded[0].x, " ").concat(rounded[0].y, " ").concat(rounded.slice(1).map(function(p) {
			return "L ".concat(p.x, " ").concat(p.y);
		}).join(" "), " Z") : "";
		delete attrs.points;
		return u("path", __assign$44({}, attrs));
	} else {
		attrs.points = points.map(function(point) {
			return point.join(",");
		}).join(" ");
		return u("polygon", __assign$44({}, attrs));
	}
}
//#endregion
//#region node_modules/@logicflow/core/es/view/shape/Polyline.js
var __assign$43 = function() {
	__assign$43 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$43.apply(this, arguments);
};
var __read$26 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
function Polyline(props) {
	var className = props.className;
	var attrs = {
		points: "",
		fill: "none"
	};
	forEach(toPairs(props), function(_a) {
		var _b = __read$26(_a, 2), k = _b[0], v = _b[1];
		if (k === "style") attrs[k] = v;
		else if (typeof v !== "object") attrs[k] = v;
	});
	if (className) attrs.className = "".concat(className);
	return u("polyline", __assign$43({}, attrs));
}
//#endregion
//#region node_modules/@logicflow/core/es/view/Control.js
var __extends$41 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$42 = function() {
	__assign$42 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$42.apply(this, arguments);
};
var __rest$3 = function(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
};
var __read$25 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var ResizeControlIndex;
(function(ResizeControlIndex) {
	ResizeControlIndex[ResizeControlIndex["LEFT_TOP"] = 0] = "LEFT_TOP";
	ResizeControlIndex[ResizeControlIndex["RIGHT_TOP"] = 1] = "RIGHT_TOP";
	ResizeControlIndex[ResizeControlIndex["RIGHT_BOTTOM"] = 2] = "RIGHT_BOTTOM";
	ResizeControlIndex[ResizeControlIndex["LEFT_BOTTOM"] = 3] = "LEFT_BOTTOM";
})(ResizeControlIndex || (ResizeControlIndex = {}));
var ResizeControl = function(_super) {
	__extends$41(ResizeControl, _super);
	function ResizeControl(props) {
		var _this = _super.call(this) || this;
		_this.isShiftPressed = false;
		_this.bindKeyboardEvents = function() {
			document.addEventListener("keydown", _this.handleKeyDown);
			document.addEventListener("keyup", _this.handleKeyUp);
		};
		_this.handleKeyDown = function(event) {
			if (event.key === "Shift") _this.isShiftPressed = true;
		};
		_this.handleKeyUp = function(event) {
			if (event.key === "Shift") _this.isShiftPressed = false;
		};
		_this.updateEdgePointByAnchors = function() {
			var _a = _this.nodeModel, id = _a.id, anchors = _a.anchors;
			forEach(_this.graphModel.getNodeEdges(id), function(edge) {
				if (edge.sourceNodeId === id) {
					var anchorItem = find(anchors, function(anchor) {
						return anchor.id === edge.sourceAnchorId;
					});
					if (anchorItem) edge.updateStartPoint({
						x: anchorItem.x,
						y: anchorItem.y
					});
				} else if (edge.targetNodeId === id) {
					var anchorItem = find(anchors, function(anchor) {
						return anchor.id === edge.targetAnchorId;
					});
					if (anchorItem) edge.updateEndPoint({
						x: anchorItem.x,
						y: anchorItem.y
					});
				}
			});
		};
		_this.triggerResizeEvent = function(preNodeData, curNodeData, deltaX, deltaY, index, nodeModel) {
			_this.graphModel.eventCenter.emit(EventType.NODE_RESIZE, {
				preData: preNodeData,
				data: curNodeData,
				deltaX,
				deltaY,
				index,
				model: nodeModel
			});
		};
		/**
		* 计算 Control 拖动后，节点的高度信息
		* @param index
		* @param resizeInfo
		* @param pct
		* @param freezeWidth
		* @param freezeHeight
		*/
		_this.recalcResizeInfo = function(index, resizeInfo, pct, freezeWidth, freezeHeight) {
			if (pct === void 0) pct = 1;
			if (freezeWidth === void 0) freezeWidth = false;
			if (freezeHeight === void 0) freezeHeight = false;
			var nextResizeInfo = cloneDeep(resizeInfo);
			var deltaX = nextResizeInfo.deltaX, deltaY = nextResizeInfo.deltaY;
			var width = nextResizeInfo.width, height = nextResizeInfo.height, PCTResizeInfo = nextResizeInfo.PCTResizeInfo;
			if (PCTResizeInfo) {
				var sensitivity = 4;
				var deltaScale = 0;
				var combineDelta = 0;
				switch (index) {
					case ResizeControlIndex.LEFT_TOP:
						combineDelta = (deltaX * -1 - deltaY) / sensitivity;
						break;
					case ResizeControlIndex.RIGHT_TOP:
						combineDelta = (deltaX - deltaY) / sensitivity;
						break;
					case ResizeControlIndex.RIGHT_BOTTOM:
						combineDelta = (deltaX + deltaY) / sensitivity;
						break;
					case ResizeControlIndex.LEFT_BOTTOM:
						combineDelta = (deltaX * -1 + deltaY) / sensitivity;
						break;
					default: break;
				}
				if (combineDelta !== 0) deltaScale = Math.round(combineDelta / PCTResizeInfo.ResizeBasis.basisHeight * 1e5) / 1e3;
				PCTResizeInfo.ResizePCT.widthPCT = Math.max(Math.min(PCTResizeInfo.ResizePCT.widthPCT + deltaScale, PCTResizeInfo.ScaleLimit.maxScaleLimit), PCTResizeInfo.ScaleLimit.minScaleLimit);
				PCTResizeInfo.ResizePCT.heightPCT = Math.max(Math.min(PCTResizeInfo.ResizePCT.heightPCT + deltaScale, PCTResizeInfo.ScaleLimit.maxScaleLimit), PCTResizeInfo.ScaleLimit.minScaleLimit);
				var spcWidth = Math.round(PCTResizeInfo.ResizePCT.widthPCT * PCTResizeInfo.ResizeBasis.basisWidth / 100);
				var spcHeight = Math.round(PCTResizeInfo.ResizePCT.heightPCT * PCTResizeInfo.ResizeBasis.basisHeight / 100);
				switch (index) {
					case ResizeControlIndex.LEFT_TOP:
						deltaX = width - spcWidth;
						deltaY = height - spcHeight;
						break;
					case ResizeControlIndex.RIGHT_TOP:
						deltaX = spcWidth - width;
						deltaY = height - spcHeight;
						break;
					case ResizeControlIndex.RIGHT_BOTTOM:
						deltaX = spcWidth - width;
						deltaY = spcHeight - height;
						break;
					case ResizeControlIndex.LEFT_BOTTOM:
						deltaX = width - spcWidth;
						deltaY = spcHeight - height;
						break;
					default: break;
				}
				return nextResizeInfo;
			}
			switch (index) {
				case ResizeControlIndex.LEFT_TOP:
					nextResizeInfo.width = freezeWidth ? width : width - deltaX * pct;
					nextResizeInfo.height = freezeHeight ? height : height - deltaY * pct;
					break;
				case ResizeControlIndex.RIGHT_TOP:
					nextResizeInfo.width = freezeWidth ? width : width + deltaX * pct;
					nextResizeInfo.height = freezeHeight ? height : height - deltaY * pct;
					break;
				case ResizeControlIndex.RIGHT_BOTTOM:
					nextResizeInfo.width = freezeWidth ? width : width + deltaX * pct;
					nextResizeInfo.height = freezeHeight ? height : height + deltaY * pct;
					break;
				case ResizeControlIndex.LEFT_BOTTOM:
					nextResizeInfo.width = freezeWidth ? width : width - deltaX * pct;
					nextResizeInfo.height = freezeHeight ? height : height + deltaY * pct;
					break;
				default: break;
			}
			return nextResizeInfo;
		};
		_this.resizeNode = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			var index = _this.index;
			var _b = _this.props, model = _b.model, graphModel = _b.graphModel, x = _b.x, y = _b.y;
			handleResize({
				x,
				y,
				deltaX,
				deltaY,
				index,
				nodeModel: model,
				graphModel,
				forceProportional: _this.isShiftPressed,
				cancelCallback: function() {
					_this.dragHandler.cancelDrag();
				}
			});
		};
		_this.onDragStart = function() {
			_this.graphModel.selectNodeById(_this.nodeModel.id);
		};
		_this.onDragging = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			var transformModel = _this.graphModel.transformModel;
			var _b = __read$25(transformModel.fixDeltaXY(deltaX, deltaY), 2), dx = _b[0], dy = _b[1];
			_this.resizeNode({
				deltaX: dx,
				deltaY: dy
			});
		};
		_this.onDragEnd = function() {
			var x = _this.nodeModel.x;
			var y = _this.nodeModel.y;
			_this.nodeModel.moveTo(x, y);
			_this.updateEdgePointByAnchors();
		};
		_this.onPointerDown = function(e) {
			e.stopPropagation();
			_this.dragHandler.handleMouseDown(e);
		};
		var index = props.index, model = props.model, graphModel = props.graphModel;
		_this.index = index;
		_this.nodeModel = model;
		_this.graphModel = graphModel;
		_this.dragHandler = new StepDrag({
			onDragStart: _this.onDragStart,
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd,
			step: graphModel.gridSize
		});
		_this.bindKeyboardEvents();
		return _this;
	}
	ResizeControl.prototype.componentWillUnmount = function() {
		this.dragHandler.destroy();
		document.removeEventListener("keydown", this.handleKeyDown);
		document.removeEventListener("keyup", this.handleKeyUp);
	};
	ResizeControl.prototype.getViewPosition = function(direction, x, y) {
		var _a = this.props.model.getResizeControlStyle(), _b = _a.width, width = _b === void 0 ? 8 : _b, _c = _a.height, height = _c === void 0 ? 8 : _c;
		switch (direction) {
			case "nw": return {
				x: x - width / 2,
				y: y - height / 2
			};
			case "ne": return {
				x: x + width / 2,
				y: y - height / 2
			};
			case "se": return {
				x: x + width / 2,
				y: y + height / 2
			};
			case "sw": return {
				x: x - width / 2,
				y: y + height / 2
			};
			default: return {
				x,
				y
			};
		}
	};
	ResizeControl.prototype.render = function() {
		var _a = this.props, x = _a.x, y = _a.y, direction = _a.direction;
		var _b = _a.model.getResizeControlStyle(), width = _b.width, height = _b.height, restStyle = __rest$3(_b, ["width", "height"]);
		var _c = this.getViewPosition(direction, x, y), viewX = _c.x, viewY = _c.y;
		return u("g", {
			className: "lf-resize-control lf-resize-control-".concat(direction),
			children: [u(Rect, __assign$42({
				className: "lf-resize-control-content",
				x: viewX,
				y: viewY,
				width: width !== null && width !== void 0 ? width : 7,
				height: height !== null && height !== void 0 ? height : 7
			}, restStyle)), u(Rect, {
				className: "lf-resize-control-content",
				x: viewX,
				y: viewY,
				width: width ? width + 5 : 25,
				height: width ? width + 5 : 25,
				fill: "transparent",
				stroke: "transparent",
				onPointerDown: this.onPointerDown
			})]
		});
	};
	return ResizeControl;
}(C);
var ResizeControlGroup = function(_super) {
	__extends$41(ResizeControlGroup, _super);
	function ResizeControlGroup() {
		return _super.call(this) || this;
	}
	ResizeControlGroup.prototype.getResizeControl = function() {
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var _b = getNodeBBox(model), minX = _b.minX, minY = _b.minY, maxX = _b.maxX, maxY = _b.maxY;
		return map([
			{
				index: ResizeControlIndex.LEFT_TOP,
				direction: "nw",
				x: minX,
				y: minY
			},
			{
				index: ResizeControlIndex.RIGHT_TOP,
				direction: "ne",
				x: maxX,
				y: minY
			},
			{
				index: ResizeControlIndex.RIGHT_BOTTOM,
				direction: "se",
				x: maxX,
				y: maxY
			},
			{
				index: ResizeControlIndex.LEFT_BOTTOM,
				direction: "sw",
				x: minX,
				y: maxY
			}
		], function(control) {
			return u(ResizeControl, __assign$42({}, control, {
				model,
				graphModel
			}));
		});
	};
	ResizeControlGroup.prototype.getResizeOutline = function() {
		var model = this.props.model;
		var x = model.x, y = model.y, width = model.width, height = model.height;
		var style = model.getResizeOutlineStyle();
		return u(Rect, __assign$42({}, style, {
			"pointer-events": "none",
			x,
			y,
			width: width + 10,
			height: height + 10
		}));
	};
	ResizeControlGroup.prototype.render = function() {
		return u("g", {
			className: "lf-resize-control-group",
			children: [this.getResizeOutline(), this.getResizeControl()]
		});
	};
	return ResizeControlGroup;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/algorithm/rotate.js
/**
* 根据两个点获取中心点坐标
*/
function getNewCenter(startPoint, endPoint) {
	var x1 = startPoint.x, y1 = startPoint.y;
	var x2 = endPoint.x, y2 = endPoint.y;
	return {
		x: x1 + (x2 - x1) / 2,
		y: y1 + (y2 - y1) / 2
	};
}
/**
* 旋转矩阵公式，可以获取某一个坐标旋转angle后的坐标
* @param p 当前坐标
* @param center 旋转中心
* @param angle 旋转角度（不是弧度）
*/
function calculatePointAfterRotateAngle(p, center, angle) {
	var radian = angleToRadian(angle);
	var dx = p.x - center.x;
	var dy = p.y - center.y;
	return {
		x: dx * Math.cos(radian) - dy * Math.sin(radian) + center.x,
		y: dx * Math.sin(radian) + dy * Math.cos(radian) + center.y
	};
}
/**
* 角度转弧度
* @param angle 角度
*/
function angleToRadian(angle) {
	return angle * Math.PI / 180;
}
/**
* 弧度转角度
* @param radian 弧度
*/
function radianToAngle(radian) {
	return radian / Math.PI * 180;
}
//#endregion
//#region node_modules/@logicflow/core/es/util/resize.js
function calculateWidthAndHeight(startRotatedTouchControlPoint, endRotatedTouchControlPoint, oldCenter, angle, freezeWidth, freezeHeight, oldWidth, oldHeight) {
	if (freezeWidth === void 0) freezeWidth = false;
	if (freezeHeight === void 0) freezeHeight = false;
	var freezePoint = {
		x: oldCenter.x - (startRotatedTouchControlPoint.x - oldCenter.x),
		y: oldCenter.y - (startRotatedTouchControlPoint.y - oldCenter.y)
	};
	var newCenter = getNewCenter(freezePoint, endRotatedTouchControlPoint);
	var endZeroTouchControlPoint = calculatePointAfterRotateAngle(endRotatedTouchControlPoint, newCenter, -angle);
	var zeroFreezePoint = calculatePointAfterRotateAngle(freezePoint, newCenter, -angle);
	if (freezeWidth) {
		var widthDx = Math.abs(endZeroTouchControlPoint.x - zeroFreezePoint.x) - oldWidth;
		if (newCenter.x > endZeroTouchControlPoint.x) newCenter.x = newCenter.x + widthDx / 2;
		else newCenter.x = newCenter.x - widthDx / 2;
	}
	if (freezeHeight) {
		var heightDy = Math.abs(endZeroTouchControlPoint.y - zeroFreezePoint.y) - oldHeight;
		if (newCenter.y > endZeroTouchControlPoint.y) newCenter.y = newCenter.y + heightDy / 2;
		else newCenter.y = newCenter.y - heightDy / 2;
	}
	if (freezeWidth || freezeHeight) {
		var nowFreezePoint = calculatePointAfterRotateAngle(zeroFreezePoint, newCenter, angle);
		var dx = nowFreezePoint.x - freezePoint.x;
		var dy = nowFreezePoint.y - freezePoint.y;
		newCenter.x = newCenter.x - dx;
		newCenter.y = newCenter.y - dy;
		zeroFreezePoint = calculatePointAfterRotateAngle(freezePoint, newCenter, -angle);
		endZeroTouchControlPoint = {
			x: newCenter.x - (zeroFreezePoint.x - newCenter.x),
			y: newCenter.y - (zeroFreezePoint.y - newCenter.y)
		};
	}
	var width = Math.abs(endZeroTouchControlPoint.x - zeroFreezePoint.x);
	var height = Math.abs(endZeroTouchControlPoint.y - zeroFreezePoint.y);
	if (freezeWidth) width = oldWidth;
	if (freezeHeight) height = oldHeight;
	return {
		width,
		height,
		center: newCenter
	};
}
function recalcRotatedResizeInfo(pct, resizeInfo, rotate, controlX, controlY, oldCenterX, oldCenterY, freezeWidth, freezeHeight) {
	if (freezeWidth === void 0) freezeWidth = false;
	if (freezeHeight === void 0) freezeHeight = false;
	var deltaX = resizeInfo.deltaX, deltaY = resizeInfo.deltaY, oldWidth = resizeInfo.width, oldHeight = resizeInfo.height;
	var angle = radianToAngle(rotate);
	var startZeroTouchControlPoint = {
		x: controlX,
		y: controlY
	};
	var oldCenter = {
		x: oldCenterX,
		y: oldCenterY
	};
	var startRotatedTouchControlPoint = calculatePointAfterRotateAngle(startZeroTouchControlPoint, oldCenter, angle);
	var _a = calculateWidthAndHeight(startRotatedTouchControlPoint, {
		x: startRotatedTouchControlPoint.x + deltaX,
		y: startRotatedTouchControlPoint.y + deltaY
	}, oldCenter, angle, freezeWidth, freezeHeight, oldWidth, oldHeight), newWidth = _a.width, newHeight = _a.height, newCenter = _a.center;
	resizeInfo.width = newWidth * pct;
	resizeInfo.height = newHeight * pct;
	resizeInfo.deltaX = (newCenter.x - oldCenter.x) * 2;
	resizeInfo.deltaY = (newCenter.y - oldCenter.y) * 2;
	return resizeInfo;
}
/**
* 计算 Control 拖动后，节点的高度信息
* @param index
* @param resizeInfo
* @param pct
* @param freezeWidth
* @param freezeHeight
*/
var recalcResizeInfo = function(index, resizeInfo, pct, freezeWidth, freezeHeight, rotate, controlX, controlY, oldCenterX, oldCenterY, forceProportional) {
	if (pct === void 0) pct = 1;
	if (freezeWidth === void 0) freezeWidth = false;
	if (freezeHeight === void 0) freezeHeight = false;
	if (rotate === void 0) rotate = 0;
	if (forceProportional === void 0) forceProportional = false;
	var nextResizeInfo = cloneDeep(resizeInfo);
	var deltaX = nextResizeInfo.deltaX, deltaY = nextResizeInfo.deltaY;
	var width = nextResizeInfo.width, height = nextResizeInfo.height, PCTResizeInfo = nextResizeInfo.PCTResizeInfo;
	if (PCTResizeInfo) {
		var sensitivity = 4;
		var deltaScale = 0;
		var combineDelta = 0;
		switch (index) {
			case ResizeControlIndex.LEFT_TOP:
				combineDelta = (deltaX * -1 - deltaY) / sensitivity;
				break;
			case ResizeControlIndex.RIGHT_TOP:
				combineDelta = (deltaX - deltaY) / sensitivity;
				break;
			case ResizeControlIndex.RIGHT_BOTTOM:
				combineDelta = (deltaX + deltaY) / sensitivity;
				break;
			case ResizeControlIndex.LEFT_BOTTOM:
				combineDelta = (deltaX * -1 + deltaY) / sensitivity;
				break;
			default: break;
		}
		if (combineDelta !== 0) deltaScale = Math.round(combineDelta / PCTResizeInfo.ResizeBasis.basisHeight * 1e5) / 1e3;
		PCTResizeInfo.ResizePCT.widthPCT = Math.max(Math.min(PCTResizeInfo.ResizePCT.widthPCT + deltaScale, PCTResizeInfo.ScaleLimit.maxScaleLimit), PCTResizeInfo.ScaleLimit.minScaleLimit);
		PCTResizeInfo.ResizePCT.heightPCT = Math.max(Math.min(PCTResizeInfo.ResizePCT.heightPCT + deltaScale, PCTResizeInfo.ScaleLimit.maxScaleLimit), PCTResizeInfo.ScaleLimit.minScaleLimit);
		var spcWidth = Math.round(PCTResizeInfo.ResizePCT.widthPCT * PCTResizeInfo.ResizeBasis.basisWidth / 100);
		var spcHeight = Math.round(PCTResizeInfo.ResizePCT.heightPCT * PCTResizeInfo.ResizeBasis.basisHeight / 100);
		switch (index) {
			case ResizeControlIndex.LEFT_TOP:
				deltaX = width - spcWidth;
				deltaY = height - spcHeight;
				break;
			case ResizeControlIndex.RIGHT_TOP:
				deltaX = spcWidth - width;
				deltaY = height - spcHeight;
				break;
			case ResizeControlIndex.RIGHT_BOTTOM:
				deltaX = spcWidth - width;
				deltaY = spcHeight - height;
				break;
			case ResizeControlIndex.LEFT_BOTTOM:
				deltaX = width - spcWidth;
				deltaY = spcHeight - height;
				break;
			default: break;
		}
		return nextResizeInfo;
	}
	if (rotate % (2 * Math.PI) !== 0 && controlX !== void 0 && controlY !== void 0) return recalcRotatedResizeInfo(pct, nextResizeInfo, rotate, controlX, controlY, oldCenterX, oldCenterY, freezeWidth, freezeHeight);
	if (forceProportional) {
		var aspectRatio = width / height;
		var primaryDelta = 0;
		var newWidth = width;
		var newHeight = height;
		switch (index) {
			case ResizeControlIndex.LEFT_TOP:
				primaryDelta = Math.abs(deltaX) > Math.abs(deltaY) ? -deltaX : -deltaY;
				if (aspectRatio >= 1) {
					newWidth = width + primaryDelta;
					newHeight = newWidth / aspectRatio;
				} else {
					newHeight = height + primaryDelta;
					newWidth = newHeight * aspectRatio;
				}
				nextResizeInfo.width = newWidth;
				nextResizeInfo.height = newHeight;
				nextResizeInfo.deltaX = width - newWidth;
				nextResizeInfo.deltaY = height - newHeight;
				break;
			case ResizeControlIndex.RIGHT_TOP:
				primaryDelta = Math.abs(deltaX) > Math.abs(deltaY) ? deltaX : -deltaY;
				if (aspectRatio >= 1) {
					newWidth = width + primaryDelta;
					newHeight = newWidth / aspectRatio;
				} else {
					newHeight = height - primaryDelta;
					newWidth = newHeight * aspectRatio;
				}
				nextResizeInfo.width = newWidth;
				nextResizeInfo.height = newHeight;
				nextResizeInfo.deltaX = newWidth - width;
				nextResizeInfo.deltaY = height - newHeight;
				break;
			case ResizeControlIndex.RIGHT_BOTTOM:
				primaryDelta = Math.abs(deltaX) > Math.abs(deltaY) ? deltaX : deltaY;
				if (aspectRatio >= 1) {
					newWidth = width + primaryDelta;
					newHeight = newWidth / aspectRatio;
				} else {
					newHeight = height + primaryDelta;
					newWidth = newHeight * aspectRatio;
				}
				nextResizeInfo.width = newWidth;
				nextResizeInfo.height = newHeight;
				nextResizeInfo.deltaX = newWidth - width;
				nextResizeInfo.deltaY = newHeight - height;
				break;
			case ResizeControlIndex.LEFT_BOTTOM:
				primaryDelta = Math.abs(deltaX) > Math.abs(deltaY) ? -deltaX : deltaY;
				if (aspectRatio >= 1) {
					newWidth = width - primaryDelta;
					newHeight = newWidth / aspectRatio;
				} else {
					newHeight = height + primaryDelta;
					newWidth = newHeight * aspectRatio;
				}
				nextResizeInfo.width = newWidth;
				nextResizeInfo.height = newHeight;
				nextResizeInfo.deltaX = width - newWidth;
				nextResizeInfo.deltaY = newHeight - height;
				break;
			default: break;
		}
		return nextResizeInfo;
	}
	switch (index) {
		case ResizeControlIndex.LEFT_TOP:
			nextResizeInfo.width = freezeWidth ? width : width - deltaX * pct;
			nextResizeInfo.height = freezeHeight ? height : height - deltaY * pct;
			break;
		case ResizeControlIndex.RIGHT_TOP:
			nextResizeInfo.width = freezeWidth ? width : width + deltaX * pct;
			nextResizeInfo.height = freezeHeight ? height : height - deltaY * pct;
			break;
		case ResizeControlIndex.RIGHT_BOTTOM:
			nextResizeInfo.width = freezeWidth ? width : width + deltaX * pct;
			nextResizeInfo.height = freezeHeight ? height : height + deltaY * pct;
			break;
		case ResizeControlIndex.LEFT_BOTTOM:
			nextResizeInfo.width = freezeWidth ? width : width - deltaX * pct;
			nextResizeInfo.height = freezeHeight ? height : height + deltaY * pct;
			break;
		default: break;
	}
	return nextResizeInfo;
};
var updateEdgePointByAnchors = function(nodeModel, graphModel) {
	var id = nodeModel.id, anchors = nodeModel.anchors;
	forEach(graphModel.getNodeEdges(id), function(edge) {
		if (edge.sourceNodeId === id) {
			var anchorItem = find(anchors, function(anchor) {
				return anchor.id === edge.sourceAnchorId;
			});
			if (anchorItem) edge.updateStartPoint({
				x: anchorItem.x,
				y: anchorItem.y
			});
		} else if (edge.targetNodeId === id) {
			var anchorItem = find(anchors, function(anchor) {
				return anchor.id === edge.targetAnchorId;
			});
			if (anchorItem) edge.updateEndPoint({
				x: anchorItem.x,
				y: anchorItem.y
			});
		}
	});
};
var triggerResizeEvent = function(preNodeData, curNodeData, deltaX, deltaY, index, nodeModel, graphModel) {
	graphModel.eventCenter.emit(EventType.NODE_RESIZE, {
		preData: preNodeData,
		data: curNodeData,
		deltaX,
		deltaY,
		index,
		model: nodeModel
	});
};
/**
* 处理节点的 resize 事件，提出来放到 utils 中，方便在外面（extension）中使用
* @param x
* @param y
* @param deltaX
* @param deltaY
* @param index
* @param nodeModel
* @param graphModel
* @param cancelCallback
* @param forceProportional
*/
var handleResize = function(_a) {
	var x = _a.x, y = _a.y, deltaX = _a.deltaX, deltaY = _a.deltaY, index = _a.index, nodeModel = _a.nodeModel, graphModel = _a.graphModel, cancelCallback = _a.cancelCallback, _b = _a.forceProportional, forceProportional = _b === void 0 ? false : _b;
	var r = nodeModel.r, rx = nodeModel.rx, ry = nodeModel.ry, width = nodeModel.width, height = nodeModel.height, PCTResizeInfo = nodeModel.PCTResizeInfo, minWidth = nodeModel.minWidth, minHeight = nodeModel.minHeight, maxWidth = nodeModel.maxWidth, maxHeight = nodeModel.maxHeight, rotate = nodeModel.rotate, oldCenterX = nodeModel.x, oldCenterY = nodeModel.y;
	var isFreezeWidth = minWidth === maxWidth;
	var isFreezeHeight = minHeight === maxHeight;
	var resizeInfo = {
		width: r || rx || width,
		height: r || ry || height,
		deltaX,
		deltaY,
		PCTResizeInfo
	};
	var pct = r || rx && ry ? 1 / 2 : 1;
	var controlX = x;
	var controlY = y;
	var nextSize = recalcResizeInfo(index, resizeInfo, pct, isFreezeWidth, isFreezeHeight, rotate, controlX, controlY, oldCenterX, oldCenterY, forceProportional);
	if (nextSize.width < minWidth || nextSize.width > maxWidth || nextSize.height < minHeight || nextSize.height > maxHeight) {
		cancelCallback === null || cancelCallback === void 0 || cancelCallback();
		return;
	}
	if (rotate % (2 * Math.PI) == 0 || PCTResizeInfo || controlX === void 0 || controlY === void 0) {
		nextSize.deltaX = isFreezeWidth ? 0 : nextSize.deltaX;
		nextSize.deltaY = isFreezeHeight ? 0 : nextSize.deltaY;
	}
	var preNodeData = nodeModel.getData();
	var curNodeData = nodeModel.resize(nextSize);
	if (preNodeData.x === curNodeData.x && preNodeData.y === curNodeData.y) return;
	updateEdgePointByAnchors(nodeModel, graphModel);
	triggerResizeEvent(preNodeData, curNodeData, deltaX, deltaY, index, nodeModel, graphModel);
};
//#endregion
//#region node_modules/@logicflow/core/es/util/sampling.js
var SAMPLING_FREQUENCY = 100;
var normal = {
	x: 1,
	y: 0,
	z: 0
};
function sampleCubic(p1, cp1, cp2, p2, offset) {
	var program = function(t) {
		if (t < 0 || t > 1) throw new RangeError("The value range of parameter \"t\" is [0,1]");
		return {
			x: p1.x * Math.pow(1 - t, 3) + 3 * cp1.x * t * Math.pow(1 - t, 2) + 3 * cp2.x * Math.pow(t, 2) * (1 - t) + p2.x * Math.pow(t, 3),
			y: p1.y * Math.pow(1 - t, 3) + 3 * cp1.y * t * Math.pow(1 - t, 2) + 3 * cp2.y * Math.pow(t, 2) * (1 - t) + p2.y * Math.pow(t, 3)
		};
	};
	var arrowDistance = 0;
	var t = 2;
	var x1 = p2.x, y1 = p2.y;
	var point = p2;
	while (arrowDistance < offset && t < 50) {
		point = program(1 - t / SAMPLING_FREQUENCY);
		var x2 = point.x, y2 = point.y;
		arrowDistance = distance(x1, y1, x2, y2);
		t++;
	}
	return point;
}
function crossByZ(v, v1) {
	return v.x * v1.y - v.y * v1.x;
}
function dot(v, w) {
	var v1 = [
		v.x,
		v.y,
		v.z
	];
	return [
		w.x,
		w.y,
		w.z
	].reduce(function(prev, cur, index) {
		return prev + cur * v1[index];
	});
}
function angle(v1, v2) {
	var negative = crossByZ(v1, v2);
	var r = Math.acos(dot(normalize(v1), normalize(v2)));
	return negative >= 0 ? r : -r;
}
function normalize(v) {
	var len = Math.hypot(v.x, v.y);
	return {
		x: v.x / len,
		y: v.y / len,
		z: 0
	};
}
function getThetaOfVector(v) {
	return angle(normal, v);
}
function degrees(radians) {
	return radians * (180 / Math.PI);
}
//#endregion
//#region node_modules/@logicflow/core/es/util/theme.js
var setupTheme = function(customTheme, themeMode) {
	var theme = cloneDeep(themeModeMap[themeMode || "default"]) || cloneDeep(themeModeMap.default);
	if (customTheme)
 /**
	* 为了不让默认样式被覆盖，使用 merge 方法
	* @docs https://lodash.com/docs/4.17.15#merge
	* 例如：锚点主题 hover，用户传入如下 ->
	* lf.setTheme({
	*   anchor: {
	*     fill: 'red'
	*   }
	* })
	*
	* 预期得到的结果如下：
	* {
	*   // ...
	*   anchor: {
	*     stroke: '#000',
	*     fill: 'red',
	*     r: 4,
	*     hover: {
	*       r: 10,
	*       fill: '#949494',
	*       fillOpacity: 0.5,
	*       stroke: '#949494',
	*     },
	*   },
	*   // ...
	* }
	*/
	theme = merge(theme, customTheme);
	return theme;
};
var addThemeMode = function(themeMode, style) {
	if (themeModeMap[themeMode]) {
		console.warn("theme mode ".concat(themeMode, " already exists"));
		return;
	}
	themeModeMap[themeMode] = style;
	backgroundModeMap[themeMode] = style.background || defaultBackground;
	gridModeMap[themeMode] = style.grid || defaultGrid;
};
var removeThemeMode = function(themeMode) {
	delete themeModeMap[themeMode];
	delete backgroundModeMap[themeMode];
	delete gridModeMap[themeMode];
};
var clearThemeMode = function() {
	var resetTheme = {
		colorful: {},
		dark: {},
		retro: {},
		default: {}
	};
	assign(themeModeMap, resetTheme);
	assign(backgroundModeMap, resetTheme);
	assign(gridModeMap, resetTheme);
};
var updateTheme = setupTheme;
//#endregion
//#region node_modules/@logicflow/core/es/util/zIndex.js
var maxIndex = 1e3;
var minIndex = 999;
var getZIndex = function() {
	return ++maxIndex;
};
var getMinIndex = function() {
	return --minIndex;
};
//#endregion
//#region node_modules/@logicflow/core/es/util/index.js
var util_exports = /* @__PURE__ */ __exportAll({
	Matrix: () => Matrix,
	Point: () => Point,
	RotateMatrix: () => RotateMatrix,
	ScaleMatrix: () => ScaleMatrix,
	StepDrag: () => StepDrag,
	TranslateMatrix: () => TranslateMatrix,
	Vector: () => Vector,
	action: () => action,
	addThemeMode: () => addThemeMode,
	calculateWidthAndHeight: () => calculateWidthAndHeight,
	cancelRaf: () => cancelRaf,
	clearThemeMode: () => clearThemeMode,
	computed: () => computed,
	configure: () => configure,
	costByPoints: () => costByPoints,
	createEdgeGenerator: () => createEdgeGenerator,
	createRaf: () => createRaf,
	createUuid: () => createUuid,
	defaultAnimationOffConfig: () => defaultAnimationOffConfig,
	defaultAnimationOnConfig: () => defaultAnimationOnConfig,
	degrees: () => degrees,
	distance: () => distance,
	estimateDistance: () => estimateDistance,
	filterRepeatPoints: () => filterRepeatPoints,
	formatAnchorConnectValidateData: () => formatAnchorConnectValidateData,
	formatData: () => formatData,
	generateRoundedCorners: () => generateRoundedCorners,
	getAnchors: () => getAnchors,
	getAppendAttributes: () => getAppendAttributes,
	getBBoxCrossPointsByPoint: () => getBBoxCrossPointsByPoint,
	getBBoxOfPoints: () => getBBoxOfPoints,
	getBBoxXCrossPoints: () => getBBoxXCrossPoints,
	getBBoxYCrossPoints: () => getBBoxYCrossPoints,
	getBezierControlPoints: () => getBezierControlPoints,
	getBezierPoints: () => getBezierPoints,
	getBoxByOriginNode: () => getBoxByOriginNode,
	getBytesLength: () => getBytesLength,
	getClosestAnchor: () => getClosestAnchor,
	getClosestPointOfPolyline: () => getClosestPointOfPolyline,
	getClosestRadiusCenter: () => getClosestRadiusCenter,
	getCrossPointInRect: () => getCrossPointInRect,
	getCrossPointWithCircle: () => getCrossPointWithCircle,
	getCrossPointWithEllipse: () => getCrossPointWithEllipse,
	getCrossPointWithPolygon: () => getCrossPointWithPolygon,
	getEndTangent: () => getEndTangent,
	getExpandedBBox: () => getExpandedBBox,
	getExpandedBBoxPoint: () => getExpandedBBoxPoint,
	getGridOffset: () => getGridOffset,
	getHtmlTextHeight: () => getHtmlTextHeight,
	getLongestEdge: () => getLongestEdge,
	getMinIndex: () => getMinIndex,
	getNextNeighborPoints: () => getNextNeighborPoints,
	getNodeAnchorPosition: () => getNodeAnchorPosition,
	getNodeBBox: () => getNodeBBox,
	getPointsFromBBox: () => getPointsFromBBox,
	getPolylinePoints: () => getPolylinePoints,
	getRectRadiusCircle: () => getRectRadiusCircle,
	getSimplePoints: () => getSimplePoints,
	getSimplePolyline: () => getSimplePolyline,
	getSvgTextSize: () => getSvgTextSize,
	getSvgTextWidthHeight: () => getSvgTextWidthHeight,
	getTextWidth: () => getTextWidth,
	getThetaOfVector: () => getThetaOfVector,
	getZIndex: () => getZIndex,
	handleResize: () => handleResize,
	heuristicCostEstimate: () => heuristicCostEstimate,
	inStraightLineOfRect: () => inStraightLineOfRect,
	isBboxOverLapping: () => isBboxOverLapping,
	isIe: () => isIe,
	isInNode: () => isInNode,
	isInNodeBbox: () => isInNodeBbox,
	isMultipleSelect: () => isMultipleSelect,
	isObservable: () => isObservable,
	isPointInArea: () => isPointInArea,
	isPointOutsideBBox: () => isPointOutsideBBox,
	isSegmentCrossingBBox: () => isSegmentCrossingBBox,
	isSegmentsCrossNode: () => isSegmentsCrossNode,
	isSegmentsInNode: () => isSegmentsInNode,
	isSegmentsIntersected: () => isSegmentsIntersected,
	mergeBBox: () => mergeBBox,
	normalizePolygon: () => normalizePolygon,
	observable: () => observable,
	pathFinder: () => pathFinder,
	pickEdgeConfig: () => pickEdgeConfig,
	pickNodeConfig: () => pickNodeConfig,
	pointDirection: () => pointDirection,
	pointEdgeDirection: () => pointEdgeDirection,
	pointFilter: () => pointFilter,
	points2PointsList: () => points2PointsList,
	reaction: () => reaction,
	rebuildPath: () => rebuildPath,
	recalcResizeInfo: () => recalcResizeInfo,
	refreshGraphId: () => refreshGraphId,
	removeClosePointFromOpenList: () => removeClosePointFromOpenList,
	removeThemeMode: () => removeThemeMode,
	sampleCubic: () => sampleCubic,
	segmentDirection: () => segmentDirection,
	setupAnimation: () => setupAnimation,
	setupEdgeModel: () => setupEdgeModel,
	setupTheme: () => setupTheme,
	snapToGrid: () => snapToGrid,
	targetNodeInfo: () => targetNodeInfo,
	toJS: () => toJS,
	triggerResizeEvent: () => triggerResizeEvent,
	twoPointDistance: () => twoPointDistance,
	updateAnimation: () => updateAnimation,
	updateEdgePointByAnchors: () => updateEdgePointByAnchors,
	updateTheme: () => updateTheme
});
//#endregion
//#region node_modules/@logicflow/core/es/model/edge/BaseEdgeModel.js
var __assign$41 = function() {
	__assign$41 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$41.apply(this, arguments);
};
var __decorate$27 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var BaseEdgeModel = function() {
	function BaseEdgeModel(data, graphModel) {
		var _a;
		this.BaseType = ElementType.EDGE;
		this.id = "";
		this.type = "";
		this.sourceNodeId = "";
		this.targetNodeId = "";
		this.textMode = TextMode.TEXT;
		this.text = {
			value: "",
			x: 0,
			y: 0,
			draggable: false,
			editable: true
		};
		this.points = "";
		this.pointsList = [];
		this.virtual = false;
		this.isSelected = false;
		this.isHovered = false;
		this.isHitable = true;
		this.isHittable = true;
		this.draggable = true;
		this.visible = true;
		this.isAnimation = false;
		this.isShowAdjustPoint = false;
		this.zIndex = 0;
		this.state = ElementState.DEFAULT;
		this.modelType = ModelType.EDGE;
		this.customTextPosition = false;
		this.style = {};
		this.arrowConfig = {
			markerEnd: "url(#marker-end-".concat(this.id, ")"),
			markerStart: "url(#marker-start-".concat(this.id, ")")
		};
		this.graphModel = graphModel;
		this.properties = (_a = data.properties) !== null && _a !== void 0 ? _a : {};
		this.initEdgeData(data);
		this.setAttributes();
	}
	/**
	* 初始化边数据
	* @overridable 支持重写
	* initNodeData和setAttributes的区别在于
	* initNodeData只在节点初始化的时候调用，用于初始化节点的所有属性。
	* setAttributes除了初始化调用外，还会在properties发生变化后调用。
	*/
	BaseEdgeModel.prototype.initEdgeData = function(data) {
		var _this = this;
		if (!data.properties) data.properties = {};
		if (!data.id) {
			var idGenerator = this.graphModel.idGenerator;
			var globalId = idGenerator && idGenerator(data.type);
			data.id = this.createId() || globalId || createUuid();
		}
		this.arrowConfig.markerEnd = "url(#marker-end-".concat(data.id, ")");
		this.arrowConfig.markerStart = "url(#marker-start-".concat(data.id, ")");
		var adjustEdgeStartAndEnd = this.graphModel.editConfigModel.adjustEdgeStartAndEnd;
		this.isShowAdjustPoint = adjustEdgeStartAndEnd;
		assign(this, pickEdgeConfig(data));
		var _a = this.graphModel, overlapMode = _a.overlapMode, eventCenter = _a.eventCenter;
		this.updateZIndexByOverlap(overlapMode, data.zIndex || getZIndex());
		this.setAnchors();
		this.initPoints();
		this.formatText(data);
		eventCenter.on("overlap:change", function(data) {
			var newMode = data.overlapMode;
			_this.updateZIndexByOverlap(newMode, _this.zIndex || getZIndex());
		});
	};
	/**
	* 设置model属性
	* @overridable 支持重写
	* 每次properties发生变化会触发
	*/
	BaseEdgeModel.prototype.setAttributes = function() {};
	BaseEdgeModel.prototype.createId = function() {
		return null;
	};
	/**
	* 自定义边样式
	*
	* @overridable 支持重写
	* @returns 自定义边样式
	*/
	BaseEdgeModel.prototype.getEdgeStyle = function() {
		return __assign$41(__assign$41({}, this.graphModel.theme.baseEdge), this.style);
	};
	/**
	* 自定义边调整点样式
	*
	* @overridable 支持重写
	* 在isShowAdjustPoint为true时会显示调整点。
	*/
	BaseEdgeModel.prototype.getAdjustPointStyle = function() {
		return __assign$41({}, this.graphModel.theme.edgeAdjust);
	};
	/**
	* 自定义边文本样式
	*
	* @overridable 支持重写
	*/
	BaseEdgeModel.prototype.getTextStyle = function() {
		var edgeText = this.graphModel.theme.edgeText;
		return cloneDeep(edgeText);
	};
	/**
	* 自定义边动画样式
	*
	* @overridable 支持重写
	* @example
	* getEdgeAnimationStyle() {
	*   const style = super.getEdgeAnimationStyle()
	*   style.stroke = 'blue'
	*   style.animationDuration = '30s'
	*   style.animationDirection = 'reverse'
	*   return style
	* }
	*/
	BaseEdgeModel.prototype.getEdgeAnimationStyle = function() {
		var edgeAnimation = this.graphModel.theme.edgeAnimation;
		return cloneDeep(edgeAnimation);
	};
	/**
	* 自定义边箭头样式
	*
	* @overridable 支持重写
	* @example
	* getArrowStyle() {
	*   const style = super.getArrowStyle()
	*   style.stroke = 'green'
	*   return style
	* }
	*/
	BaseEdgeModel.prototype.getArrowStyle = function() {
		var edgeStyle = this.getEdgeStyle();
		var edgeAnimationStyle = this.getEdgeAnimationStyle();
		var arrow = this.graphModel.theme.arrow;
		var stroke = this.isAnimation ? edgeAnimationStyle.stroke : edgeStyle.stroke;
		return __assign$41(__assign$41(__assign$41({}, edgeStyle), {
			fill: stroke,
			stroke
		}), arrow);
	};
	/**
	* 自定义边被选中时展示其范围的矩形框样式
	*
	* @overridable 支持重写
	* @example
	* // 隐藏outline
	* getOutlineStyle() {
	*   const style = super.getOutlineStyle()
	*   style.stroke = "none"
	*   style.hover.stroke = "none"
	*   return style
	* }
	*/
	BaseEdgeModel.prototype.getOutlineStyle = function() {
		var edgeOutline = this.graphModel.theme.edgeOutline;
		var attributes = __assign$41({}, edgeOutline);
		if (this.isHovered) {
			var hoverStyle = edgeOutline.hover || {};
			attributes = __assign$41(__assign$41({}, attributes), hoverStyle);
		}
		return cloneDeep(attributes);
	};
	/**
	* 重新自定义文本位置
	*
	* @overridable 支持重写
	*/
	BaseEdgeModel.prototype.getTextPosition = function() {
		return {
			x: 0,
			y: 0
		};
	};
	Object.defineProperty(BaseEdgeModel.prototype, "sourceNode", {
		/**
		* 边的前一个节点
		*/
		get: function() {
			var _a, _b;
			return (_b = (_a = this.graphModel) === null || _a === void 0 ? void 0 : _a.nodesMap[this.sourceNodeId]) === null || _b === void 0 ? void 0 : _b.model;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(BaseEdgeModel.prototype, "targetNode", {
		/**
		* 边的后一个节点
		*/
		get: function() {
			var _a, _b;
			return (_b = (_a = this.graphModel) === null || _a === void 0 ? void 0 : _a.nodesMap[this.targetNodeId]) === null || _b === void 0 ? void 0 : _b.model;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(BaseEdgeModel.prototype, "textPosition", {
		get: function() {
			return this.getTextPosition();
		},
		enumerable: false,
		configurable: true
	});
	/**
	* 内部方法，计算两个节点相连时的起点位置
	*/
	BaseEdgeModel.prototype.getBeginAnchor = function(sourceNode, targetNode, sourceAnchorId) {
		var position;
		var minDistance;
		var sourceAnchors = getAnchors(sourceNode);
		if (sourceAnchorId) {
			position = find(sourceAnchors, function(anchor) {
				return anchor.id === sourceAnchorId;
			});
			if (position) return position;
			console.warn("未在节点上找到指定的起点锚点".concat(sourceAnchorId, "，已使用默认锚点作为起点"));
		}
		sourceAnchors.forEach(function(anchor) {
			var distance = twoPointDistance(anchor, targetNode);
			if (minDistance === void 0) {
				minDistance = distance;
				position = anchor;
			} else if (distance < minDistance) {
				minDistance = distance;
				position = anchor;
			}
		});
		return position;
	};
	/**
	* 内部方法，计算两个节点相连时的终点位置
	*/
	BaseEdgeModel.prototype.getEndAnchor = function(targetNode, targetAnchorId) {
		var _this = this;
		var position;
		var minDistance;
		var targetAnchors = getAnchors(targetNode);
		if (targetAnchorId) {
			position = find(targetAnchors, function(anchor) {
				return anchor.id === targetAnchorId;
			});
			if (position) return position;
			console.warn("未在节点上找到指定的终点锚点".concat(targetAnchorId, "，已使用默认锚点作为终点"));
		}
		targetAnchors.forEach(function(anchor) {
			if (!_this.startPoint) return;
			var distance = twoPointDistance(anchor, _this.startPoint);
			if (minDistance === void 0) {
				minDistance = distance;
				position = anchor;
			} else if (distance < minDistance) {
				minDistance = distance;
				position = anchor;
			}
		});
		return position;
	};
	/**
	* 获取当前边的properties
	*/
	BaseEdgeModel.prototype.getProperties = function() {
		return toJS(this.properties);
	};
	/**
	* 获取被保存时返回的数据
	*
	* @overridable 支持重写
	*/
	BaseEdgeModel.prototype.getData = function() {
		var properties = this.properties;
		if (isObservable(properties)) properties = toJS(properties);
		var data = {
			id: this.id,
			type: this.type,
			properties,
			sourceNodeId: this.sourceNodeId,
			targetNodeId: this.targetNodeId,
			sourceAnchorId: this.sourceAnchorId,
			targetAnchorId: this.targetAnchorId,
			startPoint: assign({}, this.startPoint),
			endPoint: assign({}, this.endPoint)
		};
		if ([OverlapMode.INCREASE, OverlapMode.STATIC].includes(this.graphModel.overlapMode)) data.zIndex = this.zIndex;
		var _a = this.text, x = _a.x, y = _a.y, value = _a.value;
		if (value) data.text = {
			x,
			y,
			value
		};
		return data;
	};
	/**
	* 获取边的数据
	*
	* @overridable 支持重写
	* 用于在历史记录时获取节点数据。
	* 在某些情况下，如果希望某个属性变化不引起history的变化，
	* 可以重写此方法。
	*/
	BaseEdgeModel.prototype.getHistoryData = function() {
		return this.getData();
	};
	/**
	* 设置边的属性，会触发重新渲染
	* @param key 属性名
	* @param val 属性值
	*/
	BaseEdgeModel.prototype.setProperty = function(key, val) {
		set(this.properties, key, formatData(val));
		this.setAttributes();
	};
	/**
	* 删除边的属性，会触发重新渲染
	* @param key 属性名
	*/
	BaseEdgeModel.prototype.deleteProperty = function(key) {
		delete this.properties[key];
		this.setAttributes();
	};
	/**
	* 设置边的属性，会触发重新渲染
	* @param properties 要更新的 properties，会做合并
	*/
	BaseEdgeModel.prototype.setProperties = function(properties) {
		this.properties = __assign$41(__assign$41({}, toJS(this.properties)), formatData(properties));
		this.setAttributes();
	};
	/**
	* 修改边的id
	*/
	BaseEdgeModel.prototype.changeEdgeId = function(id) {
		var _a = this.arrowConfig, markerEnd = _a.markerEnd, markerStart = _a.markerStart;
		if (markerStart && markerStart === "url(#marker-start-".concat(this.id, ")")) this.arrowConfig.markerStart = "url(#marker-start-".concat(id, ")");
		if (markerEnd && markerEnd === "url(#marker-end-".concat(this.id, ")")) this.arrowConfig.markerEnd = "url(#marker-end-".concat(id, ")");
		this.id = id;
	};
	/**
	* 设置边样式，用于插件开发时跳过自定义边的渲染。大多数情况下，不需要使用此方法。
	* 如果需要设置边的样式，请使用 getEdgeStyle 方法自定义边样式。
	*/
	BaseEdgeModel.prototype.setStyle = function(key, val) {
		var _a;
		this.style = __assign$41(__assign$41({}, this.style), (_a = {}, _a[key] = formatData(val), _a));
	};
	/**
	* 设置边样式，用于插件开发时跳过自定义边的渲染。大多数情况下，不需要使用此方法。
	* 如果需要设置边的样式，请使用 getEdgeStyle 方法自定义边样式。
	*/
	BaseEdgeModel.prototype.setStyles = function(styles) {
		this.style = __assign$41(__assign$41({}, this.style), formatData(styles));
	};
	/**
	* 设置边样式，用于插件开发时跳过自定义边的渲染。大多数情况下，不需要使用此方法。
	* 如果需要设置边的样式，请使用 getEdgeStyle 方法自定义边样式。
	*/
	BaseEdgeModel.prototype.updateStyles = function(styles) {
		this.style = __assign$41({}, formatData(styles));
	};
	/**
	* 设置当前元素的文本模式
	* @param mode
	*/
	BaseEdgeModel.prototype.setTextMode = function(mode) {
		this.textMode = mode;
	};
	/**
	* 内部方法，处理初始化文本格式
	*/
	BaseEdgeModel.prototype.formatText = function(data) {
		var _a, _b, _c;
		var _d = this.graphModel.editConfigModel, edgeTextDraggable = _d.edgeTextDraggable, edgeTextEdit = _d.edgeTextEdit;
		var _e = this.textPosition, x = _e.x, y = _e.y;
		var text = data.text;
		var textConfig = {
			value: "",
			x,
			y,
			draggable: edgeTextDraggable,
			editable: edgeTextEdit
		};
		if (text) if (typeof text === "string") textConfig = __assign$41(__assign$41({}, textConfig), { value: text });
		else {
			textConfig = __assign$41(__assign$41({}, textConfig), {
				x: (_a = text.x) !== null && _a !== void 0 ? _a : x,
				y: (_b = text.y) !== null && _b !== void 0 ? _b : y,
				value: (_c = text.value) !== null && _c !== void 0 ? _c : ""
			});
			if (!isUndefined(text.draggable)) textConfig.draggable = text.draggable;
			if (!isUndefined(text.editable)) textConfig.editable = text.editable;
		}
		this.text = textConfig;
	};
	/**
	* 重置文本位置
	*/
	BaseEdgeModel.prototype.resetTextPosition = function() {
		var _a = this.textPosition, x = _a.x, y = _a.y;
		this.text.x = x;
		this.text.y = y;
	};
	/**
	* 移动边上的文本
	*/
	BaseEdgeModel.prototype.moveText = function(deltaX, deltaY) {
		var _a = this.text, x = _a.x, y = _a.y, value = _a.value, draggable = _a.draggable, editable = _a.editable;
		this.text = {
			value,
			editable,
			draggable,
			x: x + deltaX,
			y: y + deltaY
		};
	};
	/**
	* 设置文本位置和值
	*/
	BaseEdgeModel.prototype.setText = function(textConfig) {
		if (textConfig) assign(this.text, textConfig);
	};
	/**
	* 更新文本的值
	*/
	BaseEdgeModel.prototype.updateText = function(value) {
		this.text = __assign$41(__assign$41({}, toJS(this.text)), { value });
	};
	/**
	* 内部方法，计算边的起点和终点和其对于的锚点Id
	*/
	BaseEdgeModel.prototype.setAnchors = function() {
		if (!this.sourceAnchorId || !this.startPoint) {
			var anchor = this.getBeginAnchor(this.sourceNode, this.targetNode, this.sourceAnchorId);
			if (!anchor) throw new Error("无法获取beginAnchor，请检查anchors相关逻辑，anchors不能为空");
			if (!this.startPoint) this.startPoint = {
				x: anchor.x,
				y: anchor.y
			};
			if (!this.sourceAnchorId) this.sourceAnchorId = anchor.id;
		}
		if (!this.targetAnchorId || !this.endPoint) {
			var anchor = this.getEndAnchor(this.targetNode, this.targetAnchorId);
			if (!anchor) throw new Error("无法获取endAnchor，请检查anchors相关逻辑，anchors不能为空");
			if (!this.endPoint) this.endPoint = {
				x: anchor.x,
				y: anchor.y
			};
			if (!this.targetAnchorId) this.targetAnchorId = anchor.id;
		}
	};
	BaseEdgeModel.prototype.setSelected = function(flag) {
		if (flag === void 0) flag = true;
		this.isSelected = flag;
	};
	BaseEdgeModel.prototype.setHovered = function(flag) {
		if (flag === void 0) flag = true;
		this.isHovered = flag;
	};
	BaseEdgeModel.prototype.setHitable = function(flag) {
		if (flag === void 0) flag = true;
		this.isHitable = flag;
	};
	BaseEdgeModel.prototype.setHittable = function(flag) {
		if (flag === void 0) flag = true;
		this.isHittable = flag;
	};
	BaseEdgeModel.prototype.openEdgeAnimation = function() {
		this.isAnimation = true;
	};
	BaseEdgeModel.prototype.closeEdgeAnimation = function() {
		this.isAnimation = false;
	};
	BaseEdgeModel.prototype.setElementState = function(state, additionStateData) {
		this.state = state;
		this.additionStateData = additionStateData;
	};
	BaseEdgeModel.prototype.updateStartPoint = function(anchor) {
		this.startPoint = anchor;
	};
	BaseEdgeModel.prototype.moveStartPoint = function(deltaX, deltaY) {
		if (this.startPoint) {
			this.startPoint.x += deltaX;
			this.startPoint.y += deltaY;
		}
	};
	BaseEdgeModel.prototype.updateEndPoint = function(anchor) {
		this.endPoint = anchor;
	};
	BaseEdgeModel.prototype.moveEndPoint = function(deltaX, deltaY) {
		if (this.endPoint) {
			this.endPoint.x += deltaX;
			this.endPoint.y += deltaY;
		}
	};
	BaseEdgeModel.prototype.setZIndex = function(zIndex) {
		if (zIndex === void 0) zIndex = 0;
		this.zIndex = zIndex;
	};
	BaseEdgeModel.prototype.initPoints = function() {};
	BaseEdgeModel.prototype.updateAttributes = function(attributes) {
		assign(this, attributes);
	};
	BaseEdgeModel.prototype.getAdjustStart = function() {
		return this.startPoint;
	};
	BaseEdgeModel.prototype.getAdjustEnd = function() {
		return this.endPoint;
	};
	BaseEdgeModel.prototype.updateAfterAdjustStartAndEnd = function(_a) {
		var startPoint = _a.startPoint, endPoint = _a.endPoint;
		this.updateStartPoint({
			x: startPoint.x,
			y: startPoint.y
		});
		this.updateEndPoint({
			x: endPoint.x,
			y: endPoint.y
		});
	};
	BaseEdgeModel.prototype.updateZIndexByOverlap = function(overlapMode, defaultZIndex) {
		switch (overlapMode) {
			case OverlapMode.DEFAULT:
				this.zIndex = 0;
				break;
			case OverlapMode.EDGE_TOP:
				this.zIndex = 1;
				break;
			case OverlapMode.INCREASE:
				this.zIndex = defaultZIndex;
				break;
			default: break;
		}
	};
	BaseEdgeModel.BaseType = ElementType.EDGE;
	__decorate$27([observable], BaseEdgeModel.prototype, "type", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "sourceNodeId", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "targetNodeId", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "startPoint", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "endPoint", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "textMode", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "text", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "properties", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "points", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "pointsList", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "isSelected", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "isHovered", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "isHitable", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "isHittable", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "draggable", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "visible", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "isAnimation", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "isShowAdjustPoint", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "zIndex", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "state", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "style", void 0);
	__decorate$27([observable], BaseEdgeModel.prototype, "arrowConfig", void 0);
	__decorate$27([computed], BaseEdgeModel.prototype, "sourceNode", null);
	__decorate$27([computed], BaseEdgeModel.prototype, "targetNode", null);
	__decorate$27([computed], BaseEdgeModel.prototype, "textPosition", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setProperty", null);
	__decorate$27([action], BaseEdgeModel.prototype, "deleteProperty", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setProperties", null);
	__decorate$27([action], BaseEdgeModel.prototype, "changeEdgeId", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setStyle", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setStyles", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateStyles", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setTextMode", null);
	__decorate$27([action], BaseEdgeModel.prototype, "formatText", null);
	__decorate$27([action], BaseEdgeModel.prototype, "resetTextPosition", null);
	__decorate$27([action], BaseEdgeModel.prototype, "moveText", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setText", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateText", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setAnchors", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setSelected", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setHovered", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setHitable", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setHittable", null);
	__decorate$27([action], BaseEdgeModel.prototype, "openEdgeAnimation", null);
	__decorate$27([action], BaseEdgeModel.prototype, "closeEdgeAnimation", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setElementState", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateStartPoint", null);
	__decorate$27([action], BaseEdgeModel.prototype, "moveStartPoint", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateEndPoint", null);
	__decorate$27([action], BaseEdgeModel.prototype, "moveEndPoint", null);
	__decorate$27([action], BaseEdgeModel.prototype, "setZIndex", null);
	__decorate$27([action], BaseEdgeModel.prototype, "initPoints", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateAttributes", null);
	__decorate$27([action], BaseEdgeModel.prototype, "getAdjustStart", null);
	__decorate$27([action], BaseEdgeModel.prototype, "getAdjustEnd", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateAfterAdjustStartAndEnd", null);
	__decorate$27([action], BaseEdgeModel.prototype, "updateZIndexByOverlap", null);
	return BaseEdgeModel;
}();
//#endregion
//#region node_modules/@logicflow/core/es/model/edge/BezierEdgeModel.js
var __extends$40 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$40 = function() {
	__assign$40 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$40.apply(this, arguments);
};
var __decorate$26 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$24 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BezierEdgeModel = function(_super) {
	__extends$40(BezierEdgeModel, _super);
	function BezierEdgeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.BEZIER_EDGE;
		_this.path = "";
		_this.initEdgeData(data);
		_this.setAttributes();
		return _this;
	}
	BezierEdgeModel.prototype.initEdgeData = function(data) {
		this.offset = get(data, "properties.offset", 100);
		_super.prototype.initEdgeData.call(this, data);
	};
	BezierEdgeModel.prototype.getEdgeStyle = function() {
		var bezier = this.graphModel.theme.bezier;
		var style = _super.prototype.getEdgeStyle.call(this);
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$40(__assign$40(__assign$40({}, style), cloneDeep(bezier)), cloneDeep(customStyle));
	};
	BezierEdgeModel.prototype.getTextPosition = function() {
		if (this.pointsList && this.pointsList.length > 0) {
			var pointsXSum_1 = 0;
			var pointsYSum_1 = 0;
			this.pointsList.forEach(function(_a) {
				var x = _a.x, y = _a.y;
				pointsXSum_1 += x;
				pointsYSum_1 += y;
			});
			return {
				x: pointsXSum_1 / this.pointsList.length,
				y: pointsYSum_1 / this.pointsList.length
			};
		}
		return {
			x: (this.startPoint.x + this.endPoint.x) / 2,
			y: (this.startPoint.y + this.endPoint.y) / 2
		};
	};
	BezierEdgeModel.prototype.getData = function() {
		var data = _super.prototype.getData.call(this);
		var pointsList = this.pointsList.map(function(_a) {
			return {
				x: _a.x,
				y: _a.y
			};
		});
		return __assign$40(__assign$40({}, data), { pointsList });
	};
	BezierEdgeModel.prototype.getControls = function() {
		var start = this.startPoint;
		var end = this.endPoint;
		return getBezierControlPoints({
			start,
			end,
			sourceNode: this.sourceNode,
			targetNode: this.targetNode,
			offset: this.offset
		});
	};
	BezierEdgeModel.prototype.getPath = function(points) {
		var _a = __read$24(points, 4), start = _a[0], sNext = _a[1], ePre = _a[2], end = _a[3];
		return "M ".concat(start.x, " ").concat(start.y, "\n    C ").concat(sNext.x, " ").concat(sNext.y, ",\n    ").concat(ePre.x, " ").concat(ePre.y, ",\n    ").concat(end.x, " ").concat(end.y);
	};
	BezierEdgeModel.prototype.initPoints = function() {
		if (this.pointsList.length > 0) this.path = this.getPath(this.pointsList);
		else this.updatePoints();
	};
	BezierEdgeModel.prototype.updatePoints = function() {
		var _a = this.getControls(), sNext = _a.sNext, ePre = _a.ePre;
		this.updatePath(sNext, ePre);
	};
	BezierEdgeModel.prototype.updatePath = function(sNext, ePre) {
		sNext = cloneDeep(sNext);
		ePre = cloneDeep(ePre);
		var start = {
			x: this.startPoint.x,
			y: this.startPoint.y
		};
		var end = {
			x: this.endPoint.x,
			y: this.endPoint.y
		};
		if (!sNext || !ePre) {
			var control = this.getControls();
			sNext = control.sNext;
			ePre = control.ePre;
		}
		this.pointsList = [
			start,
			sNext,
			ePre,
			end
		];
		this.path = this.getPath(this.pointsList);
	};
	BezierEdgeModel.prototype.updateStartPoint = function(anchor) {
		this.startPoint = Object.assign({}, anchor);
		this.updatePoints();
	};
	BezierEdgeModel.prototype.updateEndPoint = function(anchor) {
		this.endPoint = Object.assign({}, anchor);
		this.updatePoints();
	};
	BezierEdgeModel.prototype.moveStartPoint = function(deltaX, deltaY) {
		this.startPoint.x += deltaX;
		this.startPoint.y += deltaY;
		var _a = __read$24(this.pointsList, 3), sNext = _a[1], ePre = _a[2];
		sNext.x += deltaX;
		sNext.y += deltaY;
		this.updatePath(sNext, ePre);
	};
	BezierEdgeModel.prototype.moveEndPoint = function(deltaX, deltaY) {
		this.endPoint.x += deltaX;
		this.endPoint.y += deltaY;
		var _a = __read$24(this.pointsList, 3), sNext = _a[1], ePre = _a[2];
		ePre.x += deltaX;
		ePre.y += deltaY;
		this.updatePath(sNext, ePre);
	};
	BezierEdgeModel.prototype.updateAdjustAnchor = function(anchor, type) {
		var _a;
		if (type === "sNext") this.pointsList[1] = anchor;
		else if (type === "ePre") this.pointsList[2] = anchor;
		this.path = this.getPath(this.pointsList);
		if ((_a = this.text) === null || _a === void 0 ? void 0 : _a.value) this.setText(assign({}, this.text, this.textPosition));
	};
	BezierEdgeModel.prototype.getAdjustStart = function() {
		return this.pointsList[0] || this.startPoint;
	};
	BezierEdgeModel.prototype.getAdjustEnd = function() {
		var pointsList = this.pointsList;
		return pointsList[pointsList.length - 1] || this.endPoint;
	};
	BezierEdgeModel.prototype.updateAfterAdjustStartAndEnd = function(_a) {
		var startPoint = _a.startPoint, endPoint = _a.endPoint, sourceNode = _a.sourceNode, targetNode = _a.targetNode;
		var _b = getBezierControlPoints({
			start: startPoint,
			end: endPoint,
			sourceNode,
			targetNode,
			offset: this.offset
		}), sNext = _b.sNext, ePre = _b.ePre;
		this.pointsList = [
			startPoint,
			sNext,
			ePre,
			endPoint
		];
		this.initPoints();
	};
	__decorate$26([observable], BezierEdgeModel.prototype, "offset", void 0);
	__decorate$26([observable], BezierEdgeModel.prototype, "path", void 0);
	__decorate$26([action], BezierEdgeModel.prototype, "initPoints", null);
	__decorate$26([action], BezierEdgeModel.prototype, "updatePoints", null);
	__decorate$26([action], BezierEdgeModel.prototype, "updateStartPoint", null);
	__decorate$26([action], BezierEdgeModel.prototype, "updateEndPoint", null);
	__decorate$26([action], BezierEdgeModel.prototype, "moveStartPoint", null);
	__decorate$26([action], BezierEdgeModel.prototype, "moveEndPoint", null);
	__decorate$26([action], BezierEdgeModel.prototype, "updateAdjustAnchor", null);
	__decorate$26([action], BezierEdgeModel.prototype, "getAdjustStart", null);
	__decorate$26([action], BezierEdgeModel.prototype, "getAdjustEnd", null);
	__decorate$26([action], BezierEdgeModel.prototype, "updateAfterAdjustStartAndEnd", null);
	return BezierEdgeModel;
}(BaseEdgeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/edge/LineEdgeModel.js
var __extends$39 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$39 = function() {
	__assign$39 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$39.apply(this, arguments);
};
var __read$23 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$8 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var LineEdgeModel = function(_super) {
	__extends$39(LineEdgeModel, _super);
	function LineEdgeModel() {
		var _this = _super.apply(this, __spreadArray$8([], __read$23(arguments), false)) || this;
		_this.modelType = ModelType.LINE_EDGE;
		return _this;
	}
	LineEdgeModel.prototype.getEdgeStyle = function() {
		var line = this.graphModel.theme.line;
		var style = _super.prototype.getEdgeStyle.call(this);
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$39(__assign$39(__assign$39({}, style), cloneDeep(line)), cloneDeep(customStyle));
	};
	LineEdgeModel.prototype.initEdgeData = function(data) {
		_super.prototype.initEdgeData.call(this, data);
		this.points = this.getPath([this.startPoint, this.endPoint]);
	};
	LineEdgeModel.prototype.getPath = function(points) {
		var _a = __read$23(points, 2), start = _a[0], end = _a[1];
		return "".concat(start.x, ",").concat(start.y, " ").concat(end.x, ",").concat(end.y);
	};
	LineEdgeModel.prototype.getTextPosition = function() {
		return {
			x: (this.startPoint.x + this.endPoint.x) / 2,
			y: (this.startPoint.y + this.endPoint.y) / 2
		};
	};
	return LineEdgeModel;
}(BaseEdgeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/edge/PolylineEdgeModel.js
var __extends$38 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$38 = function() {
	__assign$38 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$38.apply(this, arguments);
};
var __decorate$25 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$22 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$7 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var PolylineEdgeModel = function(_super) {
	__extends$38(PolylineEdgeModel, _super);
	function PolylineEdgeModel() {
		var _this = _super.apply(this, __spreadArray$7([], __read$22(arguments), false)) || this;
		_this.modelType = ModelType.POLYLINE_EDGE;
		_this.draggingPointList = [];
		return _this;
	}
	PolylineEdgeModel.prototype.initEdgeData = function(data) {
		var providedOffset = get(data, "properties.offset");
		this.offset = typeof providedOffset === "number" ? providedOffset : this.getDefaultOffset();
		if (data.pointsList) {
			var corrected = this.orthogonalizePath(data.pointsList);
			data.pointsList = corrected;
			this.pointsList = corrected;
		}
		_super.prototype.initEdgeData.call(this, data);
	};
	PolylineEdgeModel.prototype.setAttributes = function() {
		var newOffset = this.properties.offset;
		if (newOffset && newOffset !== this.offset) {
			this.offset = newOffset;
			this.updatePoints();
		}
	};
	PolylineEdgeModel.prototype.orthogonalizePath = function(points) {
		if (!Array.isArray(points) || points.length < 2) return points;
		var pushUnique = function(arr, p) {
			var last = arr[arr.length - 1];
			if (!last || last.x !== p.x || last.y !== p.y) arr.push({
				x: p.x,
				y: p.y
			});
		};
		var isAxisAligned = function(a, b) {
			return a.x === b.x || a.y === b.y;
		};
		var manhattanDistance = function(a, b) {
			return Math.abs(a.x - b.x) + Math.abs(a.y - b.y);
		};
		var orthogonal = [];
		pushUnique(orthogonal, points[0]);
		var previousDirection;
		for (var i = 0; i < points.length - 1; i++) {
			var current = orthogonal[orthogonal.length - 1];
			var next = points[i + 1];
			if (!current || !next) continue;
			if (isAxisAligned(current, next)) {
				pushUnique(orthogonal, next);
				previousDirection = current.x === next.x ? SegmentDirection.VERTICAL : SegmentDirection.HORIZONTAL;
				continue;
			}
			var cornerHV = {
				x: next.x,
				y: current.y
			};
			var cornerVH = {
				x: current.x,
				y: next.y
			};
			var preferredCorner = previousDirection === SegmentDirection.VERTICAL ? cornerVH : previousDirection === SegmentDirection.HORIZONTAL ? cornerHV : manhattanDistance(current, cornerHV) <= manhattanDistance(current, cornerVH) ? cornerHV : cornerVH;
			if (preferredCorner.x !== current.x || preferredCorner.y !== current.y) pushUnique(orthogonal, preferredCorner);
			pushUnique(orthogonal, next);
			var a = orthogonal[orthogonal.length - 2];
			var b = orthogonal[orthogonal.length - 1];
			previousDirection = a && b ? a.x === b.x ? SegmentDirection.VERTICAL : SegmentDirection.HORIZONTAL : previousDirection;
		}
		var simplified = [];
		for (var i = 0; i < orthogonal.length; i++) {
			var prev = orthogonal[i - 1];
			var curr = orthogonal[i];
			var next = orthogonal[i + 1];
			if (prev && curr && next && (prev.x === curr.x && curr.x === next.x || prev.y === curr.y && curr.y === next.y)) continue;
			pushUnique(simplified, curr);
		}
		if (simplified.length >= 2) {
			simplified[0] = {
				x: points[0].x,
				y: points[0].y
			};
			simplified[simplified.length - 1] = {
				x: points[points.length - 1].x,
				y: points[points.length - 1].y
			};
		}
		return simplified.length < 2 || simplified.every(function(_, idx, arr) {
			if (idx === 0) return true;
			return isAxisAligned(arr[idx - 1], arr[idx]);
		}) ? simplified : [{
			x: points[0].x,
			y: points[0].y
		}, {
			x: points[points.length - 1].x,
			y: points[points.length - 1].y
		}];
	};
	/**
	* 计算默认 offset：箭头与折线重叠长度 + 5
	* 重叠长度采用箭头样式中的 offset（沿边方向的长度）
	*/
	PolylineEdgeModel.prototype.getDefaultOffset = function() {
		var arrowStyle = this.getArrowStyle();
		return (typeof arrowStyle.offset === "number" ? arrowStyle.offset : 0) + 5;
	};
	PolylineEdgeModel.prototype.getEdgeStyle = function() {
		var polyline = this.graphModel.theme.polyline;
		var style = _super.prototype.getEdgeStyle.call(this);
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$38(__assign$38(__assign$38({}, style), cloneDeep(polyline)), cloneDeep(customStyle));
	};
	PolylineEdgeModel.prototype.getTextPosition = function() {
		var _a;
		var textValue = (_a = this.text) === null || _a === void 0 ? void 0 : _a.value;
		if (this.dbClickPosition && !textValue) {
			var _b = this.dbClickPosition;
			return {
				x: _b.x,
				y: _b.y
			};
		}
		var _c = __read$22(getLongestEdge(points2PointsList(this.points)), 2), p1 = _c[0], p2 = _c[1];
		return {
			x: (p1.x + p2.x) / 2,
			y: (p1.y + p2.y) / 2
		};
	};
	PolylineEdgeModel.prototype.getAfterAnchor = function(direction, position, anchorList) {
		var anchor;
		var minDistance;
		anchorList.forEach(function(item) {
			var distanceX;
			if (direction === SegmentDirection.HORIZONTAL) distanceX = Math.abs(position.y - item.y);
			else if (direction === SegmentDirection.VERTICAL) distanceX = Math.abs(position.x - item.x);
			if (!minDistance || minDistance > distanceX) {
				minDistance = distanceX;
				anchor = item;
			}
		});
		return anchor;
	};
	PolylineEdgeModel.prototype.getCrossPoint = function(direction, start, end) {
		var position;
		if (direction === SegmentDirection.HORIZONTAL) position = {
			x: end.x,
			y: start.y
		};
		else if (direction === SegmentDirection.VERTICAL) position = {
			x: start.x,
			y: end.y
		};
		return position;
	};
	PolylineEdgeModel.prototype.removeCrossPoints = function(startIndex, endIndex, pointList) {
		var list = pointList.map(function(i) {
			return i;
		});
		if (startIndex === 1) {
			var start_1 = list[startIndex];
			var end = list[endIndex];
			var pre_1 = list[startIndex - 1];
			if (isSegmentsInNode(pre_1, start_1, this.sourceNode)) {
				var isSegmentsCrossStartNode = isSegmentsCrossNode(start_1, end, this.sourceNode);
				if (isSegmentsCrossStartNode) {
					var point = getCrossPointInRect(start_1, end, this.sourceNode);
					if (point) {
						list[startIndex] = point;
						list.splice(startIndex - 1, 1);
						startIndex--;
						endIndex--;
					}
				}
			} else {
				var anchorList = this.sourceNode.anchors;
				anchorList.forEach(function(item) {
					if (item.x === pre_1.x && item.x === start_1.x || item.y === pre_1.y && item.y === start_1.y) {
						if (distance(item.x, item.y, start_1.x, start_1.y) < distance(pre_1.x, pre_1.y, start_1.x, start_1.y)) list[startIndex - 1] = item;
					}
				});
			}
		}
		if (endIndex === pointList.length - 2) {
			var start = list[startIndex];
			var end_1 = list[endIndex];
			var next_1 = list[endIndex + 1];
			if (isSegmentsInNode(end_1, next_1, this.targetNode)) {
				var isSegmentsCrossStartNode = isSegmentsCrossNode(start, end_1, this.targetNode);
				if (isSegmentsCrossStartNode) {
					var point = getCrossPointInRect(start, end_1, this.targetNode);
					if (point) {
						list[endIndex] = point;
						list.splice(endIndex + 1, 1);
					}
				}
			} else {
				var anchorList = this.targetNode.anchors;
				anchorList.forEach(function(item) {
					if (item.x === next_1.x && item.x === end_1.x || item.y === next_1.y && item.y === end_1.y) {
						if (distance(item.x, item.y, end_1.x, end_1.y) < distance(next_1.x, next_1.y, end_1.x, end_1.y)) list[endIndex + 1] = item;
					}
				});
			}
		}
		return list;
	};
	PolylineEdgeModel.prototype.getDraggingPoints = function(direction, positionType, position, anchorList, draggingPointList) {
		var pointList = draggingPointList.map(function(i) {
			return i;
		});
		var anchor = this.getAfterAnchor(direction, position, anchorList);
		var crossPoint = this.getCrossPoint(direction, position, anchor);
		if (positionType === "start") {
			pointList.unshift(crossPoint);
			pointList.unshift(anchor);
		} else {
			pointList.push(crossPoint);
			pointList.push(anchor);
		}
		return pointList;
	};
	PolylineEdgeModel.prototype.updateCrossPoints = function(pointList) {
		var list = pointList.map(function(i) {
			return i;
		});
		var start = pointList[0];
		var next = pointList[1];
		var pre = pointList[list.length - 2];
		var end = pointList[list.length - 1];
		var _a = this, sourceNode = _a.sourceNode, targetNode = _a.targetNode;
		var sourceModelType = sourceNode.modelType;
		var targetModelType = targetNode.modelType;
		var startPointDirection = segmentDirection(start, next);
		var startCrossPoint = list[0];
		switch (sourceModelType) {
			case ModelType.RECT_NODE:
				if (sourceNode.radius !== 0) {
					var inInnerNode = inStraightLineOfRect(start, sourceNode);
					if (!inInnerNode) startCrossPoint = getClosestRadiusCenter(start, startPointDirection, sourceNode);
				}
				break;
			case ModelType.CIRCLE_NODE:
				startCrossPoint = getCrossPointWithCircle(start, startPointDirection, sourceNode);
				break;
			case ModelType.ELLIPSE_NODE:
				startCrossPoint = getCrossPointWithEllipse(start, startPointDirection, sourceNode);
				break;
			case ModelType.DIAMOND_NODE:
				startCrossPoint = getCrossPointWithPolygon(start, startPointDirection, sourceNode);
				break;
			case ModelType.POLYGON_NODE:
				startCrossPoint = getCrossPointWithPolygon(start, startPointDirection, sourceNode);
				break;
			default: break;
		}
		if (startCrossPoint) list[0] = startCrossPoint;
		var endPointDirection = segmentDirection(pre, end);
		var endCrossPoint = list[list.length - 1];
		switch (targetModelType) {
			case ModelType.RECT_NODE:
				if (targetNode.radius !== 0) {
					var inInnerNode = inStraightLineOfRect(end, targetNode);
					if (!inInnerNode) endCrossPoint = getClosestRadiusCenter(end, endPointDirection, targetNode);
				}
				break;
			case ModelType.CIRCLE_NODE:
				endCrossPoint = getCrossPointWithCircle(end, endPointDirection, targetNode);
				break;
			case ModelType.ELLIPSE_NODE:
				endCrossPoint = getCrossPointWithEllipse(end, endPointDirection, targetNode);
				break;
			case ModelType.DIAMOND_NODE:
				endCrossPoint = getCrossPointWithPolygon(end, endPointDirection, targetNode);
				break;
			case ModelType.POLYGON_NODE:
				endCrossPoint = getCrossPointWithPolygon(end, endPointDirection, targetNode);
				break;
			default: break;
		}
		if (endCrossPoint) list[list.length - 1] = endCrossPoint;
		return list;
	};
	PolylineEdgeModel.prototype.updatePath = function(pointList) {
		this.pointsList = this.orthogonalizePath(pointList);
		this.points = this.getPath(this.pointsList);
	};
	PolylineEdgeModel.prototype.getData = function() {
		var data = _super.prototype.getData.call(this);
		var pointsList = this.pointsList.map(function(_a) {
			return {
				x: _a.x,
				y: _a.y
			};
		});
		return Object.assign({}, data, { pointsList });
	};
	PolylineEdgeModel.prototype.getPath = function(points) {
		return points.map(function(point) {
			return "".concat(point.x, ",").concat(point.y);
		}).join(" ");
	};
	PolylineEdgeModel.prototype.initPoints = function() {
		if (this.pointsList.length > 0) this.points = this.getPath(this.pointsList);
		else this.updatePoints();
	};
	PolylineEdgeModel.prototype.updatePoints = function() {
		var pointsList = getPolylinePoints({
			x: this.startPoint.x,
			y: this.startPoint.y
		}, {
			x: this.endPoint.x,
			y: this.endPoint.y
		}, this.sourceNode, this.targetNode, this.offset || 0);
		this.pointsList = this.orthogonalizePath(pointsList);
		this.points = this.pointsList.map(function(point) {
			return "".concat(point.x, ",").concat(point.y);
		}).join(" ");
	};
	PolylineEdgeModel.prototype.updateStartPoint = function(anchor) {
		this.startPoint = Object.assign({}, anchor);
		this.updatePoints();
	};
	PolylineEdgeModel.prototype.moveStartPoint = function(deltaX, deltaY) {
		this.startPoint.x += deltaX;
		this.startPoint.y += deltaY;
		this.updatePoints();
	};
	PolylineEdgeModel.prototype.updateEndPoint = function(anchor) {
		this.endPoint = Object.assign({}, anchor);
		this.updatePoints();
	};
	PolylineEdgeModel.prototype.moveEndPoint = function(deltaX, deltaY) {
		this.endPoint.x += deltaX;
		this.endPoint.y += deltaY;
		this.updatePoints();
	};
	PolylineEdgeModel.prototype.updatePointsList = function(deltaX, deltaY) {
		this.pointsList.forEach(function(item) {
			item.x += deltaX;
			item.y += deltaY;
		});
		var startPoint = this.pointsList[0];
		this.startPoint = Object.assign({}, startPoint);
		var endPoint = this.pointsList[this.pointsList.length - 1];
		this.endPoint = Object.assign({}, endPoint);
		this.initPoints();
	};
	PolylineEdgeModel.prototype.dragAppendStart = function() {
		this.draggingPointList = this.pointsList.map(function(i) {
			return i;
		});
	};
	PolylineEdgeModel.prototype.dragAppendSimple = function(appendInfo, dragInfo) {
		var _a;
		this.isDragging = true;
		var start = appendInfo.start, end = appendInfo.end, startIndex = appendInfo.startIndex, endIndex = appendInfo.endIndex, direction = appendInfo.direction;
		var pointsList = this.pointsList;
		var draggingPointList = pointsList;
		if (direction === SegmentDirection.HORIZONTAL) {
			pointsList[startIndex] = {
				x: start.x,
				y: start.y + dragInfo.y
			};
			pointsList[endIndex] = {
				x: end.x,
				y: end.y + dragInfo.y
			};
			draggingPointList = this.pointsList.map(function(i) {
				return i;
			});
		} else if (direction === SegmentDirection.VERTICAL) {
			pointsList[startIndex] = {
				x: start.x + dragInfo.x,
				y: start.y
			};
			pointsList[endIndex] = {
				x: end.x + dragInfo.x,
				y: end.y
			};
			draggingPointList = this.pointsList.map(function(i) {
				return i;
			});
		}
		this.updatePointsAfterDrag(draggingPointList);
		this.draggingPointList = draggingPointList;
		if ((_a = this.text) === null || _a === void 0 ? void 0 : _a.value) this.setText(assign({}, this.text, this.textPosition));
		return {
			start: assign({}, pointsList[startIndex]),
			end: assign({}, pointsList[endIndex]),
			startIndex,
			endIndex,
			direction
		};
	};
	PolylineEdgeModel.prototype.dragAppend = function(appendInfo, dragInfo) {
		var _a;
		this.isDragging = true;
		var start = appendInfo.start, end = appendInfo.end, startIndex = appendInfo.startIndex, endIndex = appendInfo.endIndex, direction = appendInfo.direction;
		var pointsList = this.pointsList;
		if (direction === SegmentDirection.HORIZONTAL) {
			pointsList[startIndex] = {
				x: start.x,
				y: start.y + dragInfo.y
			};
			pointsList[endIndex] = {
				x: end.x,
				y: end.y + dragInfo.y
			};
			var draggingPointList = this.pointsList.map(function(i) {
				return i;
			});
			if (startIndex !== 0 && endIndex !== this.pointsList.length - 1) draggingPointList = this.removeCrossPoints(startIndex, endIndex, draggingPointList);
			if (startIndex === 0) {
				var startPosition = {
					x: start.x,
					y: start.y + dragInfo.y
				};
				var inNode = isInNode(startPosition, this.sourceNode);
				if (!inNode) {
					var anchorList = this.sourceNode.anchors;
					draggingPointList = this.getDraggingPoints(direction, "start", startPosition, anchorList, draggingPointList);
				}
			}
			if (endIndex === this.pointsList.length - 1) {
				var endPosition = {
					x: end.x,
					y: end.y + dragInfo.y
				};
				var inNode = isInNode(endPosition, this.targetNode);
				if (!inNode) {
					var anchorList = this.targetNode.anchors;
					draggingPointList = this.getDraggingPoints(direction, "end", endPosition, anchorList, draggingPointList);
				}
			}
			this.updatePointsAfterDrag(draggingPointList);
			this.draggingPointList = draggingPointList;
		} else if (direction === SegmentDirection.VERTICAL) {
			pointsList[startIndex] = {
				x: start.x + dragInfo.x,
				y: start.y
			};
			pointsList[endIndex] = {
				x: end.x + dragInfo.x,
				y: end.y
			};
			var draggingPointList = this.pointsList.map(function(i) {
				return i;
			});
			if (startIndex !== 0 && endIndex !== this.pointsList.length - 1) draggingPointList = this.removeCrossPoints(startIndex, endIndex, draggingPointList);
			if (startIndex === 0) {
				var startPosition = {
					x: start.x + dragInfo.x,
					y: start.y
				};
				var inNode = isInNode(startPosition, this.sourceNode);
				if (!inNode) {
					var anchorList = this.sourceNode.anchors;
					draggingPointList = this.getDraggingPoints(direction, "start", startPosition, anchorList, draggingPointList);
				}
			}
			if (endIndex === this.pointsList.length - 1) {
				var endPosition = {
					x: end.x + dragInfo.x,
					y: end.y
				};
				var inNode = isInNode(endPosition, this.targetNode);
				if (!inNode) {
					var anchorList = this.targetNode.anchors;
					draggingPointList = this.getDraggingPoints(direction, "end", endPosition, anchorList, draggingPointList);
				}
			}
			this.updatePointsAfterDrag(draggingPointList);
			this.draggingPointList = draggingPointList;
		}
		if ((_a = this.text) === null || _a === void 0 ? void 0 : _a.value) this.setText(assign({}, this.text, this.textPosition));
		return {
			start: assign({}, pointsList[startIndex]),
			end: assign({}, pointsList[endIndex]),
			startIndex,
			endIndex,
			direction
		};
	};
	PolylineEdgeModel.prototype.dragAppendEnd = function() {
		if (this.draggingPointList) {
			var pointsList = pointFilter(points2PointsList(this.points));
			this.pointsList = pointsList.map(function(i) {
				return i;
			});
			this.draggingPointList = [];
			var startPoint = pointsList[0];
			this.startPoint = assign({}, startPoint);
			var endPoint = pointsList[pointsList.length - 1];
			this.endPoint = assign({}, endPoint);
		}
		this.isDragging = false;
	};
	PolylineEdgeModel.prototype.updatePointsAfterDrag = function(pointsList) {
		var list = this.updateCrossPoints(pointsList);
		this.points = list.map(function(point) {
			return "".concat(point.x, ",").concat(point.y);
		}).join(" ");
	};
	PolylineEdgeModel.prototype.getAdjustStart = function() {
		return this.pointsList[0] || this.startPoint;
	};
	PolylineEdgeModel.prototype.getAdjustEnd = function() {
		var pointsList = this.pointsList;
		return pointsList[pointsList.length - 1] || this.endPoint;
	};
	PolylineEdgeModel.prototype.updateAfterAdjustStartAndEnd = function(_a) {
		var startPoint = _a.startPoint, endPoint = _a.endPoint, sourceNode = _a.sourceNode, targetNode = _a.targetNode;
		this.pointsList = this.orthogonalizePath(getPolylinePoints({
			x: startPoint.x,
			y: startPoint.y
		}, {
			x: endPoint.x,
			y: endPoint.y
		}, sourceNode, targetNode, this.offset || 0));
		this.initPoints();
	};
	__decorate$25([observable], PolylineEdgeModel.prototype, "offset", void 0);
	__decorate$25([observable], PolylineEdgeModel.prototype, "dbClickPosition", void 0);
	__decorate$25([action], PolylineEdgeModel.prototype, "initPoints", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "updatePoints", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "updateStartPoint", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "moveStartPoint", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "updateEndPoint", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "moveEndPoint", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "updatePointsList", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "dragAppendStart", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "dragAppendSimple", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "dragAppend", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "dragAppendEnd", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "updatePointsAfterDrag", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "getAdjustStart", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "getAdjustEnd", null);
	__decorate$25([action], PolylineEdgeModel.prototype, "updateAfterAdjustStartAndEnd", null);
	return PolylineEdgeModel;
}(BaseEdgeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/BaseNodeModel.js
var __assign$37 = function() {
	__assign$37 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$37.apply(this, arguments);
};
var __decorate$24 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __values$2 = function(o) {
	var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
	if (m) return m.call(o);
	if (o && typeof o.length === "number") return { next: function() {
		if (o && i >= o.length) o = void 0;
		return {
			value: o && o[i++],
			done: !o
		};
	} };
	throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read$21 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BaseNodeModel = function() {
	function BaseNodeModel(data, graphModel) {
		var _a;
		this.BaseType = ElementType.NODE;
		this.id = "";
		this.type = "";
		this.x = 0;
		this.y = 0;
		this.textMode = TextMode.TEXT;
		this.text = {
			value: "",
			x: 0,
			y: 0,
			draggable: false,
			editable: true
		};
		this._width = 100;
		this._height = 80;
		this.minWidth = 30;
		this.minHeight = 30;
		this.maxWidth = 2e3;
		this.maxHeight = 2e3;
		this.anchorsOffset = [];
		this.virtual = false;
		this.isSelected = false;
		this.isHovered = false;
		this.isShowAnchor = false;
		this.isDragging = false;
		this.isHitable = true;
		this.isHittable = true;
		this.draggable = true;
		this.visible = true;
		this.rotatable = true;
		this.resizable = true;
		this.zIndex = 1;
		this.state = ElementState.DEFAULT;
		this.autoToFront = true;
		this.style = {};
		this._rotate = 0;
		this.modelType = ModelType.NODE;
		this.additionStateData = {};
		this.targetRules = [];
		this.sourceRules = [];
		this.moveRules = [];
		this.resizeRules = [];
		this.hasSetTargetRules = false;
		this.hasSetSourceRules = false;
		this.graphModel = graphModel;
		this.properties = (_a = data.properties) !== null && _a !== void 0 ? _a : {};
		this.initNodeData(data);
		this.setAttributes();
	}
	Object.defineProperty(BaseNodeModel.prototype, "width", {
		get: function() {
			return this._width;
		},
		set: function(value) {
			this._width = value;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(BaseNodeModel.prototype, "height", {
		get: function() {
			return this._height;
		},
		set: function(value) {
			this._height = value;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(BaseNodeModel.prototype, "rotate", {
		get: function() {
			return this._rotate;
		},
		set: function(value) {
			this._rotate = value;
			var _a = this, _b = _a.x, x = _b === void 0 ? 0 : _b, _c = _a.y, y = _c === void 0 ? 0 : _c;
			this.transform = new TranslateMatrix(-x, -y).rotate(value).translate(x, y).toString();
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(BaseNodeModel.prototype, "incoming", {
		/**
		* 获取进入当前节点的边和节点
		*/
		get: function() {
			return {
				nodes: this.graphModel.getNodeIncomingNode(this.id),
				edges: this.graphModel.getNodeIncomingEdge(this.id)
			};
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(BaseNodeModel.prototype, "outgoing", {
		get: function() {
			return {
				nodes: this.graphModel.getNodeOutgoingNode(this.id),
				edges: this.graphModel.getNodeOutgoingEdge(this.id)
			};
		},
		enumerable: false,
		configurable: true
	});
	/**
	* @overridable 可以重写
	* 初始化节点数据
	* initNodeData 和 setAttributes 的区别在于
	* initNodeData 只在节点初始化的时候调用，用于初始化节点的所有属性。
	* setAttributes 除了初始化调用外，还会在 properties 发生变化了调用。
	*/
	BaseNodeModel.prototype.initNodeData = function(data) {
		var _this = this;
		if (!data.properties) data.properties = {};
		if (!data.id) {
			var idGenerator = this.graphModel.idGenerator;
			var globalId = idGenerator && idGenerator(data.type);
			data.id = this.createId() || globalId || createUuid();
		}
		this.formatText(data);
		assign(this, pickNodeConfig(data));
		var _a = this.graphModel, overlapMode = _a.overlapMode, eventCenter = _a.eventCenter;
		this.updateZIndexByOverlap(overlapMode, data.zIndex || getZIndex());
		eventCenter.on("overlap:change", function(data) {
			var newMode = data.overlapMode;
			_this.updateZIndexByOverlap(newMode, _this.zIndex || getZIndex());
		});
	};
	/**
	* 设置model属性，每次properties发生变化会触发
	* 例如设置节点的宽度
	* @example
	*
	* setAttributes () {
	*   this.width = 300
	*   this.height = 200
	* }
	*
	* @overridable 支持重写
	*/
	BaseNodeModel.prototype.setAttributes = function() {};
	/**
	* @overridable 支持重写，自定义此类型节点默认生成方式
	* @returns string | null
	*/
	BaseNodeModel.prototype.createId = function() {
		return null;
	};
	/**
	* 设置当前元素的文本模式
	* @param mode
	*/
	BaseNodeModel.prototype.setTextMode = function(mode) {
		this.textMode = mode;
	};
	/**
	* 始化文本属性
	*/
	BaseNodeModel.prototype.formatText = function(data) {
		var _a, _b, _c;
		var _d = this.graphModel.editConfigModel, nodeTextDraggable = _d.nodeTextDraggable, nodeTextEdit = _d.nodeTextEdit;
		var x = data.x, y = data.y, text = data.text;
		var textConfig = {
			value: "",
			x,
			y,
			draggable: nodeTextDraggable,
			editable: nodeTextEdit
		};
		if (text) if (typeof text === "string") textConfig.value = text;
		else {
			textConfig = __assign$37(__assign$37({}, textConfig), {
				x: (_a = text.x) !== null && _a !== void 0 ? _a : x,
				y: (_b = text.y) !== null && _b !== void 0 ? _b : y,
				value: (_c = text.value) !== null && _c !== void 0 ? _c : ""
			});
			if (!isUndefined(text.draggable)) textConfig.draggable = text.draggable;
			if (!isUndefined(text.editable)) textConfig.editable = text.editable;
		}
		data.text = textConfig;
	};
	/**
	* @overridable 支持重写
	* 计算节点 resize 时
	*/
	BaseNodeModel.prototype.resize = function(resizeInfo) {
		var width = resizeInfo.width, height = resizeInfo.height, deltaX = resizeInfo.deltaX, deltaY = resizeInfo.deltaY;
		if (!this.isAllowResizeNode(deltaX, deltaY, width, height)) return this.getData();
		this.move(deltaX / 2, deltaY / 2);
		this.width = width;
		this.height = height;
		this.setProperties({
			width,
			height
		});
		return this.getData();
	};
	BaseNodeModel.prototype.proportionalResize = function() {};
	/**
	* 获取被保存时返回的数据
	* @overridable 支持重写
	*/
	BaseNodeModel.prototype.getData = function() {
		var _a = this.text, x = _a.x, y = _a.y, value = _a.value;
		var properties = this.properties;
		if (isObservable(properties)) properties = toJS(properties);
		if (isNil(properties.width)) properties.width = this.width;
		if (isNil(properties.height)) properties.height = this.height;
		var data = {
			id: this.id,
			type: this.type,
			x: this.x,
			y: this.y,
			properties
		};
		if (this.rotate) data.rotate = this.rotate;
		if ([OverlapMode.INCREASE, OverlapMode.STATIC].includes(this.graphModel.overlapMode)) data.zIndex = this.zIndex;
		if (value) data.text = {
			x,
			y,
			value
		};
		return data;
	};
	/**
	* 用于在历史记录时获取节点数据，
	* 在某些情况下，如果希望某个属性变化不引起history的变化，
	* 可以重写此方法。
	*/
	BaseNodeModel.prototype.getHistoryData = function() {
		return this.getData();
	};
	/**
	* 获取当前节点的properties
	*/
	BaseNodeModel.prototype.getProperties = function() {
		return toJS(this.properties);
	};
	/**
	* @overridable 支持重写
	* 获取当前节点最外层g标签Attributes, 例如 className
	* @returns 自定义节点样式
	*/
	BaseNodeModel.prototype.getOuterGAttributes = function() {
		return { className: "" };
	};
	/**
	* @overridable 支持重写
	* 获取当前节点样式
	* @returns 自定义节点样式
	*/
	BaseNodeModel.prototype.getNodeStyle = function() {
		return __assign$37(__assign$37({}, this.graphModel.theme.baseNode), this.style);
	};
	/**
	* @overridable 支持重写
	* 获取当前节点文本样式
	*/
	BaseNodeModel.prototype.getTextStyle = function() {
		var nodeText = this.graphModel.theme.nodeText;
		var _a = this.properties.textStyle, textStyle = _a === void 0 ? {} : _a;
		return __assign$37(__assign$37({}, cloneDeep(nodeText)), cloneDeep(textStyle));
	};
	/**
	* @overridable 支持重写
	* 获取当前节点旋转控制点的样式
	*/
	BaseNodeModel.prototype.getRotateControlStyle = function() {
		var rotateControl = this.graphModel.theme.rotateControl;
		return cloneDeep(rotateControl);
	};
	/**
	* @overrideable 支持重写
	* 获取当前节点缩放控制节点的样式
	*/
	BaseNodeModel.prototype.getResizeControlStyle = function() {
		var resizeControl = this.graphModel.theme.resizeControl;
		return cloneDeep(resizeControl);
	};
	BaseNodeModel.prototype.getResizeOutlineStyle = function() {
		var resizeOutline = this.graphModel.theme.resizeOutline;
		var attributes = __assign$37({}, resizeOutline);
		if (this.isHovered) {
			var hoverStyle = resizeOutline.hover || {};
			attributes = __assign$37(__assign$37({}, attributes), hoverStyle);
		}
		return cloneDeep(attributes);
	};
	/**
	* @overridable 支持重写
	* 获取当前节点锚点样式
	* @returns 自定义样式
	*/
	BaseNodeModel.prototype.getAnchorStyle = function(_anchorInfo) {
		var anchor = this.graphModel.theme.anchor;
		return cloneDeep(anchor);
	};
	/**
	* @overridable 支持重写
	* 获取当前节点锚点拖出连线样式
	* @returns 自定义锚点拖出样式
	*/
	BaseNodeModel.prototype.getAnchorLineStyle = function(_anchorInfo) {
		var anchorLine = this.graphModel.theme.anchorLine;
		return cloneDeep(anchorLine);
	};
	/**
	* @overridable 支持重写
	* 获取outline样式，重写可以定义此类型节点outline样式， 默认使用主题样式
	* @returns 自定义outline样式
	*/
	BaseNodeModel.prototype.getOutlineStyle = function() {
		var outline = this.graphModel.theme.outline;
		return cloneDeep(outline);
	};
	/**
	* @overridable 在连接边时，是否允许这个节点为 source 节点，边到 target 节点
	* @param target 目标节点
	* @param sourceAnchor 源锚点
	* @param targetAnchor 目标锚点
	* @param edgeId 调整后边的 id，在开启 adjustEdgeStartAndEnd 后调整边连接的节点时会传入
	* 详见：https://github.com/didi/LogicFlow/issues/926#issuecomment-1371823306
	*/
	BaseNodeModel.prototype.isAllowConnectedAsSource = function(target, sourceAnchor, targetAnchor, edgeId) {
		var rules = !this.hasSetSourceRules ? this.getConnectedSourceRules() : this.sourceRules;
		this.hasSetSourceRules = true;
		var isAllPass = true;
		var msg = "";
		for (var i = 0; i < rules.length; i++) {
			var rule = rules[i];
			if (!rule.validate.call(this, this, target, sourceAnchor, targetAnchor, edgeId)) {
				isAllPass = false;
				msg = rule.message;
				break;
			}
		}
		return {
			isAllPass,
			msg
		};
	};
	/**
	* 获取当前节点作为连接的起始节点规则。
	*/
	BaseNodeModel.prototype.getConnectedSourceRules = function() {
		return this.sourceRules;
	};
	/**
	* @overridable 在连线时，判断是否允许这个节点为 target 节点
	* @param source 源节点
	* @param sourceAnchor 源锚点
	* @param targetAnchor 目标锚点
	* @param edgeId 调整后边的 id，在开启 adjustEdgeStartAndEnd 后调整边连接的节点时会传入
	* 详见：https://github.com/didi/LogicFlow/issues/926#issuecomment-1371823306
	*/
	BaseNodeModel.prototype.isAllowConnectedAsTarget = function(source, sourceAnchor, targetAnchor, edgeId) {
		var rules = !this.hasSetTargetRules ? this.getConnectedTargetRules() : this.targetRules;
		this.hasSetTargetRules = true;
		var isAllPass = true;
		var msg = "";
		for (var i = 0; i < rules.length; i++) {
			var rule = rules[i];
			if (!rule.validate.call(this, source, this, sourceAnchor, targetAnchor, edgeId)) {
				isAllPass = false;
				msg = rule.message;
				break;
			}
		}
		return {
			isAllPass,
			msg
		};
	};
	/**
	* 内部方法
	* 是否允许移动节点到新的位置
	*/
	BaseNodeModel.prototype.isAllowMoveNode = function(deltaX, deltaY) {
		var e_1, _a;
		var isAllowMoveX = true;
		var isAllowMoveY = true;
		var rules = this.moveRules.concat(this.graphModel.nodeMoveRules);
		try {
			for (var rules_1 = __values$2(rules), rules_1_1 = rules_1.next(); !rules_1_1.done; rules_1_1 = rules_1.next()) {
				var rule = rules_1_1.value;
				var r = rule(this, deltaX, deltaY);
				if (!r) return false;
				if (typeof r === "object") {
					var r1 = r;
					if (!r1.x && !r1.y) return false;
					isAllowMoveX = isAllowMoveX && r1.x;
					isAllowMoveY = isAllowMoveY && r1.y;
				}
			}
		} catch (e_1_1) {
			e_1 = { error: e_1_1 };
		} finally {
			try {
				if (rules_1_1 && !rules_1_1.done && (_a = rules_1.return)) _a.call(rules_1);
			} finally {
				if (e_1) throw e_1.error;
			}
		}
		return {
			x: isAllowMoveX,
			y: isAllowMoveY
		};
	};
	/**
	* 获取作为连线终点时的所有规则。
	*/
	BaseNodeModel.prototype.getConnectedTargetRules = function() {
		return this.targetRules;
	};
	/**
	* @returns Point[] 锚点坐标构成的数组
	*/
	BaseNodeModel.prototype.getAnchorsByOffset = function() {
		var _a = this, anchorsOffset = _a.anchorsOffset, id = _a.id, x = _a.x, y = _a.y;
		if (anchorsOffset && anchorsOffset.length > 0) return anchorsOffset.map(function(el, idx) {
			if (el.length) {
				el = el;
				return {
					id: "".concat(id, "_").concat(idx),
					x: x + el[0],
					y: y + el[1]
				};
			}
			el = el;
			return __assign$37(__assign$37({}, el), {
				x: x + el.x,
				y: y + el.y,
				id: el.id || "".concat(id, "_").concat(idx)
			});
		});
		return this.getDefaultAnchor();
	};
	/**
	* @overridable 子类重写此方法设置默认锚点
	* 获取节点默认情况下的锚点
	*/
	BaseNodeModel.prototype.getDefaultAnchor = function() {
		return [];
	};
	/**
	* @overridable 子类重写此方法获取手动连接边到节点时，需要连接的锚点
	* 手动连接边到节点时，需要连接的锚点
	*/
	BaseNodeModel.prototype.getTargetAnchor = function(position) {
		var _a;
		var customTargetAnchor = this.graphModel.customTargetAnchor;
		return (_a = customTargetAnchor === null || customTargetAnchor === void 0 ? void 0 : customTargetAnchor(this, position)) !== null && _a !== void 0 ? _a : getClosestAnchor(position, this);
	};
	/**
	* 获取节点BBox
	*/
	BaseNodeModel.prototype.getBounds = function() {
		return {
			minX: this.x - this.width / 2,
			minY: this.y - this.height / 2,
			maxX: this.x + this.width / 2,
			maxY: this.y + this.height / 2
		};
	};
	Object.defineProperty(BaseNodeModel.prototype, "anchors", {
		get: function() {
			var anchors = this.getAnchorsByOffset();
			var _a = this, x = _a.x, y = _a.y, rotate = _a.rotate;
			anchors.forEach(function(anchor) {
				var anchorX = anchor.x, anchorY = anchor.y;
				var _a = __read$21(new Matrix([
					anchorX,
					anchorY,
					1
				]).translate(-x, -y).rotate(rotate).translate(x, y)[0], 2), e = _a[0], f = _a[1];
				anchor.x = e;
				anchor.y = f;
			});
			return anchors;
		},
		enumerable: false,
		configurable: true
	});
	BaseNodeModel.prototype.getAnchorInfo = function(anchorId) {
		if (isNil(anchorId)) return void 0;
		for (var i = 0; i < this.anchors.length; i++) {
			var anchor = this.anchors[i];
			if (anchor.id === anchorId) return anchor;
		}
	};
	BaseNodeModel.prototype.addNodeMoveRules = function(fn) {
		if (!this.moveRules.includes(fn)) this.moveRules.push(fn);
	};
	BaseNodeModel.prototype.isAllowMoveByXORY = function(deltaX, deltaY, isIgnoreRule) {
		var isAllowMoveX;
		var isAllowMoveY;
		if (isIgnoreRule) {
			isAllowMoveX = true;
			isAllowMoveY = true;
		} else {
			var r = this.isAllowMoveNode(deltaX, deltaY);
			if (typeof r === "boolean") {
				isAllowMoveX = r;
				isAllowMoveY = r;
			} else {
				isAllowMoveX = r.x;
				isAllowMoveY = r.y;
			}
		}
		return {
			isAllowMoveX,
			isAllowMoveY
		};
	};
	BaseNodeModel.prototype.move = function(deltaX, deltaY, isIgnoreRule) {
		if (isIgnoreRule === void 0) isIgnoreRule = false;
		var _a = this.isAllowMoveByXORY(deltaX, deltaY, isIgnoreRule), isAllowMoveX = _a.isAllowMoveX, isAllowMoveY = _a.isAllowMoveY;
		if (isAllowMoveX) {
			this.x = this.x + deltaX;
			this.text && this.moveText(deltaX, 0);
		}
		if (isAllowMoveY) {
			this.y = this.y + deltaY;
			this.text && this.moveText(0, deltaY);
		}
		if (isAllowMoveX || isAllowMoveY) this.rotate = this._rotate;
		return isAllowMoveX || isAllowMoveY;
	};
	BaseNodeModel.prototype.getMoveDistance = function(deltaX, deltaY, isIgnoreRule) {
		if (isIgnoreRule === void 0) isIgnoreRule = false;
		var _a = this.isAllowMoveByXORY(deltaX, deltaY, isIgnoreRule), isAllowMoveX = _a.isAllowMoveX, isAllowMoveY = _a.isAllowMoveY;
		var moveX = 0;
		var moveY = 0;
		if (isAllowMoveX && deltaX) {
			this.x = this.x + deltaX;
			this.text && this.moveText(deltaX, 0);
			moveX = deltaX;
		}
		if (isAllowMoveY && deltaY) {
			this.y = this.y + deltaY;
			this.text && this.moveText(0, deltaY);
			moveY = deltaY;
		}
		this.transform = new TranslateMatrix(-this.x, -this.y).rotate(this.rotate).translate(this.x, this.y).toString();
		return [moveX, moveY];
	};
	BaseNodeModel.prototype.moveTo = function(x, y, isIgnoreRule) {
		if (isIgnoreRule === void 0) isIgnoreRule = false;
		var deltaX = x - this.x;
		var deltaY = y - this.y;
		if (!isIgnoreRule && !this.isAllowMoveNode(deltaX, deltaY)) return false;
		this.text && this.moveText(deltaX, deltaY);
		this.x = x;
		this.y = y;
		return true;
	};
	BaseNodeModel.prototype.moveText = function(deltaX, deltaY) {
		var _a = this.text, x = _a.x, y = _a.y, value = _a.value, draggable = _a.draggable, editable = _a.editable;
		this.text = {
			value,
			editable,
			draggable,
			x: x + deltaX,
			y: y + deltaY
		};
	};
	BaseNodeModel.prototype.updateText = function(value) {
		this.text = __assign$37(__assign$37({}, toJS(this.text)), { value });
	};
	BaseNodeModel.prototype.addNodeResizeRules = function(fn) {
		if (!this.resizeRules.includes(fn)) this.resizeRules.push(fn);
	};
	/**
	* 内部方法
	* 是否允许resize节点到新的位置
	*/
	BaseNodeModel.prototype.isAllowResizeNode = function(deltaX, deltaY, width, height) {
		var e_2, _a;
		var rules = this.resizeRules.concat(this.graphModel.nodeResizeRules);
		try {
			for (var rules_2 = __values$2(rules), rules_2_1 = rules_2.next(); !rules_2_1.done; rules_2_1 = rules_2.next()) {
				var rule = rules_2_1.value;
				if (!rule(this, deltaX, deltaY, width, height)) return false;
			}
		} catch (e_2_1) {
			e_2 = { error: e_2_1 };
		} finally {
			try {
				if (rules_2_1 && !rules_2_1.done && (_a = rules_2.return)) _a.call(rules_2);
			} finally {
				if (e_2) throw e_2.error;
			}
		}
		return true;
	};
	BaseNodeModel.prototype.setSelected = function(flag) {
		if (flag === void 0) flag = true;
		this.isSelected = flag;
	};
	BaseNodeModel.prototype.setHovered = function(flag) {
		if (flag === void 0) flag = true;
		this.isHovered = flag;
		this.setIsShowAnchor(flag);
	};
	BaseNodeModel.prototype.setIsShowAnchor = function(flag) {
		if (flag === void 0) flag = true;
		this.isShowAnchor = flag;
	};
	BaseNodeModel.prototype.setRotatable = function(flag) {
		if (flag === void 0) flag = true;
		this.rotatable = flag;
	};
	BaseNodeModel.prototype.setResizable = function(flag) {
		if (flag === void 0) flag = true;
		this.resizable = flag;
	};
	BaseNodeModel.prototype.setHitable = function(flag) {
		if (flag === void 0) flag = true;
		this.isHitable = flag;
	};
	BaseNodeModel.prototype.setHittable = function(flag) {
		if (flag === void 0) flag = true;
		this.isHittable = flag;
	};
	BaseNodeModel.prototype.setElementState = function(state, additionStateData) {
		this.state = state;
		this.additionStateData = additionStateData;
	};
	BaseNodeModel.prototype.updateProperties = function(nextProperties, updateKeys) {
		var preProperties = toJS(this.properties);
		this.properties = nextProperties;
		this.setAttributes();
		this.graphModel.eventCenter.emit(EventType.NODE_PROPERTIES_CHANGE, {
			id: this.id,
			keys: updateKeys,
			preProperties,
			properties: nextProperties
		});
	};
	BaseNodeModel.prototype.setProperty = function(key, val) {
		var nextProperties = cloneDeep(toJS(this.properties));
		set$1(nextProperties, key, formatData(val));
		this.updateProperties(nextProperties, [key]);
	};
	BaseNodeModel.prototype.setProperties = function(properties) {
		var preProperties = toJS(this.properties);
		var nextProperties = __assign$37(__assign$37({}, preProperties), formatData(properties));
		var updateKeys = [];
		mapKeys(properties, function(val, key) {
			if (has(preProperties, key) && preProperties[key] !== val || !has(preProperties, key)) updateKeys.push(key);
		});
		this.updateProperties(nextProperties, updateKeys);
	};
	BaseNodeModel.prototype.deleteProperty = function(key) {
		delete this.properties[key];
		this.setAttributes();
	};
	BaseNodeModel.prototype.setStyle = function(key, val) {
		var _a;
		this.style = __assign$37(__assign$37({}, this.style), (_a = {}, _a[key] = formatData(val), _a));
	};
	BaseNodeModel.prototype.setStyles = function(styles) {
		this.style = __assign$37(__assign$37({}, this.style), formatData(styles));
	};
	BaseNodeModel.prototype.updateStyles = function(styles) {
		this.style = __assign$37({}, formatData(styles));
	};
	BaseNodeModel.prototype.setZIndex = function(zIndex) {
		if (zIndex === void 0) zIndex = 1;
		this.zIndex = zIndex;
	};
	BaseNodeModel.prototype.updateAttributes = function(attributes) {
		assign(this, attributes);
	};
	BaseNodeModel.prototype.updateZIndexByOverlap = function(overlapMode, defaultZIndex) {
		switch (overlapMode) {
			case OverlapMode.DEFAULT:
				this.zIndex = 1;
				break;
			case OverlapMode.EDGE_TOP:
				this.zIndex = 0;
				break;
			case OverlapMode.INCREASE:
				this.zIndex = defaultZIndex;
				break;
			default: break;
		}
	};
	BaseNodeModel.BaseType = ElementType.NODE;
	__decorate$24([observable], BaseNodeModel.prototype, "type", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "x", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "y", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "textMode", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "text", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "properties", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "_width", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "_height", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "anchorsOffset", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "isSelected", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "isHovered", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "isShowAnchor", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "isDragging", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "isHitable", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "isHittable", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "draggable", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "visible", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "rotatable", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "resizable", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "zIndex", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "state", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "autoToFront", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "style", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "transform", void 0);
	__decorate$24([observable], BaseNodeModel.prototype, "_rotate", void 0);
	__decorate$24([computed], BaseNodeModel.prototype, "incoming", null);
	__decorate$24([computed], BaseNodeModel.prototype, "outgoing", null);
	__decorate$24([action], BaseNodeModel.prototype, "setTextMode", null);
	__decorate$24([action], BaseNodeModel.prototype, "addNodeMoveRules", null);
	__decorate$24([action], BaseNodeModel.prototype, "move", null);
	__decorate$24([action], BaseNodeModel.prototype, "getMoveDistance", null);
	__decorate$24([action], BaseNodeModel.prototype, "moveTo", null);
	__decorate$24([action], BaseNodeModel.prototype, "moveText", null);
	__decorate$24([action], BaseNodeModel.prototype, "updateText", null);
	__decorate$24([action], BaseNodeModel.prototype, "addNodeResizeRules", null);
	__decorate$24([action], BaseNodeModel.prototype, "setSelected", null);
	__decorate$24([action], BaseNodeModel.prototype, "setHovered", null);
	__decorate$24([action], BaseNodeModel.prototype, "setIsShowAnchor", null);
	__decorate$24([action], BaseNodeModel.prototype, "setRotatable", null);
	__decorate$24([action], BaseNodeModel.prototype, "setResizable", null);
	__decorate$24([action], BaseNodeModel.prototype, "setHitable", null);
	__decorate$24([action], BaseNodeModel.prototype, "setHittable", null);
	__decorate$24([action], BaseNodeModel.prototype, "setElementState", null);
	__decorate$24([action], BaseNodeModel.prototype, "setProperty", null);
	__decorate$24([action], BaseNodeModel.prototype, "setProperties", null);
	__decorate$24([action], BaseNodeModel.prototype, "deleteProperty", null);
	__decorate$24([action], BaseNodeModel.prototype, "setStyle", null);
	__decorate$24([action], BaseNodeModel.prototype, "setStyles", null);
	__decorate$24([action], BaseNodeModel.prototype, "updateStyles", null);
	__decorate$24([action], BaseNodeModel.prototype, "setZIndex", null);
	__decorate$24([action], BaseNodeModel.prototype, "updateAttributes", null);
	__decorate$24([action], BaseNodeModel.prototype, "updateZIndexByOverlap", null);
	return BaseNodeModel;
}();
//#endregion
//#region node_modules/@logicflow/core/es/model/node/CircleNodeModel.js
var __extends$37 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$36 = function() {
	__assign$36 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$36.apply(this, arguments);
};
var __decorate$23 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var CircleNodeModel = function(_super) {
	__extends$37(CircleNodeModel, _super);
	function CircleNodeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.CIRCLE_NODE;
		_this.r = 50;
		_this.initNodeData(data);
		_this.setAttributes();
		return _this;
	}
	Object.defineProperty(CircleNodeModel.prototype, "width", {
		get: function() {
			return this.r * 2;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(CircleNodeModel.prototype, "height", {
		get: function() {
			return this.r * 2;
		},
		enumerable: false,
		configurable: true
	});
	CircleNodeModel.prototype.setAttributes = function() {
		_super.prototype.setAttributes.call(this);
		var r = this.properties.r;
		if (r) this.r = r;
	};
	CircleNodeModel.prototype.getNodeStyle = function() {
		var style = _super.prototype.getNodeStyle.call(this);
		var circle = this.graphModel.theme.circle;
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$36(__assign$36(__assign$36({}, style), cloneDeep(circle)), cloneDeep(customStyle));
	};
	CircleNodeModel.prototype.getDefaultAnchor = function() {
		var _a = this, x = _a.x, y = _a.y, r = _a.r;
		return [
			{
				x,
				y: y - r,
				id: "".concat(this.id, "_0")
			},
			{
				x: x + r,
				y,
				id: "".concat(this.id, "_1")
			},
			{
				x,
				y: y + r,
				id: "".concat(this.id, "_2")
			},
			{
				x: x - r,
				y,
				id: "".concat(this.id, "_3")
			}
		];
	};
	CircleNodeModel.prototype.resize = function(resizeInfo) {
		var width = resizeInfo.width, deltaX = resizeInfo.deltaX, deltaY = resizeInfo.deltaY;
		this.move(deltaX / 2, deltaY / 2);
		this.r = width;
		this.setProperties({ r: width });
		return this.getData();
	};
	__decorate$23([observable], CircleNodeModel.prototype, "r", void 0);
	__decorate$23([computed], CircleNodeModel.prototype, "width", null);
	__decorate$23([computed], CircleNodeModel.prototype, "height", null);
	return CircleNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/DiamondNodeModel.js
var __extends$36 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$35 = function() {
	__assign$35 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$35.apply(this, arguments);
};
var __decorate$22 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$20 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var DiamondNodeModel = function(_super) {
	__extends$36(DiamondNodeModel, _super);
	function DiamondNodeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.DIAMOND_NODE;
		_this.rx = 30;
		_this.ry = 50;
		_this.initNodeData(data);
		_this.setAttributes();
		return _this;
	}
	DiamondNodeModel.prototype.setAttributes = function() {
		_super.prototype.setAttributes.call(this);
		var _a = this.properties, rx = _a.rx, ry = _a.ry;
		if (rx) this.rx = rx;
		if (ry) this.ry = ry;
	};
	DiamondNodeModel.prototype.getNodeStyle = function() {
		var style = _super.prototype.getNodeStyle.call(this);
		var diamond = this.graphModel.theme.diamond;
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$35(__assign$35(__assign$35({}, style), cloneDeep(diamond)), cloneDeep(customStyle));
	};
	Object.defineProperty(DiamondNodeModel.prototype, "points", {
		get: function() {
			var _a = this, x = _a.x, y = _a.y, rx = _a.rx, ry = _a.ry;
			return [
				[x, y - ry],
				[x + rx, y],
				[x, y + ry],
				[x - rx, y]
			];
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(DiamondNodeModel.prototype, "pointsPosition", {
		get: function() {
			return map(this.points, function(_a) {
				var _b = __read$20(_a, 2);
				return {
					x: _b[0],
					y: _b[1]
				};
			});
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(DiamondNodeModel.prototype, "width", {
		get: function() {
			var min = Number.MAX_SAFE_INTEGER;
			var max = Number.MIN_SAFE_INTEGER;
			forEach(this.points, function(_a) {
				var x = __read$20(_a, 1)[0];
				if (x < min) min = x;
				if (x > max) max = x;
			});
			return max - min;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(DiamondNodeModel.prototype, "height", {
		get: function() {
			var min = Number.MAX_SAFE_INTEGER;
			var max = Number.MIN_SAFE_INTEGER;
			forEach(this.points, function(_a) {
				var y = __read$20(_a, 2)[1];
				if (y < min) min = y;
				if (y > max) max = y;
			});
			return max - min;
		},
		enumerable: false,
		configurable: true
	});
	DiamondNodeModel.prototype.getDefaultAnchor = function() {
		var _this = this;
		return map(this.points, function(_a, idx) {
			var _b = __read$20(_a, 2);
			return {
				x: _b[0],
				y: _b[1],
				id: "".concat(_this.id, "_").concat(idx)
			};
		});
	};
	DiamondNodeModel.prototype.resize = function(resizeInfo) {
		var width = resizeInfo.width, height = resizeInfo.height, deltaX = resizeInfo.deltaX, deltaY = resizeInfo.deltaY;
		this.move(deltaX / 2, deltaY / 2);
		this.rx = width;
		this.ry = height;
		this.setProperties({
			rx: width,
			ry: height
		});
		return this.getData();
	};
	__decorate$22([observable], DiamondNodeModel.prototype, "rx", void 0);
	__decorate$22([observable], DiamondNodeModel.prototype, "ry", void 0);
	__decorate$22([computed], DiamondNodeModel.prototype, "points", null);
	__decorate$22([computed], DiamondNodeModel.prototype, "pointsPosition", null);
	__decorate$22([computed], DiamondNodeModel.prototype, "width", null);
	__decorate$22([computed], DiamondNodeModel.prototype, "height", null);
	return DiamondNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/EllipseNodeModel.js
var __extends$35 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$34 = function() {
	__assign$34 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$34.apply(this, arguments);
};
var __decorate$21 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var EllipseNodeModel = function(_super) {
	__extends$35(EllipseNodeModel, _super);
	function EllipseNodeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.ELLIPSE_NODE;
		_this.rx = 30;
		_this.ry = 45;
		_this.initNodeData(data);
		_this.setAttributes();
		return _this;
	}
	EllipseNodeModel.prototype.setAttributes = function() {
		_super.prototype.setAttributes.call(this);
		var _a = this.properties, rx = _a.rx, ry = _a.ry;
		if (rx) this.rx = rx;
		if (ry) this.ry = ry;
	};
	EllipseNodeModel.prototype.getNodeStyle = function() {
		var style = _super.prototype.getNodeStyle.call(this);
		var ellipse = this.graphModel.theme.ellipse;
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$34(__assign$34(__assign$34({}, style), cloneDeep(ellipse)), cloneDeep(customStyle));
	};
	Object.defineProperty(EllipseNodeModel.prototype, "width", {
		get: function() {
			return this.rx * 2;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(EllipseNodeModel.prototype, "height", {
		get: function() {
			return this.ry * 2;
		},
		enumerable: false,
		configurable: true
	});
	EllipseNodeModel.prototype.getDefaultAnchor = function() {
		var _a = this, x = _a.x, y = _a.y, rx = _a.rx, ry = _a.ry;
		return [
			{
				x,
				y: y - ry,
				id: "".concat(this.id, "_0")
			},
			{
				x: x + rx,
				y,
				id: "".concat(this.id, "_1")
			},
			{
				x,
				y: y + ry,
				id: "".concat(this.id, "_2")
			},
			{
				x: x - rx,
				y,
				id: "".concat(this.id, "_3")
			}
		];
	};
	EllipseNodeModel.prototype.resize = function(resizeInfo) {
		var width = resizeInfo.width, height = resizeInfo.height, deltaX = resizeInfo.deltaX, deltaY = resizeInfo.deltaY;
		this.move(deltaX / 2, deltaY / 2);
		this.rx = width;
		this.ry = height;
		this.setProperties({
			rx: width,
			ry: height
		});
		return this.getData();
	};
	__decorate$21([observable], EllipseNodeModel.prototype, "rx", void 0);
	__decorate$21([observable], EllipseNodeModel.prototype, "ry", void 0);
	__decorate$21([computed], EllipseNodeModel.prototype, "width", null);
	__decorate$21([computed], EllipseNodeModel.prototype, "height", null);
	return EllipseNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/PolygonNodeModel.js
var __extends$34 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$33 = function() {
	__assign$33 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$33.apply(this, arguments);
};
var __decorate$20 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$19 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var PolygonNodeModel = function(_super) {
	__extends$34(PolygonNodeModel, _super);
	function PolygonNodeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.POLYGON_NODE;
		_this.points = [
			[50, 0],
			[100, 50],
			[50, 100],
			[0, 50]
		];
		_this.initNodeData(data);
		_this.setAttributes();
		return _this;
	}
	PolygonNodeModel.prototype.setAttributes = function() {
		_super.prototype.setAttributes.call(this);
		var _a = this.properties, points = _a.points, width = _a.width, height = _a.height;
		var nextPoints = points || this.points;
		this.points = normalizePolygon(nextPoints, width, height);
	};
	PolygonNodeModel.prototype.getNodeStyle = function() {
		var style = _super.prototype.getNodeStyle.call(this);
		var _a = this.graphModel, polygon = _a.theme.polygon, customPolygon = _a.customStyles.polygon;
		var _b = this.properties.style, customStyle = _b === void 0 ? {} : _b;
		return __assign$33(__assign$33(__assign$33(__assign$33({}, style), cloneDeep(polygon)), cloneDeep(customPolygon)), cloneDeep(customStyle));
	};
	Object.defineProperty(PolygonNodeModel.prototype, "pointsPosition", {
		/**
		* 由于大多数情况下，我们初始化拿到的多边形坐标都是基于原点的（例如绘图工具到处的svg）。
		* 在logicflow中对多边形进行移动，我们不需要去更新points，
		* 而是去更新多边形中心点即可。
		*/
		get: function() {
			var _a = this, x = _a.x, y = _a.y, width = _a.width, height = _a.height;
			return this.points.map(function(item) {
				return {
					x: item[0] + x - width / 2,
					y: item[1] + y - height / 2
				};
			});
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(PolygonNodeModel.prototype, "width", {
		get: function() {
			var min = Number.MAX_SAFE_INTEGER;
			var max = Number.MIN_SAFE_INTEGER;
			this.points.forEach(function(_a) {
				var x = __read$19(_a, 1)[0];
				if (x < min) min = x;
				if (x > max) max = x;
			});
			return max - min;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(PolygonNodeModel.prototype, "height", {
		get: function() {
			var min = Number.MAX_SAFE_INTEGER;
			var max = Number.MIN_SAFE_INTEGER;
			this.points.forEach(function(_a) {
				var y = __read$19(_a, 2)[1];
				if (y < min) min = y;
				if (y > max) max = y;
			});
			return max - min;
		},
		enumerable: false,
		configurable: true
	});
	PolygonNodeModel.prototype.getDefaultAnchor = function() {
		var _this = this;
		var _a = this, x = _a.x, y = _a.y, width = _a.width, height = _a.height;
		return _a.points.map(function(_a, idx) {
			var _b = __read$19(_a, 2), x1 = _b[0], y1 = _b[1];
			return {
				x: x + x1 - width / 2,
				y: y + y1 - height / 2,
				id: "".concat(_this.id, "_").concat(idx)
			};
		});
	};
	PolygonNodeModel.prototype.resize = function(resizeInfo) {
		var _this = this;
		var width = resizeInfo.width, height = resizeInfo.height, deltaX = resizeInfo.deltaX, deltaY = resizeInfo.deltaY;
		this.move(deltaX / 2, deltaY / 2);
		var nextPoints = map(this.points, function(_a) {
			var _b = __read$19(_a, 2), x = _b[0], y = _b[1];
			return [x * width / _this.width, y * height / _this.height];
		});
		this.points = nextPoints;
		this.properties.points = nextPoints;
		return this.getData();
	};
	__decorate$20([observable], PolygonNodeModel.prototype, "points", void 0);
	__decorate$20([computed], PolygonNodeModel.prototype, "pointsPosition", null);
	__decorate$20([computed], PolygonNodeModel.prototype, "width", null);
	__decorate$20([computed], PolygonNodeModel.prototype, "height", null);
	return PolygonNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/RectNodeModel.js
var __extends$33 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$32 = function() {
	__assign$32 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$32.apply(this, arguments);
};
var __decorate$19 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var RectNodeModel = function(_super) {
	__extends$33(RectNodeModel, _super);
	function RectNodeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.RECT_NODE;
		_this.radius = 0;
		_this.initNodeData(data);
		_this.setAttributes();
		return _this;
	}
	RectNodeModel.prototype.setAttributes = function() {
		_super.prototype.setAttributes.call(this);
		var _a = this.properties, width = _a.width, height = _a.height, radius = _a.radius;
		var styleRadius = this.getNodeStyle().radius;
		if (!isNil(width)) this.width = width;
		if (!isNil(height)) this.height = height;
		if (!isNil(radius)) this.radius = radius;
		if (!isNil(styleRadius)) this.radius = styleRadius;
	};
	RectNodeModel.prototype.getDefaultAnchor = function() {
		var _a = this, x = _a.x, y = _a.y, width = _a.width, height = _a.height;
		return [
			{
				x,
				y: y - height / 2,
				id: "".concat(this.id, "_0")
			},
			{
				x: x + width / 2,
				y,
				id: "".concat(this.id, "_1")
			},
			{
				x,
				y: y + height / 2,
				id: "".concat(this.id, "_2")
			},
			{
				x: x - width / 2,
				y,
				id: "".concat(this.id, "_3")
			}
		];
	};
	RectNodeModel.prototype.getNodeStyle = function() {
		var style = _super.prototype.getNodeStyle.call(this);
		var rect = this.graphModel.theme.rect;
		var _a = this.properties.style, customStyle = _a === void 0 ? {} : _a;
		return __assign$32(__assign$32(__assign$32({}, style), cloneDeep(rect)), cloneDeep(customStyle));
	};
	__decorate$19([observable], RectNodeModel.prototype, "radius", void 0);
	return RectNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/TextNodeModel.js
var __extends$32 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$31 = function() {
	__assign$31 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$31.apply(this, arguments);
};
var __decorate$18 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$18 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$6 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var TextNodeModel = function(_super) {
	__extends$32(TextNodeModel, _super);
	function TextNodeModel() {
		var _this = _super.apply(this, __spreadArray$6([], __read$18(arguments), false)) || this;
		_this.modelType = ModelType.TEXT_NODE;
		return _this;
	}
	TextNodeModel.prototype.getTextStyle = function() {
		var style = _super.prototype.getTextStyle.call(this);
		var text = this.graphModel.theme.text;
		var textStyle = this.properties.textStyle;
		return __assign$31(__assign$31(__assign$31({}, style), cloneDeep(text)), cloneDeep(textStyle));
	};
	Object.defineProperty(TextNodeModel.prototype, "width", {
		get: function() {
			var rows = String(this.text.value).split(/[\r\n]/g);
			var fontSize = this.getTextStyle().fontSize;
			return getSvgTextWidthHeight({
				rows,
				fontSize,
				rowsLength: rows.length
			}).width;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(TextNodeModel.prototype, "height", {
		get: function() {
			var rows = String(this.text.value).split(/[\r\n]/g);
			var fontSize = this.getTextStyle().fontSize;
			return getSvgTextWidthHeight({
				rows,
				fontSize,
				rowsLength: rows.length
			}).height;
		},
		enumerable: false,
		configurable: true
	});
	__decorate$18([computed], TextNodeModel.prototype, "width", null);
	__decorate$18([computed], TextNodeModel.prototype, "height", null);
	return TextNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/node/HtmlNodeModel.js
var __extends$31 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$30 = function() {
	__assign$30 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$30.apply(this, arguments);
};
var HtmlNodeModel = function(_super) {
	__extends$31(HtmlNodeModel, _super);
	function HtmlNodeModel(data, graphModel) {
		var _this = _super.call(this, data, graphModel) || this;
		_this.modelType = ModelType.HTML_NODE;
		_this.setAttributes();
		return _this;
	}
	HtmlNodeModel.prototype.setAttributes = function() {
		_super.prototype.setAttributes.call(this);
		var _a = this.properties, width = _a.width, height = _a.height;
		if (width) this.width = width;
		if (height) this.height = height;
	};
	HtmlNodeModel.prototype.getDefaultAnchor = function() {
		var _a = this, x = _a.x, y = _a.y, width = _a.width, height = _a.height;
		return [
			{
				x,
				y: y - height / 2,
				id: "".concat(this.id, "_0")
			},
			{
				x: x + width / 2,
				y,
				id: "".concat(this.id, "_1")
			},
			{
				x,
				y: y + height / 2,
				id: "".concat(this.id, "_2")
			},
			{
				x: x - width / 2,
				y,
				id: "".concat(this.id, "_3")
			}
		];
	};
	HtmlNodeModel.prototype.getNodeStyle = function() {
		var style = _super.prototype.getNodeStyle.call(this);
		var _a = this.graphModel.theme, baseNode = _a.baseNode, html = _a.html;
		var _b = this.properties.style, customStyle = _b === void 0 ? {} : _b;
		return __assign$30(__assign$30(__assign$30(__assign$30({}, style), cloneDeep(baseNode)), cloneDeep(html)), cloneDeep(customStyle));
	};
	return HtmlNodeModel;
}(BaseNodeModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/EditConfigModel.js
var __decorate$17 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var silentModeConfig = {
	stopZoomGraph: false,
	stopScrollGraph: false,
	stopMoveGraph: false,
	adjustEdge: false,
	adjustEdgeStartAndEnd: false,
	adjustNodePosition: false,
	hideAnchors: true,
	allowRotate: false,
	allowResize: false,
	nodeSelectedOutline: true,
	textEdit: false,
	nodeTextEdit: false,
	edgeTextEdit: false,
	textDraggable: false,
	nodeTextDraggable: false,
	edgeTextDraggable: false
};
var allKeys = [
	"isSilentMode",
	"stopZoomGraph",
	"stopScrollGraph",
	"stopMoveGraph",
	"snapGrid",
	"adjustEdge",
	"adjustEdgeMiddle",
	"adjustEdgeStartAndEnd",
	"adjustEdgeStart",
	"adjustEdgeEnd",
	"adjustNodePosition",
	"hideAnchors",
	"allowRotate",
	"allowResize",
	"autoExpand",
	"hoverOutline",
	"nodeSelectedOutline",
	"edgeSelectedOutline",
	"textEdit",
	"nodeTextEdit",
	"edgeTextEdit",
	"textDraggable",
	"nodeTextDraggable",
	"edgeTextDraggable",
	"multipleSelectKey",
	"textMode",
	"nodeTextMode",
	"edgeTextMode",
	"nodeTextMultiple",
	"edgeTextMultiple",
	"nodeTextVertical",
	"edgeTextVertical",
	"isPinching"
];
/**
* 页面编辑配置
*/
var EditConfigModel = function() {
	function EditConfigModel(config) {
		/*********************************************************
		* 画布相关配置
		********************************************************/
		this.isSilentMode = false;
		this.stopZoomGraph = false;
		this.stopMoveGraph = false;
		this.stopScrollGraph = false;
		this.snapGrid = false;
		this.isPinching = false;
		/*********************************************************
		* 文本相关配置（全局）
		********************************************************/
		this.textMode = TextMode.TEXT;
		this.textEdit = true;
		this.textDraggable = false;
		this.nodeTextEdit = true;
		this.nodeTextDraggable = false;
		this.nodeTextMultiple = false;
		this.nodeTextVertical = false;
		this.nodeTextMode = TextMode.TEXT;
		this.edgeTextMode = TextMode.TEXT;
		this.edgeTextEdit = true;
		this.edgeTextDraggable = false;
		this.edgeTextMultiple = false;
		this.edgeTextVertical = false;
		/*********************************************************
		* 节点相关配置
		********************************************************/
		this.hideAnchors = false;
		this.allowRotate = false;
		this.allowResize = false;
		this.hoverOutline = true;
		this.nodeSelectedOutline = true;
		this.adjustNodePosition = true;
		this.autoExpand = false;
		/*********************************************************
		* 边相关配置
		********************************************************/
		this.adjustEdge = true;
		this.adjustEdgeMiddle = false;
		this.adjustEdgeStartAndEnd = false;
		this.adjustEdgeStart = false;
		this.adjustEdgeEnd = false;
		this.edgeSelectedOutline = true;
		/*********************************************************
		* 其他
		********************************************************/
		this.multipleSelectKey = "";
		assign(this, this.computeConfig(config));
	}
	EditConfigModel.prototype.updateEditConfig = function(config) {
		var newConfig = this.computeConfig(config);
		assign(this, newConfig);
	};
	EditConfigModel.prototype.computeConfig = function(config) {
		var isSilentMode = config.isSilentMode, textDraggable = config.textDraggable, textMode = config.textMode, textEdit = config.textEdit, adjustEdgeStartAndEnd = config.adjustEdgeStartAndEnd;
		var conf = {};
		if (isSilentMode === false) assign(conf, this.stagedConfig);
		if (isSilentMode === true && isSilentMode !== this.isSilentMode) {
			var silentConfig = pick(silentModeConfig, allKeys);
			this.stagedConfig = pick(this, allKeys);
			assign(conf, silentConfig);
		}
		if (!isUndefined(textEdit)) assign(conf, {
			nodeTextEdit: textEdit,
			edgeTextEdit: textEdit
		});
		if (!isUndefined(textDraggable)) assign(conf, {
			nodeTextDraggable: textDraggable,
			edgeTextDraggable: textDraggable
		});
		if (textMode) assign(conf, {
			nodeTextMode: textMode,
			edgeTextMode: textMode
		});
		if (isBoolean(adjustEdgeStartAndEnd)) assign(conf, {
			adjustEdgeStart: adjustEdgeStartAndEnd,
			adjustEdgeEnd: adjustEdgeStartAndEnd
		});
		return assign(conf, pick(config, allKeys));
	};
	EditConfigModel.prototype.updateTextMode = function(textMode) {
		this.textMode = textMode;
		this.edgeTextMode = textMode;
		this.nodeTextMode = textMode;
	};
	EditConfigModel.prototype.getConfig = function() {
		return pick(this, allKeys);
	};
	__decorate$17([observable], EditConfigModel.prototype, "isSilentMode", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "stopZoomGraph", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "stopMoveGraph", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "stopScrollGraph", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "snapGrid", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "isPinching", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "textMode", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "textEdit", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "textDraggable", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "nodeTextEdit", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "nodeTextDraggable", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "nodeTextMultiple", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "nodeTextVertical", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "nodeTextMode", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "edgeTextMode", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "edgeTextEdit", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "edgeTextDraggable", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "edgeTextMultiple", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "edgeTextVertical", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "hideAnchors", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "allowRotate", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "allowResize", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "hoverOutline", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "nodeSelectedOutline", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "adjustNodePosition", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "autoExpand", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "adjustEdge", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "adjustEdgeMiddle", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "adjustEdgeStartAndEnd", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "adjustEdgeStart", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "adjustEdgeEnd", void 0);
	__decorate$17([observable], EditConfigModel.prototype, "edgeSelectedOutline", void 0);
	__decorate$17([action], EditConfigModel.prototype, "updateEditConfig", null);
	__decorate$17([action], EditConfigModel.prototype, "updateTextMode", null);
	return EditConfigModel;
}();
//#endregion
//#region node_modules/@logicflow/core/es/event/eventEmitter.js
var WILDCARD = "*";
var EventEmitter = function() {
	function EventEmitter() {
		this._events = {};
	}
	EventEmitter.prototype.on = function(evt, callback, once) {
		var _this = this;
		evt === null || evt === void 0 || evt.split(",").forEach(function(evKey) {
			evKey = evKey.trim();
			if (!_this._events[evKey]) _this._events[evKey] = [];
			_this._events[evKey].push({
				callback,
				once: !!once
			});
		});
	};
	EventEmitter.prototype.once = function(evt, callback) {
		var _this = this;
		evt === null || evt === void 0 || evt.split(",").forEach(function(evKey) {
			evKey = evKey.trim();
			_this.on(evKey, callback, true);
		});
	};
	EventEmitter.prototype.emit = function(evts, eventArgs) {
		var _this = this;
		evts === null || evts === void 0 || evts.split(",").forEach(function(evt) {
			var events = _this._events[evt] || [];
			var wildcardEvents = _this._events[WILDCARD] || [];
			var doEmit = function(es) {
				var length = es.length;
				for (var i = 0; i < length; i++) {
					if (!es[i]) continue;
					var _a = es[i], callback = _a.callback;
					if (_a.once) {
						es.splice(i, 1);
						if (es.length === 0) delete _this._events[evt];
						length--;
						i--;
					}
					callback.apply(_this, [eventArgs]);
				}
			};
			doEmit(events);
			doEmit(wildcardEvents);
		});
	};
	EventEmitter.prototype.off = function(evts, callback) {
		var _this = this;
		if (!evts) this._events = {};
		evts.split(",").forEach(function(evt) {
			if (!callback) delete _this._events[evt];
			else {
				var events = _this._events[evt] || [];
				var length_1 = events.length;
				for (var i = 0; i < length_1; i++) if (events[i].callback === callback) {
					events.splice(i, 1);
					length_1--;
					i--;
				}
				if (events.length === 0) delete _this._events[evt];
			}
		});
	};
	EventEmitter.prototype.getEvents = function() {
		return this._events;
	};
	EventEmitter.prototype.destroy = function() {
		this._events = {};
	};
	return EventEmitter;
}();
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/CanvasOverlay.js
var __extends$30 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$16 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var CanvasOverlay = function(_super) {
	__extends$30(CanvasOverlay, _super);
	function CanvasOverlay(props) {
		var _this = _super.call(this) || this;
		_this.stepScrollX = 0;
		_this.stepScrollY = 0;
		_this.pointers = /* @__PURE__ */ new Map();
		_this.onDragging = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			if (_this.longPressTimer) {
				clearTimeout(_this.longPressTimer);
				_this.longPressTimer = void 0;
			}
			_this.setState({ isDragging: true });
			var _b = _this.props.graphModel, transformModel = _b.transformModel;
			if (_b.editConfigModel.stopMoveGraph === true) return;
			transformModel.translate(deltaX, deltaY);
		};
		_this.onDragEnd = function() {
			_this.setState({ isDragging: false });
		};
		_this.zoomHandler = function(ev) {
			var _a = _this.props, _b = _a.graphModel, editConfigModel = _b.editConfigModel, transformModel = _b.transformModel, gridSize = _b.gridSize, graphModel = _a.graphModel;
			var eX = ev.deltaX, eY = ev.deltaY;
			var stopScrollGraph = editConfigModel.stopScrollGraph, stopZoomGraph = editConfigModel.stopZoomGraph;
			if (!stopScrollGraph && !ev.ctrlKey && !ev.metaKey) {
				ev.preventDefault();
				_this.stepScrollX += eX;
				_this.stepScrollY += eY;
				if (Math.abs(_this.stepScrollX) >= gridSize) {
					var remainderX = _this.stepScrollX % gridSize;
					var moveDistance = _this.stepScrollX - remainderX;
					transformModel.translate(-moveDistance * transformModel.SCALE_X, 0);
					_this.stepScrollX = remainderX;
				}
				if (Math.abs(_this.stepScrollY) >= gridSize) {
					var remainderY = _this.stepScrollY % gridSize;
					var moveDistanceY = _this.stepScrollY - remainderY;
					transformModel.translate(0, -moveDistanceY * transformModel.SCALE_Y);
					_this.stepScrollY = remainderY;
				}
				return;
			}
			if (!stopZoomGraph) {
				ev.preventDefault();
				var _c = graphModel.getPointByClient({
					x: ev.clientX,
					y: ev.clientY
				}).canvasOverlayPosition, x = _c.x, y = _c.y;
				transformModel.zoom(ev.deltaY < 0, [x, y]);
			}
		};
		_this.clickHandler = function(ev) {
			if (ev.target.getAttribute("name") === "canvas-overlay") {
				var graphModel = _this.props.graphModel;
				if (graphModel.selectElements.size > 0) graphModel.clearSelectElements();
				if (_this.state.isDragging) return;
				graphModel.eventCenter.emit(EventType.BLANK_CLICK, { e: ev });
			}
		};
		_this.handleContextMenu = function(ev) {
			if (ev.target.getAttribute("name") === "canvas-overlay") {
				ev.preventDefault();
				var graphModel = _this.props.graphModel;
				var position = graphModel.getPointByClient({
					x: ev.clientX,
					y: ev.clientY
				});
				graphModel.eventCenter.emit(EventType.BLANK_CONTEXTMENU, {
					e: ev,
					position
				});
			}
		};
		_this.pointerDownHandler = function(ev) {
			var _a = _this.props.graphModel, eventCenter = _a.eventCenter, editConfigModel = _a.editConfigModel, SCALE_X = _a.transformModel.SCALE_X, gridSize = _a.gridSize;
			_this.pointers.set(ev.pointerId, {
				x: ev.clientX,
				y: ev.clientY
			});
			if (_this.longPressTimer) clearTimeout(_this.longPressTimer);
			if (ev.pointerType === "touch") _this.longPressTimer = window.setTimeout(function() {
				_this.handleContextMenu(ev);
			}, 500);
			if (_this.pointers.size === 2) {
				var _b = _this.props.graphModel, transformModel = _b.transformModel, editConfigModel_1 = _b.editConfigModel;
				var pts = Array.from(_this.pointers.values());
				var dx = pts[0].x - pts[1].x;
				var dy = pts[0].y - pts[1].y;
				var cx = (pts[0].x + pts[1].x) / 2;
				var cy = (pts[0].y + pts[1].y) / 2;
				_this.pinchStartDistance = Math.hypot(dx, dy);
				_this.pinchStartScale = transformModel.SCALE_X;
				_this.stepDrag.cancelDrag();
				_this.pinchLastCenterX = cx;
				_this.pinchLastCenterY = cy;
				editConfigModel_1.updateEditConfig({ isPinching: true });
				return;
			}
			var adjustEdge = editConfigModel.adjustEdge, adjustNodePosition = editConfigModel.adjustNodePosition, stopMoveGraph = editConfigModel.stopMoveGraph;
			var target = ev.target;
			var isFrozenElement = !adjustEdge && !adjustNodePosition;
			if (target.getAttribute("name") === "canvas-overlay" || isFrozenElement) if (stopMoveGraph !== true) {
				_this.stepDrag.setStep(gridSize * SCALE_X);
				_this.stepDrag.handleMouseDown(ev);
			} else eventCenter.emit(EventType.BLANK_MOUSEDOWN, { e: ev });
		};
		_this.pointerMoveHandler = function(ev) {
			var _a;
			_this.pointers.set(ev.pointerId, {
				x: ev.clientX,
				y: ev.clientY
			});
			if (_this.pinchStartDistance && _this.pointers.size >= 2) {
				var _b = _this.props, graphModel = _b.graphModel, _c = _b.graphModel, editConfigModel = _c.editConfigModel, transformModel = _c.transformModel;
				if (editConfigModel.stopZoomGraph) return;
				if (_this.longPressTimer) clearTimeout(_this.longPressTimer);
				var pts = Array.from(_this.pointers.values());
				var dx = pts[0].x - pts[1].x;
				var dy = pts[0].y - pts[1].y;
				var dist = Math.hypot(dx, dy);
				var scale = ((_a = _this.pinchStartScale) !== null && _a !== void 0 ? _a : transformModel.SCALE_X) * (dist / _this.pinchStartDistance);
				var cx = (pts[0].x + pts[1].x) / 2;
				var cy = (pts[0].y + pts[1].y) / 2;
				var _d = graphModel.getPointByClient({
					x: cx,
					y: cy
				}).canvasOverlayPosition, x = _d.x, y = _d.y;
				transformModel.zoom(scale, [x, y]);
				if (!editConfigModel.stopMoveGraph || editConfigModel.isPinching) {
					var deltaX = _this.pinchLastCenterX === void 0 ? 0 : cx - _this.pinchLastCenterX;
					var deltaY = _this.pinchLastCenterY === void 0 ? 0 : cy - _this.pinchLastCenterY;
					transformModel.translate(deltaX, deltaY);
					_this.pinchLastCenterX = cx;
					_this.pinchLastCenterY = cy;
				}
				ev.preventDefault();
			}
		};
		_this.pointerUpHandler = function(ev) {
			_this.pointers.delete(ev.pointerId);
			if (_this.longPressTimer) {
				clearTimeout(_this.longPressTimer);
				_this.longPressTimer = void 0;
			}
			if (_this.pointers.size < 2) {
				_this.pinchStartDistance = void 0;
				_this.pinchStartScale = void 0;
				_this.pinchLastCenterX = void 0;
				_this.pinchLastCenterY = void 0;
				_this.props.graphModel.editConfigModel.updateEditConfig({ isPinching: false });
				_this.clickHandler(ev);
			}
		};
		var _a = props.graphModel, gridSize = _a.gridSize, eventCenter = _a.eventCenter;
		_this.stepDrag = new StepDrag({
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd,
			step: gridSize,
			eventType: "BLANK",
			isStopPropagation: false,
			eventCenter,
			model: void 0
		});
		_this.state = { isDragging: false };
		return _this;
	}
	CanvasOverlay.prototype.render = function() {
		var transform = this.props.graphModel.transformModel.getTransformStyle().transform;
		var children = this.props.children;
		var isDragging = this.state.isDragging;
		return u("svg", {
			xmlns: "http://www.w3.org/2000/svg",
			width: "100%",
			height: "100%",
			name: "canvas-overlay",
			onWheel: this.zoomHandler,
			onPointerDown: this.pointerDownHandler,
			onPointerMove: this.pointerMoveHandler,
			onPointerUp: this.pointerUpHandler,
			onPointerCancel: this.pointerUpHandler,
			onContextMenu: this.handleContextMenu,
			style: {
				touchAction: "none",
				WebkitUserSelect: "none"
			},
			className: isDragging ? "lf-canvas-overlay lf-dragging" : "lf-canvas-overlay lf-drag-able",
			children: u("g", {
				transform,
				children
			})
		});
	};
	CanvasOverlay = __decorate$16([observer], CanvasOverlay);
	return CanvasOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/BezierAdjustOverlay.js
var __extends$29 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$29 = function() {
	__assign$29 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$29.apply(this, arguments);
};
var __decorate$15 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$17 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BezierAdjustAnchor = function(_super) {
	__extends$29(BezierAdjustAnchor, _super);
	function BezierAdjustAnchor() {
		var _this = _super.call(this) || this;
		_this.onDragging = function(_a) {
			var event = _a.event;
			var _b = _this.props, graphModel = _b.graphModel, bezierModel = _b.bezierModel, type = _b.type;
			var _c = graphModel.getPointByClient({
				x: event.clientX,
				y: event.clientY
			}).canvasOverlayPosition, x = _c.x, y = _c.y;
			bezierModel.updateAdjustAnchor({
				x,
				y
			}, type);
			graphModel.eventCenter.emit(EventType.EDGE_ADJUST, { data: bezierModel.getData() });
		};
		_this.onDragEnd = function() {
			var bezierModel = _this.props.bezierModel;
			bezierModel.isDragging = false;
		};
		_this.dragHandler = new StepDrag({
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd
		});
		return _this;
	}
	BezierAdjustAnchor.prototype.render = function() {
		var _this = this;
		var position = this.props.position;
		var x = position.x, y = position.y;
		var adjustAnchor = this.props.bezierModel.getEdgeStyle().adjustAnchor;
		return u(Circle, __assign$29({
			className: "lf-bezier-adjust-anchor",
			x,
			y
		}, adjustAnchor, { onPointerDown: function(ev) {
			_this.dragHandler.handleMouseDown(ev);
		} }));
	};
	return BezierAdjustAnchor;
}(C);
var BezierAdjustOverlay = function(_super) {
	__extends$29(BezierAdjustOverlay, _super);
	function BezierAdjustOverlay() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	BezierAdjustOverlay.prototype.getBezierAdjust = function(bezier, graphModel) {
		var path = bezier.path, id = bezier.id;
		var _a = __read$17(getBezierPoints(path), 4), start = _a[0], sNext = _a[1], ePre = _a[2], end = _a[3];
		var adjustLine = bezier.getEdgeStyle().adjustLine;
		var result = [];
		result.push(u(Line, __assign$29({
			x1: start.x,
			y1: start.y,
			x2: sNext.x,
			y2: sNext.y
		}, adjustLine)));
		result.push(u(BezierAdjustAnchor, {
			position: sNext,
			bezierModel: bezier,
			graphModel,
			type: "sNext"
		}, "".concat(id, "_ePre")));
		result.push(u(Line, __assign$29({
			x1: end.x,
			y1: end.y,
			x2: ePre.x,
			y2: ePre.y
		}, adjustLine)));
		result.push(u(BezierAdjustAnchor, {
			position: ePre,
			bezierModel: bezier,
			graphModel,
			type: "ePre"
		}, "".concat(id, "_sNext")));
		return result;
	};
	BezierAdjustOverlay.prototype.selectedBezierEdge = function() {
		var graphModel = this.props.graphModel;
		var edgeList = graphModel.edges;
		var edgeAdjust = [];
		for (var i = 0; i < edgeList.length; i++) {
			var edge = edgeList[i];
			if (edge.isSelected && edge.modelType === ModelType.BEZIER_EDGE && edge.draggable) edgeAdjust.push(this.getBezierAdjust(edge, graphModel));
		}
		return edgeAdjust;
	};
	BezierAdjustOverlay.prototype.render = function() {
		return u("g", {
			className: "lf-bezier-adjust",
			children: this.selectedBezierEdge()
		});
	};
	BezierAdjustOverlay = __decorate$15([observer], BezierAdjustOverlay);
	return BezierAdjustOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/BackgroundOverlay.js
var __extends$28 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$14 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var BackgroundOverlay = function(_super) {
	__extends$28(BackgroundOverlay, _super);
	function BackgroundOverlay() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	BackgroundOverlay.prototype.render = function() {
		var background = this.props.background;
		return u("div", {
			className: "lf-background",
			children: u("div", {
				style: isObject$1(background) ? background : {},
				className: "lf-background-area"
			})
		});
	};
	BackgroundOverlay = __decorate$14([observer], BackgroundOverlay);
	return BackgroundOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/gridConfig.js
var __assign$28 = function() {
	__assign$28 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$28.apply(this, arguments);
};
var __values$1 = function(o) {
	var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
	if (m) return m.call(o);
	if (o && typeof o.length === "number") return { next: function() {
		if (o && i >= o.length) o = void 0;
		return {
			value: o && o[i++],
			done: !o
		};
	} };
	throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var defaultGridConfig = {
	opacity: 1,
	boldIndices: [5],
	dashArrayConfig: { pattern: [2, 1] },
	customBoldWidth: 2
};
/**
* 校验并规范化 GridConfig。
* - 将 opacity 限制在 [0,1] 范围
* - boldIndices 过滤为正整数并去重
*/
function validateGridConfig(cfg) {
	var e_1, _a;
	var _b, _c;
	var opacity = Math.max(0, Math.min(1, (_b = cfg.opacity) !== null && _b !== void 0 ? _b : defaultGridConfig.opacity));
	var boldIndicesSet = /* @__PURE__ */ new Set();
	var boldRaw = Array.isArray(cfg.boldIndices) ? cfg.boldIndices : defaultGridConfig.boldIndices;
	try {
		for (var boldRaw_1 = __values$1(boldRaw), boldRaw_1_1 = boldRaw_1.next(); !boldRaw_1_1.done; boldRaw_1_1 = boldRaw_1.next()) {
			var n = boldRaw_1_1.value;
			if (typeof n === "number" && Number.isFinite(n) && n > 0) boldIndicesSet.add(Math.floor(n));
		}
	} catch (e_1_1) {
		e_1 = { error: e_1_1 };
	} finally {
		try {
			if (boldRaw_1_1 && !boldRaw_1_1.done && (_a = boldRaw_1.return)) _a.call(boldRaw_1);
		} finally {
			if (e_1) throw e_1.error;
		}
	}
	var boldIndices = Array.from(boldIndicesSet);
	var dashArrayConfig = { pattern: Array.isArray((_c = cfg.dashArrayConfig) === null || _c === void 0 ? void 0 : _c.pattern) ? cfg.dashArrayConfig.pattern : defaultGridConfig.dashArrayConfig.pattern };
	var customBoldWidth = cfg.customBoldWidth;
	return {
		opacity,
		boldIndices: boldIndices.length ? boldIndices : defaultGridConfig.boldIndices,
		dashArrayConfig,
		customBoldWidth
	};
}
/**
* 合并用户行为配置与默认值。
* - false → 关闭模式：opacity=1，bold=[]，关闭虚线
* - true → 默认模式：采用 defaultGridConfig
* - object → 自定义模式：校验后使用用户配置
*/
function mergeMajorBoldConfig(input) {
	if (input === false) return {
		mode: "disabled",
		config: {
			opacity: 1,
			boldIndices: [],
			dashArrayConfig: { pattern: [] }
		}
	};
	if (input === true || input == null) return {
		mode: "default",
		config: __assign$28({}, defaultGridConfig)
	};
	return {
		mode: "custom",
		config: validateGridConfig(input)
	};
}
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/Grid.js
var __extends$27 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$13 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$16 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$5 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var Grid = function(_super) {
	__extends$27(Grid, _super);
	function Grid(props) {
		var _this = _super.call(this, props) || this;
		_this.id = createUuid();
		_this.gridOptions = _this.props.graphModel.grid;
		return _this;
	}
	Grid.prototype.renderDot = function() {
		var _a = this.gridOptions, config = _a.config, _b = _a.size, size = _b === void 0 ? 1 : _b, visible = _a.visible;
		var _c = config !== null && config !== void 0 ? config : {}, color = _c.color, _d = _c.thickness;
		var radius = Math.min(Math.max(2, _d === void 0 ? 2 : _d), size / 4);
		return u("g", {
			fill: color,
			opacity: visible ? 1 : 0,
			children: [
				u("circle", {
					cx: 0,
					cy: 0,
					r: radius / 2
				}),
				u("circle", {
					cx: 0,
					cy: size,
					r: radius / 2
				}),
				u("circle", {
					cx: size,
					cy: 0,
					r: radius / 2
				}),
				u("circle", {
					cx: size,
					cy: size,
					r: radius / 2
				})
			]
		});
	};
	Grid.prototype.getDashArrayForSize = function(dashCfg) {
		var _a;
		var direct = (_a = dashCfg === null || dashCfg === void 0 ? void 0 : dashCfg.pattern) === null || _a === void 0 ? void 0 : _a.filter(function(n) {
			return typeof n === "number" && n > 0;
		});
		return (direct === null || direct === void 0 ? void 0 : direct.join(",")) || "2,1";
	};
	Grid.prototype.getPeriod = function(advanced) {
		var list = Array.isArray(advanced === null || advanced === void 0 ? void 0 : advanced.boldIndices) ? advanced.boldIndices.filter(function(n) {
			return typeof n === "number" && n > 0;
		}) : [];
		return list.length ? Math.max.apply(Math, __spreadArray$5([], __read$16(list), false)) : 0;
	};
	Grid.prototype.getBoldStrokeWidth = function(advanced, size, thickness, period) {
		if (typeof (advanced === null || advanced === void 0 ? void 0 : advanced.customBoldWidth) === "number") return advanced.customBoldWidth;
		var baseThickness = Math.max(1, thickness !== null && thickness !== void 0 ? thickness : 1);
		var p = Math.max(1, period !== null && period !== void 0 ? period : this.getPeriod(advanced));
		return Math.min(baseThickness, size * p / 2) / 2;
	};
	Grid.prototype.renderMeshEdgeLines = function(size, color, strokeWidth, opacity, dash) {
		return u("g", {
			opacity,
			fill: "transparent",
			children: [
				{ d: "M 0 0 H ".concat(size) },
				{ d: "M 0 ".concat(size, " H ").concat(size) },
				{ d: "M 0 0 V ".concat(size) },
				{ d: "M ".concat(size, " 0 V ").concat(size) }
			].map(function(seg) {
				return u("path", {
					d: seg.d,
					stroke: color,
					strokeWidth: strokeWidth / 2,
					strokeDasharray: dash,
					strokeLinecap: "butt",
					fill: "transparent"
				});
			})
		});
	};
	Grid.prototype.renderMesh = function() {
		var _a, _b;
		var _c = this.gridOptions, config = _c.config, _d = _c.size, size = _d === void 0 ? 1 : _d, visible = _c.visible, majorBold = _c.majorBold;
		var advanced = mergeMajorBoldConfig(majorBold).config;
		var baseOpacity = advanced.opacity;
		var color = (_a = config === null || config === void 0 ? void 0 : config.color) !== null && _a !== void 0 ? _a : "#D7DEEB";
		var thickness = (_b = config === null || config === void 0 ? void 0 : config.thickness) !== null && _b !== void 0 ? _b : 1;
		var strokeWidth = Math.min(Math.max(1, thickness), size / 2);
		var opacity = visible ? baseOpacity : 0;
		var dash = majorBold === false ? void 0 : this.getDashArrayForSize(advanced.dashArrayConfig);
		return this.renderMeshEdgeLines(size, color, strokeWidth, opacity, dash);
	};
	Grid.prototype.render = function() {
		var _this = this;
		var _a, _b, _c;
		var _d = this.props.graphModel, transformModel = _d.transformModel, grid = _d.grid;
		this.gridOptions = grid;
		var _e = this.gridOptions, type = _e.type, _f = _e.config, config = _f === void 0 ? {} : _f, _g = _e.size, size = _g === void 0 ? 1 : _g, majorBold = _e.majorBold;
		var showMajorBold = majorBold !== false && !isNil(majorBold);
		var advanced = mergeMajorBoldConfig(majorBold).config;
		var matrixString = [
			transformModel.SCALE_X,
			transformModel.SKEW_Y,
			transformModel.SKEW_X,
			transformModel.SCALE_Y,
			transformModel.TRANSLATE_X,
			transformModel.TRANSLATE_Y
		].join(",");
		var transform = "matrix(".concat(matrixString, ")");
		var radius = Math.min(Math.max(2, (_a = config.thickness) !== null && _a !== void 0 ? _a : 1), size / 4);
		var opacity = showMajorBold ? advanced.opacity : 1;
		return u("div", {
			className: "lf-grid",
			children: u("svg", {
				xmlns: "http://www.w3.org/2000/svg",
				version: "1.1",
				width: "100%",
				height: "100%",
				children: [
					u("defs", { children: [
						u("pattern", {
							id: this.id,
							patternUnits: "userSpaceOnUse",
							patternTransform: transform,
							x: "0",
							y: "0",
							width: size,
							height: size,
							children: [type === "dot" && this.renderDot(), type === "mesh" && this.renderMesh()]
						}),
						type === "dot" && advanced.boldIndices.length ? u("pattern", {
							id: "".concat(this.id, "-dot-major"),
							patternUnits: "userSpaceOnUse",
							patternTransform: transform,
							x: "0",
							y: "0",
							width: size * this.getPeriod(advanced),
							height: size * this.getPeriod(advanced),
							children: u("g", {
								fill: (_c = (_b = this.gridOptions.config) === null || _b === void 0 ? void 0 : _b.color) !== null && _c !== void 0 ? _c : "#D7DEEB",
								opacity: this.gridOptions.visible ? opacity : 0,
								children: [
									u("circle", {
										cx: 0,
										cy: 0,
										r: radius * 1.5 / 2
									}),
									u("circle", {
										cx: size * this.getPeriod(advanced),
										cy: 0,
										r: radius * 1.5 / 2
									}),
									u("circle", {
										cx: 0,
										cy: size * this.getPeriod(advanced),
										r: radius * 1.5 / 2
									}),
									u("circle", {
										cx: size * this.getPeriod(advanced),
										cy: size * this.getPeriod(advanced),
										r: radius * 1.5 / 2
									})
								]
							})
						}) : null,
						type === "mesh" && advanced.boldIndices.length ? u("pattern", {
							id: "".concat(this.id, "-major"),
							patternUnits: "userSpaceOnUse",
							patternTransform: transform,
							x: "0",
							y: "0",
							width: size * this.getPeriod(advanced),
							height: size * this.getPeriod(advanced),
							children: advanced.boldIndices.map(function(i) {
								var _a, _b, _c, _d, _e, _f;
								return u("g", { children: [u("path", {
									d: "M ".concat(size * i, " 0 V ").concat(size * _this.getPeriod(advanced)),
									stroke: (_b = (_a = _this.gridOptions.config) === null || _a === void 0 ? void 0 : _a.color) !== null && _b !== void 0 ? _b : "#D7DEEB",
									strokeWidth: _this.getBoldStrokeWidth(advanced, size, ((_c = _this.gridOptions.config) !== null && _c !== void 0 ? _c : {}).thickness, _this.getPeriod(advanced)),
									opacity: _this.gridOptions.visible ? opacity : 0,
									fill: "transparent"
								}), u("path", {
									d: "M 0 ".concat(size * i, " H ").concat(size * _this.getPeriod(advanced)),
									stroke: (_e = (_d = _this.gridOptions.config) === null || _d === void 0 ? void 0 : _d.color) !== null && _e !== void 0 ? _e : "#D7DEEB",
									strokeWidth: _this.getBoldStrokeWidth(advanced, size, ((_f = _this.gridOptions.config) !== null && _f !== void 0 ? _f : {}).thickness, _this.getPeriod(advanced)),
									opacity: _this.gridOptions.visible ? opacity : 0,
									fill: "transparent"
								})] });
							})
						}) : null
					] }),
					u("rect", {
						width: "100%",
						height: "100%",
						fill: "url(#".concat(this.id, ")")
					}),
					type === "dot" && showMajorBold && advanced.boldIndices.length ? u("rect", {
						width: "100%",
						height: "100%",
						fill: "url(#".concat(this.id, "-dot-major)")
					}) : null,
					type === "mesh" && showMajorBold && advanced.boldIndices.length ? u("rect", {
						width: "100%",
						height: "100%",
						fill: "url(#".concat(this.id, "-major)")
					}) : null
				]
			})
		});
	};
	Grid = __decorate$13([observer], Grid);
	return Grid;
}(C);
(function(Grid) {
	function getGridOptions(options) {
		var defaultOptions = cloneDeep(defaultGrid);
		if (typeof options === "number") return assign(defaultOptions, { size: options });
		else if (typeof options === "boolean") return assign(defaultOptions, { visible: options });
		else return assign(defaultOptions, options);
	}
	Grid.getGridOptions = getGridOptions;
})(Grid || (Grid = {}));
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/ModificationOverlay.js
var __extends$26 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$12 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var ModificationOverlay = function(_super) {
	__extends$26(ModificationOverlay, _super);
	function ModificationOverlay() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	ModificationOverlay.prototype.render = function() {
		var transform = this.props.graphModel.transformModel.getTransformStyle().transform;
		var children = this.props.children;
		return u("svg", {
			xmlns: "http://www.w3.org/2000/svg",
			version: "1.1",
			width: "100%",
			height: "100%",
			className: "modification-overlay",
			children: u("g", {
				transform,
				children
			})
		});
	};
	ModificationOverlay = __decorate$12([observer], ModificationOverlay);
	return ModificationOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/OutlineOverlay.js
var __extends$25 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$27 = function() {
	__assign$27 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$27.apply(this, arguments);
};
var __decorate$11 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var OutlineOverlay = function(_super) {
	__extends$25(OutlineOverlay, _super);
	function OutlineOverlay() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	OutlineOverlay.prototype.renderRectOutline = function(pointsList, style, className, defaultOffsets) {
		var _a = style || {}, _b = _a.widthOffset, widthOffset = _b === void 0 ? defaultOffsets.widthOffset : _b, _c = _a.heightOffset;
		var _d = getBBoxOfPoints(pointsList, widthOffset, _c === void 0 ? defaultOffsets.heightOffset : _c), x = _d.x, y = _d.y, width = _d.width, height = _d.height;
		return u(Rect, __assign$27({
			className,
			x,
			y,
			width,
			height
		}, style));
	};
	OutlineOverlay.prototype.getNodesOutline = function() {
		var graphModel = this.props.graphModel;
		var nodes = graphModel.nodes, _a = graphModel.editConfigModel, hoverOutline = _a.hoverOutline, nodeSelectedOutline = _a.nodeSelectedOutline;
		var nodeOutline = [];
		nodes.forEach(function(element) {
			if (element.isHovered || element.isSelected) {
				var isHovered = element.isHovered, isSelected = element.isSelected, x = element.x, y = element.y, width = element.width, height = element.height;
				if (nodeSelectedOutline && isSelected || hoverOutline && isHovered) {
					var style_1 = element.getOutlineStyle() || {};
					var attributes_1 = {};
					Object.keys(style_1).forEach(function(key) {
						if (key !== "hover") attributes_1[key] = style_1[key];
					});
					if (isHovered) {
						var hoverStyle = style_1.hover;
						attributes_1 = __assign$27(__assign$27({}, attributes_1), hoverStyle);
					}
					nodeOutline.push(u(Rect, __assign$27({
						transform: element.transform,
						className: "lf-outline-node",
						x,
						y,
						width: width + 4,
						height: height + 4
					}, attributes_1)));
				}
			}
		});
		return nodeOutline;
	};
	OutlineOverlay.prototype.getEdgeOutline = function() {
		var _a = this.props.graphModel, edgeList = _a.edges, _b = _a.editConfigModel, edgeSelectedOutline = _b.edgeSelectedOutline, hoverOutline = _b.hoverOutline;
		var edgeOutline = [];
		for (var i = 0; i < edgeList.length; i++) {
			var edge = edgeList[i];
			if (edgeSelectedOutline && edge.isSelected || hoverOutline && edge.isHovered) {
				if (edge.modelType === ModelType.LINE_EDGE) edgeOutline.push(this.getLineOutline(edge));
				else if (edge.modelType === ModelType.POLYLINE_EDGE) edgeOutline.push(this.getPolylineOutline(edge));
				else if (edge.modelType === ModelType.BEZIER_EDGE) edgeOutline.push(this.getBezierOutline(edge));
			}
		}
		return edgeOutline;
	};
	OutlineOverlay.prototype.getLineOutline = function(line) {
		var startPoint = line.startPoint, endPoint = line.endPoint;
		var style = line.getOutlineStyle();
		return this.renderRectOutline([startPoint, endPoint], style, "lf-outline-edge", {
			widthOffset: 10,
			heightOffset: 10
		});
	};
	OutlineOverlay.prototype.getPolylineOutline = function(polyline) {
		var points = polyline.points;
		var pointsList = points2PointsList(points);
		var style = polyline.getOutlineStyle();
		return this.renderRectOutline(pointsList, style, "lf-outline", {
			widthOffset: 8,
			heightOffset: 16
		});
	};
	OutlineOverlay.prototype.getBezierOutline = function(bezier) {
		var path = bezier.path;
		var pointsList = getBezierPoints(path);
		var style = bezier.getOutlineStyle();
		return this.renderRectOutline(pointsList, style, "lf-outline", {
			widthOffset: 8,
			heightOffset: 16
		});
	};
	OutlineOverlay.prototype.render = function() {
		return u("g", {
			className: "lf-outline",
			children: [this.getNodesOutline(), this.getEdgeOutline()]
		});
	};
	OutlineOverlay = __decorate$11([observer], OutlineOverlay);
	return OutlineOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/SnaplineOverlay.js
var __extends$24 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$26 = function() {
	__assign$26 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$26.apply(this, arguments);
};
var __decorate$10 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var SnaplineOverlay = function(_super) {
	__extends$24(SnaplineOverlay, _super);
	function SnaplineOverlay() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	SnaplineOverlay.prototype.render = function() {
		var snaplineModel = this.props.snaplineModel;
		var _a = snaplineModel !== null && snaplineModel !== void 0 ? snaplineModel : {}, position = _a.position, isShowHorizontal = _a.isShowHorizontal, isShowVertical = _a.isShowVertical;
		var style = snaplineModel === null || snaplineModel === void 0 ? void 0 : snaplineModel.getStyle();
		var _b = position !== null && position !== void 0 ? position : {}, _c = _b.x, x = _c === void 0 ? 0 : _c, _d = _b.y, y = _d === void 0 ? 0 : _d;
		var horizontalLine = __assign$26(__assign$26({
			x1: -1e5,
			y1: y,
			x2: 1e5,
			y2: y
		}, style), { stroke: isShowHorizontal ? style === null || style === void 0 ? void 0 : style.stroke : "none" });
		var verticalLine = __assign$26(__assign$26({
			x1: x,
			y1: -1e5,
			x2: x,
			y2: 1e5
		}, style), { stroke: isShowVertical ? style === null || style === void 0 ? void 0 : style.stroke : "none" });
		return u("g", {
			className: "lf-snapline",
			children: [u(Line, __assign$26({}, horizontalLine)), u(Line, __assign$26({}, verticalLine))]
		});
	};
	SnaplineOverlay = __decorate$10([observer], SnaplineOverlay);
	return SnaplineOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/overlay/ToolOverlay.js
var __extends$23 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$9 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var ToolOverlay = function(_super) {
	__extends$23(ToolOverlay, _super);
	function ToolOverlay() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	ToolOverlay.prototype.componentDidMount = function() {
		this.triggerToolRender();
	};
	ToolOverlay.prototype.componentDidUpdate = function() {
		this.triggerToolRender();
	};
	/**
	* 外部传入的一般是HTMLElement
	*/
	ToolOverlay.prototype.getTools = function() {
		var _a = this.props, tool = _a.tool, graphModel = _a.graphModel;
		var textEditElement = graphModel.textEditElement;
		var components = tool.getTools().map(function(t) {
			return k$1(t, {
				textEditElement,
				graphModel,
				lf: tool.instance
			});
		});
		tool.components = components;
		return components;
	};
	ToolOverlay.prototype.triggerToolRender = function() {
		var _a = this.props, tool = _a.tool, graphModel = _a.graphModel;
		var ToolOverlayElement = document.querySelector("#ToolOverlay_".concat(graphModel.flowId));
		var lf = tool.getInstance();
		lf.components.forEach(function(render) {
			return render(lf, ToolOverlayElement);
		});
		lf.components = [];
	};
	ToolOverlay.prototype.render = function() {
		var graphModel = this.props.graphModel;
		return u("div", {
			className: "lf-tool-overlay",
			id: "ToolOverlay_".concat(graphModel.flowId),
			children: this.getTools()
		});
	};
	ToolOverlay = __decorate$9([observer], ToolOverlay);
	return ToolOverlay;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/model/TransformModel.js
var __decorate$8 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$15 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var translateLimitsMap = {
	false: [
		-Infinity,
		-Infinity,
		Infinity,
		Infinity
	],
	true: [
		-Infinity,
		-Infinity,
		Infinity,
		Infinity
	],
	vertical: [
		-Infinity,
		0,
		Infinity,
		0
	],
	horizontal: [
		0,
		-Infinity,
		0,
		Infinity
	]
};
var TransformModel = function() {
	function TransformModel(eventCenter, options) {
		this.MINI_SCALE_SIZE = .2;
		this.MAX_SCALE_SIZE = 16;
		this.SCALE_X = 1;
		this.SKEW_Y = 0;
		this.SKEW_X = 0;
		this.SCALE_Y = 1;
		this.TRANSLATE_X = 0;
		this.TRANSLATE_Y = 0;
		this.ZOOM_SIZE = .04;
		this.translateLimitMinX = -Infinity;
		this.translateLimitMinY = -Infinity;
		this.translateLimitMaxX = Infinity;
		this.translateLimitMaxY = Infinity;
		this.eventCenter = eventCenter;
		var _a = options.stopMoveGraph, stopMoveGraph = _a === void 0 ? false : _a;
		this.updateTranslateLimits(stopMoveGraph);
	}
	TransformModel.prototype.setZoomMiniSize = function(size) {
		this.MINI_SCALE_SIZE = size;
	};
	TransformModel.prototype.setZoomMaxSize = function(size) {
		this.MAX_SCALE_SIZE = size;
	};
	/**
	* 将最外层graph上的点基于缩放转换为canvasOverlay层上的点。
	* @param point HTML点
	*/
	TransformModel.prototype.HtmlPointToCanvasPoint = function(point) {
		var _a = __read$15(point, 2), x = _a[0], y = _a[1];
		return [(x - this.TRANSLATE_X) / this.SCALE_X, (y - this.TRANSLATE_Y) / this.SCALE_Y];
	};
	/**
	* 将最外层canvasOverlay层上的点基于缩放转换为graph上的点。
	* @param point HTML点
	*/
	TransformModel.prototype.CanvasPointToHtmlPoint = function(point) {
		var _a = __read$15(point, 2), x = _a[0], y = _a[1];
		return [x * this.SCALE_X + this.TRANSLATE_X, y * this.SCALE_Y + this.TRANSLATE_Y];
	};
	/**
	* 将一个在canvas上的点，向x轴方向移动directionX距离，向y轴方向移动directionY距离。
	* 因为canvas可能被缩小或者放大了，所以其在canvas层移动的距离需要计算上缩放的量。
	* @param point 点
	* @param directionX x轴距离
	* @param directionY y轴距离
	*/
	TransformModel.prototype.moveCanvasPointByHtml = function(point, directionX, directionY) {
		var _a = __read$15(point, 2), x = _a[0], y = _a[1];
		return [x + directionX / this.SCALE_X, y + directionY / this.SCALE_Y];
	};
	/**
	* 根据缩放情况，获取缩放后的delta距离
	* @param deltaX x轴距离变化
	* @param deltaY y轴距离变化
	*/
	TransformModel.prototype.fixDeltaXY = function(deltaX, deltaY) {
		return [deltaX / this.SCALE_X, deltaY / this.SCALE_Y];
	};
	/**
	* 基于当前的缩放，获取画布渲染样式transform值
	*/
	TransformModel.prototype.getTransformStyle = function() {
		var matrixString = [
			this.SCALE_X,
			this.SKEW_Y,
			this.SKEW_X,
			this.SCALE_Y,
			this.TRANSLATE_X,
			this.TRANSLATE_Y
		].join(",");
		return { transform: "matrix(".concat(matrixString, ")") };
	};
	/**
	* 放大缩小图形
	* @param zoomSize 放大缩小的值，支持传入0-n之间的数字。小于1表示缩小，大于1表示放大。也支持传入true和false按照内置的刻度放大缩小
	* @param point 缩放的原点
	* @returns {string} -放大缩小的比例
	*/
	TransformModel.prototype.zoom = function(zoomSize, point) {
		if (zoomSize === void 0) zoomSize = false;
		var newScaleX = this.SCALE_X;
		var newScaleY = this.SCALE_Y;
		if (typeof zoomSize === "number") {
			newScaleX = zoomSize;
			newScaleY = zoomSize;
		} else if (zoomSize) {
			newScaleX += this.ZOOM_SIZE;
			newScaleY += this.ZOOM_SIZE;
		} else {
			newScaleX -= this.ZOOM_SIZE;
			newScaleY -= this.ZOOM_SIZE;
		}
		if (newScaleX < this.MINI_SCALE_SIZE || newScaleX > this.MAX_SCALE_SIZE) return "".concat(this.SCALE_X * 100, "%");
		if (point) {
			this.TRANSLATE_X -= (newScaleX - this.SCALE_X) * point[0];
			this.TRANSLATE_Y -= (newScaleY - this.SCALE_Y) * point[1];
		}
		this.SCALE_X = newScaleX;
		this.SCALE_Y = newScaleY;
		this.emitGraphTransform("zoom");
		return "".concat(this.SCALE_X * 100, "%");
	};
	TransformModel.prototype.emitGraphTransform = function(type) {
		this.eventCenter.emit(EventType.GRAPH_TRANSFORM, {
			type,
			transform: {
				SCALE_X: this.SCALE_X,
				SKEW_Y: this.SKEW_Y,
				SKEW_X: this.SKEW_X,
				SCALE_Y: this.SCALE_Y,
				TRANSLATE_X: this.TRANSLATE_X,
				TRANSLATE_Y: this.TRANSLATE_Y
			}
		});
	};
	TransformModel.prototype.resetZoom = function() {
		this.SCALE_X = 1;
		this.SCALE_Y = 1;
		this.emitGraphTransform("resetZoom");
	};
	TransformModel.prototype.translate = function(x, y) {
		if (this.TRANSLATE_X + x <= this.translateLimitMaxX && this.TRANSLATE_X + x >= this.translateLimitMinX) this.TRANSLATE_X += x;
		if (this.TRANSLATE_Y + y <= this.translateLimitMaxY && this.TRANSLATE_Y + y >= this.translateLimitMinY) this.TRANSLATE_Y += y;
		this.emitGraphTransform("translate");
	};
	/**
	* 将图形定位到画布中心
	* @param targetX 图形当前x坐标
	* @param targetY 图形当前y坐标
	* @param width 画布宽
	* @param height 画布高
	*/
	TransformModel.prototype.focusOn = function(targetX, targetY, width, height) {
		var _a = __read$15(this.CanvasPointToHtmlPoint([targetX, targetY]), 2), x = _a[0], y = _a[1];
		var _b = __read$15([width / 2 - x, height / 2 - y], 2), deltaX = _b[0], deltaY = _b[1];
		this.TRANSLATE_X += deltaX;
		this.TRANSLATE_Y += deltaY;
		this.emitGraphTransform("focusOn");
	};
	/**
	* 更新画布可以移动范围
	*/
	TransformModel.prototype.updateTranslateLimits = function(limit) {
		var _a = __read$15(Array.isArray(limit) && limit.length === 4 ? limit : translateLimitsMap[limit.toString()], 4);
		this.translateLimitMinX = _a[0], this.translateLimitMinY = _a[1], this.translateLimitMaxX = _a[2], this.translateLimitMaxY = _a[3];
	};
	__decorate$8([observable], TransformModel.prototype, "SCALE_X", void 0);
	__decorate$8([observable], TransformModel.prototype, "SKEW_Y", void 0);
	__decorate$8([observable], TransformModel.prototype, "SKEW_X", void 0);
	__decorate$8([observable], TransformModel.prototype, "SCALE_Y", void 0);
	__decorate$8([observable], TransformModel.prototype, "TRANSLATE_X", void 0);
	__decorate$8([observable], TransformModel.prototype, "TRANSLATE_Y", void 0);
	__decorate$8([observable], TransformModel.prototype, "ZOOM_SIZE", void 0);
	__decorate$8([action], TransformModel.prototype, "zoom", null);
	__decorate$8([action], TransformModel.prototype, "resetZoom", null);
	__decorate$8([action], TransformModel.prototype, "translate", null);
	__decorate$8([action], TransformModel.prototype, "focusOn", null);
	return TransformModel;
}();
//#endregion
//#region node_modules/@logicflow/core/es/model/NestedTransformModel.js
var __extends$22 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __read$14 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var NestedTransformModel = function(_super) {
	__extends$22(NestedTransformModel, _super);
	function NestedTransformModel(eventCenter, options) {
		var _this = _super.call(this, eventCenter, options) || this;
		_this.parentTransform = options.parentTransform;
		return _this;
	}
	/**
	* 设置父级变换
	* @param parentTransform 父级变换模型
	*/
	NestedTransformModel.prototype.setParentTransform = function(parentTransform) {
		this.parentTransform = parentTransform;
	};
	/**
	* 获取累积的缩放值
	* 计算包括所有父级的累积缩放
	*/
	NestedTransformModel.prototype.getCumulativeScale = function() {
		var scaleX = this.SCALE_X;
		var scaleY = this.SCALE_Y;
		if (this.parentTransform) if (this.parentTransform instanceof NestedTransformModel) {
			var parentScale = this.parentTransform.getCumulativeScale();
			scaleX *= parentScale.scaleX;
			scaleY *= parentScale.scaleY;
		} else {
			scaleX *= this.parentTransform.SCALE_X;
			scaleY *= this.parentTransform.SCALE_Y;
		}
		return {
			scaleX,
			scaleY
		};
	};
	/**
	* 获取累积的平移值
	* 计算包括所有父级的累积平移
	*/
	NestedTransformModel.prototype.getCumulativeTranslate = function() {
		var translateX = this.TRANSLATE_X;
		var translateY = this.TRANSLATE_Y;
		if (this.parentTransform && this.parentTransform instanceof NestedTransformModel) {
			var _a = this.parentTransform.getCumulativeScale(), scaleX = _a.scaleX, scaleY = _a.scaleY;
			translateX = scaleX * translateX;
			translateY = scaleY * translateY;
		}
		return {
			translateX,
			translateY
		};
	};
	/**
	* 将最外层graph上的点基于缩放转换为canvasOverlay层上的点。
	* 重写以支持嵌套变换
	* @param point HTML点
	*/
	NestedTransformModel.prototype.HtmlPointToCanvasPoint = function(point) {
		var _a = __read$14(point, 2), x = _a[0], y = _a[1];
		var _b = this.getCumulativeScale(), scaleX = _b.scaleX, scaleY = _b.scaleY;
		var _c = this.getCumulativeTranslate(), translateX = _c.translateX, translateY = _c.translateY;
		return [(x - translateX) / scaleX, (y - translateY) / scaleY];
	};
	/**
	* 将最外层canvasOverlay层上的点基于缩放转换为graph上的点。
	* 重写以支持嵌套变换
	* @param point Canvas点
	*/
	NestedTransformModel.prototype.CanvasPointToHtmlPoint = function(point) {
		var _a = __read$14(point, 2), x = _a[0], y = _a[1];
		var _b = this.getCumulativeScale(), scaleX = _b.scaleX, scaleY = _b.scaleY;
		var _c = this.getCumulativeTranslate(), translateX = _c.translateX, translateY = _c.translateY;
		return [x * scaleX + translateX, y * scaleY + translateY];
	};
	/**
	* 将一个在canvas上的点，向x轴方向移动directionX距离，向y轴方向移动directionY距离。
	* 重写以支持嵌套变换
	* @param point 点
	* @param directionX x轴距离
	* @param directionY y轴距离
	*/
	NestedTransformModel.prototype.moveCanvasPointByHtml = function(point, directionX, directionY) {
		var _a = __read$14(point, 2), x = _a[0], y = _a[1];
		var _b = this.getCumulativeScale(), scaleX = _b.scaleX, scaleY = _b.scaleY;
		return [x + directionX / scaleX, y + directionY / scaleY];
	};
	/**
	* 根据缩放情况，获取缩放后的delta距离
	* 重写以支持嵌套变换
	* @param deltaX x轴距离变化
	* @param deltaY y轴距离变化
	*/
	NestedTransformModel.prototype.fixDeltaXY = function(deltaX, deltaY) {
		var _a = this.getCumulativeScale(), scaleX = _a.scaleX, scaleY = _a.scaleY;
		return [deltaX / scaleX, deltaY / scaleY];
	};
	return NestedTransformModel;
}(TransformModel);
//#endregion
//#region node_modules/@logicflow/core/es/model/GraphModel.js
var __assign$25 = function() {
	__assign$25 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$25.apply(this, arguments);
};
var __decorate$7 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __values = function(o) {
	var s = typeof Symbol === "function" && Symbol.iterator, m = s && o[s], i = 0;
	if (m) return m.call(o);
	if (o && typeof o.length === "number") return { next: function() {
		if (o && i >= o.length) o = void 0;
		return {
			value: o && o[i++],
			done: !o
		};
	} };
	throw new TypeError(s ? "Object is not iterable." : "Symbol.iterator is not defined.");
};
var __read$13 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$4 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var GraphModel = function() {
	function GraphModel(options) {
		var _this = this;
		var _a, _b;
		this.themeMode = "default";
		this.modelMap = /* @__PURE__ */ new Map();
		this.nodeModelMap = /* @__PURE__ */ new Map();
		this.edgeModelMap = /* @__PURE__ */ new Map();
		this.elementsModelMap = /* @__PURE__ */ new Map();
		/**
		* 节点移动规则判断
		* 在节点移动的时候，会触发此数组中的所有规则判断
		*/
		this.nodeMoveRules = [];
		/**
		* 节点resize规则判断
		* 在节点resize的时候，会触发此数组中的所有规则判断
		*/
		this.nodeResizeRules = [];
		this.nodes = [];
		this.edges = [];
		/**
		* 元素重合时堆叠模式：
		* - DEFAULT（默认模式）：节点和边被选中，会被显示在最上面。当取消选中后，元素会恢复之前的层级
		* - INCREASE（递增模式）：节点和边被选中，会被显示在最上面。当取消选中后，元素会保持当前层级
		*/
		this.overlapMode = OverlapMode.DEFAULT;
		this.gridSize = 1;
		this.partial = false;
		this.waitCleanEffects = [];
		var container = options.container, partial = options.partial, _c = options.background, background = _c === void 0 ? {} : _c, grid = options.grid, idGenerator = options.idGenerator, edgeGenerator = options.edgeGenerator, animation = options.animation, customTrajectory = options.customTrajectory, customTargetAnchor = options.customTargetAnchor;
		this.themeMode = options.themeMode || "default";
		var initialGrid = gridModeMap[this.themeMode] || gridModeMap["default"];
		var initialBackground = backgroundModeMap[this.themeMode] || backgroundModeMap["default"];
		this.rootEl = container;
		this.partial = !!partial;
		this.background = background;
		if (typeof grid === "object" && options.snapGrid) this.gridSize = grid.size || 1;
		this.customStyles = options.style || {};
		this.theme = setupTheme(options.style, options.themeMode);
		this.grid = Grid.getGridOptions(assign({}, initialGrid, grid));
		this.theme.grid = cloneDeep(this.grid);
		if (background) {
			this.background = cloneDeep(assign({}, initialBackground, background));
			this.theme.background = cloneDeep(assign({}, initialBackground, background));
		}
		this.edgeType = options.edgeType || "polyline";
		this.animation = setupAnimation(animation);
		this.overlapMode = options.overlapMode || OverlapMode.DEFAULT;
		this.isMiniMap = options.isMiniMap || false;
		this.width = (_a = options.width) !== null && _a !== void 0 ? _a : this.rootEl.getBoundingClientRect().width;
		this.isContainerWidth = isNil(options.width);
		this.height = (_b = options.height) !== null && _b !== void 0 ? _b : this.rootEl.getBoundingClientRect().height;
		this.isContainerHeight = isNil(options.height);
		var resizeObserver = new ResizeObserver(debounce((function(entries) {
			var e_1, _a;
			try {
				for (var entries_1 = __values(entries), entries_1_1 = entries_1.next(); !entries_1_1.done; entries_1_1 = entries_1.next()) {
					var entry = entries_1_1.value;
					if (entry.target === _this.rootEl) {
						if (!document.body.contains(_this.rootEl)) return;
						_this.resize();
						_this.eventCenter.emit("graph:resize", {
							target: _this.rootEl,
							contentRect: entry.contentRect
						});
					}
				}
			} catch (e_1_1) {
				e_1 = { error: e_1_1 };
			} finally {
				try {
					if (entries_1_1 && !entries_1_1.done && (_a = entries_1.return)) _a.call(entries_1);
				} finally {
					if (e_1) throw e_1.error;
				}
			}
		}), 16));
		resizeObserver.observe(this.rootEl);
		this.waitCleanEffects.push(function() {
			resizeObserver.disconnect();
		});
		this.eventCenter = new EventEmitter();
		this.editConfigModel = new EditConfigModel(options);
		this.transformModel = new NestedTransformModel(this.eventCenter, options);
		this.flowId = createUuid();
		this.idGenerator = idGenerator;
		this.edgeGenerator = createEdgeGenerator(this, edgeGenerator);
		this.customTrajectory = customTrajectory;
		this.customTargetAnchor = customTargetAnchor;
	}
	Object.defineProperty(GraphModel.prototype, "nodesMap", {
		get: function() {
			return this.nodes.reduce(function(nMap, model, index) {
				nMap[model.id] = {
					index,
					model
				};
				return nMap;
			}, {});
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(GraphModel.prototype, "edgesMap", {
		get: function() {
			return this.edges.reduce(function(eMap, model, index) {
				eMap[model.id] = {
					index,
					model
				};
				return eMap;
			}, {});
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(GraphModel.prototype, "modelsMap", {
		get: function() {
			return __spreadArray$4(__spreadArray$4([], __read$13(this.nodes), false), __read$13(this.edges), false).reduce(function(eMap, model) {
				eMap[model.id] = model;
				return eMap;
			}, {});
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(GraphModel.prototype, "sortElements", {
		/**
		* 基于zIndex对元素进行排序。
		* todo: 性能优化
		*/
		get: function() {
			var sortElement = function(list) {
				return __spreadArray$4([], __read$13(list), false).sort(function(a, b) {
					return a.zIndex - b.zIndex;
				});
			};
			var elements = sortElement(__spreadArray$4(__spreadArray$4([], __read$13(this.nodes), false), __read$13(this.edges), false));
			var visibleElements = [];
			var visibleLt = [-200, -200];
			var visibleRb = [this.width + 200, this.height + 200];
			for (var i = 0; i < elements.length; i++) {
				var currentItem = elements[i];
				if (currentItem.visible && (!this.partial || currentItem.isSelected || this.isElementInArea(currentItem, visibleLt, visibleRb, false, false))) visibleElements.push(currentItem);
			}
			return visibleElements;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(GraphModel.prototype, "textEditElement", {
		/**
		* 当前编辑的元素，低频操作，先循环找。
		*/
		get: function() {
			var textEditNode = this.nodes.find(function(node) {
				return node.state === ElementState.TEXT_EDIT;
			});
			var textEditEdge = this.edges.find(function(edge) {
				return edge.state === ElementState.TEXT_EDIT;
			});
			return textEditNode || textEditEdge;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(GraphModel.prototype, "selectElements", {
		/**
		* 当前画布所有被选中的元素
		*/
		get: function() {
			var elements = /* @__PURE__ */ new Map();
			this.nodes.forEach(function(node) {
				if (node.isSelected) elements.set(node.id, node);
			});
			this.edges.forEach(function(edge) {
				if (edge.isSelected) elements.set(edge.id, edge);
			});
			return elements;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(GraphModel.prototype, "selectNodes", {
		get: function() {
			var nodes = [];
			this.nodes.forEach(function(node) {
				if (node.isSelected) nodes.push(node);
			});
			return nodes;
		},
		enumerable: false,
		configurable: true
	});
	/**
	* 获取指定区域内的所有元素
	* @param leftTopPoint 表示区域左上角的点
	* @param rightBottomPoint 表示区域右下角的点
	* @param wholeEdge 是否要整个边都在区域内部
	* @param wholeNode 是否要整个节点都在区域内部
	* @param ignoreHideElement 是否忽略隐藏的节点
	*/
	GraphModel.prototype.getAreaElement = function(leftTopPoint, rightBottomPoint, wholeEdge, wholeNode, ignoreHideElement) {
		var _this = this;
		if (wholeEdge === void 0) wholeEdge = true;
		if (wholeNode === void 0) wholeNode = true;
		if (ignoreHideElement === void 0) ignoreHideElement = false;
		var areaElements = [];
		forEach(__spreadArray$4(__spreadArray$4([], __read$13(this.nodes), false), __read$13(this.edges), false), function(element) {
			var isElementInArea = _this.isElementInArea(element, leftTopPoint, rightBottomPoint, wholeEdge, wholeNode);
			if ((!ignoreHideElement || element.visible) && isElementInArea) areaElements.push(element);
		});
		return areaElements;
	};
	/**
	* 获取指定类型元素对应的Model
	*/
	GraphModel.prototype.getModel = function(type) {
		return this.modelMap.get(type);
	};
	/**
	* 基于Id获取节点的model
	*/
	GraphModel.prototype.getNodeModelById = function(nodeId) {
		var _a;
		if (this.fakeNode && nodeId === this.fakeNode.id) return this.fakeNode;
		return (_a = this.nodesMap[nodeId]) === null || _a === void 0 ? void 0 : _a.model;
	};
	/**
	* 因为流程图所在的位置可以是页面任何地方
	* 当内部事件需要获取触发事件时，其相对于画布左上角的位置
	* 需要事件触发位置减去画布相对于client的位置
	*/
	GraphModel.prototype.getPointByClient = function(_a) {
		var x1 = _a.x, y1 = _a.y;
		var bbox = this.rootEl.getBoundingClientRect();
		var domOverlayPosition = {
			x: x1 - bbox.left,
			y: y1 - bbox.top
		};
		var _b = __read$13(this.transformModel.HtmlPointToCanvasPoint([domOverlayPosition.x, domOverlayPosition.y]), 2);
		return {
			domOverlayPosition,
			canvasOverlayPosition: {
				x: _b[0],
				y: _b[1]
			}
		};
	};
	/**
	* 判断一个元素是否在指定矩形区域内。
	* @param element 节点或者边
	* @param lt 左上角点
	* @param rb 右下角点
	* @param wholeEdge 边的起点和终点都在区域内才算
	* @param wholeNode 节点的box都在区域内才算
	*/
	GraphModel.prototype.isElementInArea = function(element, lt, rb, wholeEdge, wholeNode) {
		var _a;
		if (wholeEdge === void 0) wholeEdge = true;
		if (wholeNode === void 0) wholeNode = true;
		if (element.BaseType === ElementType.NODE) {
			element = element;
			var _b = getNodeBBox(element), minX = _b.minX, minY = _b.minY, maxX = _b.maxX, maxY = _b.maxY;
			var bboxPointsList = [
				{
					x: minX,
					y: minY
				},
				{
					x: maxX,
					y: minY
				},
				{
					x: maxX,
					y: maxY
				},
				{
					x: minX,
					y: maxY
				}
			];
			var inArea = wholeNode;
			for (var i = 0; i < bboxPointsList.length; i++) {
				var _c = bboxPointsList[i], x = _c.x, y = _c.y;
				_a = __read$13(this.transformModel.CanvasPointToHtmlPoint([x, y]), 2), x = _a[0], y = _a[1];
				if (isPointInArea([x, y], lt, rb) !== wholeNode) {
					inArea = !wholeNode;
					break;
				}
			}
			return inArea;
		}
		if (element.BaseType === ElementType.EDGE) {
			element = element;
			var startPoint = element.startPoint, endPoint = element.endPoint;
			var startHtmlPoint = this.transformModel.CanvasPointToHtmlPoint([startPoint.x, startPoint.y]);
			var endHtmlPoint = this.transformModel.CanvasPointToHtmlPoint([endPoint.x, endPoint.y]);
			var isStartInArea = isPointInArea(startHtmlPoint, lt, rb);
			var isEndInArea = isPointInArea(endHtmlPoint, lt, rb);
			return wholeEdge ? isStartInArea && isEndInArea : isStartInArea || isEndInArea;
		}
		return false;
	};
	/**
	* 使用新的数据重新设置整个画布的元素
	* 注意：将会清除画布上所有已有的节点和边
	* @param { object } graphData 图数据
	*/
	GraphModel.prototype.graphDataToModel = function(graphData) {
		var _this = this;
		if (!graphData) {
			this.clearData();
			return;
		}
		this.elementsModelMap.clear();
		this.nodeModelMap.clear();
		this.edgeModelMap.clear();
		if (graphData.nodes) this.nodes = map(graphData.nodes, function(node) {
			var nodeModel = _this.getModelAfterSnapToGrid(node);
			_this.elementsModelMap.set(nodeModel.id, nodeModel);
			_this.nodeModelMap.set(nodeModel.id, nodeModel);
			return nodeModel;
		});
		else this.nodes = [];
		if (graphData.edges) {
			var currEdgeType_1 = this.edgeType;
			this.edges = map(graphData.edges, function(edge) {
				var _a;
				var Model = _this.getModel((_a = edge.type) !== null && _a !== void 0 ? _a : currEdgeType_1);
				if (!Model) throw new Error("找不到".concat(edge.type, "对应的边。"));
				var edgeModel = new Model(edge, _this);
				_this.edgeModelMap.set(edgeModel.id, edgeModel);
				_this.elementsModelMap.set(edgeModel.id, edgeModel);
				return edgeModel;
			});
		} else this.edges = [];
	};
	/**
	* 获取画布数据
	*/
	GraphModel.prototype.modelToGraphData = function() {
		var edges = [];
		this.edges.forEach(function(edge) {
			var data = edge.getData();
			if (data && !edge.virtual) edges.push(data);
		});
		var nodes = [];
		this.nodes.forEach(function(node) {
			var data = node.getData();
			if (data && !node.virtual) nodes.push(data);
		});
		return {
			nodes,
			edges
		};
	};
	GraphModel.prototype.modelToHistoryData = function() {
		var nodeDragging = false;
		var nodes = [];
		for (var i = 0; i < this.nodes.length; i++) {
			var nodeModel = this.nodes[i];
			if (nodeModel.isDragging) {
				nodeDragging = true;
				break;
			} else nodes.push(nodeModel.getHistoryData());
		}
		if (nodeDragging) return false;
		var edgeDragging = false;
		var edges = [];
		for (var j = 0; j < this.edges.length; j++) {
			var edgeMode = this.edges[j];
			if (edgeMode.isDragging) {
				edgeDragging = true;
				break;
			}
			if (!edgeMode.virtual) edges.push(edgeMode.getHistoryData());
		}
		if (edgeDragging) return false;
		return {
			nodes,
			edges
		};
	};
	/**
	* 获取边的model
	*/
	GraphModel.prototype.getEdgeModelById = function(edgeId) {
		var _a;
		return (_a = this.edgesMap[edgeId]) === null || _a === void 0 ? void 0 : _a.model;
	};
	/**
	* 获取节点或者边的model
	*/
	GraphModel.prototype.getElement = function(id) {
		return this.modelsMap[id];
	};
	/**
	* 所有节点上所有边的model
	*/
	GraphModel.prototype.getNodeEdges = function(nodeId) {
		var edges = [];
		for (var i = 0; i < this.edges.length; i++) {
			var edgeModel = this.edges[i];
			var nodeAsSource = edgeModel.sourceNodeId === nodeId;
			var nodeAsTarget = edgeModel.targetNodeId === nodeId;
			if (nodeAsSource || nodeAsTarget) edges.push(edgeModel);
		}
		return edges;
	};
	/**
	* 获取选中的元素数据
	* @param isIgnoreCheck 是否包括sourceNode和targetNode没有被选中的边,默认包括。
	* 复制的时候不能包括此类边, 因为复制的时候不允许悬空的边
	*/
	GraphModel.prototype.getSelectElements = function(isIgnoreCheck) {
		if (isIgnoreCheck === void 0) isIgnoreCheck = true;
		var elements = this.selectElements;
		var graphData = {
			nodes: [],
			edges: []
		};
		elements.forEach(function(element) {
			if (element.BaseType === ElementType.NODE) graphData.nodes.push(element.getData());
			if (element.BaseType === ElementType.EDGE) {
				var edgeData = element.getData();
				var isNodeSelected = elements.get(edgeData.sourceNodeId) && elements.get(edgeData.targetNodeId);
				if (isIgnoreCheck || isNodeSelected) graphData.edges.push(edgeData);
			}
		});
		return graphData;
	};
	/**
	* 修改对应元素 model 中的属性
	* 注意：此方法慎用，除非您对logicflow内部有足够的了解。
	* 大多数情况下，请使用setProperties、updateText、changeNodeId等方法。
	* 例如直接使用此方法修改节点的id,那么就是会导致连接到此节点的边的sourceNodeId出现找不到的情况。
	* @param {string} id 元素id
	* @param {object} attributes 需要更新的属性
	*/
	GraphModel.prototype.updateAttributes = function(id, attributes) {
		var element = this.getElement(id);
		element === null || element === void 0 || element.updateAttributes(attributes);
	};
	/**
	* 修改节点的id， 如果不传新的id，会内部自动创建一个。
	* @param { string } nodeId 将要被修改的id
	* @param { string } newId 可选，修改后的id
	* @returns 修改后的节点id, 如果传入的oldId不存在，返回空字符串
	*/
	GraphModel.prototype.changeNodeId = function(nodeId, newId) {
		if (!newId) newId = createUuid();
		if (this.nodesMap[newId]) {
			console.warn("当前流程图已存在节点".concat(newId, ", 修改失败"));
			return "";
		}
		if (!this.nodesMap[nodeId]) {
			console.warn("当前流程图找不到节点".concat(nodeId, ", 修改失败"));
			return "";
		}
		this.edges.forEach(function(edge) {
			if (edge.sourceNodeId === nodeId) edge.sourceNodeId = newId;
			if (edge.targetNodeId === nodeId) edge.targetNodeId = newId;
		});
		this.nodesMap[nodeId].model.id = newId;
		this.nodesMap[newId] = this.nodesMap[nodeId];
		return newId;
	};
	/**
	* 修改边的id， 如果不传新的id，会内部自动创建一个。
	* @param { string } oldId 将要被修改的id
	* @param { string } newId 可选，修改后的id
	* @returns 修改后的节点id, 如果传入的oldId不存在，返回空字符串
	*/
	GraphModel.prototype.changeEdgeId = function(oldId, newId) {
		if (!newId) newId = createUuid();
		if (this.edgesMap[newId]) {
			console.warn("当前流程图已存在边: ".concat(newId, ", 修改失败"));
			return "";
		}
		if (!this.edgesMap[oldId]) {
			console.warn("当前流程图找不到边: ".concat(newId, ", 修改失败"));
			return "";
		}
		this.edges.forEach(function(edge) {
			if (edge.id === oldId) edge.changeEdgeId(newId);
		});
		return newId;
	};
	/**
	* 获取元素的文本模式
	* @param model
	*/
	GraphModel.prototype.getTextModel = function(model) {
		var _a = this.editConfigModel, textMode = _a.textMode, nodeTextMode = _a.nodeTextMode, edgeTextMode = _a.edgeTextMode;
		if (model.BaseType === ElementType.NODE) return model.textMode || nodeTextMode || textMode || TextMode.TEXT;
		if (model.BaseType === ElementType.EDGE) return model.textMode || edgeTextMode || textMode || TextMode.TEXT;
	};
	/**
	* 内部保留方法，请勿直接使用
	*/
	/**
	* 设置重叠模式
	* @param mode 重叠模式
	*/
	GraphModel.prototype.setOverlapMode = function(mode) {
		this.overlapMode = mode;
		this.eventCenter.emit("overlap:change", { overlapMode: mode });
	};
	/**
	* 更新元素的文本模式
	* @param mode
	* @param model
	*/
	GraphModel.prototype.setTextMode = function(mode, model) {
		if (model) {}
		this.editConfigModel.updateEditConfig({ textMode: mode });
	};
	/**
	* 内部保留方法，请勿直接使用
	*/
	GraphModel.prototype.setFakeNode = function(nodeModel) {
		this.fakeNode = nodeModel;
	};
	/**
	* 内部保留方法，请勿直接使用
	*/
	GraphModel.prototype.removeFakeNode = function() {
		this.fakeNode = null;
	};
	/**
	* 设置指定类型的Model,请勿直接使用
	*/
	GraphModel.prototype.setModel = function(type, ModelClass) {
		return this.modelMap.set(type, ModelClass);
	};
	/**
	* 将某个元素放置到顶部。
	* 如果堆叠模式为默认模式，则将原置顶元素重新恢复原有层级。
	* 如果堆叠模式为递增模式，则将需指定元素zIndex设置为当前最大zIndex + 1。
	* @see todo link 堆叠模式
	* @param id 元素Id
	*/
	GraphModel.prototype.toFront = function(id) {
		var _a, _b, _c;
		var element = ((_a = this.nodesMap[id]) === null || _a === void 0 ? void 0 : _a.model) || ((_b = this.edgesMap[id]) === null || _b === void 0 ? void 0 : _b.model);
		if (element) {
			if (this.overlapMode === OverlapMode.STATIC) return;
			if (this.overlapMode === OverlapMode.INCREASE) {
				this.setElementZIndex(id, "top");
				return;
			}
			(_c = this.topElement) === null || _c === void 0 || _c.setZIndex();
			element.setZIndex(ELEMENT_MAX_Z_INDEX);
			this.topElement = element;
		}
	};
	/**
	* 设置元素的zIndex.
	* 注意：默认堆叠模式下，不建议使用此方法。
	* @see todo link 堆叠模式
	* @param id 元素id
	* @param zIndex zIndex的值，可以传数字，也支持传入 'top' 和 'bottom'
	*/
	GraphModel.prototype.setElementZIndex = function(id, zIndex) {
		var _a, _b;
		var element = ((_a = this.nodesMap[id]) === null || _a === void 0 ? void 0 : _a.model) || ((_b = this.edgesMap[id]) === null || _b === void 0 ? void 0 : _b.model);
		if (element) {
			var index = void 0;
			if (typeof zIndex === "number") index = zIndex;
			else {
				if (zIndex === "top") index = getZIndex();
				if (zIndex === "bottom") index = getMinIndex();
			}
			element.setZIndex(index);
		}
	};
	/**
	* 删除节点
	* @param {string} nodeId 节点Id
	*/
	GraphModel.prototype.deleteNode = function(nodeId) {
		var nodeModel = this.nodesMap[nodeId].model;
		var nodeData = nodeModel.getData();
		this.deleteEdgeBySource(nodeId);
		this.deleteEdgeByTarget(nodeId);
		this.nodes.splice(this.nodesMap[nodeId].index, 1);
		this.eventCenter.emit(EventType.NODE_DELETE, {
			data: nodeData,
			model: nodeModel
		});
	};
	/**
	* 添加节点
	* @param nodeConfig 节点配置
	* @param eventType 新增节点事件类型，默认EventType.NODE_ADD, 在Dnd拖拽时，会传入EventType.NODE_DND_ADD
	* @param event MouseEvent 鼠标事件
	*/
	GraphModel.prototype.addNode = function(nodeConfig, eventType, event) {
		if (eventType === void 0) eventType = EventType.NODE_ADD;
		var originNodeData = formatData(nodeConfig);
		var id = originNodeData.id;
		if (id && this.nodesMap[id]) delete originNodeData.id;
		var nodeModel = this.getModelAfterSnapToGrid(originNodeData);
		this.nodes.push(nodeModel);
		var eventData = { data: nodeModel.getData() };
		if (event) eventData.e = event;
		this.eventCenter.emit(eventType, eventData);
		return nodeModel;
	};
	/**
	* 将node节点位置进行grid修正
	* 同时处理node内文字的偏移量
	* 返回一个位置修正过的复制节点NodeModel
	* @param node
	*/
	GraphModel.prototype.getModelAfterSnapToGrid = function(node) {
		var Model = this.getModel(node.type);
		var snapGrid = this.editConfigModel.snapGrid;
		if (!Model) throw new Error("找不到".concat(node.type, "对应的节点，请确认是否已注册此类型节点。"));
		var nodeX = node.x, nodeY = node.y;
		if (nodeX && nodeY) {
			node.x = snapToGrid(nodeX, this.gridSize, snapGrid);
			node.y = snapToGrid(nodeY, this.gridSize, snapGrid);
			if (typeof node.text === "object" && node.text !== null) {
				node.text.x += node.x - nodeX;
				node.text.y += node.y - nodeY;
			}
		}
		var nodeModel = new Model(node, this);
		this.nodeModelMap.set(nodeModel.id, nodeModel);
		this.elementsModelMap.set(nodeModel.id, nodeModel);
		return nodeModel;
	};
	/**
	* 克隆节点
	* @param nodeId 节点Id
	*/
	GraphModel.prototype.cloneNode = function(nodeId) {
		var targetNode = this.getNodeModelById(nodeId);
		var data = targetNode === null || targetNode === void 0 ? void 0 : targetNode.getData();
		if (data) {
			data.x += 30;
			data.y += 30;
			data.id = "";
			if (typeof data.text === "object" && data.text !== null) {
				data.text.x += 30;
				data.text.y += 30;
			}
			var nodeModel = this.addNode(data);
			nodeModel.setSelected(true);
			targetNode === null || targetNode === void 0 || targetNode.setSelected(false);
			return nodeModel.getData();
		}
	};
	/**
	* 移动节点-相对位置
	* @param nodeId 节点Id
	* @param deltaX X轴移动距离
	* @param deltaY Y轴移动距离
	* @param isIgnoreRule 是否忽略移动规则限制
	*/
	GraphModel.prototype.moveNode = function(nodeId, deltaX, deltaY, isIgnoreRule) {
		var _a;
		if (isIgnoreRule === void 0) isIgnoreRule = false;
		var node = this.nodesMap[nodeId];
		if (!node) {
			console.warn("不存在id为".concat(nodeId, "的节点"));
			return;
		}
		var nodeModel = node.model;
		_a = __read$13(nodeModel.getMoveDistance(deltaX, deltaY, isIgnoreRule), 2), deltaX = _a[0], deltaY = _a[1];
		this.moveEdge(nodeId, deltaX, deltaY);
	};
	/**
	* 移动节点-绝对位置
	* @param nodeId 节点Id
	* @param x X轴目标位置
	* @param y Y轴目标位置
	* @param isIgnoreRule 是否忽略条件，默认为 false
	*/
	GraphModel.prototype.moveNode2Coordinate = function(nodeId, x, y, isIgnoreRule) {
		if (isIgnoreRule === void 0) isIgnoreRule = false;
		var node = this.nodesMap[nodeId];
		if (!node) {
			console.warn("不存在id为".concat(nodeId, "的节点"));
			return;
		}
		var nodeModel = node.model;
		var originX = nodeModel.x, originY = nodeModel.y;
		var deltaX = x - originX;
		var deltaY = y - originY;
		this.moveNode(nodeId, deltaX, deltaY, isIgnoreRule);
	};
	/**
	* 显示节点、连线文本编辑框
	* @param id 节点 or 连线 id
	*/
	GraphModel.prototype.editText = function(id) {
		this.setElementStateById(id, ElementState.TEXT_EDIT);
	};
	/**
	* 给两个节点之间添加一条边
	* @param {object} edgeConfig
	*/
	GraphModel.prototype.addEdge = function(edgeConfig) {
		var edgeOriginData = formatData(edgeConfig);
		var type = edgeOriginData.type;
		if (!type) type = this.edgeType;
		if (edgeOriginData.id && this.edgesMap[edgeOriginData.id]) {
			delete edgeOriginData.id;
			delete edgeOriginData.sourceAnchorId;
			delete edgeOriginData.targetAnchorId;
		}
		var Model = this.getModel(type);
		if (!Model) throw new Error("找不到".concat(type, "对应的边，请确认是否已注册此类型边。"));
		var edgeModel = new Model(__assign$25(__assign$25({}, edgeOriginData), { type }), this);
		this.edgeModelMap.set(edgeModel.id, edgeModel);
		this.elementsModelMap.set(edgeModel.id, edgeModel);
		var edgeData = edgeModel.getData();
		this.edges.push(edgeModel);
		this.eventCenter.emit(EventType.EDGE_ADD, { data: edgeData });
		return edgeModel;
	};
	/**
	* 移动边，内部方法，请勿直接使用
	*/
	GraphModel.prototype.moveEdge = function(nodeId, deltaX, deltaY) {
		for (var i = 0; i < this.edges.length; i++) {
			var edgeModel = this.edges[i];
			var _a = edgeModel.textPosition, x = _a.x, y = _a.y;
			var nodeAsSource = this.edges[i].sourceNodeId === nodeId;
			var nodeAsTarget = this.edges[i].targetNodeId === nodeId;
			if (nodeAsSource) edgeModel.moveStartPoint(deltaX, deltaY);
			if (nodeAsTarget) edgeModel.moveEndPoint(deltaX, deltaY);
			if (nodeAsSource || nodeAsTarget) this.handleEdgeTextMove(edgeModel, x, y);
		}
	};
	/**
	* 如果有文案了，当节点移动引起文案位置修改时，找出当前文案位置与最新边距离最短距离的点
	* 最大程度保持节点位置不变且在边上
	* @param edgeModel 边的数据管理类
	* @param x X轴移动距离
	* @param y Y轴移动距离
	*/
	GraphModel.prototype.handleEdgeTextMove = function(edgeModel, x, y) {
		var _a;
		if (edgeModel.customTextPosition) {
			edgeModel.resetTextPosition();
			return;
		}
		if (edgeModel.modelType === ModelType.POLYLINE_EDGE && ((_a = edgeModel.text) === null || _a === void 0 ? void 0 : _a.value)) {
			var textPosition = edgeModel.text;
			var newPoint = getClosestPointOfPolyline(textPosition, edgeModel.points);
			edgeModel.moveText(newPoint.x - textPosition.x, newPoint.y - textPosition.y);
		}
		var _b = edgeModel.textPosition, x1 = _b.x, y1 = _b.y;
		edgeModel.moveText(x1 - x, y1 - y);
	};
	/**
	* 删除两节点之间的边
	* @param sourceNodeId 边的起始节点
	* @param targetNodeId 边的目的节点
	*/
	GraphModel.prototype.deleteEdgeBySourceAndTarget = function(sourceNodeId, targetNodeId) {
		for (var i = 0; i < this.edges.length; i++) if (this.edges[i].sourceNodeId === sourceNodeId && this.edges[i].targetNodeId === targetNodeId) {
			var edgeData = this.edges[i].getData();
			this.edges.splice(i, 1);
			i--;
			this.eventCenter.emit(EventType.EDGE_DELETE, { data: edgeData });
		}
	};
	/**
	* 基于边Id删除边
	*/
	GraphModel.prototype.deleteEdgeById = function(id) {
		if (!this.edgesMap[id]) return;
		var idx = this.edgesMap[id].index;
		var edgeData = this.edgesMap[id].model.getData();
		this.edges.splice(idx, 1);
		this.eventCenter.emit(EventType.EDGE_DELETE, { data: edgeData });
	};
	/**
	* 删除以节点Id为起点的所有边
	*/
	GraphModel.prototype.deleteEdgeBySource = function(sourceNodeId) {
		for (var i = 0; i < this.edges.length; i++) if (this.edges[i].sourceNodeId === sourceNodeId) {
			var edgeData = this.edges[i].getData();
			this.edges.splice(i, 1);
			i--;
			this.eventCenter.emit(EventType.EDGE_DELETE, { data: edgeData });
		}
	};
	/**
	* 删除以节点Id为终点的所有边
	*/
	GraphModel.prototype.deleteEdgeByTarget = function(targetNodeId) {
		for (var i = 0; i < this.edges.length; i++) if (this.edges[i].targetNodeId === targetNodeId) {
			var edgeData = this.edges[i].getData();
			this.edges.splice(i, 1);
			i--;
			this.eventCenter.emit(EventType.EDGE_DELETE, { data: edgeData });
		}
	};
	/**
	* 设置元素的状态，在需要保证整个画布上所有的元素只有一个元素拥有此状态时可以调用此方法。
	* 例如文本编辑、菜单显示等。
	* additionStateData: 传递的额外值，如菜单显示的时候，需要传递期望菜单显示的位置。
	*/
	GraphModel.prototype.setElementStateById = function(id, state, additionStateData) {
		this.nodes.forEach(function(node) {
			if (node.id === id) node.setElementState(state, additionStateData);
			else node.setElementState(ElementState.DEFAULT);
		});
		this.edges.forEach(function(edge) {
			if (edge.id === id) edge.setElementState(state, additionStateData);
			else edge.setElementState(ElementState.DEFAULT);
		});
	};
	/**
	* 更新节点或边的文案
	* @param id 节点或者边id
	* @param value 文案内容
	*/
	GraphModel.prototype.updateText = function(id, value) {
		var element = find(__spreadArray$4(__spreadArray$4([], __read$13(this.nodes), false), __read$13(this.edges), false), function(item) {
			return item.id === id;
		});
		element === null || element === void 0 || element.updateText(value);
	};
	/**
	* 选中节点
	* @param id 节点Id
	* @param multiple 是否为多选，如果为多选，则不去掉原有已选择节点的选中状态
	*/
	GraphModel.prototype.selectNodeById = function(id, multiple) {
		var _a;
		if (multiple === void 0) multiple = false;
		if (!multiple) this.clearSelectElements();
		var selectElement = (_a = this.nodesMap[id]) === null || _a === void 0 ? void 0 : _a.model;
		selectElement === null || selectElement === void 0 || selectElement.setSelected(true);
	};
	/**
	* 选中边
	* @param id 边Id
	* @param multiple 是否为多选，如果为多选，则不去掉原已选中边的状态
	*/
	GraphModel.prototype.selectEdgeById = function(id, multiple) {
		var _a;
		if (multiple === void 0) multiple = false;
		if (!multiple) this.clearSelectElements();
		var selectElement = (_a = this.edgesMap[id]) === null || _a === void 0 ? void 0 : _a.model;
		selectElement === null || selectElement === void 0 || selectElement.setSelected(true);
	};
	/**
	* 将图形选中
	* @param id 选择元素ID
	* @param multiple 是否允许多选，如果为true，不会将上一个选中的元素重置
	*/
	GraphModel.prototype.selectElementById = function(id, multiple) {
		if (multiple === void 0) multiple = false;
		if (!multiple) this.clearSelectElements();
		var selectElement = this.getElement(id);
		selectElement === null || selectElement === void 0 || selectElement.setSelected(true);
	};
	GraphModel.prototype.deselectElementById = function(id) {
		var element = this.getElement(id);
		if (element) element.setSelected(false);
	};
	/**
	* 将所有选中的元素设置为非选中
	*/
	GraphModel.prototype.clearSelectElements = function() {
		var _a;
		this.selectElements.forEach(function(element) {
			element === null || element === void 0 || element.setSelected(false);
			element === null || element === void 0 || element.setHovered(false);
		});
		this.selectElements.clear();
		/**
		* 如果堆叠模式为默认模式，则将置顶元素重新恢复原有层级
		*/
		if ([OverlapMode.DEFAULT, OverlapMode.EDGE_TOP].includes(this.overlapMode)) (_a = this.topElement) === null || _a === void 0 || _a.setZIndex();
	};
	/**
	* 批量移动节点，节点移动的时候，会动态计算所有节点与未移动节点的边位置
	* 移动的节点之间的边会保持相对位置
	*/
	GraphModel.prototype.moveNodes = function(nodeIds, deltaX, deltaY, isIgnoreRule) {
		var _a, _b, _c;
		var _this = this;
		if (isIgnoreRule === void 0) isIgnoreRule = false;
		var nodeIdMap = nodeIds.reduce(function(acc, cur) {
			var _a;
			var nodeModel = (_a = _this.nodesMap[cur]) === null || _a === void 0 ? void 0 : _a.model;
			if (nodeModel) acc[cur] = nodeModel.getMoveDistance(deltaX, deltaY, isIgnoreRule);
			return acc;
		}, {});
		for (var i = 0; i < this.edges.length; i++) {
			var edgeModel = this.edges[i];
			var _d = edgeModel.textPosition, x = _d.x, y = _d.y;
			var sourceMoveDistance = nodeIdMap[edgeModel.sourceNodeId];
			var targetMoveDistance = nodeIdMap[edgeModel.targetNodeId];
			var textDistanceX = void 0;
			var textDistanceY = void 0;
			if (sourceMoveDistance && targetMoveDistance && edgeModel.modelType === ModelType.POLYLINE_EDGE) {
				_a = __read$13(sourceMoveDistance, 2), textDistanceX = _a[0], textDistanceY = _a[1];
				edgeModel.updatePointsList(textDistanceX, textDistanceY);
			} else {
				if (sourceMoveDistance) {
					_b = __read$13(sourceMoveDistance, 2), textDistanceX = _b[0], textDistanceY = _b[1];
					edgeModel.moveStartPoint(textDistanceX, textDistanceY);
				}
				if (targetMoveDistance) {
					_c = __read$13(targetMoveDistance, 2), textDistanceX = _c[0], textDistanceY = _c[1];
					edgeModel.moveEndPoint(textDistanceX, textDistanceY);
				}
			}
			if (sourceMoveDistance || targetMoveDistance) this.handleEdgeTextMove(edgeModel, x, y);
		}
	};
	/**
	* 添加节点移动限制规则，在节点移动的时候触发。
	* 如果方法返回false, 则会阻止节点移动。
	* @param fn function
	* @example
	*
	* graphModel.addNodeMoveRules((nodeModel, x, y) => {
	*   if (nodeModel.properties.disabled) {
	*     return false
	*   }
	*   return true
	* })
	*
	*/
	GraphModel.prototype.addNodeMoveRules = function(fn) {
		if (!this.nodeMoveRules.includes(fn)) this.nodeMoveRules.push(fn);
	};
	GraphModel.prototype.addNodeResizeRules = function(fn) {
		if (!this.nodeResizeRules.includes(fn)) this.nodeResizeRules.push(fn);
	};
	/**
	* 设置默认的边类型
	* 也就是设置在节点直接有用户手动绘制的连线类型。
	* @param type LFOptions.EdgeType
	*/
	GraphModel.prototype.setDefaultEdgeType = function(type) {
		this.edgeType = type;
	};
	/**
	* 修改指定节点类型
	* @param id 节点id
	* @param type 节点类型
	*/
	GraphModel.prototype.changeNodeType = function(id, type) {
		var nodeModel = this.getNodeModelById(id);
		if (!nodeModel) {
			console.warn("找不到id为".concat(id, "的节点"));
			return;
		}
		var data = nodeModel.getData();
		data.type = type;
		var Model = this.getModel(type);
		if (!Model) throw new Error("找不到".concat(type, "对应的节点，请确认是否已注册此类型节点。"));
		var newNodeModel = new Model(data, this);
		this.nodes.splice(this.nodesMap[id].index, 1, newNodeModel);
		this.getNodeEdges(id).forEach(function(edge) {
			if (edge.sourceNodeId === id) {
				var point = getNodeAnchorPosition(newNodeModel, edge.startPoint, newNodeModel.width, newNodeModel.height);
				edge.updateStartPoint(point);
			}
			if (edge.targetNodeId === id) {
				var point = getNodeAnchorPosition(newNodeModel, edge.endPoint, newNodeModel.width, newNodeModel.height);
				edge.updateEndPoint(point);
			}
		});
	};
	/**
	* 切换边的类型
	* @param id 边Id
	* @param type 边类型
	*/
	GraphModel.prototype.changeEdgeType = function(id, type) {
		var edgeModel = this.getEdgeModelById(id);
		if (!edgeModel) {
			console.warn("找不到id为".concat(id, "的边"));
			return;
		}
		if (edgeModel.type === type) return;
		var data = edgeModel.getData();
		data.type = type;
		var Model = this.getModel(type);
		if (!Model) throw new Error("找不到".concat(type, "对应的节点，请确认是否已注册此类型节点。"));
		delete data.pointsList;
		var newEdgeModel = new Model(data, this);
		this.edges.splice(this.edgesMap[id].index, 1, newEdgeModel);
	};
	/**
	* 获取所有以此节点为终点的边
	*/
	GraphModel.prototype.getNodeIncomingEdge = function(nodeId) {
		var edges = [];
		this.edges.forEach(function(edge) {
			if (edge.targetNodeId === nodeId) edges.push(edge);
		});
		return edges;
	};
	/**
	* 获取所有以此节点为起点的边
	*/
	GraphModel.prototype.getNodeOutgoingEdge = function(nodeId) {
		var edges = [];
		this.edges.forEach(function(edge) {
			if (edge.sourceNodeId === nodeId) edges.push(edge);
		});
		return edges;
	};
	/**
	* 获取所有以此锚点为终点的边
	*/
	GraphModel.prototype.getAnchorIncomingEdge = function(anchorId) {
		var edges = [];
		this.edges.forEach(function(edge) {
			if (edge.targetAnchorId === anchorId) edges.push(edge);
		});
		return edges;
	};
	/**
	* 获取所有以此锚点为起点的边
	*/
	GraphModel.prototype.getAnchorOutgoingEdge = function(anchorId) {
		var edges = [];
		this.edges.forEach(function(edge) {
			if (edge.sourceAnchorId === anchorId) edges.push(edge);
		});
		return edges;
	};
	/**
	* 获取节点连接到的所有起始节点
	*/
	GraphModel.prototype.getNodeIncomingNode = function(nodeId) {
		var _this = this;
		var nodes = [];
		this.edges.forEach(function(edge) {
			var _a;
			if (edge.targetNodeId === nodeId) nodes.push((_a = _this.nodesMap[edge.sourceNodeId]) === null || _a === void 0 ? void 0 : _a.model);
		});
		return nodes;
	};
	/**
	* 获取节点所有的下一级节点
	*/
	GraphModel.prototype.getNodeOutgoingNode = function(nodeId) {
		var _this = this;
		var nodes = [];
		this.edges.forEach(function(edge) {
			if (edge.sourceNodeId === nodeId) nodes.push(_this.nodesMap[edge.targetNodeId].model);
		});
		return nodes;
	};
	/**
	* 设置主题
	* todo docs link
	*/
	GraphModel.prototype.setTheme = function(style, themeMode) {
		var _a;
		if (themeMode) {
			this.themeMode = themeMode;
			backgroundModeMap[themeMode] && this.updateBackgroundOptions(__assign$25(__assign$25({}, typeof this.background === "object" ? this.background : {}), backgroundModeMap[themeMode]));
			gridModeMap[themeMode] && this.updateGridOptions(Grid.getGridOptions(__assign$25(__assign$25({}, this.grid), gridModeMap[themeMode])));
		}
		if (style.background) this.updateBackgroundOptions(style.background);
		if (style.grid) {
			var formattedGrid = Grid.getGridOptions((_a = style.grid) !== null && _a !== void 0 ? _a : false);
			this.updateGridOptions(formattedGrid);
		}
		this.theme = updateTheme(__assign$25(__assign$25({}, this.customStyles), style), themeMode);
		this.customStyles = __assign$25(__assign$25({}, this.customStyles), style);
	};
	/**
	* 设置主题
	* todo docs link
	*/
	GraphModel.prototype.getTheme = function() {
		var _a = this, background = _a.background, grid = _a.grid;
		return __assign$25(__assign$25({}, cloneDeep(this.theme)), {
			background,
			grid
		});
	};
	/**
	* 更新网格配置
	*/
	GraphModel.prototype.updateGridOptions = function(options) {
		merge(this.grid, options);
	};
	/**
	* 更新网格尺寸
	*/
	GraphModel.prototype.updateGridSize = function(size) {
		this.gridSize = size;
	};
	/**
	* 更新背景配置
	*/
	GraphModel.prototype.updateBackgroundOptions = function(options) {
		if (isBoolean(options) || isBoolean(this.background)) this.background = options;
		else this.background = __assign$25(__assign$25({}, this.background), options);
	};
	/**
	* 重新设置画布的宽高
	*/
	GraphModel.prototype.resize = function(width, height) {
		if (!this.rootEl) return;
		if (!document.body.contains(this.rootEl)) return;
		var isVisible = this.rootEl.offsetParent !== null;
		if (!isVisible) return;
		try {
			this.width = width !== null && width !== void 0 ? width : this.rootEl.getBoundingClientRect().width;
			this.isContainerWidth = isNil(width);
			this.height = height !== null && height !== void 0 ? height : this.rootEl.getBoundingClientRect().height;
			this.isContainerHeight = isNil(height);
			if (isVisible && (!this.width || !this.height)) console.warn("渲染画布的时候无法获取画布宽高，请确认在container已挂载到DOM。@see https://github.com/didi/LogicFlow/issues/675");
		} catch (error) {
			console.warn("获取画布宽高时发生错误:", error);
		}
	};
	/**
	* 清空画布
	*/
	GraphModel.prototype.clearData = function() {
		this.nodes = [];
		this.edges = [];
		this.edgeModelMap.clear();
		this.nodeModelMap.clear();
		this.elementsModelMap.clear();
	};
	/**
	* 获取图形区域虚拟矩形的尺寸和中心坐标
	* @returns
	*/
	GraphModel.prototype.getVirtualRectSize = function() {
		var nodes = this.nodes;
		var nodesX = [];
		var nodesY = [];
		nodes.forEach(function(node) {
			var x = node.x, y = node.y, width = node.width, height = node.height;
			var _a = node.getNodeStyle().strokeWidth, strokeWidth = _a === void 0 ? 0 : _a;
			var maxX = x + width / 2 + strokeWidth;
			var minX = x - width / 2 - strokeWidth;
			var maxY = y + height / 2 + strokeWidth;
			var minY = y - height / 2 - strokeWidth;
			nodesX = nodesX.concat([maxX, minX].filter(function(num) {
				return !Number.isNaN(num);
			}));
			nodesY = nodesY.concat([maxY, minY].filter(function(num) {
				return !Number.isNaN(num);
			}));
		});
		var minX = Math.min.apply(Math, __spreadArray$4([], __read$13(nodesX), false));
		var maxX = Math.max.apply(Math, __spreadArray$4([], __read$13(nodesX), false));
		var minY = Math.min.apply(Math, __spreadArray$4([], __read$13(nodesY), false));
		var maxY = Math.max.apply(Math, __spreadArray$4([], __read$13(nodesY), false));
		var virtualRectWidth = maxX - minX || 0;
		var virtualRectHeight = maxY - minY || 0;
		return {
			width: virtualRectWidth,
			height: virtualRectHeight,
			x: minX + virtualRectWidth / 2,
			y: minY + virtualRectHeight / 2
		};
	};
	/**
	* 将图形整体移动到画布中心
	*/
	GraphModel.prototype.translateCenter = function() {
		var _a = this, nodes = _a.nodes, width = _a.width, height = _a.height, rootEl = _a.rootEl, transformModel = _a.transformModel;
		if (!nodes.length) return;
		var containerWidth = width || rootEl.clientWidth;
		var containerHeight = height || rootEl.clientHeight;
		var _b = this.getVirtualRectSize(), virtualRectCenterPositionX = _b.x, virtualRectCenterPositionY = _b.y;
		transformModel.focusOn(virtualRectCenterPositionX, virtualRectCenterPositionY, containerWidth, containerHeight);
	};
	/**
	* 画布图形适应屏幕大小
	* @param verticalOffset number 距离盒子上下的距离， 默认为20
	* @param horizontalOffset number 距离盒子左右的距离， 默认为20
	*/
	GraphModel.prototype.fitView = function(verticalOffset, horizontalOffset) {
		if (verticalOffset === void 0) verticalOffset = 20;
		if (horizontalOffset === void 0) horizontalOffset = 20;
		var _a = this, nodes = _a.nodes, width = _a.width, height = _a.height, rootEl = _a.rootEl, transformModel = _a.transformModel;
		if (!nodes.length) return;
		var containerWidth = width || rootEl.clientWidth;
		var containerHeight = height || rootEl.clientHeight;
		var _b = this.getVirtualRectSize(), virtualRectWidth = _b.width, virtualRectHeight = _b.height, virtualRectCenterPositionX = _b.x, virtualRectCenterPositionY = _b.y;
		var zoomRatioX = (virtualRectWidth + horizontalOffset) / containerWidth;
		var zoomRatioY = (virtualRectHeight + verticalOffset) / containerHeight;
		var zoomRatio = 1 / Math.max(zoomRatioX, zoomRatioY);
		var point = [containerWidth / 2, containerHeight / 2];
		transformModel.zoom(zoomRatio, point);
		transformModel.focusOn(virtualRectCenterPositionX, virtualRectCenterPositionY, containerWidth, containerHeight);
	};
	/**
	* 开启边的动画
	* @param edgeId string
	*/
	GraphModel.prototype.openEdgeAnimation = function(edgeId) {
		var edgeModel = this.getEdgeModelById(edgeId);
		edgeModel === null || edgeModel === void 0 || edgeModel.openEdgeAnimation();
	};
	/**
	* 关闭边的动画
	* @param edgeId string
	*/
	GraphModel.prototype.closeEdgeAnimation = function(edgeId) {
		var edgeModel = this.getEdgeModelById(edgeId);
		edgeModel === null || edgeModel === void 0 || edgeModel.closeEdgeAnimation();
	};
	/**
	* 获取当前局部渲染模式
	* @returns boolean
	*/
	GraphModel.prototype.getPartial = function() {
		return this.partial;
	};
	/**
	* 设置是否开启局部渲染模式
	* @param partial boolean
	* @returns
	*/
	GraphModel.prototype.setPartial = function(partial) {
		this.partial = partial;
	};
	/** 销毁当前实例 */
	GraphModel.prototype.destroy = function() {
		try {
			this.waitCleanEffects.forEach(function(fn) {
				fn();
			});
		} catch (err) {
			console.warn("error on destroy GraphModel", err);
		}
		this.waitCleanEffects.length = 0;
		this.eventCenter.destroy();
	};
	__decorate$7([observable], GraphModel.prototype, "width", void 0);
	__decorate$7([observable], GraphModel.prototype, "height", void 0);
	__decorate$7([observable], GraphModel.prototype, "theme", void 0);
	__decorate$7([observable], GraphModel.prototype, "themeMode", void 0);
	__decorate$7([observable], GraphModel.prototype, "grid", void 0);
	__decorate$7([observable], GraphModel.prototype, "edgeType", void 0);
	__decorate$7([observable], GraphModel.prototype, "nodes", void 0);
	__decorate$7([observable], GraphModel.prototype, "edges", void 0);
	__decorate$7([observable], GraphModel.prototype, "fakeNode", void 0);
	__decorate$7([observable], GraphModel.prototype, "overlapMode", void 0);
	__decorate$7([observable], GraphModel.prototype, "background", void 0);
	__decorate$7([observable], GraphModel.prototype, "gridSize", void 0);
	__decorate$7([observable], GraphModel.prototype, "transformModel", void 0);
	__decorate$7([observable], GraphModel.prototype, "editConfigModel", void 0);
	__decorate$7([observable], GraphModel.prototype, "partial", void 0);
	__decorate$7([computed], GraphModel.prototype, "nodesMap", null);
	__decorate$7([computed], GraphModel.prototype, "edgesMap", null);
	__decorate$7([computed], GraphModel.prototype, "modelsMap", null);
	__decorate$7([computed], GraphModel.prototype, "sortElements", null);
	__decorate$7([computed], GraphModel.prototype, "textEditElement", null);
	__decorate$7([computed], GraphModel.prototype, "selectElements", null);
	__decorate$7([computed], GraphModel.prototype, "selectNodes", null);
	__decorate$7([action], GraphModel.prototype, "setOverlapMode", null);
	__decorate$7([action], GraphModel.prototype, "setTextMode", null);
	__decorate$7([action], GraphModel.prototype, "setFakeNode", null);
	__decorate$7([action], GraphModel.prototype, "removeFakeNode", null);
	__decorate$7([action], GraphModel.prototype, "setModel", null);
	__decorate$7([action], GraphModel.prototype, "toFront", null);
	__decorate$7([action], GraphModel.prototype, "setElementZIndex", null);
	__decorate$7([action], GraphModel.prototype, "deleteNode", null);
	__decorate$7([action], GraphModel.prototype, "addNode", null);
	__decorate$7([action], GraphModel.prototype, "cloneNode", null);
	__decorate$7([action], GraphModel.prototype, "moveNode", null);
	__decorate$7([action], GraphModel.prototype, "moveNode2Coordinate", null);
	__decorate$7([action], GraphModel.prototype, "editText", null);
	__decorate$7([action], GraphModel.prototype, "addEdge", null);
	__decorate$7([action], GraphModel.prototype, "moveEdge", null);
	__decorate$7([action], GraphModel.prototype, "deleteEdgeBySourceAndTarget", null);
	__decorate$7([action], GraphModel.prototype, "deleteEdgeById", null);
	__decorate$7([action], GraphModel.prototype, "deleteEdgeBySource", null);
	__decorate$7([action], GraphModel.prototype, "deleteEdgeByTarget", null);
	__decorate$7([action], GraphModel.prototype, "setElementStateById", null);
	__decorate$7([action], GraphModel.prototype, "updateText", null);
	__decorate$7([action], GraphModel.prototype, "selectNodeById", null);
	__decorate$7([action], GraphModel.prototype, "selectEdgeById", null);
	__decorate$7([action], GraphModel.prototype, "selectElementById", null);
	__decorate$7([action], GraphModel.prototype, "deselectElementById", null);
	__decorate$7([action], GraphModel.prototype, "clearSelectElements", null);
	__decorate$7([action], GraphModel.prototype, "moveNodes", null);
	__decorate$7([action], GraphModel.prototype, "setDefaultEdgeType", null);
	__decorate$7([action], GraphModel.prototype, "changeNodeType", null);
	__decorate$7([action], GraphModel.prototype, "changeEdgeType", null);
	__decorate$7([action], GraphModel.prototype, "getNodeIncomingEdge", null);
	__decorate$7([action], GraphModel.prototype, "getNodeOutgoingEdge", null);
	__decorate$7([action], GraphModel.prototype, "getAnchorIncomingEdge", null);
	__decorate$7([action], GraphModel.prototype, "getAnchorOutgoingEdge", null);
	__decorate$7([action], GraphModel.prototype, "getNodeIncomingNode", null);
	__decorate$7([action], GraphModel.prototype, "getNodeOutgoingNode", null);
	__decorate$7([action], GraphModel.prototype, "setTheme", null);
	__decorate$7([action], GraphModel.prototype, "getTheme", null);
	__decorate$7([action], GraphModel.prototype, "resize", null);
	__decorate$7([action], GraphModel.prototype, "clearData", null);
	__decorate$7([action], GraphModel.prototype, "translateCenter", null);
	__decorate$7([action], GraphModel.prototype, "fitView", null);
	__decorate$7([action], GraphModel.prototype, "openEdgeAnimation", null);
	__decorate$7([action], GraphModel.prototype, "closeEdgeAnimation", null);
	__decorate$7([action], GraphModel.prototype, "setPartial", null);
	return GraphModel;
}();
//#endregion
//#region node_modules/@logicflow/core/es/model/SnaplineModel.js
var __assign$24 = function() {
	__assign$24 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$24.apply(this, arguments);
};
var __decorate$6 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var SnaplineModel = function() {
	function SnaplineModel(graphModel, epsilon) {
		if (epsilon === void 0) epsilon = 1;
		this.isShowHorizontal = false;
		this.isShowVertical = false;
		this.position = {
			x: 0,
			y: 0
		};
		this.graphModel = graphModel;
		this.epsilon = epsilon;
	}
	SnaplineModel.prototype.getStyle = function() {
		return __assign$24({}, this.graphModel.theme.snapline);
	};
	SnaplineModel.prototype.getCenterSnapLine = function(draggingNode, nodes) {
		var x = draggingNode.x, y = draggingNode.y;
		var isShowVertical = false;
		var isShowHorizontal = false;
		for (var i = 0; i < nodes.length; i++) {
			var item = nodes[i];
			if (item.id !== draggingNode.id) {
				if (equal(x, item.x, this.epsilon)) isShowVertical = true;
				if (equal(y, item.y, this.epsilon)) isShowHorizontal = true;
				if (isShowVertical && isShowHorizontal) break;
			}
		}
		return {
			isShowVertical,
			isShowHorizontal,
			position: {
				x,
				y
			}
		};
	};
	SnaplineModel.prototype.getHorizontalSnapline = function(draggingNode, nodes) {
		var isShowHorizontal = false;
		var horizontalY = 0;
		var id = draggingNode.id;
		var draggingData;
		if (id) {
			var fakeNode = this.graphModel.fakeNode;
			if (fakeNode && fakeNode.id === id) draggingData = getNodeBBox(fakeNode);
			else {
				var nodeModel = this.graphModel.getNodeModelById(id);
				if (nodeModel) draggingData = getNodeBBox(nodeModel);
			}
		}
		for (var i = 0; i < nodes.length; i++) {
			var item = nodes[i];
			if (item.id !== draggingNode.id) {
				var itemData = getNodeBBox(item);
				if (equal(itemData.minY, draggingData === null || draggingData === void 0 ? void 0 : draggingData.minY, this.epsilon) || equal(itemData.maxY, draggingData === null || draggingData === void 0 ? void 0 : draggingData.minY, this.epsilon)) {
					isShowHorizontal = true;
					horizontalY = draggingData.minY;
					break;
				}
				if (equal(itemData.minY, draggingData === null || draggingData === void 0 ? void 0 : draggingData.maxY, this.epsilon) || equal(itemData.maxY, draggingData === null || draggingData === void 0 ? void 0 : draggingData.maxY, this.epsilon)) {
					isShowHorizontal = true;
					horizontalY = draggingData.maxY;
					break;
				}
			}
		}
		return {
			isShowHorizontal,
			isShowVertical: this.isShowVertical,
			position: __assign$24(__assign$24({}, this.position), { y: horizontalY })
		};
	};
	SnaplineModel.prototype.getVerticalSnapline = function(draggingNode, nodes) {
		var isShowVertical = false;
		var verticalX = 0;
		var id = draggingNode.id;
		var draggingData;
		if (id) {
			var fakeNode = this.graphModel.fakeNode;
			if (fakeNode && fakeNode.id === id) draggingData = getNodeBBox(fakeNode);
			else {
				var nodeModel = this.graphModel.getNodeModelById(id);
				if (nodeModel) draggingData = getNodeBBox(nodeModel);
			}
		}
		for (var i = 0; i < nodes.length; i++) {
			var item = nodes[i];
			if (item.id !== draggingNode.id) {
				var itemData = getNodeBBox(item);
				if (equal(itemData.minX, draggingData === null || draggingData === void 0 ? void 0 : draggingData.minX, this.epsilon)) {
					isShowVertical = true;
					verticalX = draggingData.minX;
					break;
				}
				if (equal(itemData.minX, draggingData === null || draggingData === void 0 ? void 0 : draggingData.minX, this.epsilon) || equal(itemData.maxX, draggingData === null || draggingData === void 0 ? void 0 : draggingData.minX, this.epsilon)) {
					isShowVertical = true;
					verticalX = draggingData.minX;
					break;
				}
				if (equal(itemData.minX, draggingData === null || draggingData === void 0 ? void 0 : draggingData.maxX, this.epsilon) || equal(itemData.maxX, draggingData === null || draggingData === void 0 ? void 0 : draggingData.maxX, this.epsilon)) {
					isShowVertical = true;
					verticalX = draggingData.maxX;
					break;
				}
			}
		}
		return {
			isShowHorizontal: this.isShowHorizontal,
			isShowVertical,
			position: __assign$24(__assign$24({}, this.position), { x: verticalX })
		};
	};
	SnaplineModel.prototype.getSnapLinePosition = function(draggingNode, nodes) {
		var snaplineInfo = this.getCenterSnapLine(draggingNode, nodes);
		var isShowHorizontal = snaplineInfo.isShowHorizontal, isShowVertical = snaplineInfo.isShowVertical;
		if (!isShowHorizontal) {
			var horizontalSnapline = this.getHorizontalSnapline(draggingNode, nodes);
			if (horizontalSnapline.isShowHorizontal) {
				snaplineInfo.isShowHorizontal = horizontalSnapline.isShowHorizontal;
				snaplineInfo.position.y = horizontalSnapline.position.y;
			}
		}
		if (!isShowVertical) {
			var verticalSnapline = this.getVerticalSnapline(draggingNode, nodes);
			if (verticalSnapline.isShowVertical) {
				snaplineInfo.isShowVertical = verticalSnapline.isShowVertical;
				snaplineInfo.position.x = verticalSnapline.position.x;
			}
		}
		return snaplineInfo;
	};
	SnaplineModel.prototype.setSnaplineInfo = function(snaplineInfo) {
		var isShowHorizontal = snaplineInfo.isShowHorizontal, isShowVertical = snaplineInfo.isShowVertical, position = snaplineInfo.position;
		this.position = position;
		this.isShowHorizontal = isShowHorizontal;
		this.isShowVertical = isShowVertical;
	};
	SnaplineModel.prototype.clearSnapline = function() {
		this.position = {
			x: 0,
			y: 0
		};
		this.isShowHorizontal = false;
		this.isShowVertical = false;
	};
	SnaplineModel.prototype.setNodeSnapLine = function(nodeData) {
		var nodes = this.graphModel.nodes;
		var info = this.getSnapLinePosition(nodeData, nodes);
		this.setSnaplineInfo(info);
	};
	__decorate$6([observable], SnaplineModel.prototype, "isShowHorizontal", void 0);
	__decorate$6([observable], SnaplineModel.prototype, "isShowVertical", void 0);
	__decorate$6([observable], SnaplineModel.prototype, "position", void 0);
	__decorate$6([action], SnaplineModel.prototype, "clearSnapline", null);
	__decorate$6([action], SnaplineModel.prototype, "setNodeSnapLine", null);
	return SnaplineModel;
}();
function equal(num1, num2, epsilon) {
	if (Math.abs(num1 - num2) <= epsilon) return true;
	else return false;
}
//#endregion
//#region node_modules/@logicflow/core/es/view/Graph.js
var __extends$21 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$5 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$12 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$3 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var Graph = function(_super) {
	__extends$21(Graph, _super);
	function Graph() {
		var _this = _super.apply(this, __spreadArray$3([], __read$12(arguments), false)) || this;
		_this.handleResize = function() {
			var _a = _this.props, graphModel = _a.graphModel, options = _a.options;
			var width = graphModel.width, height = graphModel.height, isContainerWidth = graphModel.isContainerWidth, isContainerHeight = graphModel.isContainerHeight;
			var resizeWidth = width;
			var resizeHeight = height;
			var needUpdate = false;
			if (isContainerWidth) {
				resizeWidth = void 0;
				needUpdate = true;
			}
			if (isContainerHeight) {
				resizeHeight = void 0;
				needUpdate = true;
			}
			if (needUpdate) graphModel.resize(resizeWidth, resizeHeight);
			options.width = width;
			options.height = height;
		};
		_this.throttleResize = throttle(_this.handleResize, 200);
		return _this;
	}
	Graph.prototype.componentDidMount = function() {
		window.addEventListener("resize", this.throttleResize);
	};
	Graph.prototype.componentDidUpdate = function() {
		var data = this.props.graphModel.modelToGraphData();
		this.props.graphModel.eventCenter.emit(EventType.GRAPH_UPDATED, { data });
	};
	Graph.prototype.componentWillUnmount = function() {
		window.removeEventListener("resize", this.throttleResize);
	};
	Graph.prototype.getComponent = function(model, graphModel, overlay) {
		if (overlay === void 0) overlay = "canvas-overlay";
		var getView = this.props.getView;
		var View = getView(model.type);
		if (View) return u(View, {
			model,
			graphModel,
			overlay
		}, model.id);
		return null;
	};
	Graph.prototype.render = function() {
		var _this = this;
		var _a = this.props, graphModel = _a.graphModel, tool = _a.tool, options = _a.options, dnd = _a.dnd, snaplineModel = _a.snaplineModel;
		var style = {};
		if (options.width) style.width = "".concat(graphModel.width, "px");
		if (options.height) style.height = "".concat(graphModel.height, "px");
		var fakeNode = graphModel.fakeNode, editConfigModel = graphModel.editConfigModel, background = graphModel.background;
		var adjustEdge = editConfigModel.adjustEdge;
		return u("div", {
			className: "lf-graph",
			"flow-id": graphModel.flowId,
			style,
			children: [
				u(CanvasOverlay, {
					graphModel,
					dnd,
					children: [u("g", {
						className: "lf-base",
						children: map(graphModel.sortElements, function(nodeModel) {
							return _this.getComponent(nodeModel, graphModel);
						})
					}), fakeNode ? this.getComponent(fakeNode, graphModel) : ""]
				}),
				u(ModificationOverlay, {
					graphModel,
					children: [
						u(OutlineOverlay, { graphModel }),
						adjustEdge ? u(BezierAdjustOverlay, { graphModel }) : "",
						options.snapline !== false ? u(SnaplineOverlay, { snaplineModel }) : ""
					]
				}),
				u(ToolOverlay, {
					graphModel,
					tool
				}),
				background && u(BackgroundOverlay, { background }),
				u(Grid, { graphModel })
			]
		});
	};
	Graph = __decorate$5([observer], Graph);
	return Graph;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/Anchor.js
var __extends$20 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$23 = function() {
	__assign$23 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$23.apply(this, arguments);
};
var __read$11 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var Anchor = function(_super) {
	__extends$20(Anchor, _super);
	function Anchor() {
		var _this = _super.call(this) || this;
		_this.onDragStart = function(_a) {
			var event = _a.event;
			var _b = _this.props, anchorData = _b.anchorData, nodeModel = _b.nodeModel, graphModel = _b.graphModel;
			graphModel.selectNodeById(nodeModel.id);
			if (nodeModel.autoToFront) graphModel.toFront(nodeModel.id);
			graphModel.eventCenter.emit(EventType.ANCHOR_DRAGSTART, {
				data: anchorData,
				e: event,
				nodeModel
			});
			_this.setState({
				startX: anchorData.x,
				startY: anchorData.y,
				endX: anchorData.x,
				endY: anchorData.y
			});
		};
		_this.onDragging = function(_a) {
			var event = _a.event;
			var _b = _this.props, graphModel = _b.graphModel, nodeModel = _b.nodeModel, anchorData = _b.anchorData;
			var transformModel = graphModel.transformModel, eventCenter = graphModel.eventCenter, width = graphModel.width, height = graphModel.height, _c = graphModel.editConfigModel, autoExpand = _c.autoExpand, stopMoveGraph = _c.stopMoveGraph;
			if (!event) return;
			var clientX = event.clientX, clientY = event.clientY;
			var _d = graphModel.getPointByClient({
				x: clientX,
				y: clientY
			}), _e = _d.domOverlayPosition, x = _e.x, y = _e.y, _f = _d.canvasOverlayPosition, x1 = _f.x, y1 = _f.y;
			if (_this.t) cancelRaf(_this.t);
			var nearBoundary = [];
			var size = 10;
			if (x < 10) nearBoundary = [size, 0];
			else if (x + 10 > width) nearBoundary = [-size, 0];
			else if (y < 10) nearBoundary = [0, size];
			else if (y + 10 > height) nearBoundary = [0, -size];
			_this.setState({
				endX: x1,
				endY: y1,
				dragging: true
			});
			_this.moveAnchorEnd(x1, y1, event);
			if (nearBoundary.length > 0 && !stopMoveGraph && autoExpand) _this.t = createRaf(function() {
				var _a = __read$11(nearBoundary, 2), translateX = _a[0], translateY = _a[1];
				transformModel.translate(translateX, translateY);
				var _b = _this.state, endX = _b.endX, endY = _b.endY;
				_this.setState({
					endX: endX - translateX,
					endY: endY - translateY
				});
				_this.moveAnchorEnd(endX - translateX, endY - translateY, event);
			});
			eventCenter.emit(EventType.ANCHOR_DRAG, {
				data: anchorData,
				e: event,
				nodeModel
			});
		};
		_this.onDragEnd = function(_a) {
			var event = _a.event;
			if (_this.t) cancelRaf(_this.t);
			var edgeModel = _this.checkEnd(event);
			_this.setState({
				startX: 0,
				startY: 0,
				endX: 0,
				endY: 0,
				dragging: false
			});
			_this.sourceRuleResults.clear();
			_this.targetRuleResults.clear();
			var _b = _this.props, graphModel = _b.graphModel, nodeModel = _b.nodeModel, anchorData = _b.anchorData;
			if (_this.preTargetNode) {
				_this.preTargetNode.setHovered(false);
				_this.preTargetNode = void 0;
			}
			graphModel.eventCenter.emit(EventType.ANCHOR_DRAGEND, {
				data: anchorData,
				e: event,
				nodeModel,
				edgeModel: edgeModel !== null && edgeModel !== void 0 ? edgeModel : void 0
			});
		};
		_this.checkEnd = function(event) {
			var _a;
			var _b = _this.props, graphModel = _b.graphModel, nodeModel = _b.nodeModel, _c = _b.anchorData, x = _c.x, y = _c.y, id = _c.id;
			var _d = _this.state, endX = _d.endX, endY = _d.endY, dragging = _d.dragging;
			var info = targetNodeInfo({
				x: endX,
				y: endY
			}, graphModel);
			if (_this.preTargetNode && _this.preTargetNode.state !== ElementState.DEFAULT) _this.preTargetNode.setElementState(ElementState.DEFAULT);
			if (!dragging) return;
			if (info && info.node) {
				var targetNode = info.node;
				var anchorId = info.anchor.id;
				var targetInfoId = "".concat(nodeModel.id, "_").concat(targetNode.id, "_").concat(anchorId, "_").concat(id);
				var _e = _this.sourceRuleResults.get(targetInfoId) || {}, isSourcePass = _e.isAllPass, sourceMsg = _e.msg;
				var _f = _this.targetRuleResults.get(targetInfoId) || {}, isTargetPass = _f.isAllPass, targetMsg = _f.msg;
				if (isSourcePass && isTargetPass) {
					targetNode.setElementState(ElementState.DEFAULT);
					var targetNodeModel = graphModel.getNodeModelById(info.node.id);
					var edgeData = (_a = graphModel.edgeGenerator) === null || _a === void 0 ? void 0 : _a.call(graphModel, nodeModel.getData(), targetNodeModel === null || targetNodeModel === void 0 ? void 0 : targetNodeModel.getData());
					var edgeModel = graphModel.addEdge(__assign$23(__assign$23({}, edgeData), {
						sourceNodeId: nodeModel.id,
						sourceAnchorId: id,
						startPoint: {
							x,
							y
						},
						targetNodeId: info.node.id,
						targetAnchorId: info.anchor.id,
						endPoint: {
							x: info.anchor.x,
							y: info.anchor.y
						}
					}));
					var anchorData = _this.props.anchorData;
					graphModel.eventCenter.emit(EventType.ANCHOR_DROP, {
						data: anchorData,
						e: event,
						nodeModel,
						edgeModel
					});
					return edgeModel;
				}
				var nodeData = targetNode.getData();
				graphModel.eventCenter.emit(EventType.CONNECTION_NOT_ALLOWED, {
					data: nodeData,
					msg: targetMsg || sourceMsg || "不允许添加连线"
				});
				return null;
			}
		};
		_this.sourceRuleResults = /* @__PURE__ */ new Map();
		_this.targetRuleResults = /* @__PURE__ */ new Map();
		_this.state = {
			startX: 0,
			startY: 0,
			endX: 0,
			endY: 0,
			dragging: false
		};
		_this.dragHandler = new StepDrag({
			onDragStart: _this.onDragStart,
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd
		});
		return _this;
	}
	Anchor.prototype.getAnchorShape = function() {
		var _a = this.props, anchorData = _a.anchorData, style = _a.style;
		var anchorShape = _a.node.getAnchorShape(anchorData);
		if (anchorShape) return anchorShape;
		var x = anchorData.x, y = anchorData.y;
		var hoverStyle = __assign$23(__assign$23({}, style), style === null || style === void 0 ? void 0 : style.hover);
		return u("g", { children: [u(Circle, __assign$23({ className: "lf-node-anchor-hover" }, hoverStyle, {
			x,
			y
		})), u(Circle, __assign$23({ className: "lf-node-anchor" }, style, {
			x,
			y
		}))] });
	};
	Object.defineProperty(Anchor.prototype, "customTrajectory", {
		get: function() {
			return this.props.graphModel.customTrajectory;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(Anchor.prototype, "relateEdges", {
		get: function() {
			var _a = this.props, _b = _a.graphModel, getAnchorIncomingEdge = _b.getAnchorIncomingEdge, getAnchorOutgoingEdge = _b.getAnchorOutgoingEdge, id = _a.anchorData.id;
			return {
				incomingEdgeList: getAnchorIncomingEdge(id),
				outgoingEdgeList: getAnchorOutgoingEdge(id)
			};
		},
		enumerable: false,
		configurable: true
	});
	Anchor.prototype.moveAnchorEnd = function(endX, endY, event) {
		var _a = this.props, graphModel = _a.graphModel, nodeModel = _a.nodeModel, anchorData = _a.anchorData;
		var info = targetNodeInfo({
			x: endX,
			y: endY
		}, graphModel);
		if (info) {
			var targetNode = info.node;
			var anchorId = info.anchor.id;
			if (this.preTargetNode && this.preTargetNode !== info.node) this.preTargetNode.setElementState(ElementState.DEFAULT);
			if (anchorData.id === anchorId) return;
			this.preTargetNode = targetNode;
			this.validateAndSetState(targetNode, anchorId, info.anchor, nodeModel, anchorData);
			if (!targetNode.isHovered) {
				var nodeData = targetNode.getData();
				if (event) graphModel.eventCenter.emit(EventType.NODE_MOUSEENTER, {
					data: nodeData,
					e: event
				});
				targetNode.setHovered(true);
			}
		} else if (this.preTargetNode && this.preTargetNode.state !== ElementState.DEFAULT) {
			this.preTargetNode.setElementState(ElementState.DEFAULT);
			var prevData = this.preTargetNode.getData();
			if (event) graphModel.eventCenter.emit(EventType.NODE_MOUSELEAVE, {
				data: prevData,
				e: event
			});
			this.preTargetNode.setHovered(false);
			this.preTargetNode = void 0;
		}
	};
	Anchor.prototype.validateAndSetState = function(targetNode, anchorId, targetAnchor, nodeModel, anchorData) {
		var _a, _b;
		var targetInfoId = "".concat(nodeModel.id, "_").concat(targetNode.id, "_").concat(anchorId, "_").concat(anchorData.id);
		if (!this.targetRuleResults.has(targetInfoId)) {
			var sourceRuleResult = nodeModel.isAllowConnectedAsSource(targetNode, anchorData, targetAnchor);
			var targetRuleResult = targetNode.isAllowConnectedAsTarget(nodeModel, anchorData, targetAnchor);
			this.sourceRuleResults.set(targetInfoId, formatAnchorConnectValidateData(sourceRuleResult));
			this.targetRuleResults.set(targetInfoId, formatAnchorConnectValidateData(targetRuleResult));
		}
		var isSourcePass = ((_a = this.sourceRuleResults.get(targetInfoId)) !== null && _a !== void 0 ? _a : {}).isAllPass;
		var isTargetPass = ((_b = this.targetRuleResults.get(targetInfoId)) !== null && _b !== void 0 ? _b : {}).isAllPass;
		if (isSourcePass && isTargetPass) targetNode.setElementState(ElementState.ALLOW_CONNECT);
		else targetNode.setElementState(ElementState.NOT_ALLOW_CONNECT);
	};
	Anchor.prototype.isShowLine = function() {
		var _a = this.state, startX = _a.startX, startY = _a.startY, endX = _a.endX, endY = _a.endY;
		return distance(startX, startY, endX, endY) > 10;
	};
	Anchor.prototype.render = function() {
		var _this = this;
		var _a = this.state, startX = _a.startX, startY = _a.startY, endX = _a.endX, endY = _a.endY;
		var _b = this.props, anchorData = _b.anchorData, edgeStyle = _b.edgeStyle, nodeModel = _b.nodeModel, graphModel = _b.graphModel;
		var edgeAddable = anchorData.edgeAddable;
		return u("g", {
			className: "lf-anchor",
			children: [u("g", {
				onClick: function(ev) {
					ev.stopPropagation();
					graphModel.eventCenter.emit(EventType.ANCHOR_CLICK, {
						data: anchorData,
						e: ev,
						nodeModel
					});
				},
				onPointerDown: function(ev) {
					graphModel.eventCenter.emit(EventType.ANCHOR_MOUSEDOWN, {
						data: anchorData,
						e: ev,
						nodeModel
					});
					if (edgeAddable !== false) _this.dragHandler.handleMouseDown(ev);
				},
				children: this.getAnchorShape()
			}), this.isShowLine() && (this.customTrajectory ? this.customTrajectory(__assign$23({
				sourcePoint: {
					x: startX,
					y: startY
				},
				targetPoint: {
					x: endX,
					y: endY
				}
			}, edgeStyle)) : u(Line, __assign$23({
				x1: startX,
				y1: startY,
				x2: endX,
				y2: endY
			}, edgeStyle, { "pointer-events": "none" })))]
		});
	};
	return Anchor;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/Rotate.js
var __extends$19 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$22 = function() {
	__assign$22 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$22.apply(this, arguments);
};
var RotateControlPoint = function(_super) {
	__extends$19(RotateControlPoint, _super);
	function RotateControlPoint(props) {
		var _this = _super.call(this, props) || this;
		_this.style = {};
		_this.onDragging = function(_a) {
			var _b;
			var event = _a.event;
			var _c = _this.props, graphModel = _c.graphModel, nodeModel = _c.nodeModel, eventCenter = _c.eventCenter;
			var selectNodes = graphModel.selectNodes;
			var x = nodeModel.x, y = nodeModel.y;
			var clientX = event.clientX, clientY = event.clientY;
			var _d = graphModel.getPointByClient({
				x: clientX,
				y: clientY
			}).canvasOverlayPosition, cx = _d.x, cy = _d.y;
			var v = new Vector(cx - x, cy - y);
			var angle = ((_b = _this.normal) === null || _b === void 0 ? void 0 : _b.angle(v)) - _this.defaultAngle;
			nodeModel.transform = new TranslateMatrix(-x, -y).rotate(angle).translate(x, y).toString();
			nodeModel.rotate = angle;
			var nodeIds = map(selectNodes, function(node) {
				return node.id;
			});
			if (nodeIds.indexOf(nodeModel.id) === -1) nodeIds = [nodeModel.id];
			var nodeIdMap = reduce(nodeIds, function(acc, nodeId) {
				var node = graphModel.getNodeModelById(nodeId);
				acc[nodeId] = node === null || node === void 0 ? void 0 : node.getMoveDistance(0, 0, false);
				return acc;
			}, {});
			nodeIds.forEach(function(nodeId) {
				graphModel.getNodeEdges(nodeId).forEach(function(edge) {
					if (nodeIdMap[edge.sourceNodeId]) {
						var model = graphModel.getNodeModelById(edge.sourceNodeId);
						var anchor = model.anchors.find(function(item) {
							return item.id === edge.sourceAnchorId;
						});
						edge.updateStartPoint(anchor);
					}
					if (nodeIdMap[edge.targetNodeId]) {
						var model = graphModel.getNodeModelById(edge.targetNodeId);
						var anchor = model.anchors.find(function(item) {
							return item.id === edge.targetAnchorId;
						});
						edge.updateEndPoint(anchor);
					}
				});
			});
			eventCenter.emit(EventType.NODE_ROTATE, {
				e: event,
				model: nodeModel,
				data: nodeModel.getData()
			});
		};
		_this.style = props.style;
		_this.stepperDrag = new StepDrag({ onDragging: _this.onDragging });
		return _this;
	}
	RotateControlPoint.prototype.render = function() {
		var _this = this;
		var nodeModel = this.props.nodeModel;
		var x = nodeModel.x, y = nodeModel.y, width = nodeModel.width, height = nodeModel.height;
		var cx = x + width / 2 + 20;
		var cy = y - height / 2 - 20;
		this.normal = new Vector(1, 0);
		this.defaultAngle = this.normal.angle(new Vector(cx - x, cy - y));
		nodeModel.defaultAngle = this.defaultAngle;
		return u("g", {
			className: "lf-rotate-control",
			children: u("g", {
				onPointerDown: function(ev) {
					_this.stepperDrag.handleMouseDown(ev);
				},
				children: u(Circle, __assign$22({}, this.style, {
					cx,
					cy
				}))
			})
		});
	};
	return RotateControlPoint;
}(C);
//#endregion
//#region node_modules/classnames/index.js
var require_classnames = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/*!
	Copyright (c) 2018 Jed Watson.
	Licensed under the MIT License (MIT), see
	http://jedwatson.github.io/classnames
	*/
	(function() {
		"use strict";
		var hasOwn = {}.hasOwnProperty;
		function classNames() {
			var classes = "";
			for (var i = 0; i < arguments.length; i++) {
				var arg = arguments[i];
				if (arg) classes = appendClass(classes, parseValue(arg));
			}
			return classes;
		}
		function parseValue(arg) {
			if (typeof arg === "string" || typeof arg === "number") return arg;
			if (typeof arg !== "object") return "";
			if (Array.isArray(arg)) return classNames.apply(null, arg);
			if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) return arg.toString();
			var classes = "";
			for (var key in arg) if (hasOwn.call(arg, key) && arg[key]) classes = appendClass(classes, key);
			return classes;
		}
		function appendClass(value, newClass) {
			if (!newClass) return value;
			if (value) return value + " " + newClass;
			return value + newClass;
		}
		if (typeof module !== "undefined" && module.exports) {
			classNames.default = classNames;
			module.exports = classNames;
		} else if (typeof define === "function" && typeof define.amd === "object" && define.amd) define("classnames", [], function() {
			return classNames;
		});
		else window.classNames = classNames;
	})();
}));
//#endregion
//#region node_modules/@logicflow/core/es/view/text/BaseText.js
var import_classnames = /* @__PURE__ */ __toESM(require_classnames());
var __extends$18 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$21 = function() {
	__assign$21 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$21.apply(this, arguments);
};
var __read$10 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BaseText = function(_super) {
	__extends$18(BaseText, _super);
	function BaseText(props) {
		var _this = _super.call(this) || this;
		_this.mouseDownHandler = function(e) {
			var _a = _this.props, draggable = _a.draggable, model = _a.model;
			var nodeTextDraggable = _a.graphModel.editConfigModel.nodeTextDraggable;
			if (draggable !== null && draggable !== void 0 ? draggable : nodeTextDraggable) {
				e.stopPropagation();
				_this.stepperDrag.model = model;
				_this.stepperDrag.handleMouseDown(e);
			}
		};
		_this.onDragging = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			var _b = _this.props, model = _b.model, transformModel = _b.graphModel.transformModel;
			if (deltaX || deltaY) {
				var _c = __read$10(transformModel.fixDeltaXY(deltaX, deltaY), 2), curDeltaX = _c[0], curDeltaY = _c[1];
				model.moveText(curDeltaX, curDeltaY);
			}
		};
		_this.dbClickHandler = function() {
			var _a = _this.props, editable = _a.editable, eventCenter = _a.graphModel.eventCenter, model = _a.model;
			if (editable) model.setElementState(ElementState.TEXT_EDIT);
			eventCenter.emit(EventType.TEXT_DBCLICK, {
				data: model.text,
				model
			});
		};
		var draggable = props.draggable;
		_this.stepperDrag = new StepDrag({
			onDragging: _this.onDragging,
			step: 1,
			eventType: "TEXT",
			isStopPropagation: draggable
		});
		return _this;
	}
	BaseText.prototype.getShape = function() {
		var _a = this.props, model = _a.model;
		var editConfigModel = _a.graphModel.editConfigModel;
		var _b = model.text, value = _b.value, x = _b.x, y = _b.y, editable = _b.editable, draggable = _b.draggable;
		var attr = {
			x,
			y,
			className: "",
			value
		};
		var style = model.getTextStyle();
		var isDraggable = editConfigModel.nodeTextDraggable || draggable;
		return u(Text, __assign$21({}, attr, style, {
			className: (0, import_classnames.default)({
				"lf-element-text": editable,
				"lf-text-draggable": !editable && isDraggable,
				"lf-text-disabled": !editable && !isDraggable
			}),
			model
		}));
	};
	BaseText.prototype.render = function() {
		if (this.props.model.text) return u("g", {
			onPointerDown: this.mouseDownHandler,
			onDblClick: this.dbClickHandler,
			children: this.getShape()
		});
	};
	return BaseText;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/text/LineText.js
var __extends$17 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$20 = function() {
	__assign$20 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$20.apply(this, arguments);
};
var __read$9 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var LineText = function(_super) {
	__extends$17(LineText, _super);
	function LineText(props) {
		var _this = _super.call(this, props) || this;
		_this.setHoverOn = function() {
			_this.setState({ isHovered: true });
		};
		_this.setHoverOff = function() {
			_this.setState({ isHovered: false });
		};
		_this.state = { isHovered: false };
		return _this;
	}
	LineText.prototype.getBackground = function() {
		var isHovered = this.state.isHovered;
		var model = this.props.model;
		var text = model.text;
		var style = model.getTextStyle();
		var backgroundStyle = style.background || {};
		if (isHovered && style.hover && style.hover.background) backgroundStyle = __assign$20(__assign$20({}, backgroundStyle), style.hover.background);
		if ((text === null || text === void 0 ? void 0 : text.value) && (backgroundStyle === null || backgroundStyle === void 0 ? void 0 : backgroundStyle.fill) !== "transparent") {
			var fontSize = style.fontSize, textWidth = style.textWidth, lineHeight = style.lineHeight, overflowMode = style.overflowMode;
			var wrapPadding = backgroundStyle.wrapPadding;
			var rows = text === null || text === void 0 ? void 0 : text.value.split(/[\r\n]/g);
			var rowsLength = rows.length;
			var x = text.x, y = text.y;
			var rectAttr = {};
			if (overflowMode === "autoWrap" && textWidth) {
				var textHeight = getHtmlTextHeight({
					rows,
					style: {
						fontSize: "".concat(fontSize, "px"),
						width: "".concat(textWidth, "px"),
						lineHeight,
						padding: wrapPadding
					},
					rowsLength,
					className: "lf-get-text-height"
				});
				rectAttr = __assign$20(__assign$20({}, backgroundStyle), {
					x,
					y,
					width: textWidth,
					height: textHeight
				});
			} else {
				var _a = getSvgTextSize({
					rows,
					rowsLength,
					fontSize
				}), width = _a.width, height = _a.height;
				if (overflowMode === "ellipsis") {
					width = textWidth;
					height = fontSize + 2;
				}
				if (typeof backgroundStyle.wrapPadding === "string") {
					var padding = backgroundStyle.wrapPadding.split(",").filter(function(padding) {
						return padding.trim();
					}).map(function(padding) {
						return parseFloat(padding.trim());
					});
					if (padding.length > 0 && padding.length <= 4) {
						if (padding.length === 1) {
							var allSides = __read$9(padding, 1)[0];
							padding = [
								allSides,
								allSides,
								allSides,
								allSides
							];
						} else if (padding.length === 2) {
							var _c = __read$9(padding, 2), vertical = _c[0], horizontal = _c[1];
							padding = [
								vertical,
								horizontal,
								vertical,
								horizontal
							];
						} else if (padding.length === 3) {
							var _d = __read$9(padding, 3), top_1 = _d[0], horizontal = _d[1];
							padding = [
								top_1,
								horizontal,
								_d[2],
								horizontal
							];
						}
						var _e = __read$9(padding, 4), top_2 = _e[0], right = _e[1], bottom = _e[2], left = _e[3];
						width += right + left;
						height += top_2 + bottom;
						x = x + (right - left) / 2;
						y = y + (bottom - top_2) / 2;
					}
				}
				rectAttr = __assign$20(__assign$20({}, backgroundStyle), {
					x: x - 1,
					y: y - 1,
					width,
					height
				});
			}
			return u(Rect, __assign$20({}, rectAttr));
		}
		return null;
	};
	LineText.prototype.getShape = function() {
		var model = this.props.model;
		var _a = model.text, x = _a.x, y = _a.y, value = _a.value;
		if (!value) return null;
		var style = model.getTextStyle();
		var attrs = __assign$20({
			x,
			y,
			value,
			model,
			className: "lf-element-text"
		}, style);
		return u("g", {
			className: "lf-line-text",
			onMouseEnter: this.setHoverOn,
			onMouseLeave: this.setHoverOff,
			children: [this.getBackground(), u(Text, __assign$20({}, attrs))]
		});
	};
	return LineText;
}(BaseText);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/BaseNode.js
var __extends$16 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$19 = function() {
	__assign$19 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$19.apply(this, arguments);
};
var __rest$2 = function(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
};
var __read$8 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BaseNode = function(_super) {
	__extends$16(BaseNode, _super);
	function BaseNode(props) {
		var _this = _super.call(this) || this;
		_this.onDragStart = function(_a) {
			var event = _a.event;
			var _b = _this.props, model = _b.model, graphModel = _b.graphModel;
			if (event) {
				var _c = graphModel.getPointByClient({
					x: event.clientX,
					y: event.clientY
				}).canvasOverlayPosition, x = _c.x, y = _c.y;
				_this.moveOffset = {
					dx: model.x - x,
					dy: model.y - y
				};
			}
		};
		_this.onDragging = function(_a) {
			var _b, _c, _d, _e;
			var event = _a.event;
			var _f = _this.props, model = _f.model, graphModel = _f.graphModel;
			var _g = graphModel.editConfigModel, stopMoveGraph = _g.stopMoveGraph, autoExpand = _g.autoExpand, snapGrid = _g.snapGrid, transformModel = graphModel.transformModel, selectNodes = graphModel.selectNodes, width = graphModel.width, height = graphModel.height, gridSize = graphModel.gridSize;
			var _h = event, clientX = _h.clientX, clientY = _h.clientY;
			var _j = _this.mouseDownPosition, mouseDownX = _j.x, mouseDownY = _j.y;
			if (clientX - mouseDownX > gridSize || clientY - mouseDownY > gridSize) model.isDragging = true;
			var _k = graphModel.getPointByClient({
				x: clientX,
				y: clientY
			}).canvasOverlayPosition, x = _k.x, y = _k.y;
			var _l = __read$8(transformModel.CanvasPointToHtmlPoint([x, y]), 2), x1 = _l[0], y1 = _l[1];
			x = x + ((_c = (_b = _this.moveOffset) === null || _b === void 0 ? void 0 : _b.dx) !== null && _c !== void 0 ? _c : 0);
			y = y + ((_e = (_d = _this.moveOffset) === null || _d === void 0 ? void 0 : _d.dy) !== null && _e !== void 0 ? _e : 0);
			x = snapToGrid(x, gridSize, snapGrid);
			y = snapToGrid(y, gridSize, snapGrid);
			if (!width || !height) {
				graphModel.moveNode2Coordinate(model.id, x, y);
				return;
			}
			if (autoExpand && !stopMoveGraph && (x1 < 0 || y1 < 0 || x1 > width || y1 > height)) return;
			var _m = __read$8(transformModel.CanvasPointToHtmlPoint([x - model.width / 2, y - model.height / 2]), 2), leftTopX = _m[0], leftTopY = _m[1];
			var _o = __read$8(transformModel.CanvasPointToHtmlPoint([x + model.width / 2, y + model.height / 2]), 2), rightBottomX = _o[0], rightBottomY = _o[1];
			var size = Math.max(gridSize, 20);
			var nearBoundary = [];
			if (leftTopX < 0) nearBoundary = [size, 0];
			else if (rightBottomX > graphModel.width) nearBoundary = [-size, 0];
			else if (leftTopY < 0) nearBoundary = [0, size];
			else if (rightBottomY > graphModel.height) nearBoundary = [0, -size];
			if (_this.t) cancelRaf(_this.t);
			var moveNodes = selectNodes.map(function(node) {
				return node.id;
			});
			if (moveNodes.indexOf(model.id) === -1) moveNodes = [model.id];
			if (nearBoundary.length > 0 && !stopMoveGraph && autoExpand) _this.t = createRaf(function() {
				var _a = __read$8(nearBoundary, 2), translateX = _a[0], translateY = _a[1];
				transformModel.translate(translateX !== null && translateX !== void 0 ? translateX : 0, translateY !== null && translateY !== void 0 ? translateY : 0);
				var deltaX = -(translateX !== null && translateX !== void 0 ? translateX : 0) / transformModel.SCALE_X;
				var deltaY = -(translateY !== null && translateY !== void 0 ? translateY : 0) / transformModel.SCALE_X;
				graphModel.moveNodes(moveNodes, deltaX, deltaY);
			});
			else graphModel.moveNodes(moveNodes, x - model.x, y - model.y);
		};
		_this.onDragEnd = function() {
			if (_this.t) cancelRaf(_this.t);
			var model = _this.props.model;
			model.isDragging = false;
		};
		_this.onMouseOut = function(ev) {
			if (isIe()) _this.setHoverOff(ev);
		};
		_this.handleMouseUp = function() {
			_this.mouseUpDrag = _this.props.model.isDragging;
			if (_this.longPressTimer) {
				clearTimeout(_this.longPressTimer);
				_this.longPressTimer = void 0;
			}
		};
		_this.handleClick = function(e) {
			var isDragging = _this.mouseUpDrag === false;
			var curTime = (/* @__PURE__ */ new Date()).getTime();
			if (!_this.startTime) return;
			var timeInterval = curTime - _this.startTime;
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			if (!isDragging && timeInterval > 300) return;
			if (!isDragging) {
				_this.onDragEnd();
				_this.handleMouseUp();
			}
			var eventOptions = {
				data: model.getData(),
				e,
				position: graphModel.getPointByClient({
					x: e.clientX,
					y: e.clientY
				}),
				isSelected: false,
				isMultiple: false
			};
			var isRightClick = e.button === 2;
			var isDoubleClick = e.detail === 2;
			if (isRightClick) return;
			var editConfigModel = graphModel.editConfigModel;
			var isMultiple = isMultipleSelect(e, editConfigModel);
			eventOptions.isMultiple = isMultiple;
			if (model.isSelected && !isDoubleClick && isMultiple) {
				eventOptions.isSelected = false;
				model.setSelected(false);
			} else {
				graphModel.selectNodeById(model.id, isMultiple);
				eventOptions.isSelected = true;
				if (!editConfigModel.isSilentMode) _this.toFront();
			}
			if (isDoubleClick) {
				if (editConfigModel.nodeTextEdit) {
					if (model.text.editable && editConfigModel.textMode === TextMode.TEXT) {
						model.setSelected(false);
						graphModel.setElementStateById(model.id, ElementState.TEXT_EDIT);
					}
				}
				graphModel.eventCenter.emit(EventType.NODE_DBCLICK, eventOptions);
			} else {
				graphModel.eventCenter.emit(EventType.ELEMENT_CLICK, eventOptions);
				graphModel.eventCenter.emit(EventType.NODE_CLICK, eventOptions);
				var el_1 = e.currentTarget;
				(!isNil(window) && isFunction(window.requestAnimationFrame) ? window.requestAnimationFrame.bind(window) : function(fn) {
					return setTimeout(fn, 0);
				})(function() {
					el_1.focus();
				});
			}
		};
		_this.handleContextMenu = function(ev) {
			ev.preventDefault();
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			var editConfigModel = graphModel.editConfigModel;
			var nodeData = model.getData();
			var position = graphModel.getPointByClient({
				x: ev.clientX,
				y: ev.clientY
			});
			graphModel.setElementStateById(model.id, ElementState.SHOW_MENU, position.domOverlayPosition);
			if (!model.isSelected) graphModel.selectNodeById(model.id);
			graphModel.eventCenter.emit(EventType.NODE_CONTEXTMENU, {
				data: nodeData,
				e: ev,
				position
			});
			if (!editConfigModel.isSilentMode) _this.toFront();
		};
		_this.handleMouseDown = function(ev) {
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			_this.mouseDownPosition = {
				x: ev.clientX,
				y: ev.clientY
			};
			_this.startTime = (/* @__PURE__ */ new Date()).getTime();
			if (graphModel.editConfigModel.adjustNodePosition && model.draggable) _this.stepDrag && _this.stepDrag.handleMouseDown(ev);
			if (_this.longPressTimer) clearTimeout(_this.longPressTimer);
			if (ev.pointerType === "touch") _this.longPressTimer = window.setTimeout(function() {
				if (!_this.props.model.isDragging) _this.handleContextMenu(ev);
			}, 500);
		};
		_this.handleFocus = function() {
			var _a = _this.props, model = _a.model;
			_a.graphModel.eventCenter.emit(EventType.NODE_FOCUS, { data: model.getData() });
		};
		_this.handleBlur = function() {
			if (!_this.props) return;
			var _a = _this.props, model = _a.model;
			_a.graphModel.eventCenter.emit(EventType.NODE_BLUR, { data: model.getData() });
		};
		_this.setHoverOn = function(ev) {
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			if (model.isHovered) return;
			var nodeData = model.getData();
			model.setHovered(true);
			graphModel.eventCenter.emit(EventType.NODE_MOUSEENTER, {
				data: nodeData,
				e: ev
			});
		};
		_this.setHoverOff = function(ev) {
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			var nodeData = model.getData();
			if (!model.isHovered) return;
			model.setHovered(false);
			graphModel.eventCenter.emit(EventType.NODE_MOUSELEAVE, {
				data: nodeData,
				e: ev
			});
		};
		var _a = props.graphModel, gridSize = _a.gridSize, eventCenter = _a.eventCenter, model = props.model;
		_this.stepDrag = new StepDrag({
			onDragStart: _this.onDragStart,
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd,
			step: gridSize,
			eventType: "NODE",
			isStopPropagation: false,
			eventCenter,
			model
		});
		_this.modelDisposer = reaction(function() {
			return _this.props;
		}, function(newProps) {
			if (newProps && newProps.model) _this.stepDrag.setModel(newProps.model);
		});
		return _this;
	}
	BaseNode.prototype.componentWillUnmount = function() {
		if (this.modelDisposer) this.modelDisposer();
		if (this.render.$mobx) this.render.$mobx.dispose();
	};
	BaseNode.prototype.componentDidMount = function() {};
	BaseNode.prototype.componentDidUpdate = function() {};
	BaseNode.prototype.getAnchorShape = function(_anchorData) {
		return null;
	};
	BaseNode.prototype.getAnchors = function() {
		var _this = this;
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var isSelected = model.isSelected, isHitable = model.isHitable, isDragging = model.isDragging, isShowAnchor = model.isShowAnchor;
		if (isHitable && (isSelected || isShowAnchor) && !isDragging) return map(model.anchors, function(anchor, index) {
			var edgeStyle = model.getAnchorLineStyle(anchor);
			return u(Anchor, {
				anchorData: anchor,
				node: _this,
				style: model.getAnchorStyle(anchor),
				edgeStyle,
				anchorIndex: index,
				nodeModel: model,
				graphModel,
				setHoverOff: _this.setHoverOff
			});
		});
		return [];
	};
	BaseNode.prototype.getRotateControl = function() {
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var _b = graphModel.editConfigModel, isSilentMode = _b.isSilentMode, allowRotate = _b.allowRotate;
		var isSelected = model.isSelected, isHitable = model.isHitable, rotatable = model.rotatable, isHovered = model.isHovered;
		var canRotate = allowRotate && rotatable;
		var style = model.getRotateControlStyle();
		if (!isSilentMode && isHitable && (isSelected || isHovered) && canRotate) return u(RotateControlPoint, {
			graphModel,
			nodeModel: model,
			eventCenter: graphModel.eventCenter,
			style
		});
	};
	BaseNode.prototype.getResizeControl = function() {
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var _b = graphModel.editConfigModel, isSilentMode = _b.isSilentMode, allowResize = _b.allowResize;
		var isSelected = model.isSelected, isHitable = model.isHitable, resizable = model.resizable, isHovered = model.isHovered;
		var canResize = allowResize && resizable;
		var style = model.getResizeControlStyle();
		if (!isSilentMode && isHitable && (isSelected || isHovered) && canResize) return u(ResizeControlGroup, {
			style,
			model,
			graphModel
		});
		return null;
	};
	BaseNode.prototype.getText = function() {
		var _a;
		var _b = this.props, model = _b.model, graphModel = _b.graphModel;
		var editConfigModel = graphModel.editConfigModel;
		if (editConfigModel.nodeTextMode !== TextMode.TEXT) return null;
		if (model.state === ElementState.TEXT_EDIT) return null;
		if (model.text) {
			var draggable = false;
			if (editConfigModel.nodeTextDraggable && model.text.draggable) draggable = true;
			return u(BaseText, {
				editable: editConfigModel.nodeTextEdit && ((_a = model.text.editable) !== null && _a !== void 0 ? _a : true),
				model,
				graphModel,
				draggable
			});
		}
		return null;
	};
	BaseNode.prototype.getStateClassName = function() {
		var _a = this.props.model, state = _a.state, isDragging = _a.isDragging, isSelected = _a.isSelected;
		var className = "lf-node";
		switch (state) {
			case ElementState.ALLOW_CONNECT:
				className += " lf-node-allow";
				break;
			case ElementState.NOT_ALLOW_CONNECT:
				className += " lf-node-not-allow";
				break;
			default:
				className += " lf-node-default";
				break;
		}
		if (isDragging) className += " lf-dragging";
		if (isSelected) className += " lf-node-selected";
		return className;
	};
	/**
	*  @overridable 支持重写, 节点置顶，可以被某些不需要置顶的节点重写，如group节点。
	*/
	BaseNode.prototype.toFront = function() {
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		if (model.autoToFront) graphModel.toFront(model.id);
	};
	BaseNode.prototype.render = function() {
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var _b = graphModel.editConfigModel, hideAnchors = _b.hideAnchors, adjustNodePosition = _b.adjustNodePosition, allowRotate = _b.allowRotate, allowResize = _b.allowResize, gridSize = graphModel.gridSize, SCALE_X = graphModel.transformModel.SCALE_X;
		var isHitable = model.isHitable, draggable = model.draggable, transform = model.transform;
		var _c = model.getOuterGAttributes(), _d = _c.className, className = _d === void 0 ? "" : _d, restAttributes = __rest$2(_c, ["className"]);
		var nodeShapeInner = u("g", {
			className: "lf-node-content",
			children: [u("g", {
				transform,
				children: [
					this.getShape(),
					this.getText(),
					allowResize && this.getResizeControl(),
					allowRotate && this.getRotateControl()
				]
			}), !hideAnchors && this.getAnchors()]
		});
		var nodeShape;
		if (!isHitable) nodeShape = u("g", __assign$19({ className: "".concat(this.getStateClassName(), " ").concat(className) }, restAttributes, { children: nodeShapeInner }));
		else {
			if (adjustNodePosition && draggable) this.stepDrag.setStep(gridSize * SCALE_X);
			nodeShape = u("g", __assign$19({
				className: "".concat(this.getStateClassName(), " ").concat(className),
				onPointerDown: this.handleMouseDown,
				onPointerUp: this.handleMouseUp,
				onClick: this.handleClick,
				onMouseEnter: this.setHoverOn,
				onMouseOver: this.setHoverOn,
				onMouseLeave: this.setHoverOff,
				onMouseOut: this.onMouseOut,
				onContextMenu: this.handleContextMenu,
				onFocus: this.handleFocus,
				onBlur: this.handleBlur
			}, restAttributes, { children: nodeShapeInner }));
		}
		return nodeShape;
	};
	BaseNode.isObserved = false;
	return BaseNode;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/RectNode.js
var __extends$15 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$18 = function() {
	__assign$18 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$18.apply(this, arguments);
};
var RectNode = function(_super) {
	__extends$15(RectNode, _super);
	function RectNode() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	RectNode.prototype.getShape = function() {
		var model = this.props.model;
		var style = model.getNodeStyle();
		return u(Rect, __assign$18({}, style, {
			x: model.x,
			y: model.y,
			width: model.width,
			height: model.height,
			radius: model.radius
		}));
	};
	return RectNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/CircleNode.js
var __extends$14 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$17 = function() {
	__assign$17 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$17.apply(this, arguments);
};
var CircleNode = function(_super) {
	__extends$14(CircleNode, _super);
	function CircleNode() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	CircleNode.prototype.getShape = function() {
		var model = this.props.model;
		var x = model.x, y = model.y, r = model.r;
		var style = model.getNodeStyle();
		return u(Circle, __assign$17({}, style, {
			x,
			y,
			r
		}));
	};
	return CircleNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/PolygonNode.js
var __extends$13 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$16 = function() {
	__assign$16 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$16.apply(this, arguments);
};
var PolygonNode = function(_super) {
	__extends$13(PolygonNode, _super);
	function PolygonNode() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	PolygonNode.prototype.getShape = function() {
		var model = this.props.model;
		var _a = model, x = _a.x, y = _a.y, width = _a.width, height = _a.height, points = _a.points;
		var style = model.getNodeStyle();
		var attr = { transform: "matrix(1 0 0 1 ".concat(x - width / 2, " ").concat(y - height / 2, ")") };
		return u("g", __assign$16({}, attr, { children: u(Polygon, __assign$16({}, style, {
			points,
			x,
			y
		})) }));
	};
	return PolygonNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/DiamondNode.js
var __extends$12 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$15 = function() {
	__assign$15 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$15.apply(this, arguments);
};
var DiamondNode = function(_super) {
	__extends$12(DiamondNode, _super);
	function DiamondNode() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	DiamondNode.prototype.getShape = function() {
		var model = this.props.model;
		var style = model.getNodeStyle();
		return u("g", { children: u(Polygon, __assign$15({}, style, {
			points: model.points,
			x: model.x,
			y: model.y
		})) });
	};
	return DiamondNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/EllipseNode.js
var __extends$11 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$14 = function() {
	__assign$14 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$14.apply(this, arguments);
};
var EllipseNode = function(_super) {
	__extends$11(EllipseNode, _super);
	function EllipseNode() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	EllipseNode.prototype.getShape = function() {
		var model = this.props.model;
		var style = model.getNodeStyle();
		return u(Ellipse, __assign$14({}, style, {
			x: model.x,
			y: model.y,
			rx: model.rx,
			ry: model.ry
		}));
	};
	return EllipseNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/TextNode.js
var __extends$10 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$13 = function() {
	__assign$13 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$13.apply(this, arguments);
};
var TextNode = function(_super) {
	__extends$10(TextNode, _super);
	function TextNode() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	TextNode.prototype.getBackground = function() {
		var model = this.props.model;
		var style = model.getTextStyle();
		var width = model.width, height = model.height, x = model.x, y = model.y;
		var rectAttr = __assign$13(__assign$13({}, style.background), {
			x,
			y,
			width,
			height
		});
		return u(Rect, __assign$13({}, rectAttr));
	};
	TextNode.prototype.getResizeControl = function() {
		return null;
	};
	TextNode.prototype.getShape = function() {
		return u("g", { children: this.getBackground() });
	};
	return TextNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/node/HtmlNode.js
var __extends$9 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$12 = function() {
	__assign$12 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$12.apply(this, arguments);
};
var __read$7 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$2 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var HtmlNode = function(_super) {
	__extends$9(HtmlNode, _super);
	function HtmlNode() {
		var _this = _super.apply(this, __spreadArray$2([], __read$7(arguments), false)) || this;
		_this.ref = M$1();
		return _this;
	}
	Object.defineProperty(HtmlNode.prototype, "rootEl", {
		get: function() {
			return this.ref.current;
		},
		enumerable: false,
		configurable: true
	});
	/**
	* @overridable 支持重写
	* 自定义HTML节点内容
	* @param {HTMLElement} rootEl 自定义HTML节点内容可以挂载的dom节点
	* @example
	* class CustomHtmlNode extends HtmlNode {
	*   setHtml(rootEl) {
	*     const input = document.createElement('input');
	*     rootEl.appendChild(input)
	*   }
	* }
	*/
	HtmlNode.prototype.setHtml = function(rootEl) {
		rootEl.appendChild(document.createElement("div"));
	};
	HtmlNode.prototype.confirmUpdate = function(rootEl) {
		this.setHtml(rootEl);
	};
	/**
	* @overridable 支持重写
	* 和react的shouldComponentUpdate类似，都是为了避免出发不必要的render.
	* 但是这里不一样的地方在于，setHtml方法，我们只在properties发生变化了后再触发。
	* 而x,y等这些坐标相关的方法发生了变化，不会再重新触发setHtml.
	*/
	HtmlNode.prototype.shouldUpdate = function() {
		if (this.preProperties && this.preProperties === this.currentProperties) return false;
		this.preProperties = this.currentProperties;
		return true;
	};
	HtmlNode.prototype.componentDidMount = function() {
		if (this.shouldUpdate() && this.rootEl) this.setHtml(this.rootEl);
	};
	HtmlNode.prototype.componentDidUpdate = function() {
		if (this.shouldUpdate() && this.rootEl) this.confirmUpdate(this.rootEl);
	};
	HtmlNode.prototype.componentWillUnmount = function() {
		_super.prototype.componentWillUnmount.call(this);
		this.rootEl.innerHTML = "";
	};
	HtmlNode.prototype.getShape = function() {
		var model = this.props.model;
		var x = model.x, y = model.y, height = model.height, width = model.width;
		var style = model.getNodeStyle();
		this.currentProperties = JSON.stringify(model.properties);
		return u("g", { children: [
			style.shadow && u("defs", { children: u("filter", {
				id: "shadow",
				x: "-50%",
				y: "-50%",
				width: "200%",
				height: "200%",
				children: u("feDropShadow", __assign$12({}, style.shadow))
			}) }),
			u("rect", __assign$12({
				x: x - width / 2,
				y: y - height / 2,
				width,
				height,
				rx: style.radius,
				ry: style.radius
			}, style, { filter: "url(#shadow)" })),
			u("foreignObject", __assign$12({}, style, {
				x: x - width / 2,
				y: y - height / 2,
				width,
				height,
				ref: this.ref
			}))
		] });
	};
	return HtmlNode;
}(BaseNode);
//#endregion
//#region node_modules/@logicflow/core/es/view/edge/AdjustPoint.js
var __extends$8 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$11 = function() {
	__assign$11 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$11.apply(this, arguments);
};
var __rest$1 = function(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
};
var __read$6 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var AdjustType;
(function(AdjustType) {
	AdjustType["SOURCE"] = "SOURCE";
	AdjustType["TARGET"] = "TARGET";
})(AdjustType || (AdjustType = {}));
var AdjustPoint = function(_super) {
	__extends$8(AdjustPoint, _super);
	function AdjustPoint(props) {
		var _this = _super.call(this) || this;
		_this.handleMouseDown = function(ev) {
			if (_this.stepDrag) _this.stepDrag.handleMouseDown(ev);
		};
		_this.onDragStart = function() {
			var _a = _this.props, x = _a.x, y = _a.y, edgeModel = _a.edgeModel;
			_this.oldEdge = {
				startPoint: edgeModel.startPoint,
				endPoint: edgeModel.endPoint,
				pointsList: edgeModel.pointsList
			};
			_this.setState({
				endX: x,
				endY: y,
				dragging: true
			});
		};
		_this.onDragging = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			var _b = _this.state, endX = _b.endX, endY = _b.endY;
			var _c = _this.props, graphModel = _c.graphModel, type = _c.type;
			var transformModel = graphModel.transformModel, editConfigModel = graphModel.editConfigModel;
			var _d = __read$6(transformModel.moveCanvasPointByHtml([endX, endY], deltaX, deltaY), 2), x = _d[0], y = _d[1];
			_this.setState({
				endX: x,
				endY: y,
				dragging: true
			});
			var edgeModel = _this.props.edgeModel;
			var info = targetNodeInfo({
				x: endX,
				y: endY
			}, graphModel);
			if (info && info.node && _this.isAllowAdjust(info).pass) {
				var startPoint = edgeModel.startPoint, endPoint = edgeModel.endPoint, sourceNode = edgeModel.sourceNode, targetNode = edgeModel.targetNode;
				var params = type === AdjustType.SOURCE ? {
					startPoint: {
						x: info.anchor.x,
						y: info.anchor.y
					},
					endPoint: {
						x: endPoint.x,
						y: endPoint.y
					},
					sourceNode: info.node,
					targetNode
				} : {
					startPoint: {
						x: startPoint.x,
						y: startPoint.y
					},
					endPoint: {
						x: info.anchor.x,
						y: info.anchor.y
					},
					sourceNode,
					targetNode: info.node
				};
				edgeModel.updateAfterAdjustStartAndEnd(params);
			} else type === AdjustType.SOURCE ? edgeModel.updateStartPoint({
				x,
				y
			}) : edgeModel.updateEndPoint({
				x,
				y
			});
			if (edgeModel.text.value && editConfigModel.adjustEdge) edgeModel.setText(Object.assign({}, edgeModel.text, edgeModel.textPosition));
		};
		_this.onDragEnd = function(_a) {
			var _b, _c, _d;
			var event = _a.event;
			try {
				_this.setState({ dragging: false });
				var _e = _this.props, graphModel = _e.graphModel, edgeModel = _e.edgeModel, type = _e.type;
				var _f = _this.state, endX = _f.endX, endY = _f.endY, dragging = _f.dragging;
				var info = targetNodeInfo({
					x: endX,
					y: endY
				}, graphModel);
				if (!dragging) return;
				var needRecoveryEdge = false;
				var createEdgeInfo = void 0;
				if (info && info.node) {
					var _g = _this.isAllowAdjust(info), pass = _g.pass, msg = _g.msg, newTargetNode = _g.newTargetNode;
					if (pass) {
						var _h = edgeModel.getData(), text = _h.text, _j = _h.sourceAnchorId, sourceAnchorId = _j === void 0 ? "" : _j, _k = _h.targetAnchorId, targetAnchorId = _k === void 0 ? "" : _k, rest = __rest$1(_h, [
							"text",
							"sourceAnchorId",
							"targetAnchorId"
						]);
						createEdgeInfo = __assign$11(__assign$11({
							sourceAnchorId,
							targetAnchorId
						}, rest), { text: (text === null || text === void 0 ? void 0 : text.value) || "" });
						if (type === AdjustType.SOURCE) {
							var sourceNode = graphModel.getNodeModelById(info.node.id);
							var targetNode = graphModel.getNodeModelById(edgeModel.targetNodeId);
							var edgeInfo = (_b = graphModel.edgeGenerator) === null || _b === void 0 ? void 0 : _b.call(graphModel, sourceNode === null || sourceNode === void 0 ? void 0 : sourceNode.getData(), targetNode === null || targetNode === void 0 ? void 0 : targetNode.getData(), createEdgeInfo);
							createEdgeInfo = __assign$11(__assign$11({}, edgeInfo), {
								sourceNodeId: info.node.id,
								sourceAnchorId: info.anchor.id,
								startPoint: {
									x: info.anchor.x,
									y: info.anchor.y
								},
								targetNodeId: edgeModel.targetNodeId,
								endPoint: __assign$11({}, edgeModel.endPoint)
							});
							if (edgeModel.sourceNodeId === info.node.id && edgeModel.sourceAnchorId === info.anchor.id) needRecoveryEdge = true;
						} else if (type === AdjustType.TARGET) {
							var sourceNode = graphModel.getNodeModelById(edgeModel.sourceNodeId);
							var targetNode = graphModel.getNodeModelById(info.node.id);
							var edgeInfo = (_c = graphModel.edgeGenerator) === null || _c === void 0 ? void 0 : _c.call(graphModel, sourceNode === null || sourceNode === void 0 ? void 0 : sourceNode.getData(), targetNode === null || targetNode === void 0 ? void 0 : targetNode.getData(), createEdgeInfo);
							createEdgeInfo = __assign$11(__assign$11({}, edgeInfo), {
								sourceNodeId: edgeModel.sourceNodeId,
								startPoint: __assign$11({}, edgeModel.startPoint),
								targetNodeId: info.node.id,
								targetAnchorId: info.anchor.id,
								endPoint: {
									x: info.anchor.x,
									y: info.anchor.y
								}
							});
							if (edgeModel.targetNodeId === info.node.id && edgeModel.targetAnchorId === info.anchor.id) needRecoveryEdge = true;
						}
					} else {
						needRecoveryEdge = true;
						var nodeData = newTargetNode.getData();
						graphModel.eventCenter.emit(EventType.CONNECTION_NOT_ALLOWED, {
							data: nodeData,
							msg
						});
					}
				} else needRecoveryEdge = true;
				if (!needRecoveryEdge) {
					var oldEdgeData = edgeModel.getData();
					graphModel.deleteEdgeById(edgeModel.id);
					var edge = graphModel.addEdge(__assign$11({}, createEdgeInfo));
					graphModel.eventCenter.emit(EventType.EDGE_EXCHANGE_NODE, { data: {
						newEdge: edge.getData(),
						oldEdge: oldEdgeData
					} });
				} else _this.recoveryEdge();
				(_d = _this.preTargetNode) === null || _d === void 0 || _d.setElementState(ElementState.DEFAULT);
			} finally {
				var graphModel = _this.props.graphModel;
				graphModel.eventCenter.emit(EventType.ADJUST_POINT_DRAGEND, {
					e: event,
					data: _this.stepDragData
				});
			}
		};
		_this.recoveryEdge = function() {
			var edgeModel = _this.props.edgeModel;
			var _a = _this.oldEdge, startPoint = _a.startPoint, endPoint = _a.endPoint, pointsList = _a.pointsList;
			edgeModel.updateStartPoint(startPoint);
			edgeModel.updateEndPoint(endPoint);
			if (edgeModel.modelType !== ModelType.LINE_EDGE) {
				edgeModel.pointsList = pointsList !== null && pointsList !== void 0 ? pointsList : [];
				edgeModel.initPoints();
			}
		};
		_this.getAdjustPointStyle = function() {
			return _this.props.graphModel.theme.edgeAdjust;
		};
		_this.state = {
			dragging: false,
			endX: 0,
			endY: 0
		};
		_this.targetRuleResults = /* @__PURE__ */ new Map();
		_this.sourceRuleResults = /* @__PURE__ */ new Map();
		var type = props.type, edgeModel = props.edgeModel;
		var eventCenter = props.graphModel.eventCenter;
		_this.stepDragData = {
			type,
			edgeData: edgeModel.getData()
		};
		_this.stepDrag = new StepDrag({
			onDragStart: _this.onDragStart,
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd,
			eventType: "ADJUST_POINT",
			isStopPropagation: false,
			eventCenter,
			data: _this.stepDragData
		});
		return _this;
	}
	AdjustPoint.prototype.isAllowAdjust = function(info) {
		var _a = this.props, _b = _a.edgeModel, id = _b.id, sourceNode = _b.sourceNode, targetNode = _b.targetNode, sourceAnchorId = _b.sourceAnchorId, targetAnchorId = _b.targetAnchorId, type = _a.type;
		var newSourceNode;
		var newTargetNode;
		var newSourceAnchor;
		var newTargetAnchor;
		if (type === AdjustType.SOURCE) {
			newSourceNode = info.node;
			newTargetNode = targetNode;
			newSourceAnchor = info.anchor;
			newTargetAnchor = targetNode.getAnchorInfo(targetAnchorId);
		} else {
			newSourceNode = sourceNode;
			newTargetNode = info.node;
			newTargetAnchor = info.anchor;
			newSourceAnchor = sourceNode.getAnchorInfo(sourceAnchorId);
		}
		if (this.preTargetNode && this.preTargetNode !== info.node) this.preTargetNode.setElementState(ElementState.DEFAULT);
		this.preTargetNode = info.node;
		if (newTargetAnchor.id === newSourceAnchor.id) return {
			pass: false,
			msg: "",
			newTargetNode
		};
		var targetInfoId = "".concat(newSourceNode.id, "_").concat(newTargetNode.id, "_").concat(newSourceAnchor.id, "_").concat(newTargetAnchor.id);
		if (!this.targetRuleResults.has(targetInfoId)) {
			var sourceRuleResult = newSourceNode.isAllowConnectedAsSource(newTargetNode, newSourceAnchor, newTargetAnchor, id);
			var targetRuleResult = newTargetNode.isAllowConnectedAsTarget(newSourceNode, newSourceAnchor, newTargetAnchor, id);
			this.sourceRuleResults.set(targetInfoId, formatAnchorConnectValidateData(sourceRuleResult));
			this.targetRuleResults.set(targetInfoId, formatAnchorConnectValidateData(targetRuleResult));
		}
		var _c = this.sourceRuleResults.get(targetInfoId), isSourcePass = _c.isAllPass, sourceMsg = _c.msg;
		var _d = this.targetRuleResults.get(targetInfoId), isTargetPass = _d.isAllPass, targetMsg = _d.msg;
		var state = isSourcePass && isTargetPass ? ElementState.ALLOW_CONNECT : ElementState.NOT_ALLOW_CONNECT;
		if (type === AdjustType.SOURCE) newSourceNode.setElementState(state);
		else newTargetNode.setElementState(state);
		return {
			pass: isSourcePass && isTargetPass,
			msg: targetMsg || sourceMsg,
			newTargetNode
		};
	};
	AdjustPoint.prototype.render = function() {
		var _a = this.props, x = _a.x, y = _a.y, getAdjustPointShape = _a.getAdjustPointShape, edgeModel = _a.edgeModel;
		var dragging = this.state.dragging;
		return u("g", {
			pointerEvents: dragging ? "none" : "",
			onPointerDown: this.handleMouseDown,
			children: !dragging ? getAdjustPointShape(x, y, edgeModel) : ""
		});
	};
	return AdjustPoint;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/edge/Arrow.js
var __extends$7 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$10 = function() {
	__assign$10 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$10.apply(this, arguments);
};
var Arrow = function(_super) {
	__extends$7(Arrow, _super);
	function Arrow() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	Arrow.prototype.getArrowAttributes = function() {
		var _a = this.props, arrowInfo = _a.arrowInfo, style = _a.style;
		var start = arrowInfo.start, end = arrowInfo.end;
		var _b = getVerticalPointOfLine({
			start,
			end,
			offset: style.offset,
			verticalLength: style.verticalLength,
			type: "end"
		}), leftX = _b.leftX, leftY = _b.leftY, rightX = _b.rightX, rightY = _b.rightY;
		return __assign$10({ d: "M".concat(leftX, " ").concat(leftY, " L").concat(end.x, " ").concat(end.y, " L").concat(rightX, " ").concat(rightY, " z") }, style);
	};
	Arrow.prototype.getShape = function() {
		var _a = this.getArrowAttributes(), d = _a.d, strokeWidth = _a.strokeWidth, stroke = _a.stroke, fill = _a.fill;
		return u(Path, {
			d,
			fill,
			strokeWidth,
			stroke
		});
	};
	Arrow.prototype.render = function() {
		return u("g", {
			className: "lf-arrow",
			children: this.getShape()
		});
	};
	return Arrow;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/edge/BaseEdge.js
var __extends$6 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$9 = function() {
	__assign$9 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$9.apply(this, arguments);
};
var __read$5 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BaseEdge = function(_super) {
	__extends$6(BaseEdge, _super);
	function BaseEdge() {
		var _this = _super.call(this) || this;
		_this.textRef = M$1();
		/**
		* 不支持重写，如果想要基于hover状态设置不同的样式，请在model中使用isHovered属性。
		*/
		_this.handleHover = function(hovered, ev) {
			var _a = _this.props, model = _a.model, eventCenter = _a.graphModel.eventCenter;
			model.setHovered(hovered);
			var eventName = hovered ? EventType.EDGE_MOUSEENTER : EventType.EDGE_MOUSELEAVE;
			var nodeData = model.getData();
			eventCenter.emit(eventName, {
				data: nodeData,
				e: ev
			});
		};
		/**
		* 不支持重写，如果想要基于hover状态设置不同的样式，请在model中使用isHovered属性。
		*/
		_this.setHoverOn = function(ev) {
			if (_this.props.model.isHovered) return;
			_this.textRef && _this.textRef.current && _this.textRef.current.setHoverOn();
			_this.handleHover(true, ev);
		};
		/**
		* 不支持重写，如果想要基于hover状态设置不同的样式，请在model中使用isHovered属性。
		*/
		_this.setHoverOff = function(ev) {
			if (!_this.props.model.isHovered) return;
			_this.textRef && _this.textRef.current && _this.textRef.current.setHoverOff();
			_this.handleHover(false, ev);
		};
		/**
		* 不支持重写，如果想要基于contextmenu事件做处理，请监听edge:contextmenu事件。
		*/
		_this.handleContextMenu = function(ev) {
			ev.preventDefault();
			_this.contextMenuTime = (/* @__PURE__ */ new Date()).getTime();
			if (_this.clickTimer) clearTimeout(_this.clickTimer);
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			var editConfigModel = graphModel.editConfigModel;
			var position = graphModel.getPointByClient({
				x: ev.clientX,
				y: ev.clientY
			});
			graphModel.setElementStateById(model.id, ElementState.SHOW_MENU, position.domOverlayPosition);
			if (!editConfigModel.isSilentMode) _this.toFront();
			if (!model.isSelected) graphModel.selectEdgeById(model.id);
			var edgeData = model === null || model === void 0 ? void 0 : model.getData();
			graphModel.eventCenter.emit(EventType.EDGE_CONTEXTMENU, {
				data: edgeData,
				e: ev,
				position
			});
		};
		/**
		* 不支持重写
		*/
		_this.handleMouseDown = function(e) {
			e.stopPropagation();
			_this.startTime = (/* @__PURE__ */ new Date()).getTime();
			if (_this.longPressTimer) clearTimeout(_this.longPressTimer);
			if (e.pointerType === "touch") _this.longPressTimer = window.setTimeout(function() {
				if (!_this.props.model.isDragging) _this.handleContextMenu(e);
			}, 500);
		};
		_this.handleMouseUp = function() {
			_this.mouseUpDrag = _this.props.model.isDragging;
			if (_this.longPressTimer) {
				clearTimeout(_this.longPressTimer);
				_this.longPressTimer = void 0;
			}
		};
		/**
		* 不支持重写
		*/
		_this.handleClick = function(e) {
			if (!_this.startTime) return;
			if (_this.mouseUpDrag) return;
			if (e.button === 2) return;
			var isDoubleClick = e.detail === 2;
			var _a = _this.props, model = _a.model, graphModel = _a.graphModel;
			var edgeData = model === null || model === void 0 ? void 0 : model.getData();
			var position = graphModel.getPointByClient({
				x: e.clientX,
				y: e.clientY
			});
			if (isDoubleClick) {
				var editConfigModel_1 = graphModel.editConfigModel, textEditElement = graphModel.textEditElement;
				var id = model.id, text = model.text, modelType = model.modelType;
				if (textEditElement && textEditElement.id === id) graphModel.setElementStateById(id, ElementState.DEFAULT);
				if (editConfigModel_1.edgeTextEdit && text.editable) {
					model.setSelected(false);
					graphModel.setElementStateById(id, ElementState.TEXT_EDIT);
				}
				if (modelType === ModelType.POLYLINE_EDGE) {
					var polylineEdgeModel = model;
					var _b = graphModel.getPointByClient({
						x: e.x,
						y: e.y
					}).canvasOverlayPosition, x = _b.x, y = _b.y;
					polylineEdgeModel.dbClickPosition = getClosestPointOfPolyline({
						x,
						y
					}, polylineEdgeModel.points);
				}
				graphModel.eventCenter.emit(EventType.EDGE_DBCLICK, {
					data: edgeData,
					e,
					position
				});
			} else {
				graphModel.eventCenter.emit(EventType.ELEMENT_CLICK, {
					data: edgeData,
					e,
					position
				});
				graphModel.eventCenter.emit(EventType.EDGE_CLICK, {
					data: edgeData,
					e,
					position
				});
				var el_1 = e.currentTarget;
				(!isNil(window) && isFunction(window.requestAnimationFrame) ? window.requestAnimationFrame.bind(window) : function(fn) {
					return setTimeout(fn, 0);
				})(function() {
					el_1.focus();
				});
			}
			var editConfigModel = graphModel.editConfigModel;
			graphModel.selectEdgeById(model.id, isMultipleSelect(e, editConfigModel));
			if (!editConfigModel.isSilentMode) _this.toFront();
		};
		_this.handleFocus = function() {
			var _a = _this.props, model = _a.model;
			_a.graphModel.eventCenter.emit(EventType.EDGE_FOCUS, { data: model.getData() });
		};
		_this.handleBlur = function() {
			var _a = _this.props, model = _a.model;
			_a.graphModel.eventCenter.emit(EventType.EDGE_BLUR, { data: model.getData() });
		};
		return _this;
	}
	/**
	* 不支持重写，请使用getEdge
	*/
	BaseEdge.prototype.getShape = function() {
		return u("g", { children: this.getEdge() });
	};
	/**
	* @deprecated 请使用model.getTextStyle
	*/
	BaseEdge.prototype.getTextStyle = function() {};
	/**
	* @overridable 可重写，自定义边文本DOM
	*/
	BaseEdge.prototype.getText = function() {
		var _a;
		var _b = this.props, model = _b.model, graphModel = _b.graphModel;
		var editConfigModel = graphModel.editConfigModel;
		if (editConfigModel.edgeTextMode !== TextMode.TEXT) return null;
		if (model.state === ElementState.TEXT_EDIT) return null;
		if (model.text) {
			var draggable = false;
			if (editConfigModel.edgeTextDraggable && model.text.draggable) draggable = true;
			return u(LineText, {
				ref: this.textRef,
				editable: editConfigModel.edgeTextEdit && ((_a = model.text.editable) !== null && _a !== void 0 ? _a : true),
				model,
				graphModel,
				draggable
			});
		}
		return null;
	};
	/**
	* @deprecated
	*/
	BaseEdge.prototype.getArrowInfo = function() {
		var model = this.props.model;
		var startPoint = model.startPoint, endPoint = model.endPoint, isSelected = model.isSelected;
		return {
			start: startPoint,
			end: endPoint,
			hover: this.state.hover,
			isSelected
		};
	};
	BaseEdge.prototype.getLastTwoPoints = function() {
		var model = this.props.model;
		return [model.startPoint, model.endPoint];
	};
	/**
	* @deprecated 请使用model.getArrowStyle
	*/
	BaseEdge.prototype.getArrowStyle = function() {
		console.error("getArrowStyle is deprecated in 1.2.0, please use model.getArrowStyle");
		return null;
	};
	/**
	* 定义边的箭头，不支持重写。请使用getStartArrow和getEndArrow
	*/
	BaseEdge.prototype.getArrow = function() {
		var model = this.props.model;
		var id = model.id;
		var _a = model.getArrowStyle(), _b = _a.refY, refY = _b === void 0 ? 0 : _b, _c = _a.refX, refX = _c === void 0 ? 2 : _c;
		var _d = __read$5(this.getLastTwoPoints(), 2), start = _d[0], end = _d[1];
		var theta = "auto";
		if (start && end) {
			var dx = end.x - start.x;
			var dy = end.y - start.y;
			if (dx !== 0 || dy !== 0) {
				var computed = degrees(getThetaOfVector({
					x: dx,
					y: dy,
					z: 0
				}));
				if (Number.isFinite(computed) && !Number.isNaN(computed)) theta = computed;
			}
		}
		return u("g", { children: u("defs", { children: [u("marker", {
			id: "marker-start-".concat(id),
			refX: -refX,
			refY,
			overflow: "visible",
			orient: "auto",
			markerUnits: "userSpaceOnUse",
			children: this.getStartArrow()
		}), u("marker", {
			id: "marker-end-".concat(id),
			refX,
			refY,
			overflow: "visible",
			orient: theta,
			markerUnits: "userSpaceOnUse",
			children: this.getEndArrow()
		})] }) });
	};
	/**
	* Private helper method to generate arrow path based on type and parameters
	*/
	BaseEdge.prototype.getArrowPath = function(arrowType, props) {
		var stroke = props.stroke, _a = props.fill, fill = _a === void 0 ? stroke : _a, strokeWidth = props.strokeWidth, _b = props.offset, offset = _b === void 0 ? 10 : _b, verticalLength = props.verticalLength, strokeLinecap = props.strokeLinecap, strokeLinejoin = props.strokeLinejoin, transform = props.transform;
		var arrowPath = "";
		switch (arrowType) {
			case "solid":
				arrowPath = "M".concat(-offset / 4, ",0 L").concat(3 * offset / 4, ",").concat(verticalLength, " L").concat(3 * offset / 4, ",-").concat(verticalLength, " Z");
				break;
			case "hollow":
				arrowPath = "M".concat(-offset / 4, ",0 L").concat(3 * offset / 4, ",").concat(verticalLength, " L").concat(-offset / 4, ",0 L").concat(3 * offset / 4, ",-").concat(verticalLength, " L").concat(-offset / 4, ",0 Z");
				break;
			case "diamond":
				arrowPath = "M".concat(-offset / 2, ",0 L0,").concat(verticalLength, " L").concat(offset / 2, ",0 L0,-").concat(verticalLength, " L").concat(-offset / 2, ",0 Z");
				break;
			case "circle":
				arrowPath = "M".concat(-offset / 2, ",0 A").concat(offset / 4, ",").concat(offset / 4, " 0 1,0 ").concat(offset / 2, ",0 A").concat(offset / 4, ",").concat(offset / 4, " 0 1,0 ").concat(-offset / 2, ",0 Z");
				break;
			default:
				arrowPath = "";
				break;
		}
		return u("path", {
			d: arrowPath,
			stroke,
			fill,
			strokeWidth,
			strokeLinecap,
			strokeLinejoin,
			transform
		});
	};
	/**
	* @overridable 可重写，自定义边起点箭头形状。
	* @example
	* getStartArrow() {
	*  const { model } = this.props;
	*  const { stroke, strokeWidth, offset, verticalLength } = model.getArrowStyle();
	*  return (
	*    h('path', {
	*      d: ''
	*    })
	*  )
	* }
	*/
	BaseEdge.prototype.getStartArrow = function() {
		var _a = this.props.model.getArrowStyle(), stroke = _a.stroke, strokeWidth = _a.strokeWidth, offset = _a.offset, verticalLength = _a.verticalLength, _b = _a.startArrowType, startArrowType = _b === void 0 ? "none" : _b, strokeLinecap = _a.strokeLinecap, strokeLinejoin = _a.strokeLinejoin;
		return this.getArrowPath(startArrowType, {
			stroke,
			strokeWidth,
			offset,
			verticalLength,
			strokeLinecap,
			strokeLinejoin
		});
	};
	/**
	* @overridable 可重写，自定义边终点箭头形状。
	* @example
	* getEndArrow() {
	*  const { model } = this.props;
	*  const { stroke, strokeWidth, offset, verticalLength } = model.getArrowStyle();
	*  return (
	*    h('path', {
	*      d: ''
	*    })
	*  )
	* }
	*/
	BaseEdge.prototype.getEndArrow = function() {
		var _a = this.props.model.getArrowStyle(), stroke = _a.stroke, strokeWidth = _a.strokeWidth, offset = _a.offset, verticalLength = _a.verticalLength, _b = _a.endArrowType, endArrowType = _b === void 0 ? "solid" : _b, strokeLinecap = _a.strokeLinecap, strokeLinejoin = _a.strokeLinejoin;
		return this.getArrowPath(endArrowType, {
			stroke,
			strokeWidth,
			offset,
			verticalLength,
			strokeLinecap,
			strokeLinejoin,
			transform: "rotate(180)"
		});
	};
	/**
	* @overridable 可重写，自定义调整边连接节点形状。在开启了adjustEdgeStartAndEnd的时候，会显示调整点。
	* @param x 调整点x坐标
	* @param y 调整点y坐标
	* @param model
	* @example
	* getAdjustPointShape(x, y) {
	*  const { model } = this.props;
	*  const style = model.getAdjustPointStyle();
	*  return (
	*    h('circle', {
	*      ...style,
	*     x,
	*     y
	*    })
	*  )
	* }
	*/
	BaseEdge.prototype.getAdjustPointShape = function(x, y, model) {
		var style = model.getAdjustPointStyle();
		return u(Circle, __assign$9({ className: "lf-edge-adjust-point" }, style, {
			x,
			y
		}));
	};
	/**
	* 不支持重写。请使用getAdjustPointShape
	*/
	BaseEdge.prototype.getAdjustPoints = function() {
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var _b = graphModel.editConfigModel, adjustEdgeStartAndEnd = _b.adjustEdgeStartAndEnd, adjustEdgeStart = _b.adjustEdgeStart, adjustEdgeEnd = _b.adjustEdgeEnd;
		var start = model.getAdjustStart();
		var end = model.getAdjustEnd();
		return u("g", { children: [adjustEdgeStartAndEnd && adjustEdgeStart && u(AdjustPoint, __assign$9({ type: AdjustType.SOURCE }, start, {
			getAdjustPointShape: this.getAdjustPointShape,
			edgeModel: model,
			graphModel
		})), adjustEdgeStartAndEnd && adjustEdgeEnd && u(AdjustPoint, __assign$9({ type: AdjustType.TARGET }, end, {
			getAdjustPointShape: this.getAdjustPointShape,
			edgeModel: model,
			graphModel
		}))] });
	};
	/**
	* @deprecated
	*/
	BaseEdge.prototype.getAnimation = function() {
		console.error("getAnimation is deprecated in 1.2.0, please use model.getEdgeAnimationStyle");
	};
	/**
	* @overridable 可重写，在完全自定义边的时候，可以重写此方法，来自定义边的选区。
	*/
	BaseEdge.prototype.getAppendWidth = function() {
		return u("g", {});
	};
	/**
	* 不建议重写，此方法为扩大边选区，方便用户点击选中边。
	* 如果需要自定义边选区，请使用getAppendWidth。
	*/
	BaseEdge.prototype.getAppend = function() {
		return u("g", {
			className: "lf-edge-append",
			children: this.getAppendWidth()
		});
	};
	/**
	* @overridable 支持重写, 此方法为获取边的形状，如果需要自定义边的形状，请重写此方法。
	* @example https://docs.logic-flow.cn/docs/#/zh/guide/basic/edge?id=%e5%9f%ba%e4%ba%8e-react-%e7%bb%84%e4%bb%b6%e8%87%aa%e5%ae%9a%e4%b9%89%e8%be%b9
	*/
	BaseEdge.prototype.getEdge = function() {
		return null;
	};
	/**
	* @overridable 支持重写, 此方法为边在被选中时将其置顶，如果不需要此功能，可以重写此方法。
	*/
	BaseEdge.prototype.toFront = function() {
		var _a = this.props, graphModel = _a.graphModel, model = _a.model;
		graphModel.toFront(model.id);
	};
	/**
	* 不建议重写，如果要自定义边的形状，请重写getEdge方法。
	*/
	BaseEdge.prototype.render = function() {
		var _a = this.props.model, isSelected = _a.isSelected, isHitable = _a.isHitable, isShowAdjustPoint = _a.isShowAdjustPoint;
		return u("g", { children: [u("g", {
			className: [
				"lf-edge",
				!isHitable && "pointer-none",
				isSelected && "lf-edge-selected"
			].filter(Boolean).join(" "),
			style: {
				touchAction: "none",
				WebkitTouchCallout: "none"
			},
			onClick: this.handleClick,
			onContextMenu: this.handleContextMenu,
			onPointerDown: this.handleMouseDown,
			onPointerUp: this.handleMouseUp,
			onPointerCancel: this.handleMouseUp,
			onMouseOver: this.setHoverOn,
			onMouseEnter: this.setHoverOn,
			onMouseLeave: this.setHoverOff,
			onFocus: this.handleFocus,
			onBlur: this.handleBlur,
			children: [
				this.getShape(),
				this.getAppend(),
				this.getText(),
				this.getArrow()
			]
		}), isShowAdjustPoint && isSelected ? this.getAdjustPoints() : ""] });
	};
	BaseEdge.isObserved = false;
	return BaseEdge;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/view/edge/BezierEdge.js
var __extends$5 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$8 = function() {
	__assign$8 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$8.apply(this, arguments);
};
var __read$4 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var BezierEdge = function(_super) {
	__extends$5(BezierEdge, _super);
	function BezierEdge() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	/**
	* @overridable 支持重写, 此方法为获取边的形状，如果需要自定义边的形状，请重写此方法。
	* @example https://docs.logic-flow.cn/docs/#/zh/guide/basic/edge?id=%e5%9f%ba%e4%ba%8e-react-%e7%bb%84%e4%bb%b6%e8%87%aa%e5%ae%9a%e4%b9%89%e8%be%b9
	*/
	BezierEdge.prototype.getEdge = function() {
		var model = this.props.model;
		var style = model.getEdgeStyle();
		var _a = model, path = _a.path, isAnimation = _a.isAnimation, arrowConfig = _a.arrowConfig;
		var animationStyle = model.getEdgeAnimationStyle();
		var strokeDasharray = animationStyle.strokeDasharray, stroke = animationStyle.stroke, strokeDashoffset = animationStyle.strokeDashoffset, animationName = animationStyle.animationName, animationDuration = animationStyle.animationDuration, animationIterationCount = animationStyle.animationIterationCount, animationTimingFunction = animationStyle.animationTimingFunction, animationDirection = animationStyle.animationDirection;
		return u(Path, __assign$8({ d: path }, style, arrowConfig, isAnimation ? {
			strokeDasharray,
			stroke,
			style: {
				strokeDashoffset,
				animationName,
				animationDuration,
				animationIterationCount,
				animationTimingFunction,
				animationDirection
			}
		} : {}));
	};
	/**
	* @overridable 可重写，在完全自定义边的时候，可以重写此方法，来自定义边的选区。
	*/
	BezierEdge.prototype.getAppendWidth = function() {
		var path = this.props.model.path;
		return u(Path, {
			d: path,
			strokeWidth: 10,
			stroke: "transparent",
			fill: "none"
		});
	};
	/**
	* @deprecated
	*/
	BezierEdge.prototype.getArrowInfo = function() {
		var model = this.props.model;
		var hover = this.state.hover;
		var isSelected = model.isSelected;
		var offset = model.getArrowStyle().offset;
		var _a = __read$4(getEndTangent(model.pointsList.map(function(point) {
			return {
				x: point.x,
				y: point.y
			};
		}), offset), 2);
		return {
			start: _a[0],
			end: _a[1],
			hover,
			isSelected
		};
	};
	BezierEdge.prototype.getLastTwoPoints = function() {
		var model = this.props.model;
		var offset = model.getArrowStyle().offset;
		return getEndTangent(model.pointsList.map(function(point) {
			return {
				x: point.x,
				y: point.y
			};
		}), offset);
	};
	return BezierEdge;
}(BaseEdge);
//#endregion
//#region node_modules/@logicflow/core/es/view/edge/LineEdge.js
var __extends$4 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$7 = function() {
	__assign$7 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$7.apply(this, arguments);
};
var LineEdge = function(_super) {
	__extends$4(LineEdge, _super);
	function LineEdge() {
		return _super !== null && _super.apply(this, arguments) || this;
	}
	/**
	* @overridable 支持重写, 此方法为获取边的形状，如果需要自定义边的形状，请重写此方法。
	* @example https://docs.logic-flow.cn/docs/#/zh/guide/basic/edge?id=%e5%9f%ba%e4%ba%8e-react-%e7%bb%84%e4%bb%b6%e8%87%aa%e5%ae%9a%e4%b9%89%e8%be%b9
	*/
	LineEdge.prototype.getEdge = function() {
		var model = this.props.model;
		var startPoint = model.startPoint, endPoint = model.endPoint, isAnimation = model.isAnimation, arrowConfig = model.arrowConfig;
		var style = model.getEdgeStyle();
		var animationStyle = model.getEdgeAnimationStyle();
		var strokeDasharray = animationStyle.strokeDasharray, stroke = animationStyle.stroke, strokeDashoffset = animationStyle.strokeDashoffset, animationName = animationStyle.animationName, animationDuration = animationStyle.animationDuration, animationIterationCount = animationStyle.animationIterationCount, animationTimingFunction = animationStyle.animationTimingFunction, animationDirection = animationStyle.animationDirection;
		return u(Line, __assign$7({}, style, {
			x1: startPoint.x,
			y1: startPoint.y,
			x2: endPoint.x,
			y2: endPoint.y
		}, arrowConfig, isAnimation ? {
			strokeDasharray,
			stroke,
			style: {
				strokeDashoffset,
				animationName,
				animationDuration,
				animationIterationCount,
				animationTimingFunction,
				animationDirection
			}
		} : {}));
	};
	/**
	* @overridable 可重写，在完全自定义边的时候，可以重写此方法，来自定义边的选区。
	*/
	LineEdge.prototype.getAppendWidth = function() {
		var model = this.props.model;
		var _a = getAppendAttributes({
			start: model.startPoint,
			end: model.endPoint
		}), d = _a.d, strokeWidth = _a.strokeWidth, fill = _a.fill, strokeDasharray = _a.strokeDasharray, stroke = _a.stroke;
		return u(Path, {
			d,
			fill,
			strokeWidth,
			stroke,
			strokeDasharray
		});
	};
	return LineEdge;
}(BaseEdge);
//#endregion
//#region node_modules/@logicflow/core/es/view/edge/PolylineEdge.js
var __extends$3 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$6 = function() {
	__assign$6 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$6.apply(this, arguments);
};
var __read$3 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var PolylineEdge = function(_super) {
	__extends$3(PolylineEdge, _super);
	function PolylineEdge() {
		var _this = _super.call(this) || this;
		/**
		* 不支持重写
		*/
		_this.onDragStart = function() {
			var polylineModel = _this.props.model;
			polylineModel.dragAppendStart();
			_this.isShowAdjustPointTemp = polylineModel.isShowAdjustPoint;
			polylineModel.isShowAdjustPoint = false;
		};
		/**
		* 不支持重写
		*/
		_this.onDragging = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			var _b = _this.props, model = _b.model, graphModel = _b.graphModel;
			_this.isDragging = true;
			var transformModel = graphModel.transformModel, editConfigModel = graphModel.editConfigModel;
			var _c = __read$3(transformModel.fixDeltaXY(deltaX, deltaY), 2), curDeltaX = _c[0], curDeltaY = _c[1];
			var polylineModel = model;
			if (editConfigModel.adjustEdgeMiddle) _this.appendInfo = polylineModel.dragAppendSimple(_this.appendInfo, {
				x: curDeltaX,
				y: curDeltaY
			});
			else _this.appendInfo = polylineModel.dragAppend(_this.appendInfo, {
				x: curDeltaX,
				y: curDeltaY
			});
		};
		/**
		* 不支持重写
		*/
		_this.onDragEnd = function() {
			var _a;
			var _b = _this.props, model = _b.model, eventCenter = _b.graphModel.eventCenter;
			var polylineModel = model;
			polylineModel.dragAppendEnd();
			_this.isDragging = false;
			polylineModel.isShowAdjustPoint = (_a = _this.isShowAdjustPointTemp) !== null && _a !== void 0 ? _a : false;
			_this.appendInfo = void 0;
			eventCenter.emit(EventType.EDGE_ADJUST, { data: polylineModel.getData() });
		};
		/**
		* 不支持重写
		*/
		_this.beforeDragStart = function(e, appendInfo) {
			if (appendInfo.draggable) _this.drag.handleMouseDown(e);
			_this.appendInfo = appendInfo;
		};
		_this.drag = new StepDrag({
			onDragStart: _this.onDragStart,
			onDragging: _this.onDragging,
			onDragEnd: _this.onDragEnd,
			isStopPropagation: false
		});
		return _this;
	}
	/**
	* @overridable 支持重写, 此方法为获取边的形状，如果需要自定义边的形状，请重写此方法。
	* @example https://docs.logic-flow.cn/docs/#/zh/guide/basic/edge?id=%e5%9f%ba%e4%ba%8e-react-%e7%bb%84%e4%bb%b6%e8%87%aa%e5%ae%9a%e4%b9%89%e8%be%b9
	*/
	PolylineEdge.prototype.getEdge = function() {
		var _a, _b;
		var model = this.props.model;
		var points = model.points, isAnimation = model.isAnimation, arrowConfig = model.arrowConfig, properties = model.properties;
		var style = model.getEdgeStyle();
		var animationStyle = model.getEdgeAnimationStyle();
		var strokeDasharray = animationStyle.strokeDasharray, stroke = animationStyle.stroke, strokeDashoffset = animationStyle.strokeDashoffset, animationName = animationStyle.animationName, animationDuration = animationStyle.animationDuration, animationIterationCount = animationStyle.animationIterationCount, animationTimingFunction = animationStyle.animationTimingFunction, animationDirection = animationStyle.animationDirection;
		var radius = (_b = (_a = properties === null || properties === void 0 ? void 0 : properties.radius) !== null && _a !== void 0 ? _a : style === null || style === void 0 ? void 0 : style.radius) !== null && _b !== void 0 ? _b : 0;
		var roundedPointsStr = (function() {
			if (!radius || radius <= 0) return points;
			return generateRoundedCorners(points2PointsList(points), radius, false).map(function(p) {
				return "".concat(p.x, ",").concat(p.y);
			}).join(" ");
		})();
		return u(Polyline, __assign$6({ points: roundedPointsStr }, style, arrowConfig, isAnimation ? {
			strokeDasharray,
			stroke,
			style: {
				strokeDashoffset,
				animationName,
				animationDuration,
				animationIterationCount,
				animationTimingFunction,
				animationDirection
			}
		} : {}));
	};
	/**
	* @deprecated
	*/
	PolylineEdge.prototype.getArrowInfo = function() {
		var model = this.props.model;
		var points = model.points, isSelected = model.isSelected;
		var hover = this.state.hover;
		var currentPositionList = points2PointsList(points);
		var startPoint = currentPositionList[0];
		var endPoint = currentPositionList[0];
		if (currentPositionList.length >= 2) {
			startPoint = currentPositionList[currentPositionList.length - 2];
			endPoint = currentPositionList[currentPositionList.length - 1];
		}
		return {
			start: startPoint,
			end: endPoint,
			hover,
			isSelected
		};
	};
	PolylineEdge.prototype.getLastTwoPoints = function() {
		var points = this.props.model.points;
		var currentPositionList = points2PointsList(points);
		var startPoint = currentPositionList[0];
		var endPoint = currentPositionList[0];
		if (currentPositionList.length >= 2) {
			startPoint = currentPositionList[currentPositionList.length - 2];
			endPoint = currentPositionList[currentPositionList.length - 1];
		}
		return [startPoint, endPoint];
	};
	PolylineEdge.prototype.getAppendAttributes = function(appendInfo) {
		var start = appendInfo.start, end = appendInfo.end;
		var d;
		if (start.x === end.x && start.y === end.y) d = "";
		else {
			var config = {
				start,
				end,
				offset: 10,
				verticalLength: 5
			};
			var startPosition = getVerticalPointOfLine(__assign$6(__assign$6({}, config), { type: "start" }));
			var endPosition = getVerticalPointOfLine(__assign$6(__assign$6({}, config), { type: "end" }));
			d = "M".concat(startPosition.leftX, " ").concat(startPosition.leftY, "\n      L").concat(startPosition.rightX, " ").concat(startPosition.rightY, "\n      L").concat(endPosition.rightX, " ").concat(endPosition.rightY, "\n      L").concat(endPosition.leftX, " ").concat(endPosition.leftY, " z");
		}
		return {
			d,
			fill: "transparent",
			stroke: "transparent",
			strokeWidth: 1,
			strokeDasharray: "4, 4"
		};
	};
	PolylineEdge.prototype.getAppendShape = function(appendInfo) {
		var _a = this.getAppendAttributes(appendInfo), d = _a.d, strokeWidth = _a.strokeWidth, fill = _a.fill, strokeDasharray = _a.strokeDasharray, stroke = _a.stroke;
		return u(Path, {
			d,
			fill,
			strokeWidth,
			stroke,
			strokeDasharray
		});
	};
	/**
	* @overridable 可重写，在完全自定义边的时候，可以重写此方法，来自定义边的选区。
	*/
	PolylineEdge.prototype.getAppendWidth = function() {
		var _this = this;
		var _a = this.props, model = _a.model, graphModel = _a.graphModel;
		var pointsList = model.pointsList, draggable = model.draggable;
		var LineAppendList = [];
		var pointsLen = pointsList.length;
		var _loop_1 = function(i) {
			var className = "lf-polyline-append";
			var appendInfo = {
				start: {
					x: pointsList[i].x,
					y: pointsList[i].y
				},
				end: {
					x: pointsList[i + 1].x,
					y: pointsList[i + 1].y
				},
				startIndex: i,
				endIndex: i + 1,
				direction: SegmentDirection.HORIZONTAL,
				draggable: true
			};
			var append = u("g", {
				className,
				children: this_1.getAppendShape(appendInfo)
			});
			var editConfigModel = graphModel.editConfigModel;
			var adjustEdge = editConfigModel.adjustEdge, adjustEdgeMiddle = editConfigModel.adjustEdgeMiddle;
			if (adjustEdge && draggable) {
				var startIndex = appendInfo.startIndex, endIndex = appendInfo.endIndex;
				appendInfo.draggable = !(adjustEdgeMiddle && (startIndex === 0 || endIndex === pointsLen - 1));
				if (appendInfo.start.x === appendInfo.end.x) {
					if (appendInfo.draggable) className += "-ew-resize";
					appendInfo.direction = SegmentDirection.VERTICAL;
				} else if (appendInfo.start.y === appendInfo.end.y) {
					if (appendInfo.draggable) className += "-ns-resize";
					appendInfo.direction = SegmentDirection.HORIZONTAL;
				}
				append = u("g", {
					className: this_1.isDragging ? "lf-dragging" : "lf-drag-able",
					onPointerDown: function(e) {
						return _this.beforeDragStart(e, appendInfo);
					},
					children: u("g", {
						className,
						children: this_1.getAppendShape(appendInfo)
					})
				});
			}
			LineAppendList.push(append);
		};
		var this_1 = this;
		for (var i = 0; i < pointsLen - 1; i++) _loop_1(i);
		return u("g", { children: LineAppendList });
	};
	return PolylineEdge;
}(BaseEdge);
//#endregion
//#region node_modules/@logicflow/core/es/view/behavior/dnd.js
var __assign$5 = function() {
	__assign$5 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$5.apply(this, arguments);
};
var Dnd = function() {
	function Dnd(params) {
		var _this = this;
		this.nodeConfig = null;
		this.fakeNode = null;
		this.stopDrag = function() {
			_this.nodeConfig = null;
			if (_this.docPointerMove) window.document.removeEventListener("pointermove", _this.docPointerMove);
			if (_this.docPointerUp) window.document.removeEventListener("pointerup", _this.docPointerUp);
		};
		this.dragEnter = function(e) {
			if (!_this.nodeConfig || _this.fakeNode) return;
			_this.fakeNode = _this.lf.createFakeNode(__assign$5(__assign$5({}, _this.nodeConfig), _this.clientToLocalPoint({
				x: e.clientX,
				y: e.clientY
			})));
		};
		this.onDragOver = function(e) {
			_this.lf.graphModel.eventCenter.emit(EventType.BLANK_CANVAS_MOUSEMOVE, { e });
			e.preventDefault();
			if (_this.fakeNode) {
				var _a = _this.clientToLocalPoint({
					x: e.clientX,
					y: e.clientY
				}), x = _a.x, y = _a.y;
				_this.fakeNode.moveTo(x, y);
				var nodeData = _this.fakeNode.getData();
				_this.lf.setNodeSnapLine(nodeData);
				_this.lf.graphModel.eventCenter.emit(EventType.NODE_DND_DRAG, {
					data: nodeData,
					e
				});
			}
			return false;
		};
		this.onDragLeave = function() {
			if (_this.fakeNode) {
				_this.lf.removeNodeSnapLine();
				_this.lf.graphModel.removeFakeNode();
				_this.fakeNode = null;
			}
		};
		this.onDrop = function(e) {
			if (!_this.lf.graphModel || !e || !_this.nodeConfig) return;
			_this.lf.addNode(__assign$5(__assign$5({}, _this.nodeConfig), _this.clientToLocalPoint({
				x: e.clientX,
				y: e.clientY
			})), EventType.NODE_DND_ADD, e);
			e.preventDefault();
			e.stopPropagation();
			_this.nodeConfig = null;
			_this.lf.removeNodeSnapLine();
			_this.lf.graphModel.removeFakeNode();
			_this.fakeNode = null;
		};
		var lf = params.lf;
		this.lf = lf;
	}
	Dnd.prototype.clientToLocalPoint = function(_a) {
		var x = _a.x, y = _a.y;
		var gridSize = get(this.lf.options, ["grid", "size"]);
		var _b = this.lf.graphModel.getPointByClient({
			x,
			y
		}).canvasOverlayPosition, x1 = _b.x, y1 = _b.y;
		var snapGrid = this.lf.graphModel.editConfigModel.snapGrid;
		return {
			x: snapToGrid(x1, gridSize, snapGrid),
			y: snapToGrid(y1, gridSize, snapGrid)
		};
	};
	Dnd.prototype.isInsideCanvas = function(e) {
		var overlay = this.lf.graphModel.rootEl.querySelector("[name=\"canvas-overlay\"]");
		var topEl = window.document.elementFromPoint(e.clientX, e.clientY);
		return topEl === overlay || topEl !== null && !!overlay && overlay.contains(topEl);
	};
	Dnd.prototype.startDrag = function(nodeConfig) {
		var _this = this;
		var editConfigModel = this.lf.graphModel.editConfigModel;
		if (editConfigModel === null || editConfigModel === void 0 ? void 0 : editConfigModel.isSilentMode) return;
		this.nodeConfig = nodeConfig;
		this.docPointerMove = function(e) {
			if (!_this.nodeConfig) return;
			if (!_this.isInsideCanvas(e)) {
				_this.onDragLeave();
				return;
			}
			if (!_this.fakeNode) {
				_this.dragEnter(e);
				return;
			}
			_this.onDragOver(e);
		};
		this.docPointerUp = function(e) {
			if (!_this.nodeConfig) return;
			if (_this.isInsideCanvas(e)) _this.onDrop(e);
			else _this.onDragLeave();
			e.preventDefault();
			e.stopPropagation();
			_this.stopDrag();
		};
		window.document.addEventListener("pointermove", this.docPointerMove);
		window.document.addEventListener("pointerup", this.docPointerUp);
	};
	return Dnd;
}();
//#endregion
//#region node_modules/@logicflow/core/es/view/behavior/snapline.js
function snapline(eventCenter, snaplineModel) {
	eventCenter.on("node:mousemove", function(_a) {
		var data = _a.data;
		snaplineModel.setNodeSnapLine(data);
	});
	eventCenter.on("node:mouseup", function() {
		snaplineModel.clearSnapline();
	});
}
//#endregion
//#region node_modules/@logicflow/core/es/tool/TextEditTool.js
var __extends$2 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __assign$4 = function() {
	__assign$4 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$4.apply(this, arguments);
};
var __decorate$4 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$2 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var TextEditTool = function(_super) {
	__extends$2(TextEditTool, _super);
	function TextEditTool(props) {
		var _this = _super.call(this, props) || this;
		_this.ref = M$1();
		_this.__prevText = {
			type: "",
			text: "",
			id: ""
		};
		_this.keyupHandler = function(ev) {
			var textEditElement = _this.props.graphModel.textEditElement;
			if (ev.key === "Enter" && ev.altKey) textEditElement === null || textEditElement === void 0 || textEditElement.setElementState(ElementState.DEFAULT);
		};
		_this.inputHandler = function(ev) {
			var value = ev.target.innerText;
			var textEditElement = _this.props.graphModel.textEditElement;
			if (textEditElement) _this.__prevText = {
				type: textEditElement.type,
				text: value.replace(/(\r\n)+$|(\n)+$/, ""),
				id: textEditElement.id
			};
		};
		_this.keydownHandler = function(ev) {
			ev.stopPropagation();
		};
		_this.state = { style: {
			left: 0,
			top: 0
		} };
		return _this;
	}
	TextEditTool.getDerivedStateFromProps = function(props) {
		var _a;
		var textEditElement = props.textEditElement, graphModel = props.graphModel;
		var transformModel = graphModel.transformModel, theme = graphModel.theme;
		var inputText = theme.inputText;
		var autoStyle;
		if (textEditElement) {
			if (!((_a = textEditElement.text) === null || _a === void 0 ? void 0 : _a.value)) {
				if (textEditElement.BaseType === ElementType.EDGE) {
					var textConfig = textEditElement.text;
					var _b = textEditElement.textPosition, x_1 = _b.x, y_1 = _b.y;
					textConfig.x = x_1;
					textConfig.y = y_1;
					textEditElement.setText(textConfig);
				}
			}
			var commonAutoStyle = {
				resize: "auto",
				whiteSpace: "normal",
				wordBreak: "break-all"
			};
			if (textEditElement.BaseType === ElementType.EDGE) {
				var _c = theme.edgeText, overflowMode = _c.overflowMode, lineHeight = _c.lineHeight, wrapPadding = _c.wrapPadding, textWidth = _c.textWidth;
				if (textWidth && overflowMode === "autoWrap") autoStyle = __assign$4(__assign$4({}, commonAutoStyle), {
					width: textWidth,
					minWidth: textWidth,
					lineHeight,
					padding: wrapPadding
				});
			} else if (textEditElement.BaseType === ElementType.NODE) {
				var _d = theme.nodeText, overflowMode = _d.overflowMode, lineHeight = _d.lineHeight, wrapPadding = _d.wrapPadding, textWidth = _d.textWidth;
				var width = textEditElement.width, modelType = textEditElement.modelType;
				var finalTextWidth = textEditElement.textWidth || textWidth || width;
				if (modelType !== ModelType.TEXT_NODE && overflowMode === "autoWrap" || modelType === ModelType.TEXT_NODE && textWidth) autoStyle = __assign$4(__assign$4({}, commonAutoStyle), {
					width: finalTextWidth,
					minWidth: finalTextWidth,
					lineHeight,
					padding: wrapPadding
				});
			}
			var _e = textEditElement.text, x = _e.x, y = _e.y;
			var _f = __read$2(transformModel.CanvasPointToHtmlPoint([x, y]), 2), left = _f[0], top_1 = _f[1];
			return { style: __assign$4(__assign$4({
				left,
				top: top_1
			}, autoStyle), inputText) };
		}
		return null;
	};
	TextEditTool.prototype.componentDidUpdate = function() {
		var graphModel = this.props.graphModel;
		if (this.ref.current) {
			this.ref.current.focus();
			this.placeCaretAtEnd(this.ref.current);
		}
		if (this.__prevText.id !== "") {
			var _a = this.__prevText, text = _a.text, id = _a.id;
			graphModel.updateText(id, text);
			graphModel.eventCenter.emit(EventType.TEXT_UPDATE, { data: __assign$4({}, this.__prevText) });
			this.__prevText.id = "";
			this.__prevText.text = "";
			this.__prevText.type = "";
		}
	};
	TextEditTool.prototype.placeCaretAtEnd = function(el) {
		if (window.getSelection !== void 0 && document.createRange !== void 0) {
			var range = document.createRange();
			range.selectNodeContents(el);
			range.collapse(false);
			var sel = window.getSelection();
			sel === null || sel === void 0 || sel.removeAllRanges();
			sel === null || sel === void 0 || sel.addRange(range);
		}
	};
	TextEditTool.prototype.render = function() {
		var _a;
		var textEditElement = this.props.graphModel.textEditElement;
		var style = this.state.style;
		return textEditElement ? u("div", {
			contentEditable: true,
			className: "lf-text-input",
			style,
			ref: this.ref,
			onKeyUp: this.keyupHandler,
			onKeyDown: this.keydownHandler,
			onKeyPress: this.keydownHandler,
			onInput: this.inputHandler,
			children: (_a = textEditElement.text) === null || _a === void 0 ? void 0 : _a.value
		}, textEditElement.id) : null;
	};
	TextEditTool.toolName = "text-edit-tool";
	TextEditTool = __decorate$4([observer], TextEditTool);
	return TextEditTool;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/tool/MultipleSelectTool.js
var __extends$1 = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (Object.prototype.hasOwnProperty.call(b, p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		if (typeof b !== "function" && b !== null) throw new TypeError("Class extends value " + String(b) + " is not a constructor or null");
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
var __decorate$3 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __read$1 = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray$1 = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var MultipleSelect = function(_super) {
	__extends$1(MultipleSelect, _super);
	function MultipleSelect(props) {
		var _this = _super.call(this, props) || this;
		_this.handleMouseDown = function(ev) {
			var _a = _this.props, gridSize = _a.graphModel.gridSize;
			var SCALE_X = _a.lf.getTransform().SCALE_X;
			_this.stepDrag.setStep(gridSize * SCALE_X);
			_this.stepDrag.handleMouseDown(ev);
		};
		_this.handleWheelEvent = function(ev) {
			var _a, _b;
			ev.preventDefault();
			var deltaX = ev.deltaX, deltaY = ev.deltaY, clientX = ev.clientX, clientY = ev.clientY, ctrlKey = ev.ctrlKey;
			var newEvent = new WheelEvent("wheel", {
				deltaX,
				deltaY,
				clientX,
				clientY,
				ctrlKey
			});
			(_b = (_a = _this.props.lf.container) === null || _a === void 0 ? void 0 : _a.querySelector(".lf-canvas-overlay[name=\"canvas-overlay\"]")) === null || _b === void 0 || _b.dispatchEvent(newEvent);
		};
		_this.onDragging = function(_a) {
			var deltaX = _a.deltaX, deltaY = _a.deltaY;
			var _b = _this.props, graphModel = _b.graphModel;
			var _c = _b.lf.getTransform(), SCALE_X = _c.SCALE_X, SCALE_Y = _c.SCALE_Y;
			var selectElements = graphModel.getSelectElements(true);
			graphModel.moveNodes(selectElements.nodes.map(function(node) {
				return node.id;
			}), deltaX / SCALE_X, deltaY / SCALE_Y);
		};
		_this.handleContextMenu = function(ev) {
			ev.preventDefault();
			var _a = _this.props, graphModel = _a.graphModel, _b = _a.graphModel, eventCenter = _b.eventCenter, selectElements = _b.selectElements;
			var position = graphModel.getPointByClient({
				x: ev.clientX,
				y: ev.clientY
			});
			var selectGraphData = {
				nodes: [],
				edges: []
			};
			__spreadArray$1([], __read$1(selectElements.values()), false).forEach(function(model) {
				if (model.BaseType === ElementType.NODE) selectGraphData.nodes.push(model.getData());
				if (model.BaseType === ElementType.EDGE) selectGraphData.edges.push(model.getData());
			});
			eventCenter.emit(EventType.SELECTION_CONTEXTMENU, {
				data: selectGraphData,
				e: ev,
				position
			});
		};
		var _a = props.graphModel, gridSize = _a.gridSize, eventCenter = _a.eventCenter;
		_this.stepDrag = new StepDrag({
			onDragging: _this.onDragging,
			step: gridSize,
			eventType: "SELECTION",
			eventCenter
		});
		return _this;
	}
	MultipleSelect.prototype.render = function() {
		var _a, _b;
		var _c = this.props.graphModel, selectElements = _c.selectElements, transformModel = _c.transformModel, theme = _c.theme;
		var _d = this.props.lf.getTransform(), SCALE_X = _d.SCALE_X, SCALE_Y = _d.SCALE_Y;
		var _e = theme.multiSelect || {}, _f = _e.xPadding, xPadding = _f === void 0 ? 8 : _f, _g = _e.yPadding, yPadding = _g === void 0 ? 8 : _g;
		if (selectElements.size <= 1) return;
		var x = Number.MAX_SAFE_INTEGER;
		var y = Number.MAX_SAFE_INTEGER;
		var x1 = Number.MIN_SAFE_INTEGER;
		var y1 = Number.MIN_SAFE_INTEGER;
		selectElements.forEach(function(element) {
			var outline;
			if (element.BaseType === ElementType.NODE) outline = getNodeOutline(element);
			if (element.BaseType === ElementType.EDGE) outline = getEdgeOutline(element);
			if (outline !== void 0) {
				x = Math.min(x, outline.x);
				y = Math.min(y, outline.y);
				x1 = Math.max(x1, outline.x1);
				y1 = Math.max(y1, outline.y1);
			}
		});
		_a = __read$1(transformModel.CanvasPointToHtmlPoint([x, y]), 2), x = _a[0], y = _a[1];
		_b = __read$1(transformModel.CanvasPointToHtmlPoint([x1, y1]), 2), x1 = _b[0], y1 = _b[1];
		return u("div", {
			className: "lf-multiple-select",
			style: {
				left: "".concat(x - 20 * SCALE_X / 2 - xPadding / 2, "px"),
				top: "".concat(y - 20 * SCALE_Y / 2 - yPadding / 2, "px"),
				width: "".concat(x1 - x + 20 * SCALE_X + xPadding, "px"),
				height: "".concat(y1 - y + 20 * SCALE_Y + yPadding, "px"),
				"border-width": "".concat(2 * SCALE_X, "px")
			},
			onPointerDown: this.handleMouseDown,
			onContextMenu: this.handleContextMenu,
			onWheel: this.handleWheelEvent
		});
	};
	MultipleSelect.toolName = "multiple-select-tool";
	MultipleSelect = __decorate$3([observer], MultipleSelect);
	return MultipleSelect;
}(C);
//#endregion
//#region node_modules/@logicflow/core/es/tool/index.js
var __decorate$2 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var defaultTools = [TextEditTool, MultipleSelect];
var Tool = function() {
	function Tool(instance) {
		var _this = this;
		this.toolMap = /* @__PURE__ */ new Map();
		this.disabledToolMap = /* @__PURE__ */ new Map();
		this.instance = instance;
		forEach(defaultTools, function(tool) {
			if (!_this.isDisabled(tool.toolName)) _this.registerTool(tool.toolName, tool);
		});
		var graphModel = instance.graphModel;
		graphModel.eventCenter.on("".concat(EventType.GRAPH_TRANSFORM, ",").concat(EventType.NODE_CLICK, ",").concat(EventType.BLANK_CLICK, " "), function() {
			var _a;
			var textEditElement = graphModel.textEditElement, _b = graphModel.editConfigModel, edgeTextEdit = _b.edgeTextEdit, nodeTextEdit = _b.nodeTextEdit;
			if ((edgeTextEdit || nodeTextEdit) && textEditElement) (_a = graphModel.textEditElement) === null || _a === void 0 || _a.setElementState(ElementState.DEFAULT);
		});
	}
	Tool.prototype.isDisabled = function(toolName) {
		var _a;
		return ((_a = this.instance.options.disabledTools) === null || _a === void 0 ? void 0 : _a.indexOf(toolName)) !== -1;
	};
	Tool.prototype.registerTool = function(name, component) {
		this.toolMap.set(name, component);
	};
	Tool.prototype.disableTool = function(name) {
		var tool = this.toolMap.get(name);
		if (tool) {
			this.disabledToolMap.set(name, tool);
			this.toolMap.delete(name);
			return true;
		}
		throw new Error("禁用失败，不存在名为 ".concat(tool, " 的工具"));
	};
	Tool.prototype.enableTool = function(name) {
		var tool = this.disabledToolMap.get(name);
		if (tool) {
			this.toolMap.set(name, tool);
			this.disabledToolMap.delete(name);
			return true;
		}
		throw new Error("不存在名为 ".concat(tool, " 的工具"));
	};
	Tool.prototype.getTools = function() {
		return Array.from(this.toolMap.values());
	};
	Tool.prototype.getInstance = function() {
		return this.instance;
	};
	Tool.prototype.destroy = function() {
		this.toolMap.clear();
		this.disabledToolMap.clear();
	};
	__decorate$2([observable], Tool.prototype, "toolMap", void 0);
	__decorate$2([action], Tool.prototype, "disableTool", null);
	__decorate$2([action], Tool.prototype, "enableTool", null);
	return Tool;
}();
//#endregion
//#region node_modules/mobx-utils/mobx-utils.module.js
function fail$1(message) {
	throw new Error("[mobx-utils] " + message);
}
function invariant(cond, message) {
	if (message === void 0) message = "Illegal state";
	if (!cond) fail$1(message);
}
var deepFields = function(x) {
	return x && x !== Object.prototype && Object.getOwnPropertyNames(x).concat(deepFields(Object.getPrototypeOf(x)) || []);
};
var distinctDeepFields = function(x) {
	var deepFieldsIndistinct = deepFields(x);
	return deepFieldsIndistinct.filter(function(item, index) {
		return deepFieldsIndistinct.indexOf(item) === index;
	});
};
var getAllMethodsAndProperties = function(x) {
	return distinctDeepFields(x).filter(function(name) {
		return name !== "constructor" && !~name.indexOf("__");
	});
};
var PENDING = "pending";
var FULFILLED = "fulfilled";
var REJECTED = "rejected";
function caseImpl(handlers) {
	switch (this.state) {
		case PENDING: return handlers.pending && handlers.pending(this.value);
		case REJECTED: return handlers.rejected && handlers.rejected(this.value);
		case FULFILLED: return handlers.fulfilled ? handlers.fulfilled(this.value) : this.value;
	}
}
/**
* `fromPromise` takes a Promise, extends it with 2 observable properties that track
* the status of the promise and returns it. The returned object has the following observable properties:
*  - `value`: either the initial value, the value the Promise resolved to, or the value the Promise was rejected with. use `.state` if you need to be able to tell the difference.
*  - `state`: one of `"pending"`, `"fulfilled"` or `"rejected"`
*
* And the following methods:
* - `case({fulfilled, rejected, pending})`: maps over the result using the provided handlers, or returns `undefined` if a handler isn't available for the current promise state.
* - `then((value: TValue) => TResult1 | PromiseLike<TResult1>, [(rejectReason: any) => any])`: chains additional handlers to the provided promise.
*
* The returned object implements `PromiseLike<TValue>`, so you can chain additional `Promise` handlers using `then`. You may also use it with `await` in `async` functions.
*
* Note that the status strings are available as constants:
* `mobxUtils.PENDING`, `mobxUtils.REJECTED`, `mobxUtil.FULFILLED`
*
* fromPromise takes an optional second argument, a previously created `fromPromise` based observable.
* This is useful to replace one promise based observable with another, without going back to an intermediate
* "pending" promise state while fetching data. For example:
*
* @example
* \@observer
* class SearchResults extends React.Component {
*   \@observable.ref searchResults
*
*   componentDidUpdate(nextProps) {
*     if (nextProps.query !== this.props.query)
*       this.searchResults = fromPromise(
*         window.fetch("/search?q=" + nextProps.query),
*         // by passing, we won't render a pending state if we had a successful search query before
*         // rather, we will keep showing the previous search results, until the new promise resolves (or rejects)
*         this.searchResults
*       )
*   }
*
*   render() {
*     return this.searchResults.case({
*        pending: (staleValue) => {
*          return staleValue || "searching" // <- value might set to previous results while the promise is still pending
*        },
*        fulfilled: (value) => {
*          return value // the fresh results
*        },
*        rejected: (error) => {
*          return "Oops: " + error
*        }
*     })
*   }
* }
*
* Observable promises can be created immediately in a certain state using
* `fromPromise.reject(reason)` or `fromPromise.resolve(value?)`.
* The main advantage of `fromPromise.resolve(value)` over `fromPromise(Promise.resolve(value))` is that the first _synchronously_ starts in the desired state.
*
* It is possible to directly create a promise using a resolve, reject function:
* `fromPromise((resolve, reject) => setTimeout(() => resolve(true), 1000))`
*
* @example
* const fetchResult = fromPromise(fetch("http://someurl"))
*
* // combine with when..
* when(
*   () => fetchResult.state !== "pending",
*   () => {
*     console.log("Got ", fetchResult.value)
*   }
* )
*
* // or a mobx-react component..
* const myComponent = observer(({ fetchResult }) => {
*   switch(fetchResult.state) {
*      case "pending": return <div>Loading...</div>
*      case "rejected": return <div>Ooops... {fetchResult.value}</div>
*      case "fulfilled": return <div>Gotcha: {fetchResult.value}</div>
*   }
* })
*
* // or using the case method instead of switch:
*
* const myComponent = observer(({ fetchResult }) =>
*   fetchResult.case({
*     pending:   () => <div>Loading...</div>,
*     rejected:  error => <div>Ooops.. {error}</div>,
*     fulfilled: value => <div>Gotcha: {value}</div>,
*   }))
*
* // chain additional handler(s) to the resolve/reject:
*
* fetchResult.then(
*   (result) =>  doSomeTransformation(result),
*   (rejectReason) => console.error('fetchResult was rejected, reason: ' + rejectReason)
* ).then(
*   (transformedResult) => console.log('transformed fetchResult: ' + transformedResult)
* )
*
* @param {IThenable<T>} promise The promise which will be observed
* @param {IThenable<T>} oldPromise? The previously observed promise
* @returns {IPromiseBasedObservable<T>}
*/
function fromPromise(origPromise, oldPromise) {
	invariant(arguments.length <= 2, "fromPromise expects up to two arguments");
	invariant(typeof origPromise === "function" || typeof origPromise === "object" && origPromise && typeof origPromise.then === "function", "Please pass a promise or function to fromPromise");
	if (origPromise.isPromiseBasedObservable === true) return origPromise;
	if (typeof origPromise === "function") origPromise = new Promise(origPromise);
	var promise = origPromise;
	origPromise.then(action("observableFromPromise-resolve", function(value) {
		promise.value = value;
		promise.state = FULFILLED;
	}), action("observableFromPromise-reject", function(reason) {
		promise.value = reason;
		promise.state = REJECTED;
	}));
	promise.isPromiseBasedObservable = true;
	promise.case = caseImpl;
	extendObservable(promise, {
		value: oldPromise && oldPromise.state === "fulfilled" ? oldPromise.value : void 0,
		state: PENDING
	}, {}, { deep: false });
	return promise;
}
(function(fromPromise) {
	fromPromise.reject = action("fromPromise.reject", function(reason) {
		var p = fromPromise(Promise.reject(reason));
		p.state = REJECTED;
		p.value = reason;
		return p;
	});
	function resolveBase(value) {
		if (value === void 0) value = void 0;
		var p = fromPromise(Promise.resolve(value));
		p.state = FULFILLED;
		p.value = value;
		return p;
	}
	fromPromise.resolve = action("fromPromise.resolve", resolveBase);
})(fromPromise || (fromPromise = {}));
var __decorate = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
(function() {
	function StreamListener(observable, initialValue) {
		var _this = this;
		runInAction(function() {
			_this.current = initialValue;
			_this.subscription = observable.subscribe(_this);
		});
	}
	StreamListener.prototype.dispose = function() {
		if (this.subscription) this.subscription.unsubscribe();
	};
	StreamListener.prototype.next = function(value) {
		this.current = value;
	};
	StreamListener.prototype.complete = function() {
		this.dispose();
	};
	StreamListener.prototype.error = function(value) {
		this.current = value;
		this.dispose();
	};
	__decorate([observable.ref], StreamListener.prototype, "current", void 0);
	__decorate([action.bound], StreamListener.prototype, "next", null);
	__decorate([action.bound], StreamListener.prototype, "complete", null);
	__decorate([action.bound], StreamListener.prototype, "error", null);
	return StreamListener;
})();
var __assign$3 = function() {
	__assign$3 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$3.apply(this, arguments);
};
var __decorate$1 = function(decorators, target, key, desc) {
	var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
	if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
	else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
	return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var RESERVED_NAMES = [
	"model",
	"reset",
	"submit",
	"isDirty",
	"isPropertyDirty",
	"resetProperty"
];
(function() {
	function ViewModel(model) {
		var _this = this;
		this.model = model;
		this.localValues = observable.map({});
		this.localComputedValues = observable.map({});
		this.isPropertyDirty = function(key) {
			return _this.localValues.has(key);
		};
		invariant(isObservableObject(model), "createViewModel expects an observable object");
		getAllMethodsAndProperties(model).forEach(function(key) {
			if (key === $mobx || key === "__mobxDidRunLazyInitializers") return;
			invariant(RESERVED_NAMES.indexOf(key) === -1, "The propertyname " + key + " is reserved and cannot be used with viewModels");
			if (isComputedProp(model, key)) {
				var derivation = getAdministration(model, key).derivation;
				_this.localComputedValues.set(key, computed(derivation.bind(_this)));
			}
			var descriptor = Object.getOwnPropertyDescriptor(model, key);
			var additionalDescriptor = descriptor ? { enumerable: descriptor.enumerable } : {};
			Object.defineProperty(_this, key, __assign$3(__assign$3({}, additionalDescriptor), {
				configurable: true,
				get: function() {
					if (isComputedProp(model, key)) return _this.localComputedValues.get(key).get();
					if (_this.isPropertyDirty(key)) return _this.localValues.get(key);
					else return _this.model[key];
				},
				set: action(function(value) {
					if (value !== _this.model[key]) _this.localValues.set(key, value);
					else _this.localValues.delete(key);
				})
			}));
		});
	}
	Object.defineProperty(ViewModel.prototype, "isDirty", {
		get: function() {
			return this.localValues.size > 0;
		},
		enumerable: false,
		configurable: true
	});
	Object.defineProperty(ViewModel.prototype, "changedValues", {
		get: function() {
			return this.localValues.toJS();
		},
		enumerable: false,
		configurable: true
	});
	ViewModel.prototype.submit = function() {
		var _this = this;
		keys(this.localValues).forEach(function(key) {
			var source = _this.localValues.get(key);
			var destination = _this.model[key];
			if (isObservableArray(destination)) destination.replace(source);
			else if (isObservableMap(destination)) {
				destination.clear();
				destination.merge(source);
			} else if (!isComputed(source)) _this.model[key] = source;
		});
		this.localValues.clear();
	};
	ViewModel.prototype.reset = function() {
		this.localValues.clear();
	};
	ViewModel.prototype.resetProperty = function(key) {
		this.localValues.delete(key);
	};
	__decorate$1([computed], ViewModel.prototype, "isDirty", null);
	__decorate$1([computed], ViewModel.prototype, "changedValues", null);
	__decorate$1([action.bound], ViewModel.prototype, "submit", null);
	__decorate$1([action.bound], ViewModel.prototype, "reset", null);
	__decorate$1([action.bound], ViewModel.prototype, "resetProperty", null);
	return ViewModel;
})();
function buildPath(entry) {
	if (!entry) return "ROOT";
	var res = [];
	while (entry.parent) {
		res.push(entry.path);
		entry = entry.parent;
	}
	return res.reverse().join("/");
}
function isRecursivelyObservable(thing) {
	return isObservableObject(thing) || isObservableArray(thing) || isObservableMap(thing);
}
/**
* Given an object, deeply observes the given object.
* It is like `observe` from mobx, but applied recursively, including all future children.
*
* Note that the given object cannot ever contain cycles and should be a tree.
*
* As benefit: path and root will be provided in the callback, so the signature of the listener is
* (change, path, root) => void
*
* The returned disposer can be invoked to clean up the listener
*
* deepObserve cannot be used on computed values.
*
* @example
* const disposer = deepObserve(target, (change, path) => {
*    console.dir(change)
* })
*/
function deepObserve(target, listener) {
	var entrySet = /* @__PURE__ */ new WeakMap();
	function genericListener(change) {
		var entry = entrySet.get(change.object);
		processChange(change, entry);
		listener(change, buildPath(entry), target);
	}
	function processChange(change, parent) {
		switch (change.type) {
			case "add":
				observeRecursively(change.newValue, parent, change.name);
				break;
			case "update":
				unobserveRecursively(change.oldValue);
				observeRecursively(change.newValue, parent, change.name || "" + change.index);
				break;
			case "remove":
			case "delete":
				unobserveRecursively(change.oldValue);
				break;
			case "splice":
				change.removed.map(unobserveRecursively);
				change.added.forEach(function(value, idx) {
					return observeRecursively(value, parent, "" + (change.index + idx));
				});
				for (var i = change.index + change.addedCount; i < change.object.length; i++) if (isRecursivelyObservable(change.object[i])) {
					var entry = entrySet.get(change.object[i]);
					if (entry) entry.path = "" + i;
				}
				break;
		}
	}
	function observeRecursively(thing, parent, path) {
		if (isRecursivelyObservable(thing)) {
			var entry = entrySet.get(thing);
			if (entry) {
				if (entry.parent !== parent || entry.path !== path) throw new Error("The same observable object cannot appear twice in the same tree," + (" trying to assign it to '" + buildPath(parent) + "/" + path + "',") + (" but it already exists at '" + buildPath(entry.parent) + "/" + entry.path + "'"));
			} else {
				var entry_1 = {
					parent,
					path,
					dispose: observe(thing, genericListener)
				};
				entrySet.set(thing, entry_1);
				entries(thing).forEach(function(_a) {
					var key = _a[0], value = _a[1];
					return observeRecursively(value, entry_1, key);
				});
			}
		}
	}
	function unobserveRecursively(thing) {
		if (isRecursivelyObservable(thing)) {
			var entry = entrySet.get(thing);
			if (!entry) return;
			entrySet.delete(thing);
			entry.dispose();
			values(thing).forEach(unobserveRecursively);
		}
	}
	observeRecursively(target, void 0, "");
	return function() {
		unobserveRecursively(target);
	};
}
var __extends = (function() {
	var extendStatics = function(d, b) {
		extendStatics = Object.setPrototypeOf || { __proto__: [] } instanceof Array && function(d, b) {
			d.__proto__ = b;
		} || function(d, b) {
			for (var p in b) if (b.hasOwnProperty(p)) d[p] = b[p];
		};
		return extendStatics(d, b);
	};
	return function(d, b) {
		extendStatics(d, b);
		function __() {
			this.constructor = d;
		}
		d.prototype = b === null ? Object.create(b) : (__.prototype = b.prototype, new __());
	};
})();
(function(_super) {
	__extends(ObservableGroupMap, _super);
	function ObservableGroupMap(base, groupBy, _a) {
		var _b = _a === void 0 ? {} : _a, _c = _b.name, name = _c === void 0 ? "ogm" + (Math.random() * 1e3 | 0) : _c, _d = _b.keyToName, keyToName = _d === void 0 ? function(x) {
			return "" + x;
		} : _d;
		var _this = _super.call(this) || this;
		_this._keyToName = keyToName;
		_this._groupBy = groupBy;
		_this._ogmInfoKey = Symbol("ogmInfo" + name);
		_this._base = base;
		for (var i = 0; i < base.length; i++) _this._addItem(base[i]);
		_this._disposeBaseObserver = observe(_this._base, function(change) {
			if ("splice" === change.type) transaction(function() {
				for (var _i = 0, _a = change.removed; _i < _a.length; _i++) {
					var removed = _a[_i];
					_this._removeItem(removed);
				}
				for (var _b = 0, _c = change.added; _b < _c.length; _b++) {
					var added = _c[_b];
					_this._addItem(added);
				}
			});
			else if ("update" === change.type) transaction(function() {
				_this._removeItem(change.oldValue);
				_this._addItem(change.newValue);
			});
			else throw new Error("illegal state");
		});
		return _this;
	}
	ObservableGroupMap.prototype.clear = function() {
		throw new Error("not supported");
	};
	ObservableGroupMap.prototype.delete = function(_key) {
		throw new Error("not supported");
	};
	ObservableGroupMap.prototype.set = function(_key, _value) {
		throw new Error("not supported");
	};
	/**
	* Disposes all observers created during construction and removes state added to base array
	* items.
	*/
	ObservableGroupMap.prototype.dispose = function() {
		this._disposeBaseObserver();
		for (var i = 0; i < this._base.length; i++) {
			var item = this._base[i];
			item[this._ogmInfoKey].reaction();
			delete item[this._ogmInfoKey];
		}
	};
	ObservableGroupMap.prototype._getGroupArr = function(key) {
		var result = _super.prototype.get.call(this, key);
		if (void 0 === result) {
			result = observable([], {
				name: "GroupArray[" + this._keyToName(key) + "]",
				deep: false
			});
			_super.prototype.set.call(this, key, result);
		}
		return result;
	};
	ObservableGroupMap.prototype._removeFromGroupArr = function(key, itemIndex) {
		var arr = _super.prototype.get.call(this, key);
		if (1 === arr.length) _super.prototype.delete.call(this, key);
		else if (itemIndex === arr.length - 1) arr.length--;
		else {
			arr[itemIndex] = arr[arr.length - 1];
			arr[itemIndex][this._ogmInfoKey].groupArrIndex = itemIndex;
			arr.length--;
		}
	};
	ObservableGroupMap.prototype._addItem = function(item) {
		var _this = this;
		var groupByValue = this._groupBy(item);
		var groupArr = this._getGroupArr(groupByValue);
		var value = {
			groupByValue,
			groupArrIndex: groupArr.length,
			reaction: reaction(function() {
				return _this._groupBy(item);
			}, function(newGroupByValue, _r) {
				var grouperItemInfo = item[_this._ogmInfoKey];
				_this._removeFromGroupArr(grouperItemInfo.groupByValue, grouperItemInfo.groupArrIndex);
				var newGroupArr = _this._getGroupArr(newGroupByValue);
				var newGroupArrIndex = newGroupArr.length;
				newGroupArr.push(item);
				grouperItemInfo.groupByValue = newGroupByValue;
				grouperItemInfo.groupArrIndex = newGroupArrIndex;
			})
		};
		Object.defineProperty(item, this._ogmInfoKey, {
			configurable: true,
			enumerable: false,
			value
		});
		groupArr.push(item);
	};
	ObservableGroupMap.prototype._removeItem = function(item) {
		var grouperItemInfo = item[this._ogmInfoKey];
		this._removeFromGroupArr(grouperItemInfo.groupByValue, grouperItemInfo.groupArrIndex);
		grouperItemInfo.reaction();
		delete item[this._ogmInfoKey];
	};
	return ObservableGroupMap;
})(ObservableMap);
/**
* @private
*/
var DeepMapEntry = function() {
	function DeepMapEntry(base, args) {
		this.base = base;
		this.args = args;
		this.closestIdx = 0;
		this.isDisposed = false;
		var current = this.closest = this.root = base;
		var i = 0;
		for (; i < this.args.length - 1; i++) {
			current = current.get(args[i]);
			if (current) this.closest = current;
			else break;
		}
		this.closestIdx = i;
	}
	DeepMapEntry.prototype.exists = function() {
		this.assertNotDisposed();
		var l = this.args.length;
		return this.closestIdx >= l - 1 && this.closest.has(this.args[l - 1]);
	};
	DeepMapEntry.prototype.get = function() {
		this.assertNotDisposed();
		if (!this.exists()) throw new Error("Entry doesn't exist");
		return this.closest.get(this.args[this.args.length - 1]);
	};
	DeepMapEntry.prototype.set = function(value) {
		this.assertNotDisposed();
		var l = this.args.length;
		var current = this.closest;
		for (var i = this.closestIdx; i < l - 1; i++) {
			var m = /* @__PURE__ */ new Map();
			current.set(this.args[i], m);
			current = m;
		}
		this.closestIdx = l - 1;
		this.closest = current;
		current.set(this.args[l - 1], value);
	};
	DeepMapEntry.prototype.delete = function() {
		this.assertNotDisposed();
		if (!this.exists()) throw new Error("Entry doesn't exist");
		var l = this.args.length;
		this.closest.delete(this.args[l - 1]);
		var c = this.root;
		var maps = [c];
		for (var i = 0; i < l - 1; i++) {
			c = c.get(this.args[i]);
			maps.push(c);
		}
		for (var i = maps.length - 1; i > 0; i--) if (maps[i].size === 0) maps[i - 1].delete(this.args[i - 1]);
		this.isDisposed = true;
	};
	DeepMapEntry.prototype.assertNotDisposed = function() {
		if (this.isDisposed) throw new Error("Concurrent modification exception");
	};
	return DeepMapEntry;
}();
(function() {
	function DeepMap() {
		this.store = /* @__PURE__ */ new Map();
		this.argsLength = -1;
	}
	DeepMap.prototype.entry = function(args) {
		if (this.argsLength === -1) this.argsLength = args.length;
		else if (this.argsLength !== args.length) throw new Error("DeepMap should be used with functions with a consistent length, expected: " + this.argsLength + ", got: " + args.length);
		if (this.last) this.last.isDisposed = true;
		return this.last = new DeepMapEntry(this.store, args);
	};
	return DeepMap;
})();
Promise.resolve();
if (typeof queueMicrotask !== "undefined");
else if (typeof process !== "undefined" && process.nextTick);
//#endregion
//#region node_modules/@logicflow/core/es/history/index.js
var __assign$2 = function() {
	__assign$2 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$2.apply(this, arguments);
};
var History = function() {
	function History(eventCenter) {
		this.undos = [];
		this.redos = [];
		this.callbacks = [];
		this.stopWatch = null;
		this.curData = null;
		this.maxSize = 50;
		this.waitTime = 100;
		this.eventCenter = eventCenter;
	}
	History.prototype.add = function(data) {
		if (isEqual(last(this.undos), data)) return;
		this.undos.push(data);
		if (!isEqual(this.curData, data)) this.redos = [];
		this.eventCenter.emit(EventType.HISTORY_CHANGE, { data: {
			undos: this.undos,
			redos: this.redos,
			undoAble: this.undoAble(),
			redoAble: this.redoAble()
		} });
		if (this.undos.length > this.maxSize) this.undos.shift();
	};
	History.prototype.undoAble = function() {
		return this.undos.length > 1;
	};
	History.prototype.undo = function() {
		if (!this.undoAble()) return;
		var preData = this.undos.pop();
		this.redos.push(preData);
		var curData = this.undos.pop();
		this.curData = cloneDeep(curData);
		return curData;
	};
	History.prototype.redoAble = function() {
		return this.redos.length > 0;
	};
	History.prototype.redo = function() {
		if (!this.redoAble()) return;
		var curData = this.redos.pop();
		this.curData = cloneDeep(curData);
		return curData;
	};
	History.prototype.watch = function(model) {
		var _this = this;
		this.stopWatch && this.stopWatch();
		this.undos.push(model.modelToGraphData());
		this.stopWatch = deepObserve(model, debounce(function() {
			var data = model.modelToHistoryData();
			if (data) _this.add(__assign$2({}, data));
		}, this.waitTime));
	};
	History.prototype.destroy = function() {
		this.undos = [];
		this.redos = [];
		this.curData = null;
		this.stopWatch && this.stopWatch();
	};
	return History;
}();
//#endregion
//#region node_modules/mousetrap/mousetrap.js
var require_mousetrap = /* @__PURE__ */ __commonJSMin(((exports, module) => {
	/**
	* Copyright 2012-2017 Craig Campbell
	*
	* Licensed under the Apache License, Version 2.0 (the "License");
	* you may not use this file except in compliance with the License.
	* You may obtain a copy of the License at
	*
	* http://www.apache.org/licenses/LICENSE-2.0
	*
	* Unless required by applicable law or agreed to in writing, software
	* distributed under the License is distributed on an "AS IS" BASIS,
	* WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
	* See the License for the specific language governing permissions and
	* limitations under the License.
	*
	* Mousetrap is a simple keyboard shortcut library for Javascript with
	* no external dependencies
	*
	* @version 1.6.5
	* @url craig.is/killing/mice
	*/
	(function(window, document, undefined) {
		if (!window) return;
		/**
		* mapping of special keycodes to their corresponding keys
		*
		* everything in this dictionary cannot use keypress events
		* so it has to be here to map to the correct keycodes for
		* keyup/keydown events
		*
		* @type {Object}
		*/
		var _MAP = {
			8: "backspace",
			9: "tab",
			13: "enter",
			16: "shift",
			17: "ctrl",
			18: "alt",
			20: "capslock",
			27: "esc",
			32: "space",
			33: "pageup",
			34: "pagedown",
			35: "end",
			36: "home",
			37: "left",
			38: "up",
			39: "right",
			40: "down",
			45: "ins",
			46: "del",
			91: "meta",
			93: "meta",
			224: "meta"
		};
		/**
		* mapping for special characters so they can support
		*
		* this dictionary is only used incase you want to bind a
		* keyup or keydown event to one of these keys
		*
		* @type {Object}
		*/
		var _KEYCODE_MAP = {
			106: "*",
			107: "+",
			109: "-",
			110: ".",
			111: "/",
			186: ";",
			187: "=",
			188: ",",
			189: "-",
			190: ".",
			191: "/",
			192: "`",
			219: "[",
			220: "\\",
			221: "]",
			222: "'"
		};
		/**
		* this is a mapping of keys that require shift on a US keypad
		* back to the non shift equivelents
		*
		* this is so you can use keyup events with these keys
		*
		* note that this will only work reliably on US keyboards
		*
		* @type {Object}
		*/
		var _SHIFT_MAP = {
			"~": "`",
			"!": "1",
			"@": "2",
			"#": "3",
			"$": "4",
			"%": "5",
			"^": "6",
			"&": "7",
			"*": "8",
			"(": "9",
			")": "0",
			"_": "-",
			"+": "=",
			":": ";",
			"\"": "'",
			"<": ",",
			">": ".",
			"?": "/",
			"|": "\\"
		};
		/**
		* this is a list of special strings you can use to map
		* to modifier keys when you specify your keyboard shortcuts
		*
		* @type {Object}
		*/
		var _SPECIAL_ALIASES = {
			"option": "alt",
			"command": "meta",
			"return": "enter",
			"escape": "esc",
			"plus": "+",
			"mod": /Mac|iPod|iPhone|iPad/.test(navigator.platform) ? "meta" : "ctrl"
		};
		/**
		* variable to store the flipped version of _MAP from above
		* needed to check if we should use keypress or not when no action
		* is specified
		*
		* @type {Object|undefined}
		*/
		var _REVERSE_MAP;
		/**
		* loop through the f keys, f1 to f19 and add them to the map
		* programatically
		*/
		for (var i = 1; i < 20; ++i) _MAP[111 + i] = "f" + i;
		/**
		* loop through to map numbers on the numeric keypad
		*/
		for (i = 0; i <= 9; ++i) _MAP[i + 96] = i.toString();
		/**
		* cross browser add event method
		*
		* @param {Element|HTMLDocument} object
		* @param {string} type
		* @param {Function} callback
		* @returns void
		*/
		function _addEvent(object, type, callback) {
			if (object.addEventListener) {
				object.addEventListener(type, callback, false);
				return;
			}
			object.attachEvent("on" + type, callback);
		}
		/**
		* takes the event and returns the key character
		*
		* @param {Event} e
		* @return {string}
		*/
		function _characterFromEvent(e) {
			if (e.type == "keypress") {
				var character = String.fromCharCode(e.which);
				if (!e.shiftKey) character = character.toLowerCase();
				return character;
			}
			if (_MAP[e.which]) return _MAP[e.which];
			if (_KEYCODE_MAP[e.which]) return _KEYCODE_MAP[e.which];
			return String.fromCharCode(e.which).toLowerCase();
		}
		/**
		* checks if two arrays are equal
		*
		* @param {Array} modifiers1
		* @param {Array} modifiers2
		* @returns {boolean}
		*/
		function _modifiersMatch(modifiers1, modifiers2) {
			return modifiers1.sort().join(",") === modifiers2.sort().join(",");
		}
		/**
		* takes a key event and figures out what the modifiers are
		*
		* @param {Event} e
		* @returns {Array}
		*/
		function _eventModifiers(e) {
			var modifiers = [];
			if (e.shiftKey) modifiers.push("shift");
			if (e.altKey) modifiers.push("alt");
			if (e.ctrlKey) modifiers.push("ctrl");
			if (e.metaKey) modifiers.push("meta");
			return modifiers;
		}
		/**
		* prevents default for this event
		*
		* @param {Event} e
		* @returns void
		*/
		function _preventDefault(e) {
			if (e.preventDefault) {
				e.preventDefault();
				return;
			}
			e.returnValue = false;
		}
		/**
		* stops propogation for this event
		*
		* @param {Event} e
		* @returns void
		*/
		function _stopPropagation(e) {
			if (e.stopPropagation) {
				e.stopPropagation();
				return;
			}
			e.cancelBubble = true;
		}
		/**
		* determines if the keycode specified is a modifier key or not
		*
		* @param {string} key
		* @returns {boolean}
		*/
		function _isModifier(key) {
			return key == "shift" || key == "ctrl" || key == "alt" || key == "meta";
		}
		/**
		* reverses the map lookup so that we can look for specific keys
		* to see what can and can't use keypress
		*
		* @return {Object}
		*/
		function _getReverseMap() {
			if (!_REVERSE_MAP) {
				_REVERSE_MAP = {};
				for (var key in _MAP) {
					if (key > 95 && key < 112) continue;
					if (_MAP.hasOwnProperty(key)) _REVERSE_MAP[_MAP[key]] = key;
				}
			}
			return _REVERSE_MAP;
		}
		/**
		* picks the best action based on the key combination
		*
		* @param {string} key - character for key
		* @param {Array} modifiers
		* @param {string=} action passed in
		*/
		function _pickBestAction(key, modifiers, action) {
			if (!action) action = _getReverseMap()[key] ? "keydown" : "keypress";
			if (action == "keypress" && modifiers.length) action = "keydown";
			return action;
		}
		/**
		* Converts from a string key combination to an array
		*
		* @param  {string} combination like "command+shift+l"
		* @return {Array}
		*/
		function _keysFromString(combination) {
			if (combination === "+") return ["+"];
			combination = combination.replace(/\+{2}/g, "+plus");
			return combination.split("+");
		}
		/**
		* Gets info for a specific key combination
		*
		* @param  {string} combination key combination ("command+s" or "a" or "*")
		* @param  {string=} action
		* @returns {Object}
		*/
		function _getKeyInfo(combination, action) {
			var keys;
			var key;
			var i;
			var modifiers = [];
			keys = _keysFromString(combination);
			for (i = 0; i < keys.length; ++i) {
				key = keys[i];
				if (_SPECIAL_ALIASES[key]) key = _SPECIAL_ALIASES[key];
				if (action && action != "keypress" && _SHIFT_MAP[key]) {
					key = _SHIFT_MAP[key];
					modifiers.push("shift");
				}
				if (_isModifier(key)) modifiers.push(key);
			}
			action = _pickBestAction(key, modifiers, action);
			return {
				key,
				modifiers,
				action
			};
		}
		function _belongsTo(element, ancestor) {
			if (element === null || element === document) return false;
			if (element === ancestor) return true;
			return _belongsTo(element.parentNode, ancestor);
		}
		function Mousetrap(targetElement) {
			var self = this;
			targetElement = targetElement || document;
			if (!(self instanceof Mousetrap)) return new Mousetrap(targetElement);
			/**
			* element to attach key events to
			*
			* @type {Element}
			*/
			self.target = targetElement;
			/**
			* a list of all the callbacks setup via Mousetrap.bind()
			*
			* @type {Object}
			*/
			self._callbacks = {};
			/**
			* direct map of string combinations to callbacks used for trigger()
			*
			* @type {Object}
			*/
			self._directMap = {};
			/**
			* keeps track of what level each sequence is at since multiple
			* sequences can start out with the same sequence
			*
			* @type {Object}
			*/
			var _sequenceLevels = {};
			/**
			* variable to store the setTimeout call
			*
			* @type {null|number}
			*/
			var _resetTimer;
			/**
			* temporary state where we will ignore the next keyup
			*
			* @type {boolean|string}
			*/
			var _ignoreNextKeyup = false;
			/**
			* temporary state where we will ignore the next keypress
			*
			* @type {boolean}
			*/
			var _ignoreNextKeypress = false;
			/**
			* are we currently inside of a sequence?
			* type of action ("keyup" or "keydown" or "keypress") or false
			*
			* @type {boolean|string}
			*/
			var _nextExpectedAction = false;
			/**
			* resets all sequence counters except for the ones passed in
			*
			* @param {Object} doNotReset
			* @returns void
			*/
			function _resetSequences(doNotReset) {
				doNotReset = doNotReset || {};
				var activeSequences = false, key;
				for (key in _sequenceLevels) {
					if (doNotReset[key]) {
						activeSequences = true;
						continue;
					}
					_sequenceLevels[key] = 0;
				}
				if (!activeSequences) _nextExpectedAction = false;
			}
			/**
			* finds all callbacks that match based on the keycode, modifiers,
			* and action
			*
			* @param {string} character
			* @param {Array} modifiers
			* @param {Event|Object} e
			* @param {string=} sequenceName - name of the sequence we are looking for
			* @param {string=} combination
			* @param {number=} level
			* @returns {Array}
			*/
			function _getMatches(character, modifiers, e, sequenceName, combination, level) {
				var i;
				var callback;
				var matches = [];
				var action = e.type;
				if (!self._callbacks[character]) return [];
				if (action == "keyup" && _isModifier(character)) modifiers = [character];
				for (i = 0; i < self._callbacks[character].length; ++i) {
					callback = self._callbacks[character][i];
					if (!sequenceName && callback.seq && _sequenceLevels[callback.seq] != callback.level) continue;
					if (action != callback.action) continue;
					if (action == "keypress" && !e.metaKey && !e.ctrlKey || _modifiersMatch(modifiers, callback.modifiers)) {
						var deleteCombo = !sequenceName && callback.combo == combination;
						var deleteSequence = sequenceName && callback.seq == sequenceName && callback.level == level;
						if (deleteCombo || deleteSequence) self._callbacks[character].splice(i, 1);
						matches.push(callback);
					}
				}
				return matches;
			}
			/**
			* actually calls the callback function
			*
			* if your callback function returns false this will use the jquery
			* convention - prevent default and stop propogation on the event
			*
			* @param {Function} callback
			* @param {Event} e
			* @returns void
			*/
			function _fireCallback(callback, e, combo, sequence) {
				if (self.stopCallback(e, e.target || e.srcElement, combo, sequence)) return;
				if (callback(e, combo) === false) {
					_preventDefault(e);
					_stopPropagation(e);
				}
			}
			/**
			* handles a character key event
			*
			* @param {string} character
			* @param {Array} modifiers
			* @param {Event} e
			* @returns void
			*/
			self._handleKey = function(character, modifiers, e) {
				var callbacks = _getMatches(character, modifiers, e);
				var i;
				var doNotReset = {};
				var maxLevel = 0;
				var processedSequenceCallback = false;
				for (i = 0; i < callbacks.length; ++i) if (callbacks[i].seq) maxLevel = Math.max(maxLevel, callbacks[i].level);
				for (i = 0; i < callbacks.length; ++i) {
					if (callbacks[i].seq) {
						if (callbacks[i].level != maxLevel) continue;
						processedSequenceCallback = true;
						doNotReset[callbacks[i].seq] = 1;
						_fireCallback(callbacks[i].callback, e, callbacks[i].combo, callbacks[i].seq);
						continue;
					}
					if (!processedSequenceCallback) _fireCallback(callbacks[i].callback, e, callbacks[i].combo);
				}
				var ignoreThisKeypress = e.type == "keypress" && _ignoreNextKeypress;
				if (e.type == _nextExpectedAction && !_isModifier(character) && !ignoreThisKeypress) _resetSequences(doNotReset);
				_ignoreNextKeypress = processedSequenceCallback && e.type == "keydown";
			};
			/**
			* handles a keydown event
			*
			* @param {Event} e
			* @returns void
			*/
			function _handleKeyEvent(e) {
				if (typeof e.which !== "number") e.which = e.keyCode;
				var character = _characterFromEvent(e);
				if (!character) return;
				if (e.type == "keyup" && _ignoreNextKeyup === character) {
					_ignoreNextKeyup = false;
					return;
				}
				self.handleKey(character, _eventModifiers(e), e);
			}
			/**
			* called to set a 1 second timeout on the specified sequence
			*
			* this is so after each key press in the sequence you have 1 second
			* to press the next key before you have to start over
			*
			* @returns void
			*/
			function _resetSequenceTimer() {
				clearTimeout(_resetTimer);
				_resetTimer = setTimeout(_resetSequences, 1e3);
			}
			/**
			* binds a key sequence to an event
			*
			* @param {string} combo - combo specified in bind call
			* @param {Array} keys
			* @param {Function} callback
			* @param {string=} action
			* @returns void
			*/
			function _bindSequence(combo, keys, callback, action) {
				_sequenceLevels[combo] = 0;
				/**
				* callback to increase the sequence level for this sequence and reset
				* all other sequences that were active
				*
				* @param {string} nextAction
				* @returns {Function}
				*/
				function _increaseSequence(nextAction) {
					return function() {
						_nextExpectedAction = nextAction;
						++_sequenceLevels[combo];
						_resetSequenceTimer();
					};
				}
				/**
				* wraps the specified callback inside of another function in order
				* to reset all sequence counters as soon as this sequence is done
				*
				* @param {Event} e
				* @returns void
				*/
				function _callbackAndReset(e) {
					_fireCallback(callback, e, combo);
					if (action !== "keyup") _ignoreNextKeyup = _characterFromEvent(e);
					setTimeout(_resetSequences, 10);
				}
				for (var i = 0; i < keys.length; ++i) {
					var wrappedCallback = i + 1 === keys.length ? _callbackAndReset : _increaseSequence(action || _getKeyInfo(keys[i + 1]).action);
					_bindSingle(keys[i], wrappedCallback, action, combo, i);
				}
			}
			/**
			* binds a single keyboard combination
			*
			* @param {string} combination
			* @param {Function} callback
			* @param {string=} action
			* @param {string=} sequenceName - name of sequence if part of sequence
			* @param {number=} level - what part of the sequence the command is
			* @returns void
			*/
			function _bindSingle(combination, callback, action, sequenceName, level) {
				self._directMap[combination + ":" + action] = callback;
				combination = combination.replace(/\s+/g, " ");
				var sequence = combination.split(" ");
				var info;
				if (sequence.length > 1) {
					_bindSequence(combination, sequence, callback, action);
					return;
				}
				info = _getKeyInfo(combination, action);
				self._callbacks[info.key] = self._callbacks[info.key] || [];
				_getMatches(info.key, info.modifiers, { type: info.action }, sequenceName, combination, level);
				self._callbacks[info.key][sequenceName ? "unshift" : "push"]({
					callback,
					modifiers: info.modifiers,
					action: info.action,
					seq: sequenceName,
					level,
					combo: combination
				});
			}
			/**
			* binds multiple combinations to the same callback
			*
			* @param {Array} combinations
			* @param {Function} callback
			* @param {string|undefined} action
			* @returns void
			*/
			self._bindMultiple = function(combinations, callback, action) {
				for (var i = 0; i < combinations.length; ++i) _bindSingle(combinations[i], callback, action);
			};
			_addEvent(targetElement, "keypress", _handleKeyEvent);
			_addEvent(targetElement, "keydown", _handleKeyEvent);
			_addEvent(targetElement, "keyup", _handleKeyEvent);
		}
		/**
		* binds an event to mousetrap
		*
		* can be a single key, a combination of keys separated with +,
		* an array of keys, or a sequence of keys separated by spaces
		*
		* be sure to list the modifier keys first to make sure that the
		* correct key ends up getting bound (the last key in the pattern)
		*
		* @param {string|Array} keys
		* @param {Function} callback
		* @param {string=} action - 'keypress', 'keydown', or 'keyup'
		* @returns void
		*/
		Mousetrap.prototype.bind = function(keys, callback, action) {
			var self = this;
			keys = keys instanceof Array ? keys : [keys];
			self._bindMultiple.call(self, keys, callback, action);
			return self;
		};
		/**
		* unbinds an event to mousetrap
		*
		* the unbinding sets the callback function of the specified key combo
		* to an empty function and deletes the corresponding key in the
		* _directMap dict.
		*
		* TODO: actually remove this from the _callbacks dictionary instead
		* of binding an empty function
		*
		* the keycombo+action has to be exactly the same as
		* it was defined in the bind method
		*
		* @param {string|Array} keys
		* @param {string} action
		* @returns void
		*/
		Mousetrap.prototype.unbind = function(keys, action) {
			var self = this;
			return self.bind.call(self, keys, function() {}, action);
		};
		/**
		* triggers an event that has already been bound
		*
		* @param {string} keys
		* @param {string=} action
		* @returns void
		*/
		Mousetrap.prototype.trigger = function(keys, action) {
			var self = this;
			if (self._directMap[keys + ":" + action]) self._directMap[keys + ":" + action]({}, keys);
			return self;
		};
		/**
		* resets the library back to its initial state.  this is useful
		* if you want to clear out the current keyboard shortcuts and bind
		* new ones - for example if you switch to another page
		*
		* @returns void
		*/
		Mousetrap.prototype.reset = function() {
			var self = this;
			self._callbacks = {};
			self._directMap = {};
			return self;
		};
		/**
		* should we stop this event before firing off callbacks
		*
		* @param {Event} e
		* @param {Element} element
		* @return {boolean}
		*/
		Mousetrap.prototype.stopCallback = function(e, element) {
			var self = this;
			if ((" " + element.className + " ").indexOf(" mousetrap ") > -1) return false;
			if (_belongsTo(element, self.target)) return false;
			if ("composedPath" in e && typeof e.composedPath === "function") {
				var initialEventTarget = e.composedPath()[0];
				if (initialEventTarget !== e.target) element = initialEventTarget;
			}
			return element.tagName == "INPUT" || element.tagName == "SELECT" || element.tagName == "TEXTAREA" || element.isContentEditable;
		};
		/**
		* exposes _handleKey publicly so it can be overwritten by extensions
		*/
		Mousetrap.prototype.handleKey = function() {
			var self = this;
			return self._handleKey.apply(self, arguments);
		};
		/**
		* allow custom key mappings
		*/
		Mousetrap.addKeycodes = function(object) {
			for (var key in object) if (object.hasOwnProperty(key)) _MAP[key] = object[key];
			_REVERSE_MAP = null;
		};
		/**
		* Init the global mousetrap functions
		*
		* This method is needed to allow the global mousetrap functions to work
		* now that mousetrap is a constructor function.
		*/
		Mousetrap.init = function() {
			var documentMousetrap = Mousetrap(document);
			for (var method in documentMousetrap) if (method.charAt(0) !== "_") Mousetrap[method] = function(method) {
				return function() {
					return documentMousetrap[method].apply(documentMousetrap, arguments);
				};
			}(method);
		};
		Mousetrap.init();
		window.Mousetrap = Mousetrap;
		if (typeof module !== "undefined" && module.exports) module.exports = Mousetrap;
		if (typeof define === "function" && define.amd) define(function() {
			return Mousetrap;
		});
	})(typeof window !== "undefined" ? window : null, typeof window !== "undefined" ? document : null);
}));
//#endregion
//#region node_modules/@logicflow/core/es/keyboard/shortcut.js
var __assign$1 = function() {
	__assign$1 = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign$1.apply(this, arguments);
};
var __rest = function(s, e) {
	var t = {};
	for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0) t[p] = s[p];
	if (s != null && typeof Object.getOwnPropertySymbols === "function") {
		for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i])) t[p[i]] = s[p[i]];
	}
	return t;
};
var selected = null;
function translateNodeData(nodeData, distance) {
	var _a, _b;
	nodeData.x += distance;
	nodeData.y += distance;
	if (!isEmpty(nodeData.text)) {
		nodeData.text.x += distance;
		nodeData.text.y += distance;
	}
	if (!isEmpty((_a = nodeData.properties) === null || _a === void 0 ? void 0 : _a._label)) (_b = nodeData.properties) === null || _b === void 0 || _b._label.forEach(function(label) {
		label.x += distance;
		label.y += distance;
	});
	return nodeData;
}
function translateEdgeData(edgeData, distance) {
	if (edgeData.startPoint) {
		edgeData.startPoint.x += distance;
		edgeData.startPoint.y += distance;
	}
	if (edgeData.endPoint) {
		edgeData.endPoint.x += distance;
		edgeData.endPoint.y += distance;
	}
	if (edgeData.pointsList && edgeData.pointsList.length > 0) edgeData.pointsList.forEach(function(point) {
		point.x += distance;
		point.y += distance;
	});
	if (!isEmpty(edgeData.text)) {
		edgeData.text.x += distance;
		edgeData.text.y += distance;
	}
	return edgeData;
}
function transformNodeData(nodeData, distance) {
	var x = nodeData.x, y = nodeData.y, text = nodeData.text;
	var nextText = text ? {
		x: text.x + distance,
		y: text.y + distance,
		value: text.value
	} : void 0;
	return __assign$1(__assign$1({}, nodeData), {
		id: "",
		x: x + distance,
		y: y + distance,
		text: nextText
	});
}
function transformEdgeData(edgeData, distance) {
	var startPoint = edgeData.startPoint, endPoint = edgeData.endPoint, pointsList = edgeData.pointsList, text = edgeData.text, edgeConfig = __rest(edgeData, [
		"startPoint",
		"endPoint",
		"pointsList",
		"text"
	]);
	edgeConfig.id = "";
	var nextStartPoint = {
		x: startPoint.x + distance,
		y: startPoint.y + distance
	};
	var nextEndPoint = {
		x: endPoint.x + distance,
		y: endPoint.y + distance
	};
	var newPointsList = map(pointsList, function(point) {
		return {
			x: point.x + distance,
			y: point.y + distance
		};
	});
	var nextText = text ? __assign$1(__assign$1({}, text), {
		x: text.x + distance,
		y: text.y + distance
	}) : void 0;
	return __assign$1(__assign$1({}, edgeConfig), {
		startPoint: nextStartPoint,
		endPoint: nextEndPoint,
		pointsList: newPointsList,
		text: nextText
	});
}
var TRANSLATION_DISTANCE = 40;
var CHILDREN_TRANSLATION_DISTANCE = 40;
function initDefaultShortcut(lf, graph) {
	var keyboard = lf.keyboard;
	var keyboardOptions = keyboard.options.keyboard;
	keyboard.on(["cmd + c", "ctrl + c"], function() {
		CHILDREN_TRANSLATION_DISTANCE = TRANSLATION_DISTANCE;
		if (!(keyboardOptions === null || keyboardOptions === void 0 ? void 0 : keyboardOptions.enabled)) return true;
		if (graph.textEditElement) return true;
		var guards = lf.options.guards;
		var elements = graph.getSelectElements(false);
		if (!(guards && guards.beforeClone ? guards.beforeClone(elements) : true) || elements.nodes.length === 0 && elements.edges.length === 0) {
			selected = null;
			return true;
		}
		selected = elements;
		selected.nodes.forEach(function(node) {
			return translateNodeData(node, TRANSLATION_DISTANCE);
		});
		selected.edges.forEach(function(edge) {
			return translateEdgeData(edge, TRANSLATION_DISTANCE);
		});
		return false;
	});
	keyboard.on(["cmd + v", "ctrl + v"], function() {
		if (!(keyboardOptions === null || keyboardOptions === void 0 ? void 0 : keyboardOptions.enabled)) return true;
		if (graph.textEditElement) return true;
		if (selected && (selected.nodes || selected.edges)) {
			lf.clearSelectElements();
			var addElements = lf.addElements(selected, CHILDREN_TRANSLATION_DISTANCE);
			if (!addElements) return true;
			addElements.nodes.forEach(function(node) {
				return lf.selectElementById(node.id, true);
			});
			addElements.edges.forEach(function(edge) {
				return lf.selectElementById(edge.id, true);
			});
			selected.nodes.forEach(function(node) {
				return translateNodeData(node, TRANSLATION_DISTANCE);
			});
			selected.edges.forEach(function(edge) {
				return translateEdgeData(edge, TRANSLATION_DISTANCE);
			});
			CHILDREN_TRANSLATION_DISTANCE = CHILDREN_TRANSLATION_DISTANCE + TRANSLATION_DISTANCE;
		}
		return false;
	});
	keyboard.on(["cmd + z", "ctrl + z"], function() {
		if (!(keyboardOptions === null || keyboardOptions === void 0 ? void 0 : keyboardOptions.enabled)) return true;
		if (graph.textEditElement) return true;
		lf.undo();
		return false;
	});
	keyboard.on(["cmd + y", "ctrl + y"], function() {
		if (!(keyboardOptions === null || keyboardOptions === void 0 ? void 0 : keyboardOptions.enabled)) return true;
		if (graph.textEditElement) return true;
		lf.redo();
		return false;
	});
	keyboard.on(["backspace"], function() {
		if (!(keyboardOptions === null || keyboardOptions === void 0 ? void 0 : keyboardOptions.enabled)) return true;
		if (graph.textEditElement) return true;
		var elements = graph.getSelectElements(true);
		lf.clearSelectElements();
		elements.edges.forEach(function(edge) {
			return edge.id && lf.deleteEdge(edge.id);
		});
		elements.nodes.forEach(function(node) {
			return node.id && lf.deleteNode(node.id);
		});
		return false;
	});
}
//#endregion
//#region node_modules/@logicflow/core/es/keyboard/index.js
var import_mousetrap = /* @__PURE__ */ __toESM(require_mousetrap());
var Keyboard = function() {
	function Keyboard(options) {
		if (!options.keyboard) options.keyboard = { enabled: false };
		this.options = options;
		var lf = options.lf;
		this.target = lf.container;
		this.mousetrap = new import_mousetrap.default(this.target);
		if (options.keyboard.enabled && !lf.options.isSilentMode) this.enable(true);
	}
	Keyboard.prototype.initShortcuts = function() {
		var _this = this;
		var _a;
		var shortcuts = ((_a = this.options.keyboard) !== null && _a !== void 0 ? _a : {}).shortcuts;
		if (shortcuts) if (isArray(shortcuts)) shortcuts.forEach(function(_a) {
			var keys = _a.keys, callback = _a.callback, action = _a.action;
			return _this.on(keys, callback, action);
		});
		else {
			var keys = shortcuts.keys, callback = shortcuts.callback, action = shortcuts.action;
			this.on(keys, callback, action);
		}
	};
	Keyboard.prototype.on = function(keys, callback, action) {
		this.mousetrap.bind(this.getKeys(keys), callback, action);
	};
	Object.defineProperty(Keyboard.prototype, "disabled", {
		get: function() {
			var _a, _b;
			return ((_b = (_a = this.options) === null || _a === void 0 ? void 0 : _a.keyboard) === null || _b === void 0 ? void 0 : _b.enabled) !== true;
		},
		enumerable: false,
		configurable: true
	});
	Keyboard.prototype.off = function(keys, action) {
		this.mousetrap.unbind(this.getKeys(keys), action);
	};
	Keyboard.prototype.enable = function(force) {
		if (this.disabled || force) {
			if (this.options.keyboard) this.options.keyboard.enabled = true;
			if (this.target instanceof HTMLElement) {
				this.target.setAttribute("tabindex", "-1");
				this.target.style.outline = "none";
			}
		}
	};
	Keyboard.prototype.disable = function() {
		if (!this.disabled) {
			if (this.options.keyboard) this.options.keyboard.enabled = false;
			if (this.target instanceof HTMLElement) this.target.removeAttribute("tabindex");
		}
	};
	Keyboard.prototype.destroy = function() {
		this.mousetrap.reset();
	};
	Keyboard.prototype.getKeys = function(keys) {
		var _this = this;
		return (Array.isArray(keys) ? keys : [keys]).map(function(key) {
			return _this.formatKey(key);
		});
	};
	Keyboard.prototype.formatKey = function(key) {
		return key.toLowerCase().replace(/\s/g, "").replace("delete", "del").replace("cmd", "command");
	};
	return Keyboard;
}();
//#endregion
//#region node_modules/@logicflow/core/es/LogicFlow.js
var __assign = function() {
	__assign = Object.assign || function(t) {
		for (var s, i = 1, n = arguments.length; i < n; i++) {
			s = arguments[i];
			for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t[p] = s[p];
		}
		return t;
	};
	return __assign.apply(this, arguments);
};
var __read = function(o, n) {
	var m = typeof Symbol === "function" && o[Symbol.iterator];
	if (!m) return o;
	var i = m.call(o), r, ar = [], e;
	try {
		while ((n === void 0 || n-- > 0) && !(r = i.next()).done) ar.push(r.value);
	} catch (error) {
		e = { error };
	} finally {
		try {
			if (r && !r.done && (m = i["return"])) m.call(i);
		} finally {
			if (e) throw e.error;
		}
	}
	return ar;
};
var __spreadArray = function(to, from, pack) {
	if (pack || arguments.length === 2) {
		for (var i = 0, l = from.length, ar; i < l; i++) if (ar || !(i in from)) {
			if (!ar) ar = Array.prototype.slice.call(from, 0, i);
			ar[i] = from[i];
		}
	}
	return to.concat(ar || Array.prototype.slice.call(from));
};
var pluginFlag = Symbol("plugin registered by Logicflow.use");
var LogicFlow = function() {
	function LogicFlow(options) {
		var _this = this;
		var _a;
		this.viewMap = /* @__PURE__ */ new Map();
		this.components = [];
		this.extension = {};
		/*********************************************************
		* Register 相关
		********************************************************/
		this.setView = function(type, component) {
			return _this.viewMap.set(type, component);
		};
		this.getView = function(type) {
			return _this.viewMap.get(type);
		};
		var initOptions = Options.get(options);
		var container = initOptions.container, width = initOptions.width, height = initOptions.height;
		this.options = initOptions;
		this.container = this.initContainer(container, width, height);
		this.graphModel = new GraphModel(__assign(__assign({}, initOptions), { container: this.container }));
		this.plugins = (_a = initOptions.plugins) !== null && _a !== void 0 ? _a : [];
		var eventCenter = this.graphModel.eventCenter;
		this.tool = new Tool(this);
		this.dnd = new Dnd({ lf: this });
		this.history = new History(eventCenter);
		this.keyboard = new Keyboard({
			lf: this,
			keyboard: initOptions.keyboard
		});
		if (initOptions.snapline !== false) {
			this.snaplineModel = new SnaplineModel(this.graphModel, initOptions.snaplineEpsilon);
			snapline(eventCenter, this.snaplineModel);
		}
		if (!initOptions.isSilentMode) {
			initDefaultShortcut(this, this.graphModel);
			this.keyboard.initShortcuts();
		}
		this.defaultRegister();
		this.installPlugins(initOptions.disabledPlugins);
	}
	LogicFlow.prototype.initContainer = function(container, width, height) {
		var lfContainer = document.createElement("div");
		lfContainer.style.position = "relative";
		lfContainer.style.width = width ? "".concat(width, "px") : "100%";
		lfContainer.style.height = height ? "".concat(height, "px") : "100%";
		container.innerHTML = "";
		container.appendChild(lfContainer);
		return lfContainer;
	};
	Object.defineProperty(LogicFlow.prototype, Symbol.toStringTag, {
		get: function() {
			return LogicFlow.toStringTag;
		},
		enumerable: false,
		configurable: true
	});
	/**
	* 注册自定义节点和边
	* 支持两种方式
	* 方式一（推荐）
	* 详情见 todo: docs link
	* @example
	* import { RectNode, RectModel } from '@logicflow/core'
	* class CustomView extends RectNode {
	* }
	* class CustomModel extends RectModel {
	* }
	* lf.register({
	*   type: 'custom',
	*   view: CustomView,
	*   model: CustomModel
	* })
	* 方式二
	* 不推荐，极个别在自定义的时候需要用到lf的情况下可以用这种方式。
	* 大多数情况下，我们可以直接在view中从this.props中获取graphModel
	* 或者model中直接this.graphModel获取model的方法。
	* @example
	* lf.register('custom', ({ RectNode, RectModel }) => {
	*    class CustomView extends RectNode {}
	*    class CustomModel extends RectModel {}
	*    return {
	*      view: CustomView,
	*      model: CustomModel
	*    }
	* })
	*/
	LogicFlow.prototype.register = function(element, fn, isObserverView) {
		if (isObserverView === void 0) isObserverView = true;
		if (typeof element !== "string") {
			this.registerElement(element);
			return;
		}
		var registerParam = {
			BaseEdge,
			BaseEdgeModel,
			BaseNode,
			BaseNodeModel,
			RectNode,
			RectNodeModel,
			CircleNode,
			CircleNodeModel,
			PolygonNode,
			PolygonNodeModel,
			TextNode,
			TextNodeModel,
			LineEdge,
			LineEdgeModel,
			DiamondNode,
			DiamondNodeModel,
			PolylineEdge,
			PolylineEdgeModel,
			BezierEdge,
			BezierEdgeModel,
			EllipseNode,
			EllipseNodeModel,
			HtmlNode,
			HtmlNodeModel,
			h: k$1,
			type: element
		};
		this.viewMap.forEach(function(component) {
			var key = component.extendKey;
			if (key) registerParam[key] = component;
		});
		this.graphModel.modelMap.forEach(function(component) {
			var key = component.extendKey;
			if (key) registerParam[key] = component;
		});
		if (fn) {
			var _a = fn(registerParam), ViewClass = _a.view, ModelClass = _a.model;
			var vClass = ViewClass;
			if (isObserverView && !vClass.isObserved) {
				vClass.isObserved = true;
				vClass = observer(vClass);
			}
			this.setView(element, vClass);
			this.graphModel.setModel(element, ModelClass);
		}
	};
	/**
	* 注册元素（节点 or 边）
	* @param config 注册元素的配置项
	* @private
	*/
	LogicFlow.prototype.registerElement = function(config) {
		var ViewComp = config.view;
		if (config.isObserverView !== false && !ViewComp.isObserved) {
			ViewComp.isObserved = true;
			ViewComp = observer(ViewComp);
		}
		this.setView(config.type, ViewComp);
		this.graphModel.setModel(config.type, config.model);
	};
	/**
	* 批量注册元素
	* @param elements 注册的元素
	*/
	LogicFlow.prototype.batchRegister = function(elements) {
		var _this = this;
		if (elements === void 0) elements = [];
		forEach(elements, function(element) {
			_this.registerElement(element);
		});
	};
	LogicFlow.prototype.defaultRegister = function() {
		var defaultElements = [
			{
				type: "rect",
				view: RectNode,
				model: RectNodeModel
			},
			{
				type: "circle",
				view: CircleNode,
				model: CircleNodeModel
			},
			{
				type: "polygon",
				view: PolygonNode,
				model: PolygonNodeModel
			},
			{
				type: "text",
				view: TextNode,
				model: TextNodeModel
			},
			{
				type: "ellipse",
				view: EllipseNode,
				model: EllipseNodeModel
			},
			{
				type: "diamond",
				view: DiamondNode,
				model: DiamondNodeModel
			},
			{
				type: "html",
				view: HtmlNode,
				model: HtmlNodeModel
			},
			{
				type: "line",
				view: LineEdge,
				model: LineEdgeModel
			},
			{
				type: "polyline",
				view: PolylineEdge,
				model: PolylineEdgeModel
			},
			{
				type: "bezier",
				view: BezierEdge,
				model: BezierEdgeModel
			}
		];
		this.batchRegister(defaultElements);
	};
	/*********************************************************
	* Node 相关方法
	********************************************************/
	/**
	* 添加节点
	* @param nodeConfig 节点配置
	* @param eventType 新增节点事件类型，默认EventType.NODE_ADD
	* @param e MouseEvent 事件
	*/
	LogicFlow.prototype.addNode = function(nodeConfig, eventType, e) {
		if (eventType === void 0) eventType = EventType.NODE_ADD;
		return this.graphModel.addNode(nodeConfig, eventType, e);
	};
	/**
	* 删除节点
	* @param {string} nodeId 节点Id
	*/
	LogicFlow.prototype.deleteNode = function(nodeId) {
		var nodeModel = this.graphModel.getNodeModelById(nodeId);
		if (!nodeModel) return false;
		var nodeData = nodeModel.getData();
		var guards = this.options.guards;
		var isEnableDelete = (guards === null || guards === void 0 ? void 0 : guards.beforeDelete) ? guards.beforeDelete(nodeData) : true;
		if (isEnableDelete) this.graphModel.deleteNode(nodeId);
		return isEnableDelete;
	};
	/**
	* 克隆节点
	* @param nodeId 节点Id
	*/
	LogicFlow.prototype.cloneNode = function(nodeId) {
		var nodeModel = this.graphModel.getNodeModelById(nodeId);
		var nodeData = nodeModel === null || nodeModel === void 0 ? void 0 : nodeModel.getData();
		if (nodeData) {
			var guards = this.options.guards;
			if ((guards === null || guards === void 0 ? void 0 : guards.beforeClone) ? guards.beforeClone(nodeData) : true) return this.graphModel.cloneNode(nodeId);
		}
	};
	/**
	* 修改节点的id，如果不传新的id，会内部自动创建一个。
	* @param { string } nodeId 将要被修改的id
	* @param { string } newId 可选，修改后的id
	* @returns 修改后的节点id, 如果传入的oldId不存在，返回空字符串
	*/
	LogicFlow.prototype.changeNodeId = function(nodeId, newId) {
		return this.graphModel.changeNodeId(nodeId, newId);
	};
	/**
	* 修改指定节点类型
	* @param nodeId 节点id
	* @param type 节点类型
	*/
	LogicFlow.prototype.changeNodeType = function(nodeId, type) {
		this.graphModel.changeNodeType(nodeId, type);
	};
	/**
	* 获取节点对象
	* @param nodeId 节点Id
	*/
	LogicFlow.prototype.getNodeModelById = function(nodeId) {
		return this.graphModel.getNodeModelById(nodeId);
	};
	/**
	* 获取节点数据
	* @param nodeId 节点
	*/
	LogicFlow.prototype.getNodeDataById = function(nodeId) {
		var nodeModel = this.getNodeModelById(nodeId);
		return nodeModel === null || nodeModel === void 0 ? void 0 : nodeModel.getData();
	};
	/**
	* 获取所有以此节点为终点的边
	* @param { string } nodeId
	*/
	LogicFlow.prototype.getNodeIncomingEdge = function(nodeId) {
		return this.graphModel.getNodeIncomingEdge(nodeId);
	};
	/**
	* 获取所有以此节点为起点的边
	* @param {string} nodeId
	*/
	LogicFlow.prototype.getNodeOutgoingEdge = function(nodeId) {
		return this.graphModel.getNodeOutgoingEdge(nodeId);
	};
	/**
	* 获取节点连接到的所有起始节点
	* @param {string} nodeId
	*/
	LogicFlow.prototype.getNodeIncomingNode = function(nodeId) {
		return this.graphModel.getNodeIncomingNode(nodeId);
	};
	/**
	* 获取节点连接到的所有目标节点
	* @param {string} nodeId
	*/
	LogicFlow.prototype.getNodeOutgoingNode = function(nodeId) {
		return this.graphModel.getNodeOutgoingNode(nodeId);
	};
	/**
	* 内部保留方法
	* 创建一个fakeNode，用于dnd插件拖动节点进画布的时候使用。
	*/
	LogicFlow.prototype.createFakeNode = function(nodeConfig) {
		var Model = this.graphModel.modelMap.get(nodeConfig.type);
		if (!Model) {
			console.warn("不存在为".concat(nodeConfig.type, "类型的节点"));
			return null;
		}
		var fakeNodeModel = new Model(__assign(__assign({}, nodeConfig), { virtual: true }), this.graphModel);
		this.graphModel.setFakeNode(fakeNodeModel);
		return fakeNodeModel;
	};
	/**
	* 内部保留方法
	* 移除fakeNode
	*/
	LogicFlow.prototype.removeFakeNode = function() {
		this.graphModel.removeFakeNode();
	};
	/**
	* 内部保留方法
	* 用于fakeNode显示对齐线
	*/
	LogicFlow.prototype.setNodeSnapLine = function(data) {
		var _a;
		(_a = this.snaplineModel) === null || _a === void 0 || _a.setNodeSnapLine(data);
	};
	/**
	* 内部保留方法
	* 用于fakeNode移除对齐线
	*/
	LogicFlow.prototype.removeNodeSnapLine = function() {
		var _a;
		(_a = this.snaplineModel) === null || _a === void 0 || _a.clearSnapline();
	};
	/*********************************************************
	* Edge 相关方法
	********************************************************/
	/**
	* 设置默认的边类型。
	* 也就是设置在节点直接由用户手动绘制的连线类型。
	* @param type LFOptions.EdgeType
	*/
	LogicFlow.prototype.setDefaultEdgeType = function(type) {
		this.graphModel.setDefaultEdgeType(type);
	};
	/**
	* 给两个节点之间添加一条边
	* @example
	* lf.addEdge({
	*   type: 'polygon'
	*   sourceNodeId: 'node_id_1',
	*   targetNodeId: 'node_id_2',
	* })
	* @param {EdgeConfig} edgeConfig
	*/
	LogicFlow.prototype.addEdge = function(edgeConfig) {
		return this.graphModel.addEdge(edgeConfig);
	};
	/**
	* 基于id获取边数据
	* @param edgeId 边Id
	* @returns EdgeData
	*/
	LogicFlow.prototype.getEdgeDataById = function(edgeId) {
		var edgeModel = this.getEdgeModelById(edgeId);
		return edgeModel === null || edgeModel === void 0 ? void 0 : edgeModel.getData();
	};
	/**
	* 基于边Id获取边的model
	* @param edgeId 边的Id
	* @return model
	*/
	LogicFlow.prototype.getEdgeModelById = function(edgeId) {
		return this.graphModel.getEdgeModelById(edgeId);
	};
	/**
	* 获取满足条件边的model
	* @param edgeFilter 过滤条件
	* @example
	* 获取所有起点为节点 A 的边的 model
	* lf.getEdgeModels({
	*   sourceNodeId: 'nodeA_id'
	* })
	* 获取所有终点为节点 B 的边的 model
	* lf.getEdgeModels({
	*   targetNodeId: 'nodeB_id'
	* })
	* 获取起点为节点 A，终点为节点 B 的边
	* lf.getEdgeModels({
	*   sourceNodeId: 'nodeA_id',
	*   targetNodeId: 'nodeB_id'
	* })
	* @return model数组
	*/
	LogicFlow.prototype.getEdgeModels = function(_a) {
		var sourceNodeId = _a.sourceNodeId, targetNodeId = _a.targetNodeId;
		var results = [];
		var edges = this.graphModel.edges;
		if (sourceNodeId && targetNodeId) forEach(edges, function(edge) {
			if (edge.sourceNodeId === sourceNodeId && edge.targetNodeId === targetNodeId) results.push(edge);
		});
		else if (sourceNodeId) forEach(edges, function(edge) {
			if (edge.sourceNodeId === sourceNodeId) results.push(edge);
		});
		else if (targetNodeId) forEach(edges, function(edge) {
			if (edge.targetNodeId === targetNodeId) results.push(edge);
		});
		return results;
	};
	/**
	* 修改边的id， 如果不传新的id，会内部自动创建一个。
	* @param { string } edgeId 将要被修改的id
	* @param { string } newId 可选，修改后的id
	* @returns 修改后的节点id, 如果传入的oldId不存在，返回空字符串
	*/
	LogicFlow.prototype.changeEdgeId = function(edgeId, newId) {
		return this.graphModel.changeEdgeId(edgeId, newId);
	};
	/**
	* 切换边的类型
	* @param edgeId 边Id
	* @param type 边类型
	*/
	LogicFlow.prototype.changeEdgeType = function(edgeId, type) {
		this.graphModel.changeEdgeType(edgeId, type);
	};
	/**
	* 删除边
	* @param {string} edgeId 边Id
	*/
	LogicFlow.prototype.deleteEdge = function(edgeId) {
		var edgeModel = this.graphModel.getEdgeModelById(edgeId);
		if (!edgeModel) return false;
		var edgeData = edgeModel.getData();
		var guards = this.options.guards;
		var isEnableDelete = (guards === null || guards === void 0 ? void 0 : guards.beforeDelete) ? guards.beforeDelete(edgeData) : true;
		if (isEnableDelete) this.graphModel.deleteEdgeById(edgeId);
		return isEnableDelete;
	};
	/**
	* 基于给定节点（作为边起点或终点，可以只传其一），删除对应的边
	* @param sourceNodeId 边的起点节点ID
	* @param targetNodeId 边的终点节点ID
	*/
	LogicFlow.prototype.deleteEdgeByNodeId = function(_a) {
		var sourceNodeId = _a.sourceNodeId, targetNodeId = _a.targetNodeId;
		if (sourceNodeId && targetNodeId) this.graphModel.deleteEdgeBySourceAndTarget(sourceNodeId, targetNodeId);
		else if (sourceNodeId) this.graphModel.deleteEdgeBySource(sourceNodeId);
		else if (targetNodeId) this.graphModel.deleteEdgeByTarget(targetNodeId);
	};
	/**
	* 获取节点连接的所有边的model
	* @param nodeId 节点ID
	* @returns model数组
	*/
	LogicFlow.prototype.getNodeEdges = function(nodeId) {
		return this.graphModel.getNodeEdges(nodeId);
	};
	/*********************************************************
	* Element 相关方法
	********************************************************/
	/**
	* 添加多个元素, 包括边和节点。
	* @param nodes
	* @param edges
	* @param distance
	*/
	LogicFlow.prototype.addElements = function(_a, distance) {
		var _this = this;
		var nodes = _a.nodes, edges = _a.edges;
		if (distance === void 0) distance = 40;
		console.log("distance", distance);
		var nodeIdMap = {};
		var elements = {
			nodes: [],
			edges: []
		};
		forEach(nodes, function(node) {
			var nodeId = node.id;
			var nodeModel = _this.addNode(node);
			if (nodeId) nodeIdMap[nodeId] = nodeModel.id;
			elements.nodes.push(nodeModel);
		});
		forEach(edges, function(edge) {
			var sourceNodeId = edge.sourceNodeId, targetNodeId = edge.targetNodeId;
			if (nodeIdMap[sourceNodeId]) sourceNodeId = nodeIdMap[sourceNodeId];
			if (nodeIdMap[targetNodeId]) targetNodeId = nodeIdMap[targetNodeId];
			var edgeModel = _this.graphModel.addEdge(__assign(__assign({}, edge), {
				sourceNodeId,
				targetNodeId
			}));
			elements.edges.push(edgeModel);
		});
		return elements;
	};
	/**
	* 将图形选中
	* @param id 选择元素ID
	* @param multiple 是否允许多选，如果为true，不会将上一个选中的元素重置
	* @param toFront 是否将选中的元素置顶，默认为true
	*/
	LogicFlow.prototype.selectElementById = function(id, multiple, toFront) {
		if (multiple === void 0) multiple = false;
		if (toFront === void 0) toFront = true;
		this.graphModel.selectElementById(id, multiple);
		if (!multiple && toFront) this.graphModel.toFront(id);
	};
	/**
	* 移除选中的元素
	* @param id 元素ID
	*/
	LogicFlow.prototype.deselectElementById = function(id) {
		this.graphModel.deselectElementById(id);
	};
	/**
	* 获取选中的元素数据
	* @param isIgnoreCheck 是否包括sourceNode和targetNode没有被选中的边,默认包括。
	* 注意：复制的时候不能包括此类边, 因为复制的时候不允许悬空的边。
	*/
	LogicFlow.prototype.getSelectElements = function(isIgnoreCheck) {
		if (isIgnoreCheck === void 0) isIgnoreCheck = true;
		return this.graphModel.getSelectElements(isIgnoreCheck);
	};
	/**
	* 将所有选中的元素设置为非选中
	*/
	LogicFlow.prototype.clearSelectElements = function() {
		this.graphModel.clearSelectElements();
	};
	/**
	* 获取节点或边对象
	* @param id id
	*/
	LogicFlow.prototype.getModelById = function(id) {
		return this.graphModel.getElement(id);
	};
	/**
	* 获取节点或边的数据
	* @param id id
	*/
	LogicFlow.prototype.getDataById = function(id) {
		var _a;
		return (_a = this.graphModel.getElement(id)) === null || _a === void 0 ? void 0 : _a.getData();
	};
	/**
	* 删除元素，在不确定当前id是节点还是边时使用
	* @param id 元素id
	*/
	LogicFlow.prototype.deleteElement = function(id) {
		var _a;
		var _b, _c;
		var model = this.getModelById(id);
		if (!model) return false;
		return (_c = (_b = (_a = {}, _a[ElementType.NODE] = this.deleteNode, _a[ElementType.EDGE] = this.deleteEdge, _a)[model.BaseType]) === null || _b === void 0 ? void 0 : _b.call(this, id)) !== null && _c !== void 0 ? _c : false;
	};
	/**
	* 设置元素的zIndex.
	* 注意：默认堆叠模式下，不建议使用此方法。
	* @see todo link 堆叠模式
	* @param id 元素id
	* @param zIndex zIndex的值，可以传数字，也支持传入 'top' 和 'bottom'
	*/
	LogicFlow.prototype.setElementZIndex = function(id, zIndex) {
		return this.graphModel.setElementZIndex(id, zIndex);
	};
	/**
	* 获取指定区域内的所有元素，此区域必须是DOM层。
	* 例如鼠标绘制选区后，获取选区内的所有元素。
	* @see todo 分层
	* @param leftTopPoint 区域左上角坐标, dom层坐标
	* @param rightBottomPoint 区域右下角坐标，dom层坐标
	* @param wholeEdge
	* @param wholeNode
	* @param ignoreHideElement
	*/
	LogicFlow.prototype.getAreaElement = function(leftTopPoint, rightBottomPoint, wholeEdge, wholeNode, ignoreHideElement) {
		if (wholeEdge === void 0) wholeEdge = true;
		if (wholeNode === void 0) wholeNode = true;
		if (ignoreHideElement === void 0) ignoreHideElement = false;
		return this.graphModel.getAreaElement(leftTopPoint, rightBottomPoint, wholeEdge, wholeNode, ignoreHideElement).map(function(element) {
			return element.getData();
		});
	};
	/**
	* 设置元素的自定义属性
	* @see http://logicflow.cn/api/detail#setproperties
	* @param id 元素的id
	* @param properties 自定义属性
	*/
	LogicFlow.prototype.setProperties = function(id, properties) {
		var _a;
		(_a = this.graphModel.getElement(id)) === null || _a === void 0 || _a.setProperties(formatData(properties));
	};
	/**
	* 获取元素的自定义属性
	* @param id 元素的id
	* @returns 自定义属性
	*/
	LogicFlow.prototype.getProperties = function(id) {
		var _a;
		return (_a = this.graphModel.getElement(id)) === null || _a === void 0 ? void 0 : _a.getProperties();
	};
	LogicFlow.prototype.deleteProperty = function(id, key) {
		var _a;
		(_a = this.graphModel.getElement(id)) === null || _a === void 0 || _a.deleteProperty(key);
	};
	/**
	* FBI WARNING !!! 慎用 === 不要用
	* 修改对应元素 model 中的属性
	* 注意：此方法慎用，除非您对logicflow内部有足够的了解。
	* 大多数情况下，请使用setProperties、updateText、changeNodeId等方法。
	* 例如直接使用此方法修改节点的id,那么就是会导致连接到此节点的边的sourceNodeId出现找不到的情况。
	* @param {string} id 元素id
	* @param {object} attributes 需要更新的属性
	*/
	LogicFlow.prototype.updateAttributes = function(id, attributes) {
		this.graphModel.updateAttributes(id, attributes);
	};
	/*********************************************************
	* Text 相关方法
	********************************************************/
	/**
	* 显示节点、连线文本编辑框
	* @param id 元素id
	*/
	LogicFlow.prototype.editText = function(id) {
		this.graphModel.editText(id);
	};
	/**
	* 更新节点或边的文案
	* @param id 节点或者边id
	* @param value 文案内容
	*/
	LogicFlow.prototype.updateText = function(id, value) {
		this.graphModel.updateText(id, value);
	};
	/*********************************************************
	* EditConfig 相关方法
	********************************************************/
	/**
	* 更新流程图编辑相关设置
	* @param {object} config 编辑配置
	* @see http://logicflow.cn/api/detail#updateeditconfig
	*/
	LogicFlow.prototype.updateEditConfig = function(config) {
		var _a = this.graphModel, editConfigModel = _a.editConfigModel, transformModel = _a.transformModel;
		var currentSnapGrid = editConfigModel.snapGrid;
		editConfigModel.updateEditConfig(config);
		if ((config === null || config === void 0 ? void 0 : config.stopMoveGraph) !== void 0) transformModel.updateTranslateLimits(config.stopMoveGraph);
		(config === null || config === void 0 ? void 0 : config.isSilentMode) ? this.keyboard.disable() : this.keyboard.enable(true);
		if (!isNil(config === null || config === void 0 ? void 0 : config.snapGrid) && config.snapGrid !== currentSnapGrid) {
			var _b = this.graphModel.grid.size, size = _b === void 0 ? 1 : _b;
			this.graphModel.updateGridSize(config.snapGrid ? size : 1);
		}
		this.emit(EventType.EDIT_CONFIG_CHANGED, { data: editConfigModel.getConfig() });
	};
	/**
	* 获取流程图当前编辑相关设置
	* @see http://logicflow.cn/api/detail#geteditconfig
	*/
	LogicFlow.prototype.getEditConfig = function() {
		return this.graphModel.editConfigModel.getConfig();
	};
	/*********************************************************
	* Graph 相关方法
	********************************************************/
	/**
	* 设置主题样式
	* @param { object } style 自定义主题样式
	* todo docs link
	*/
	LogicFlow.prototype.setTheme = function(style, themeMode) {
		this.graphModel.setTheme(style, themeMode);
	};
	/**
	* 获取当前主题样式
	* @see todo docs link
	*/
	LogicFlow.prototype.getTheme = function() {
		return this.graphModel.getTheme();
	};
	LogicFlow.prototype.focusByElement = function(id) {
		var coordinate = void 0;
		var nodeModel = this.getNodeModelById(id);
		if (nodeModel) {
			var _a = nodeModel.getData(), x = _a.x, y = _a.y;
			coordinate = {
				x,
				y
			};
		}
		var edgeModel = this.getEdgeModelById(id);
		if (edgeModel) {
			var _b = edgeModel.textPosition, x = _b.x, y = _b.y;
			coordinate = {
				x,
				y
			};
		}
		if (coordinate) this.focusByCoordinate(coordinate);
	};
	LogicFlow.prototype.focusByCoordinate = function(coordinate) {
		var _a = this.graphModel, transformModel = _a.transformModel, width = _a.width, height = _a.height;
		var x = coordinate.x, y = coordinate.y;
		transformModel.focusOn(x, y, width, height);
	};
	LogicFlow.prototype.focusOn = function(focusOnArgs) {
		if (typeof focusOnArgs === "string") this.focusByElement(focusOnArgs);
		else if ("x" in focusOnArgs && "y" in focusOnArgs) this.focusByCoordinate(focusOnArgs);
		else {
			var id = focusOnArgs.id, coordinate = focusOnArgs.coordinate;
			if (id) this.focusByElement(id);
			if (coordinate) this.focusByCoordinate(coordinate);
		}
	};
	/**
	* 重新设置画布的宽高
	* 不传会自动计算画布宽高
	*/
	LogicFlow.prototype.resize = function(width, height) {
		this.graphModel.resize(width, height);
		this.options.width = this.graphModel.width;
		this.options.height = this.graphModel.height;
	};
	/**
	* 将某个元素放置到顶部。
	* 如果堆叠模式为默认模式，则将原置顶元素重新恢复原有层级。
	* 如果堆叠模式为递增模式，则将需指定元素zIndex设置为当前最大zIndex + 1。
	* @see todo link 堆叠模式
	* @param id 元素Id
	*/
	LogicFlow.prototype.toFront = function(id) {
		this.graphModel.toFront(id);
	};
	LogicFlow.prototype.getPointByClient = function(x, y) {
		if (typeof x === "object") return this.graphModel.getPointByClient(x);
		else if (typeof y === "number") return this.graphModel.getPointByClient({
			x,
			y
		});
	};
	/**
	* 获取流程绘图数据
	* 注意: getGraphData返回的数据受到adapter影响，所以其数据格式不一定是logicflow内部图数据格式。
	* 如果实现通用插件，请使用getGraphRawData
	*/
	LogicFlow.prototype.getGraphData = function() {
		var params = [];
		for (var _i = 0; _i < arguments.length; _i++) params[_i] = arguments[_i];
		var data = this.getGraphRawData();
		if (this.adapterOut) return this.adapterOut.apply(this, __spreadArray([data], __read(params), false));
		return data;
	};
	/**
	* 获取流程绘图原始数据
	* 在存在adapter时，可以使用getGraphRawData获取图原始数据
	*/
	LogicFlow.prototype.getGraphRawData = function() {
		return this.graphModel.modelToGraphData();
	};
	/**
	* 清空画布
	*/
	LogicFlow.prototype.clearData = function() {
		this.graphModel.clearData();
		this.render({});
	};
	LogicFlow.prototype.setOverlapMode = function(mode) {
		this.graphModel.setOverlapMode(mode);
	};
	/*********************************************************
	* LogicFlow Render方法
	********************************************************/
	LogicFlow.prototype.renderRawData = function(graphRawData) {
		this.graphModel.graphDataToModel(formatData(graphRawData));
		if (this.options.history !== false) this.history.watch(this.graphModel);
		nn(u(Graph, {
			getView: this.getView,
			tool: this.tool,
			options: this.options,
			dnd: this.dnd,
			snaplineModel: this.snaplineModel,
			graphModel: this.graphModel
		}), this.container);
		this.emit(EventType.GRAPH_RENDERED, {
			data: this.graphModel.modelToGraphData(),
			graphModel: this.graphModel
		});
	};
	/**
	* 渲染图
	* @example
	* lf.render({
	*   nodes: [
	*     {
	*       id: 'node_1',
	*       type: 'rect',
	*       x: 100,
	*       y: 100
	*     },
	*     {
	*       id: 'node_2',
	*       type: 'circle',
	*       x: 300,
	*       y: 200
	*     }
	*   ],
	*   edges: [
	*     {
	*       sourceNodeId: 'node_1',
	*       targetNodeId: 'node_2',
	*       type: 'polyline'
	*     }
	*   ]
	* })
	* @param graphData 图数据
	*/
	LogicFlow.prototype.render = function(graphData) {
		var graphRawData = cloneDeep(graphData);
		if (this.adapterIn) graphRawData = this.adapterIn(graphRawData);
		this.renderRawData(graphRawData);
	};
	/*********************************************************
	* History/Resize 相关方法
	********************************************************/
	/**
	* 历史记录操作
	* 返回上一步
	*/
	LogicFlow.prototype.undo = function() {
		if (!this.history.undoAble()) return;
		var graphData = formatData(this.history.undo());
		this.clearSelectElements();
		this.graphModel.graphDataToModel(graphData);
	};
	/**
	* 历史记录操作
	* 恢复下一步
	*/
	LogicFlow.prototype.redo = function() {
		if (!this.history.redoAble()) return;
		var graphData = formatData(this.history.redo());
		this.clearSelectElements();
		this.graphModel.graphDataToModel(graphData);
	};
	/**
	* 放大缩小图形
	* @param zoomSize 放大缩小的值，支持传入0-n之间的数字。小于1表示缩小，大于1表示放大。也支持传入true和false按照内置的刻度放大缩小
	* @param point 缩放的原点
	* @returns {string} -放大缩小的比例
	*/
	LogicFlow.prototype.zoom = function(zoomSize, point) {
		return this.graphModel.transformModel.zoom(zoomSize, point);
	};
	/**
	* 重置图形的放大缩写比例为默认
	*/
	LogicFlow.prototype.resetZoom = function() {
		this.graphModel.transformModel.resetZoom();
	};
	/**
	* 设置图形缩小时，能缩放到的最小倍数。参数为0-1自己。默认0.2
	* @param size 图形缩小的最小值
	*/
	LogicFlow.prototype.setZoomMiniSize = function(size) {
		this.graphModel.transformModel.setZoomMiniSize(size);
	};
	/**
	* 设置图形放大时，能放大到的最大倍数，默认16
	* @param size 图形放大的最大值
	*/
	LogicFlow.prototype.setZoomMaxSize = function(size) {
		this.graphModel.transformModel.setZoomMaxSize(size);
	};
	/**
	* 获取缩放的值和平移的值。
	*/
	LogicFlow.prototype.getTransform = function() {
		var _a = this.graphModel.transformModel;
		return {
			SCALE_X: _a.SCALE_X,
			SCALE_Y: _a.SCALE_Y,
			TRANSLATE_X: _a.TRANSLATE_X,
			TRANSLATE_Y: _a.TRANSLATE_Y
		};
	};
	/**
	* 平移图
	* @param x 向x轴移动距离
	* @param y 向y轴移动距离
	*/
	LogicFlow.prototype.translate = function(x, y) {
		this.graphModel.transformModel.translate(x, y);
	};
	/**
	* 还原图形为初始位置
	*/
	LogicFlow.prototype.resetTranslate = function() {
		var transformModel = this.graphModel.transformModel;
		var TRANSLATE_X = transformModel.TRANSLATE_X, TRANSLATE_Y = transformModel.TRANSLATE_Y;
		this.translate(-TRANSLATE_X, -TRANSLATE_Y);
	};
	/**
	* 图形画布居中显示
	*/
	LogicFlow.prototype.translateCenter = function() {
		this.graphModel.translateCenter();
	};
	/**
	* 图形适应屏幕大小
	* @param verticalOffset number 距离盒子上下的距离， 默认为20
	* @param horizontalOffset number 距离盒子左右的距离， 默认为20
	*/
	LogicFlow.prototype.fitView = function(verticalOffset, horizontalOffset) {
		if (horizontalOffset === void 0) horizontalOffset = verticalOffset;
		this.graphModel.fitView(verticalOffset, horizontalOffset);
	};
	/**
	* 开启边的动画
	* @param edgeId string
	*/
	LogicFlow.prototype.openEdgeAnimation = function(edgeId) {
		this.graphModel.openEdgeAnimation(edgeId);
	};
	/**
	* 关闭边的动画
	* @param edgeId string
	*/
	LogicFlow.prototype.closeEdgeAnimation = function(edgeId) {
		this.graphModel.closeEdgeAnimation(edgeId);
	};
	LogicFlow.prototype.on = function(evt, callback) {
		this.graphModel.eventCenter.on(evt, callback);
	};
	LogicFlow.prototype.off = function(evt, callback) {
		this.graphModel.eventCenter.off(evt, callback);
	};
	LogicFlow.prototype.once = function(evt, callback) {
		this.graphModel.eventCenter.once(evt, callback);
	};
	LogicFlow.prototype.emit = function(evt, arg) {
		this.graphModel.eventCenter.emit(evt, arg);
	};
	/*********************************************************
	* 插件系统方法
	********************************************************/
	/**
	* 添加扩展, 待讨论，这里是不是静态方法好一些？
	* 重复添加插件的时候，把上一次添加的插件的销毁。
	* @param extension
	* @param props
	*/
	LogicFlow.use = function(extension, props) {
		var _a;
		var pluginName = extension.pluginName;
		if (!pluginName) throw new Error("请给插件指定 pluginName!");
		this.extensions.set(pluginName, (_a = {}, _a[pluginFlag] = pluginFlag, _a.extension = extension, _a.props = props, _a));
	};
	/**
	* 添加主题模式
	* @param themeMode 主题模式
	* @param style 主题样式
	*/
	LogicFlow.addThemeMode = function(themeMode, style) {
		addThemeMode(themeMode, style);
	};
	LogicFlow.removeThemeMode = function(themeMode) {
		removeThemeMode(themeMode);
	};
	LogicFlow.clearThemeMode = function() {
		clearThemeMode();
	};
	LogicFlow.prototype.installPlugins = function(disabledPlugins) {
		var _this = this;
		if (disabledPlugins === void 0) disabledPlugins = [];
		var extensionsAddByUse = Array.from(LogicFlow.extensions, function(_a) {
			return __read(_a, 2)[1];
		});
		forEach(__spreadArray(__spreadArray([], __read(this.plugins), false), __read(extensionsAddByUse), false), function(ext) {
			var extension;
			var props;
			if (pluginFlag in ext) {
				extension = ext.extension;
				props = ext.props;
			} else extension = ext;
			var pluginName = extension === null || extension === void 0 ? void 0 : extension.pluginName;
			if (indexOf(disabledPlugins, pluginName) === -1) _this.installPlugin(extension, props);
		});
	};
	/**
	* 加载插件-内部方法
	*/
	LogicFlow.prototype.installPlugin = function(extension, props) {
		var _a, _b;
		if ("pluginName" in extension && "install" in extension) {
			var pluginName_1 = extension.pluginName, install = extension.install, render_1 = extension.render;
			if (pluginName_1) {
				install && install.call(extension, this, LogicFlow);
				render_1 && this.components.push(render_1.bind(extension));
				this.extension[pluginName_1] = extension;
			}
			return;
		}
		var ExtensionCtor = extension;
		var pluginName = ExtensionCtor.pluginName;
		var extensionIns = new ExtensionCtor({
			lf: this,
			LogicFlow,
			props,
			options: (_b = (_a = this.options.pluginsOptions) === null || _a === void 0 ? void 0 : _a[pluginName]) !== null && _b !== void 0 ? _b : {}
		});
		extensionIns.render && this.components.push(extensionIns.render.bind(extensionIns));
		this.extension[pluginName] = extensionIns;
	};
	/** 销毁当前实例 */
	LogicFlow.prototype.destroy = function() {
		var _a;
		this.clearData();
		nn(null, this.container);
		this.keyboard.destroy();
		this.graphModel.destroy();
		this.tool.destroy();
		this.history.destroy();
		for (var extensionName in this.extension) {
			var extensionInstance = this.extension[extensionName];
			if ("destroy" in extensionInstance) (_a = extensionInstance.destroy) === null || _a === void 0 || _a.call(extensionInstance);
		}
	};
	LogicFlow.extensions = /* @__PURE__ */ new Map();
	return LogicFlow;
}();
(function(LogicFlow) {
	LogicFlow.toStringTag = "LF.".concat(LogicFlow.name);
})(LogicFlow || (LogicFlow = {}));
var LogicFlow_default = LogicFlow;
//#endregion
//#region node_modules/@logicflow/core/es/index.js
function observer(props) {
	return observer$1(props);
}
var es_default = LogicFlow_default;
//#endregion
export { setupTheme as $, ELEMENT_MAX_Z_INDEX as $n, toJS as $r, costByPoints as $t, GraphModel as A, isSegmentsInNode as An, reduce as Ar, getHtmlTextHeight as At, DiamondNodeModel as B, segmentDirection as Bn, toPairs as Br, targetNodeInfo as Bt, CircleNode as C, getSvgTextSize as Cn, formatData as Cr, formatAnchorConnectValidateData as Ct, BaseText as D, isPointOutsideBBox as Dn, setupAnimation as Dr, getCrossPointWithCircle as Dt, LineText as E, isBboxOverLapping as En, defaultAnimationOnConfig as Er, getClosestRadiusCenter as Et, HtmlNodeModel as F, pointDirection as Fn, indexOf as Fr, inStraightLineOfRect as Ft, BezierEdgeModel as G, isInSegment as Gn, assign as Gr, Point as Gt, BaseNodeModel as H, twoPointDistance as Hn, forEach as Hr, RotateMatrix as Ht, TextNodeModel as I, pointFilter as In, has as Ir, isInNode as It, getMinIndex as J, getLineOutline as Jn, computed as Jr, isPointInArea as Jt, BaseEdgeModel as K, getBezierOutline as Kn, u as Kr, Vector as Kt, RectNodeModel as L, points2PointsList as Ln, find as Lr, isInNodeBbox as Lt, TransformModel as M, mergeBBox as Mn, mapKeys as Mr, getNodeBBox as Mt, EventEmitter as N, pathFinder as Nn, isEmpty as Nr, getRectRadiusCircle as Nt, require_classnames as O, isSegmentCrossingBBox as On, updateAnimation as Or, getCrossPointWithEllipse as Ot, EditConfigModel as P, pickEdgeConfig as Pn, isBoolean as Pr, getSvgTextWidthHeight as Pt, removeThemeMode as Q, DEFAULT_VISIBLE_SPACE as Qn, reaction as Qr, snapToGrid as Qt, PolygonNodeModel as R, rebuildPath as Rn, findIndex as Rr, pickNodeConfig as Rt, PolygonNode as S, getSimplePolyline as Sn, themeModeMap as Sr, distance as St, BaseNode as T, heuristicCostEstimate as Tn, defaultAnimationOffConfig as Tr, getClosestAnchor as Tt, PolylineEdgeModel as U, getVerticalPointOfLine as Un, castFunction as Ur, ScaleMatrix as Ut, CircleNodeModel as V, setupEdgeModel as Vn, createToPairs as Vr, Matrix as Vt, LineEdgeModel as W, getCrossPointOfLine as Wn, arrayReduce as Wr, TranslateMatrix as Wt, addThemeMode as X, getPolylineOutline as Xn, isObservable as Xr, getGridOffset as Xt, getZIndex as Y, getNodeOutline as Yn, configure as Yr, generateRoundedCorners as Yt, clearThemeMode as Z, StepDrag as Zn, observable as Zr, normalizePolygon as Zt, AdjustType as _, getLongestEdge as _n, defaultTheme as _r, renderHtmlText as _t, initDefaultShortcut as a, getBBoxOfPoints as an, SegmentDirection as ar, handleResize as at, EllipseNode as b, getPolylinePoints as bn, retroGrid as br, createUuid as bt, translateEdgeData as c, getBezierControlPoints as cn, backgroundModeMap as cr, updateEdgePointByAnchors as ct, PolylineEdge as d, getBytesLength as dn, colorfulTheme as dr, Ellipse as dt, C as ei, createEdgeGenerator as en, ElementState as er, updateTheme as et, LineEdge as f, getClosestPointOfPolyline as fn, darkBackground as fr, Circle as ft, AdjustPoint as g, getExpandedBBoxPoint as gn, defaultGrid as gr, Text as gt, Arrow as h, getExpandedBBox as hn, defaultBackground as hr, Line as ht, Keyboard as i, getBBoxCrossPointsByPoint as in, OverlapMode as ir, calculateWidthAndHeight as it, NestedTransformModel as j, isSegmentsIntersected as jn, baseReduce as jr, getNodeAnchorPosition as jt, SnaplineModel as k, isSegmentsCrossNode as kn, Options as kr, getCrossPointWithPolygon as kt, translateNodeData as l, getBezierPoints as ln, colorfulBackground as lr, Polyline as lt, BaseEdge as m, getEndTangent as mn, darkTheme as mr, Rect as mt, observer as n, k$1 as ni, filterRepeatPoints as nn, EventType as nr, getThetaOfVector as nt, transformEdgeData as o, getBBoxXCrossPoints as on, TextMode as or, recalcResizeInfo as ot, BezierEdge as p, getCrossPointInRect as pn, darkGrid as pr, Path as pt, util_exports as q, getEdgeOutline as qn, action as qr, isMultipleSelect as qt, LogicFlow_default as r, getAppendAttributes as rn, ModelType as rr, sampleCubic as rt, transformNodeData as s, getBBoxYCrossPoints as sn, DEFAULT_GRID_SIZE as sr, triggerResizeEvent as st, es_default as t, M$1 as ti, estimateDistance as tn, ElementType as tr, degrees as tt, Tool as u, getBoxByOriginNode as un, colorfulGrid as ur, Polygon as ut, HtmlNode as v, getNextNeighborPoints as vn, gridModeMap as vr, cancelRaf as vt, RectNode as w, getTextWidth as wn, isIe as wr, getAnchors as wt, DiamondNode as x, getSimplePoints as xn, retroTheme as xr, refreshGraphId as xt, TextNode as y, getPointsFromBBox as yn, retroBackground as yr, createRaf as yt, EllipseNodeModel as z, removeClosePointFromOpenList as zn, createFind as zr, pointEdgeDirection as zt };

//# sourceMappingURL=es-BBsbqssn.js.map